# GPAO-T5 최우선 코어 작업지시서

작성: 2026-07-28
상태: **최우선 코어 개발 지시 · 후속 확장 보류**
적용: T5의 말귀, 모델 운용, 터미널/로컬 PC, 도구/연결, 기억/맥락, 자동화, UI/UX 전부
상위 정본: `GPAO-T5-DEVELOPMENT-ABSOLUTE-PRINCIPLES-2026-07-24-ko.md` §0 · `GPAO-T5-MODEL-OS-OPERATING-LOOP-2026-07-27-ko.md` · `docs/03-product-plan/GPAO-T5-VISION-AND-PERFORMANCE-PHILOSOPHY-2026-07-27-ko.md`

## T5 코어 3대축에서의 역할

이 문서는 **3축: 현재 실행 순서·공정감시·완료 증거**다.

| 축 | 이 작업지시서가 받는 제약 |
|---|---|
| 1. 성능 철학·말귀 | 개발의 목적은 일반 사용자의 자연어 목적 수행이다. 개발자용 기능 전시나 설정 학습을 성공으로 세지 않는다. |
| 2. 모델-OS 공동 운영 순환 | 모델 판단을 대체하지 않고, Runtime이 원문·현실·경계·결과·승계를 정확히 공급하는가를 검사한다. |
| 3. 이 문서 | 단계별 공정감시, 반대 검증, 실제 모델·인간 시나리오·표면 도달 증거가 없으면 완료를 보류한다. |

새 세션은 1축 → 2축 → 3축 → 현재 실행 보드/감사 순서로 읽는다. 이 문서는 제품 철학을 새로 정의하거나 모델 판단을 규칙으로 바꾸지 않는다. 대신 두 축이 실제 사용자 경험으로 입증되는 공정을 강제한다.

## 0. 지시

T5의 남은 개발은 기능을 각각 완성하는 일이 아니다. **말귀와 커뮤니케이션에 강한 모델**과 **컴퓨터·도구·연결·자동화에 강한 엔지니어링 운영체제**가 일반 사용자에게 한 존재처럼 작동하게 만드는 일이다.

```text
사용자는 자기 목적을 평범한 말로 부탁한다.
T5는 그 말·흐름·현재 현실을 모델 앞에 정확히 조립한다.
모델은 사용자 편에서 목적·경로·표현을 판단한다.
T5는 터미널·파일·웹·브라우저·연결·자동화 손발로 직접 조사하고 실행한다.
권한·동의·비밀·취향만 사용자에게 묻는다.
결과와 못 한 일은 원장으로 구분하고 다음 턴으로 잇는다.
```

T5는 개발자처럼 말하는 사용자를 위한 개발자 도구가 아니다. 개발자 수준의 손발을 **개발 지식이 없는 사용자에게 보이지 않게** 운용하는 AI 운영체제다.

이 작업이 닫히기 전에는 새 서비스별 커넥터, 새 OAuth/CLI 종류, 대형 설정 화면, 기능 수 경쟁을 우선하지 않는다. 이미 만든 손발을 사용자의 자연어 목적 수행으로 묶는 일이 우선이다.

## 1. 제품 완료 기준

"완벽"은 테스트 수나 한 번의 데모로 선언하지 않는다. 아래가 반복해서 성립할 때만 이 코어를 닫을 수 있다.

1. 일반 사용자가 결과만 말해도 T5가 먼저 확인·탐색·실행한다.
2. 모델은 현재 대상, 가능한 손발, 연결·권한 상태, 직전 실행 사실을 알고 판단한다.
3. T5가 할 수 있는 일을 경로·명령·설정·복붙으로 사용자에게 떠넘기지 않는다.
4. 실제 권한 행위만 사용자에게 남고, 그 경계는 무엇을 허락하는지 이해 가능하다.
5. 답변, 카드, 실제 실행, 원장, 다음 턴이 같은 사실을 본다.
6. 실패하면 막다른 답 대신 실제 남은 손과 다음 길을 제시한다.
7. UI와 채널은 내부 용어를 숨기고, 진행·승인·결과를 사람 말로 전달한다.
8. 실제 모델과 실제 사용자 시나리오에서 위 경험이 변형 문장·실패·복구 조건까지 통과한다.

## 2. 고정 역할 분리

| 주체 | 맡는 일 | 맡지 않는 일 |
|---|---|---|
| 사용자 | 목표·우선순위·취향 말하기, 승인, OAuth 동의, 비밀 직접 입력 | 경로 찾기, 명령 실행, 설정 파일 편집, 로그 판독, 도구 상태 확인 |
| 모델 | 발화·생략·맥락 해석, 목적/경로/표현 판단, 손 선택 | 없는 능력·연결·실행 결과를 지어내기, 권한을 우회하기 |
| T5 Runtime | 원문 보존, 현실 조립, 실행, 보호, 승인, 원장, 승계 | 서비스별 답변 대본으로 모델 판단을 대체하기 |
| 도구/커넥터 | 자기 능력·한계·미리보기·다음 대상·연결 방식을 선언 | 사용자 화면에 내부 프로토콜을 강제하기 |
| UI/채널 | 진행·영향·승인·결과를 쉬운 말로 전달 | 도구 id, MCP, PKCE, cwd, 상태 코드의 기본 노출 |

## 3. 반드시 장착할 코어 환경

### 3.1 모델 앞 Prompt Assembly Contract

시스템 프롬프트는 T5의 강력한 기관이다. 단, 답을 미리 써 주는 대본이 아니라 모델이 사용자 편에서 사고하도록 만드는 운영 헌장과 현실 조립 계약이다.

| 층 | T5가 공급하는 것 | 금지 |
|---|---|---|
| 고정 헌장 | 사용자 우선, 정직성, 승인 원칙, 응답 태도 | 사례별 금지문/답변 템플릿 누적 |
| 안정 현실 | 실제 executable 손, 선언된 한계, 연결 상태 | descriptor/handler 밖의 능력 추측 |
| 현재 작업 | 원문, active work, 대상 후보, 관련 맥락, 대기 경계 | 모델 계획을 사용자 의도로 확정 |
| 실행 사실 | 실제 인자, 성공/실패, 본 범위, 출처, 복구 후보 | 실패를 성공처럼 요약 |
| 도구 표면 | 실제 handler가 있는 schema만 | 도구명만 보여 주고 역할을 지어내게 두기 |

**추적 계약**: 매 라이브 턴에서 어떤 현실이 왜 모델 입력에 들어갔는지, 실제 모델이 어떤 손을 골랐는지, 실행 뒤 무엇이 다음 턴으로 승계됐는지를 원장과 별도 진단면에서 대조할 수 있어야 한다. 모델의 사고 원문을 저장/노출하는 것은 범위가 아니다.

### 3.2 Engineering Operator Contract

T5는 다음과 같은 일반형 엔지니어링 능력을 모델이 쓸 수 있는 현실로 제공한다.

```text
observe     현재 컴퓨터·자료·연결·프로세스·웹 현실을 직접 확인
locate      사용자가 부른 자료·프로젝트·장소·기존 설정을 찾음
discover    낯선 서비스/요청의 가능한 경로와 근거를 찾음
act         파일·터미널·브라우저·연결·채널을 경계 안에서 실행
verify      실행 결과, 도구 편입, 외부 효과, 실패 원인을 확인
recover     같은 실패를 반복하지 않고 다른 실제 손/범위로 전환
continue    현재 대상·대기 승인·중단 작업을 다음 턴으로 승계
```

이것은 사용자에게 보이는 메뉴가 아니라 모델이 판단할 수 있는 운영 환경이다. 새 서비스가 생겨도 서비스별 if를 늘리지 않고 이 계약을 탄다.

### 3.3 General User Surface Contract

장기 작업에는 사실 기반 진행 표시가 있어야 한다. 사용자는 모델의 생각을 읽을 필요가 없고, 아래만 알면 된다.

- 지금 무엇을 확인 중인가
- 무엇을 찾았고 무엇이 아직 안 됐는가
- 무엇이 바뀌기 직전인가
- 사용자에게 필요한 선택은 무엇인가
- 결과가 어디에 남았는가

기본 화면은 Work Chat이다. 연결·도구·기억·승인·자동화는 대화에서 필요할 때 자연스럽게 열리고, 설정 페이지는 관리 장소일 뿐 사용자를 그곳으로 먼저 보내는 길이 아니다.

## 4. 실제 사례 기반 검증군

사례는 문장 맞추기용 fixture가 아니다. 실제 사용자의 목적, 실제 서비스 권한 구조, 실패 조건을 모델과 T5가 일반형으로 다루는지 보는 표본이다.

| 군 | 실제 사용자 말의 예 | 검증할 운영 능력 | 반드시 볼 변수 |
|---|---|---|---|
| A. 로컬 PC | "컴퓨터가 왜 느린지 봐줘" | 터미널/프로세스/저장공간을 먼저 읽고 설명 | 읽기만 수행, 변경 제안, 권한 부족, 장시간 조사 진행 표시 |
| B. 내 자료 | "지난달 정산 자료 정리해줘" | 찾기 → 읽기 → 정리 계획 → 파일 변경 승인 → 결과 전달 | 후보 여러 개, 외장 볼륨, 한글 이름, 파일 없음, 승인 거절, 되돌리기 |
| C. 낯선 외부 일 | "카페24 주문 가져와줘" | 미선언 서비스의 설치/MCP/CLI/공개 웹/로컬 흔적을 먼저 조사 | 아무 흔적 없음, 기존 설정 존재, 권한 부족, API를 지어내지 않음 |
| D. 실제 OAuth/MCP | "노션 붙여줘" | 발견 → 연결 실행 → 사용자 OAuth 동의 → tool admission → 실제 읽기/쓰기 범위 확인 | 동의 취소, 토큰 만료, 재인증, 연결 해제, 도구 미편입 |
| E. 범위 있는 API | "구글 드라이브 자료 찾아줘" | 필요한 scope와 읽기/쓰기 차이를 사실로 다룸 | 좁은 scope, 동의 거부, API 미설정, rate limit, 계정 전환 |
| F. 외부 전달 | "이 내용 거래처에 보내줘" | 초안/대상/경로 준비, 전송 전 승인, delivery ledger | 수신자 모호, 승인 거절, 전송 실패, 중복 방지, 채널 복귀 |
| G. 대화 승계 | "아까 그거 이어서 해" | active work/대상/대기 경계를 사실로 회수 | 후보 둘 이상, 이미 끝난 일, 중간 취소, 무관한 새 요청 |
| H. 기억과 성장 | "지난번 방식대로 해줘" | retrieved/admitted 구분과 선호 적용 | 잘못된 과거 기억, 미승인 후보, rollback, 새 요청 우선 |

### 사례 근거

- T5의 실제 실패 기록: 경로 복사·명령어 떠넘김, 승인 턴 침묵, 원장-답변 불일치, 브라우저 로그인 사실 과장, 연결 설정 화면 떠넘김.
- [Notion 공식 MCP](https://developers.notion.com/guides/mcp/get-started-with-mcp)는 사용자 OAuth 동의를 요구하며, 연결 후 workspace 접근 범위가 권한에 따라 정해진다. T5는 동의 전후를 정확히 구분해야 한다.
- [Google Drive API scope 안내](https://developers.google.com/workspace/drive/api/guides/api-specific-auth)는 OAuth consent screen에서 요청 범위를 사용자가 판단하게 한다. T5는 필요한 범위를 최소화하고, 읽기와 쓰기를 섞지 않는다.
- [Cafe24 API 시작 안내](https://developers.cafe24.com/docs-new/en/docs/guide/intro)는 앱 등록/클라이언트 정보/OAuth/권한 범위를 실제 전제로 둔다. T5는 이를 모른 채 "바로 가져왔다"고 말하지 않으며, 가능한 자동 조사 후 실제로 사용자·사업자 권한이 필요한 경계만 남긴다.

## 5. 작업 단계와 공정감시

다음 단계는 순서대로 진행한다. 한 단계의 사용자 경로가 닫히지 않으면 다음 단계 구현을 시작하지 않는다. 조사·문서·테스트 설계는 병행할 수 있지만, 제품 기능 확장은 보류한다.

### 0단계. 현재 실행 보드와 기준선 봉인

**목표**: 정본 문서의 철학, 현재 브랜치, 실제 검증 증거가 한 화면에서 만난다.

**작업**

- 7대 기관과 v3.1 사용자 작업 10개를 `구현됨 / 코드 검증 / 실제 모델 / 실제 UI·채널 / 미검증`으로 나눈 Current Execution Board를 만든다.
- 현재 `SCENARIO.md`와 별도로 위 A~H를 T5 사용자 운영 시나리오 정본으로 만든다.
- 각 시나리오에 원문 변형, 권한 경계, 실패/복구, 증거 위치를 적는다.

**공정감시**

- 이전 보고의 "통과"가 코드/게이트/라이브 중 어디에 속하는지 다시 분류한다.
- 실제 모델을 안 쓴 결과를 라이브 통과로 승격하지 않는다.
- 오래된 계획의 ✗/◐는 근거 없이 지우지 않는다.

**통과 조건**: 다음 개발자가 문서만 보고 현재 가능한 것, 아직 미확인인 것, 다음 실사용 시나리오를 오해 없이 알 수 있다.

### 1단계. P-OP-1 공통 운영 현실과 Prompt Assembly

**목표**: 모든 턴에서 모델이 T5가 먼저 맡을 수 있는 손발과 한계를 정확히 안다. 단, 현실이 프롬프트를 잡음으로 덮지 않는다.

**작업**

- `operatorFact`를 descriptor 단일 진실로 유지하고, 실제 executable 손만 파생한다.
- 먼저 현재 운영 현실의 프롬프트 예산, 손 누락, 판단 혼선을 실제 모델에서 측정한다.
- 관측된 혼선 또는 예산 문제가 있을 때만, 있는 손을 숨기지 않는 공통 압축 계약을 추가한다. 키워드·서비스 이름·임의 추정으로 관련성을 고르지 않는다.
- 압축을 도입한 경우에만 Prompt Assembly trace를 진단면에 남긴다. 사용자 화면에는 노출하지 않는다.

**공정감시**

- 서비스별 if, 응답 템플릿, "~하지 마라" 금지문, 특정 문장용 처방이 새로 들어오면 즉시 중단한다.
- 새 사실은 descriptor/handler/probe/receipt 중 어느 진실에서 왔는지 명시한다.
- 프롬프트 예산은 고정·가변을 분리해 측정하며, 긴 목록으로 모델 판단을 오염시키지 않는다.

**실전 라이브**: A, B, C의 대표 사용자 흐름을 먼저 실제 모델로 실행한다. 같은 실패가 표현 변형에서 다시 생기거나 모델별 차이가 의심될 때만 변형을 늘린다. 사용자가 터미널/경로/설정 확인을 시키지 않았는지, T5가 실제 조사했는지, 답과 원장이 같은지 본다.

**반대 검증**: 손/한계/연결 사실을 일부러 제거하거나 잘못 넣었을 때 새 게이트와 라이브 재현이 실패를 드러내야 한다.

### 2단계. P-OP-2 Active Work / 맥락 승계

**목표**: T5가 방금 하던 일, 현재 대상, 실패, 대기 승인·동의·비밀 입력을 다음 턴에 실제 사실로 잇는다.

**작업**

- `workingState`, `subjectOf`, `calledWith`, receipt, pending approval, `surfaceRequest`를 근거별로 합성한다.
- 사용자 원문 목표, 모델 제안, 실행된 사실을 서로 다른 상태로 보관한다.
- 중단된 일은 실제 대상/기다리는 경계만 남기고 비밀값·추정 계획은 남기지 않는다.
- 새 요청·취소·완료·오래된 일은 기존 상태를 감쇠/해제한다.

**공정감시**

- 이전 일을 과도하게 되살리는 반대 시나리오가 반드시 있다.
- "그거"를 임의로 하나 고르는 코드가 생기면 중단하고 후보/근거 계약으로 되돌린다.
- 모델이 한 추정을 durable 사실로 승격하지 않는다.

**실전 라이브**: B/D/G를 다중 턴으로 실행한다. 승인 창을 닫고 다시 말하기, 연결 중단 후 재개, 완료 후 "그거 다시"를 함께 본다.

### 3단계. P-OP-3 공통 엔지니어링 탐색

**목표**: 낯선 서비스·로컬 문제에서 T5가 먼저 근거를 조사하고 연결/실행 경로 후보를 만든다.

**작업**

- `discover`는 앱·CLI·MCP·동기화 폴더·프로젝트 설정·공개 웹·기존 자격/연결 흔적을 읽기 전용으로 탐색한다.
- 발견 결과는 `work candidate` 또는 `connection candidate`다. 발견만으로 설치/등록/권한 상승/도구 편입을 하지 않는다.
- 연결·설치·비밀 입력이 필요해지면 기존 approval/consent/secret surface로 넘긴다.

**공정감시**

- 서비스 이름을 러너/커널에 하드코딩하지 않는다.
- 없는 API/CLI/권한을 모델에게 약속하지 않는다.
- 탐색 범위, 읽은 설정의 비밀 보호, 네트워크 조사 경계를 원장에 남긴다.

**실전 라이브**: C를 "아무 흔적 없음", "기존 MCP 있음", "CLI만 있음", "권한 부족" 조건으로 반복한다.

### 4단계. P-OP-4 실행·복구·장기 작업

**목표**: 한 손이 막혀도 T5가 실제 남은 손으로 전환하고, 사용자는 먹통/반복/떠넘김을 겪지 않는다.

**작업**

- receipt에서 실패의 성격·범위·시도 인자·이미 본 장소를 압축해 모델에 준다.
- 같은 손과 같은 인자의 무의미한 반복을 막되, 모델의 다른 합리적 재시도는 막지 않는다.
- 진행 상태, 취소, 재개, 실패 후 다음 길을 모든 표면에 전달한다.

**공정감시**

- 회복 규칙이 서비스별 사다리나 고정 문구가 되지 않았는지 확인한다.
- 모델이 말한 복구와 실제 실행한 복구를 원장으로 대조한다.

**실전 라이브**: A/B/D/F에서 타임아웃, 권한 거부, rate limit, 승인 거절, 연결 해제, 중간 새 요청을 주입한다.

### 5단계. P-OP-5 기억·성장 입장 제어

**목표**: 기억은 많이 회수하되, 현재 사용자와 현재 일을 오염시키지 않는다.

**작업**

- retrieved와 admitted를 계속 분리한다.
- 반복된 실패/성공은 candidate일 뿐이며, replay와 사용자 확인 전에는 운영 원리가 아니다.
- 어떤 기억/원리가 이번 행동에 영향을 줬는지 설명 가능한 trace를 둔다.

**공정감시**

- 한 번의 관찰이나 모델 추정을 장기 규칙으로 승격하지 않는다.
- 이전 사용자 요청이 현재 요청을 이기는 사례를 반대 검증한다.

**실전 라이브**: H와 G를 함께 실행해, 이전 선호를 적절히 쓰는 경우와 무시해야 하는 경우를 모두 확인한다.

### 6단계. P-OP-6 일반 사용자 UI/UX 실증

**목표**: 강한 내부 운영이 사용자에게는 편안하고 이해 가능한 경험으로 도달한다.

**작업**

- 진행, 승인, 비밀 입력, 연결, 실패/복구, 결과, 기억 찾기, 설정 진입을 실제 화면과 채널에서 정리한다.
- 내부 용어는 진단면으로만 분리하고, 사용자면은 대상·영향·다음 선택·결과를 말한다.
- 긴 작업 중에는 사실 기반 상태를 보여 먹통 오해를 막는다.

**공정감시**

- 커널 데이터가 있어도 화면에 안 보이면 실패다.
- 화면이 코드를 설명하거나 사용자를 관리자/개발자로 만들면 실패다.
- 데스크톱과 실제 채널에서 같은 사실을 보되, 표면에 맞는 말로 전달하는지 확인한다.

**실전 라이브**: A~H 각각에서 web, 승인 카드, 최소 한 실제 채널을 교차 확인한다.

### 7단계. P-OP-7 모델별 실제 성능 보정

**목표**: 특정 모델의 습관이 아니라 T5의 현실 조립이 성능을 높이는지 검증한다.

**작업**

- 동일한 시나리오를 최소 두 실제 provider/model에서 실행하고 원장 기준으로 비교한다.
- 차이는 tool calling, 맥락 누락, 장황함, 재시도, 승인 설명, 복구 판단으로 분해한다.
- 보정은 공통 헌장에 넣지 않고 얇은 provider profile에 격리한다.

**공정감시**

- 특정 모델을 맞추려고 서비스별 문장·기능 제한을 공통 코드에 넣지 않는다.
- 보정 전후의 반대 검증과 라이브 비교가 없으면 합치지 않는다.

## 6. 모든 단계의 다층 검증 의무

아래 여덟 층은 **증거 지도의 항목**이다. 라이브에서 먼저 사용자 흐름을 깨뜨려 보고, 발견한 결함을 해당 층의 회귀 계약으로 번역한다. 해당 단계와 무관하거나 권한상 할 수 없는 층은 억지로 채우지 않고 `해당 없음` 또는 `미검증`으로 남긴다. 숫자 채우기가 목적이 되면 안 된다.

### 슬라이스 경계

한 슬라이스는 도구·영수증·프롬프트를 따로 닫는 단위가 아니라, **사용자 문장 하나의 실제 끝이 이전과 달라지는 단위**다. 손을 만들었다면 그 결과가 모델 판단·승인·답변까지 이어져 해당 라이브 시나리오가 달라질 때만 슬라이스로 부른다. 중간 계약은 같은 슬라이스 안의 구현 단계일 뿐 완료 보고 대상이 아니다.

### 표면 증거의 두 종류

- `단계 도달`: 이번 단계가 만든 사실·카드·답이 해당 웹 또는 채널 표면에 실제로 도달하는지. 각 단계가 직접 확인한다.
- `전체 UX`: 정보 구조·설정 화면·상태 표현·반복 사용성의 정리. 이것이 P-OP-6의 범위다.

P-OP-6이 아직이라는 이유로 앞 단계의 단계 도달을 영원히 미검증으로 두지 않는다. 반대로 단계 도달 한 번으로 전체 UX가 끝났다고도 하지 않는다.

| 층 | 확인할 것 |
|---|---|
| 1. 계약 검사 | descriptor, handler, schema, 상태, 원장, UI 파생이 같은 진실을 보는가 |
| 2. 회귀 검사 | 기존 경로를 깨지 않았는가 (`npm test`, `npm run gate`) |
| 3. 반대 검증 | 수정 전/일부러 망가뜨린 상태를 새 검사가 실제로 잡는가 |
| 4. 통합 실행 | 실제 서버/파일/프로세스/연결 레이어를 통과하는가 |
| 5. 실제 모델 | 실제 provider가 현실을 읽고 도구를 판단하는가 |
| 6. 인간 시나리오 | 일반 사용자의 원문·표현 변형·생략·감정 섞인 요청을 처리하는가 |
| 7. 변수·복구 | 미연결, 권한 거부, 취소, timeout, rate limit, 다중 후보, 새 요청, 재시작 뒤에도 정직하게 이어지는가 |
| 8. 표면 도달 | 웹·승인 카드·채널에 실제 사람이 이해할 사실이 보이는가 |

### 공정 중단 규칙

아래가 나오면 다음 단계로 가지 않고 현재 단계를 다시 연다.

- 테스트는 통과하지만 실제 모델이 사용자를 터미널/설정/복붙으로 보낸다.
- 모델이 실제 손을 모르거나 없는 손을 있다고 말한다.
- 카드/원장/답변/실행 결과가 다르다.
- 실제 화면이 내부 기술 용어를 노출하거나 필요한 상태를 숨긴다.
- 한 사례를 고치려고 서비스별 if, 문구 대본, 키워드 분기를 넣었다.
- 실제 사용자 지적이 아직 재현/불변식/시나리오로 번역되지 않았다.

## 7. 공정감시 운영 방식

### 작업 시작 전

구현자는 다음 여섯 줄을 제출한다.

```text
사용자 목적:
채우는 운영 순환 칸:
현재 사실/진실 원천:
모델 판단 침범 위험:
권한/비밀/외부효과 경계:
이번 단계의 실제 사용자 시나리오와 실패 변수:
```

### 구현 중

- 코드 변경은 하나의 공통 계약 또는 운영 칸을 채우는 범위로 제한한다.
- 새 테스트는 정상·반대·표면 도달 중 적어도 하나를 직접 증명한다.
- 실제 사례 조사에서 얻은 서비스 요건은 선언/연결 방식/권한 경계에만 반영하고 사용자 답변 대본으로 넣지 않는다.
- 설계 결정을 바꾸면 현재 실행 보드와 이 지시서의 해당 단계에 근거를 남긴다.

### 단계 종료 전 독립 감사

감사자는 구현자가 제출한 증거를 그대로 믿지 않고 다음을 확인한다.

1. 사용자 원문에서 시작해 실제 모델·도구·승인·원장·다음 턴까지 다시 본다.
2. 사용자에게 개발자 역할을 떠넘기는 문장/화면/복구 경로가 없는지 본다.
3. 새 계약이 새 손에도 일반화되는지, 서비스별 코드가 새지 않았는지 본다.
4. 반대 검증이 진짜로 실패를 잡는지 본다.
5. 라이브 미확인은 통과로 번역하지 않는다.

구현자와 감사자가 같은 사람이라면 `Dual-Role`로 표시하고, 다음 독립 감사에서 가장 먼저 재검토한다. 커밋, 푸시, 실계정 OAuth, 외부 전송, 비밀 입력, 삭제/공개는 각각 별도 권한 경계다.

## 8. 증거와 보고

각 단계의 증거는 `docs/03-verification/evidence/core-harness/<단계>/`에 둔다. 민감정보, 토큰, 실제 사용자 자료 원문은 넣지 않는다.

실사용자 지적에서 나온 결함은 같은 단계의 `user-findings.md` 또는 회귀 검사에 **사용자 문장 → 실제 실패 → 새 불변식/시나리오 → 재현 여부**로 남긴다. 이 번역물이 없으면 해당 지적은 처리 완료가 아니다.

종료 보고는 아래 형식을 지킨다.

```text
사용자에게 실제로 달라진 경험:
채운 운영 순환 칸:
모델 판단 침범 여부:
코드/게이트 검증:
반대 검증:
실제 모델·실제 사용자 시나리오:
실제 UI/채널 도달:
실패/복구 변수:
라이브 미확인 또는 남은 경계:
다음 단계 진입 판정:
```

## 9. 종료 선언

이 코어 작업은 새 기능을 많이 만들었다고 끝나지 않는다. A~H 실전 시나리오가 실제 모델과 실제 표면에서 반복 통과하고, 사용자 지적이 남긴 실패가 구조적 불변식·복구·UX로 흡수되며, 새 연결/도구/기억/자동화가 같은 운영 계약에 자연스럽게 들어올 때 닫는다.

그 전까지 T5의 개발은 이 작업을 최우선으로 한다. 이 작업이 닫힌 뒤에야 P5-B 확장, P7 깊이, 캔버스, 릴리스/설치/자동 업데이트를 "완성 마무리" 작업으로 진행한다.
