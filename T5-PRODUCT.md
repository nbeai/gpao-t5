# T5 Product Definition

상태: `OWNER_PRODUCT_SOURCE`

이 문서는 T5가 무엇인지 정의한다. 구현 구조와 현재 진행 상태는 정의하지 않는다. 자세한 학습 원문은
`오픈클로역사와티파이브.txt`에 보존하며, 이 문서와 뜻이 다르면 오너에게 확인한다.

## 존재 이유

> 사용자는 T5를 배우지 않는다. T5가 사용자를 배우고, 사용자가 평소 말로 목적을 말하면
> T5는 컴퓨터와 외부 자원을 활용해 실제로 끝낸다.

T5는 개발자용 agent framework가 아니다. 일반 사용자가 agent, model, tool, skill, MCP, prompt,
token, cron, sandbox를 몰라도 자신을 아는 한 명의 유능한 조력자와 대화하듯 사용하는 개인 AI
운영체제다.

## 제품 경험

사용자는 기능이나 앱을 선택하지 않고 목적을 말한다. T5는 내부에서 필요한 능력을 조합한다.

```
Human Experience
대화 · 결과물 · 필요한 순간의 Preview · Undo · 알림
        ↓
Interaction Intelligence
목적·상황·사용자 이해 · 최소 질문 · 자율성 판단 · 결과 표현
        ↓
Agent Infrastructure
모델 · 터미널 · 파일 · 웹 · 브라우저 · 앱 · 기억 · 스킬 · 실행
```

기본 UI는 대화다. 표, 지도, 문서, 파일, 일정, 비교 카드 같은 artifact는 필요할 때만 나타난다.
내부 조직도와 기술 용어는 기본 사용자 경험에 노출하지 않는다.

## 실제로 해내는 손

T5는 사용자의 입력을 답변 요청으로만 보지 않는다.

```
사용자 목적 → 현재 현실 관측 → 적절한 수단 선택 → 권한 판정 → 실행
→ 결과 관측 → 목적과 대조 → 미달이면 다른 수단 → 실제 달성 → 답
```

터미널은 컴퓨터 생태계 전체로 들어가는 기본 손이다. T5는 `git`, `curl`, `python`, `ffmpeg`,
패키지 관리자와 사용자가 이미 설치·인증한 CLI를 새로 구현하지 않고 활용한다. 전용 파일·웹·브라우저·
앱 도구는 더 적절한 수단일 때 함께 사용한다.

모델은 의도, 계획, 도구 선택, 복구, 목적 달성 판단과 사용자 문장을 담당한다. 런타임은 정확한 현실,
도구, 권한, 실행, 영수증, 취소와 복구를 담당한다. 런타임은 모델을 규칙 기계로 대체하지 않는다.

## 자연스러운 Interaction Intelligence

- 질문할 수 있다는 이유로 묻지 않는다. 안전하게 진행할 수 없을 때만 최소한으로 묻는다.
- 이미 아는 사용자 선호와 현재 맥락을 다시 입력받지 않는다.
- 내부 계획과 기술 오류를 그대로 사용자에게 떠넘기지 않는다.
- 실패하면 무엇이 성공했고 무엇이 남았으며 어떤 길이 가능한지 말한다.
- 사용자가 시스템에 적응하는 것이 아니라 시스템이 사용자의 언어와 습관에 적응한다.

Interaction Intelligence는 반드시 별도 규칙 엔진을 뜻하지 않는다. 모델이 잘 판단하도록 현실과 상태를
공급하고, 기계적으로 강제해야 하는 최소 경계만 런타임에 둔다.

## 자동성과 통제권

생각·검색·분석·준비와 사용자가 명시한 범위의 가역적 행동은 자동성을 우선한다. 멈춰 묻는 기본 경계는:

1. 비밀값 입력
2. 백업 없는 파괴
3. 새 상대에게 첫 외부 전송
4. 돈이 나가는 일

가능한 행동은 Preview, 실행 기록, inverse operation과 Undo를 제공한다. Undo 가능성이 잘못된 외부 행동의
면허가 되지는 않는다. 로컬 효과와 외부 효과는 별도로 판정한다.

## 기억과 성장

기억과 스킬은 사용자가 관리해야 하는 내부 기능이 아니다. 사용자는 대화로 기억시키고, 확인하고, 고치고,
지운다. 반복 작업은 점점 사용자의 방식에 맞아야 한다.

- Memory는 사실·선호·결정이다.
- Episode는 실제 과거 경험이다.
- Skill은 재사용 가능한 방법이다.
- Learning은 즉시 덮어쓰기가 아니라 제안·시험·비교·승격이다.

성장은 개수나 레벨이 아니라 더 적은 설명, 더 적은 교정, 더 나은 결과로 느껴져야 한다.

## 제품 판정

T5의 가치는 다음 곱으로 판정한다.

```
실제로 해내는 힘
× 사용자가 아무것도 배우지 않고 그 힘을 끌어내는 자연스러움
× 사용할수록 사용자를 알아가는 지속성
```

기능 하나, 테스트 하나, 도구 호출 하나는 제품 완료가 아니다. 관련 과업 영역에서 실제 목적 달성,
질문·승인 부담, 수단 전환, 진실성, 속도와 사용자 체감을 기존 T5 및 Claude Code, Codex, OpenClaw,
Hermes와 비교한다.

궁극적으로 사용자는 “AI agent를 사용한다”고 느끼지 않고, 자신의 일을 아는 유능한 조력자와 이야기한다고
느껴야 한다.
