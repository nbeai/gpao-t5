{
  "name": "gpao-t5",
  "version": "0.0.0-slice1",
  "private": true,
  "type": "module",
  "description": "GPAO-T5 Original AI Operating System — First Build Slice (Work Chat kernel vertical)",
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "gpao-t5": "bin/gpao-t5.mjs"
  },
  "files": [
    "bin",
    "src"
  ],
  "scripts": {
    "test": "node --test",
    "start": "node src/surface/server.js",
    "audit:workspace": "node scripts/audit-project-entry.mjs",
    "verify:package": "node scripts/verify-package.mjs",
    "gate": "node scripts/gate.mjs"
  }
}
