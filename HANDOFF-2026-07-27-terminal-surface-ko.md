# 인수인계 — Terminal / Process / Work Surface (2026-07-27)

작성: Claude (세션 종료 시점)
브랜치: `p2-conversational-quality` · HEAD `0511945` · **tracked 변경 없음**
검증: `npm test` 815건 통과 / `npm run gate` PASS (3.0초)

---

## 0. 먼저 읽을 것 — 완료 기준이 바뀌었다

**오늘부터 테스트 통과는 완료가 아니다.** 오너 지시:

> 완료는 실제 T5 라이브 시나리오, 테스트/게이트, 반대 검증, 사용자 실사용 기준을 **모두** 통과해야 한다.
> Codex도 예외가 아니다. 확인하지 못한 것은 확인하지 못했다고 말해야 한다.
> 보고서와 숫자로 서로 통과시켜주는 방식은 중단한다.

**왜 이렇게 됐는가 — 이게 이 문서에서 가장 중요하다.**
라이브를 돌릴 때마다 결함이 나왔다. 예외가 없었다:

| 결함 | 그때 테스트 상태 |
|---|---|
| `cwd: ""` 가 `??` 를 통과해 엉뚱한 자리에서 실행 | 785건 통과 중 |
| 사용자 데이터를 `src/surface/processes.json` 에 저장 | 776건 통과 중 |
| 서버 켜기가 승인 없이 실행 | 799건 통과 중 |
| 등급 판정이 모델이 고른 인자를 안 봄 | 799건 통과 중 |
| 승인 카드에 "실행 중인 것 실행" | 799건 통과 중 |
| 실행 루프 상한에서 빈 답변 | 815건 통과 중 (**미해결**) |

단위 테스트는 **내가 상상한 경로만** 지나간다. 라이브가 진짜 검사다.

그리고 **내가 만든 게이트가 세 번 약했다** — 전부 반대 검증에서만 잡혔다:
- `blocked` 만 보다 파일이 없어서 막힌 것(ENOENT)까지 "보호 작동"으로 셈
- 게이트가 `status`(읽기)만 불러서 파일이 안 생겼고, 경로가 소스 트리로 잡혀도 초록
- locate 게이트가 폴더 **이름**만 맞아도 통과 → 업무 자료 판정을 통째로 빼도 초록

**반대 검증은 선택이 아니라 조건이다.**

---

## 1. 지금 당장 할 일 (W3 마무리)

### ① 실행 루프가 상한에 닿으면 빈 답변이 나간다 ← 더 심각

**재현:** 라이브에서 사용자가 "작업용SSD"라고만 답했을 때 T5가 **아무 말도 안 했다.**
그 턴에 도구는 여러 번 돌았는데(원장 확인) 답변이 비었다.

**자리:** `src/kernel/turn.js` — 실행 루프 뒤 `reply` 를 만드는 부분.
```js
if (steps >= MAX_TOOL_STEPS && !멈춘이유) 멈춘이유 = '한 번에 할 수 있는 만큼 하고 멈췄어요';
let reply = typeof finalOut === 'string' ? finalOut : finalOut?.text ?? '';
if (멈춘이유 && !reply.trim()) reply = '';   // ← 여기가 빈 문자열을 그대로 둔다
```
`멈춘이유` 를 만들어 놓고 **쓰지 않는다.** 상한에 닿으면 마지막 모델 호출에서 tools 를 빼는데
(`...(steps < MAX_TOOL_STEPS ? { tools } : {})`), 그때 모델이 빈 텍스트를 돌려주면 그대로 나간다.

**고칠 방향:** 상한·승인·되풀이로 멈췄으면 **여기까지 한 일과 다음 할 일**을 사실로 실어
한 번 더 답하게 하거나, `멈춘이유` + `fallbackReplyFrom(turnReceipts)` 로 떨어뜨린다.
오너 지시 원문: *"상한에 닿으면 여기까지 한 일과 다음 할 일을 정직하게 답하기"*.

### ② 되풀이 방지가 느슨하다

**재현:** 원장에 같은 성격의 `find` 가 maxdepth 만 3→5→8→10 으로 바뀌며 6번 돌았다.

**자리:** `src/kernel/turn.js` 의 `지문of`
```js
const 지문of = (toolId, args) => `${toolId}:${args?.command ?? args?.path ?? args?.request ?? JSON.stringify(args ?? {})}`;
```
명령 문자열이 조금만 달라도 다른 지문이 된다.

**주의:** 너무 조이면 정당한 재시도(depth 를 넓혀 다시 찾기 — 우리가 **설계로 의도한 것**)까지 막는다.
`local.locate` 는 넓혀 가며 부르는 게 정상이다. 명령의 **대상**이 같고 범위만 다른 경우를
어떻게 볼지 정해야 한다. 무조건 막지 말 것.

### ③ 그다음 순서

1. 위 둘을 고친다
2. **라이브 4개 시나리오를 다시 돌린다** (아래 6절에 그대로 실행할 수 있는 절차)
3. **반대 검증** — 상한 처리를 빼면 빈 답변이 다시 나는지, 되풀이 방지를 빼면 반복이 잡히는지
4. `npm test` / `npm run gate`
5. 그 뒤에야 W3 완료라고 보고

---

## 2. 오늘 만든 것 (전부 커밋됨)

### 롤백 하나 — 먼저 알아야 한다

오전에 만든 **Local Surface 5단계를 전부 롤백했다**(오너 지시).
파일 도구를 11개 액션으로 늘리던 방향이 틀렸다 — 셸의 부분집합을 손으로 재구현한 것이었다.
보관 브랜치: `보관/local-surface-2026-07-27` (필요하면 파일 단위로 꺼낼 수 있음).
그중 `local-protection.js` 만 되살렸다 — 셸을 주면 **더** 필요해진다.

### T1~T3 · 터미널과 프로세스

| 파일 | 무엇 |
|---|---|
| `src/runtime/sandbox.js` | `sandbox-exec` 프로파일. probe(쓰기·네트워크·비밀읽기 차단) / granted(비밀만 차단) |
| `src/runtime/terminal-run.js` | 셸 실행. 출력 접기·타임아웃·stdin 차단·환경변수 마스킹·`executionBlock()` |
| `src/runtime/local-terminal.js` | 도구. probe 로 등급을 정하고, 막히면 승인 요청 |
| `src/runtime/local-process.js` | 장기 프로세스. start/status/logs/stop, **살아있는지 커널에 묻는다** |
| `src/runtime/process-store.js` | 켠 것을 파일에 기록(재시작 후에도 기억) |
| `src/runtime/lifecycle-guard.js` | T5 자기 프로세스·데이터·launchctl 보호 |

**핵심 설계 — 위험 명령 목록을 쓰지 않는다.**
목록은 항상 뚫린다(실측: `find -delete`, `python shutil.rmtree`, `perl unlink`, `node fs.rmSync`,
`osascript`, `sed -i`, `eval` + 백틱, `r""m`, `$()`, 백그라운드, 임시 스크립트 — 전부 목록 밖).
대신 **커널이 판정한다**: probe 로 먼저 돌려서 성공하면 아무것도 안 바꿨다는 **증명**이다.

`test/terminal-adversarial.test.js` 가 우회 30건을 던지고 **미끼 파일이 살아있는지**만 본다.
메시지는 거짓말할 수 있지만 파일시스템은 못 한다. 반대 검증: 샌드박스를 끄면 28건이 뚫린다.

### 실행 루프

`turn.js` 가 마지막 모델 응답의 `toolCalls` 를 **버리고 있었다.** 그래서 모델이 다음 걸음을
알고도 못 걸었다(실측: "package.json 존재만 확인됐고 실제 테스트는 아직 실행되지 않았습니다"
라며 사용자에게 `npm test` 를 치라고 함). 최대 4걸음, 걸음마다 기존 승인 판정을 그대로 탄다.

### W1~W3 · 현재 작업 대상

| 파일 | 무엇 |
|---|---|
| `working-state.js` | 지금 자리 · 방금 실행 · 방금 켠 것 · **볼 수 있는 자리** 를 사실로 투영 |
| `src/runtime/local-locate.js` | 사용자가 부른 대상의 후보를 근거·확신도와 함께. `places()` 도 낸다 |

**코드 프로젝트 찾기가 아니다.** 정산 엑셀·계약서 pdf·원고·시안을 같은 무게로 본다.
게이트는 **가짜 홈**에서 돈다 — 특정 사용자 경로를 박으면 그 자리에서 깨진다.

---

## 3. 반복한 실수 — 같은 함정을 또 밟지 마라

### `??` 는 빈 문자열을 통과시킨다 (2회)

**모델은 안 쓰는 인자도 `""` 로 채워 보낸다.**
- `local.scope`: `path: ''` 가 통과해 이름으로 여는 길이 통째로 죽었다
- `local.terminal`: `cwd: ''` 가 통과해 엉뚱한 자리에서 실행됐다

`blank()` 헬퍼가 `local-terminal.js` 에 있다. **`??` 를 쓸 때마다 빈 문자열을 의심할 것.**

### `git checkout` 이 미커밋 작업을 지운다 (2회)

반대 검증에서 고의로 망가뜨린 뒤 `git checkout <file>` 로 되돌리다 **아직 커밋 안 한 작업까지
날렸다.** 두 번 다 복구했지만 운이었다.

**반대 검증 전에 반드시 커밋한다.** 테스트·게이트가 통과했으면 커밋할 근거는 이미 있다.

### 커널에 도구별 `if` 를 늘리면 새 도구가 조용히 샌다 (3회)

`action-plan.js` 에서 세 번 샜다 — 등급 판정 인자 → 승인 문구 → 판정 인자.
결국 **`previewOf(args)` 계약**으로 바꿨다(도구가 자기 미리보기를 낸다).
새 도구를 만들 때 커널을 고쳐야 한다면 **그 구조가 잘못된 것**이다.

### python heredoc 에서 따옴표 이스케이프

`python3 - <<'PY'` 안에서 JS 문자열의 따옴표를 다루다 파일을 깨뜨려 테스트 72건이 죽었다.
큰 문자열을 넣을 땐 **Write 도구를 쓸 것.**

---

## 4. 안전 경계 — 건드리기 전에 이해할 것

```
① 커널 샌드박스   probe: 쓰기·네트워크·비밀읽기 차단 → 통과하면 자동(A0)
                  granted: 승인 후. 변경·네트워크 열되 **비밀은 여전히 닫힘**
② 보호 영역       secret(읽기까지) / system(변경만). 루트·승인과 독립
③ lifecycle guard T5 자기 프로세스·데이터·launchctl. 샌드박스가 못 잡는다(시그널은 파일쓰기가 아님)
④ 승인 등급       A0~A3. terminal 읽기 자동 · process start 는 A2+안전바닥 · status/logs 자동 · stop A1
```

**`executionBlock()` 은 안전 판정이 아니다.** 사용자에게 뭐라고 말할지(샌드박스 차단 / 실행 환경 /
코드 실패)를 고르는 것뿐이다. 안전은 이미 커널이 보장했다. 틀려도 최악은 승인을 더 묻는 것이다.

---

## 5. 아직 안 한 것 / 알려진 한계

- **W3 미완료** — 위 1절 두 가지
- **반대 검증 미실시** — W3 자리 배선(`places`)에 대해
- **폴더 선택 다이얼로그를 넣지 않았다** — `osascript choose folder` 는 되지만 T5의 주 표면은
  채팅(웹·텔레그램)이라 텔레그램에서 죽은 기능이 된다. 웹 UI 제품 UX로 남긴다(오너 승인 사항)
- **`npm test` 가 probe 에서 EPERM listen 으로 죽는 경우** — 지금은 통과하지만 포트를 여는
  테스트가 늘면 다시 난다. `executionBlock` 이 sandbox 로 분류해 승인 카드로 보낸다
- **게이트 시간이 빠듯하다** — 기준선 5초, 실측 3.0초. 무거운 검사를 더 넣으면 넘는다.
  머신 부하에 따라 흔들린다(내 환경에선 6~7초까지 갔고 오너 환경에선 PASS였다)
- **`docs/`, `.beai-harness/`, `workspace-notes/`** 가 untracked로 남아 있다. 내가 만든 게 아니다

---

## 6. 라이브 확인 절차 (그대로 실행 가능)

```bash
cd /Users/jyp/Developer/gpao-t5 && PORT=7500 npm start
```

모델은 저장된 연결(gpt-5.5)을 쓴다. **격리된 데이터 디렉터리를 쓰면 모델 연결을 잃는다** —
내가 그것 때문에 "라이브 실측 불가"라고 잘못 보고한 적이 있다.

세션 만들고 발화 보내기:
```js
const {id} = await (await fetch('http://localhost:7500/sessions',{method:'POST'})).json();
await fetch('http://localhost:7500/turn', {method:'POST',
  headers:{'content-type':'application/json'},
  body: JSON.stringify({sessionId:id, text:'외장하드에 있는 정산 자료 봐줘'})});
```

**결과 검증은 답변만 보지 말고 원장을 읽어라** — 무슨 도구를 어떤 인자로 불렀는지 거기 있다:
```
~/.local/state/gpao-t5/sessions/<sessionId>.json  →  ledgerEntries[].actualCall
```
"모델이 이상하다" 싶을 때 원장을 보면 **런타임이 못 했거나 안 알려준 것**이 대부분이었다(오늘 6회).

### 외장 볼륨이 필요한 시나리오

sudo 없이 진짜 볼륨을 만들 수 있다:
```bash
hdiutil create -size 20m -fs APFS -volname "작업용SSD" /tmp/작업용SSD.dmg -quiet
hdiutil attach /tmp/작업용SSD.dmg -quiet
# ... 자료를 심고 확인 ...
hdiutil detach "/Volumes/작업용SSD" -quiet && rm -f /tmp/작업용SSD.dmg
```
**확인 뒤 반드시 정리할 것.** 서버·켠 프로세스도 마찬가지다(`pkill -f "node src/surface/server.js"`).

### W3 필수 시나리오 4개

1. "외장하드에 있는 정산 자료 봐줘"
2. "어떻게 알려주면 돼?"
3. "작업용SSD"  ← **현재 여기서 빈 답변**
4. "그 안에서 다시 찾아봐"

실패 기준: "경로를 복사해서 붙여넣으세요" / Finder 우클릭 / Option / 경로명 복사가 나오면 실패.

---

## 7. 지켜야 할 원칙 (오너가 주요 문서에 박아둔 것)

작업 전 스스로 물어야 하는 6가지:

1. 이 기능을 **막고** 있는가, 더 자연스럽게 **가능하게** 만들고 있는가?
2. Codex·Claude Code·OpenClaw·Hermes가 하는 일을 **T5만 못 하게 축소**하고 있지 않은가?
3. 위험을 이유로 **능력을 제거**했는가, 실행 순간의 **승인·원장·보호·복구**로 다뤘는가?
4. 사용자가 할 일을 T5가 할 수 있는데 **떠넘기고** 있지 않은가?
5. **기능 목록을 늘리는** 중인가, **하나의 근본 표면**을 열고 있는가?
6. 모델에게 **금지문을 더 넣고** 있는가, **더 정확한 현실과 손발**을 주고 있는가?

오늘 오전에 이걸 어겼다. "터미널 떠넘김이 실패"라고 진단해 놓고 처방을 "터미널을 안 써도 되게
파일 도구를 늘린다"로 잡았다. 전부 롤백됐다.

그리고 §24: **코드는 경계와 사실, 모델은 이해와 선택.** 규칙으로 굳히면 모델이 멍청해진다.
오늘 이게 두 번 증명됐다 — 위험 명령 목록 대신 커널 판정, 이름 파싱 대신 자리 목록 제공.

---

## 8. 판정 기준

- `npm test` 와 `npm run gate` **두 명령만** 판정에 쓴다. `node --test test/` 같은 임의 명령으로
  "통과했다"고 하지 않는다(오너 지적 사항)
- 출력은 파이프하지 말고 **파일로** 받는다
- 30초 넘으면 코드 실패인지 실행 방식 문제인지 **분리해서** 보고한다
- 게이트 기준선을 통과시키려고 검사를 약하게 만들지 않는다

---

수고 많으셨습니다. 남은 두 가지는 원인이 명확하니 오래 걸리지 않을 겁니다.
