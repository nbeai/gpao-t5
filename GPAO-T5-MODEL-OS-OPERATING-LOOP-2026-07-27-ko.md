# T5 모델-OS 공동 운영 순환 (T5 Model Operating Loop)

작성: 2026-07-27 · 지위: **모든 도구·커넥터·브라우저·터미널·채널 개발의 감사 기준** (오너 승인)
정본 상위: `GPAO-T5-DEVELOPMENT-ABSOLUTE-PRINCIPLES-2026-07-24-ko.md` §0 · `AGENTS.md` 첫머리

> T5는 모델을 제어하는 시스템이 아니라, 모델이 더 정확히 사고하게 하는 AI OS다.
> 모델은 매 턴 아무것도 모른 채 태어난다. 그 턴에 모델이 얼마나 똑똑하게 구는가는
> 모델 자체가 아니라 **그 순간 앞에 놓인 세계가 얼마나 정확한가**로 정해진다.

원칙 여섯 줄(오너 원문):

```
신호는 얇게
현실은 정확하게
판단은 모델에게
위험은 실행 경계에서
결과는 원장에
다음 턴은 이어받게
```

---

## 순환

```
사용자의 말 (원문 그대로 — 해석해서 닫지 않는다)
   ↓
T5가 세계를 구성한다
   · 나는 누구고 어떤 손이 있는가          ← 자기자각 (selfState)
   · 이 대화에서 지금까지 뭘 했는가         ← 흐름 (workingState · 승계)
   · 방금 실행은 무엇으로 불러 어떻게 됐는가  ← 사실 (원장 · calledWith)
   · 바깥 세계엔 어떻게 닿을 수 있는가       ← 연결 현실 (executable/connectable/planned)
   · 지금이 언제고 여기가 어디인가
   ↓
모델이 사고한다 — 말귀·판단·길 선택·표현. 전부 모델의 것.
   ↓
T5가 실행한다 — 위험한 순간만 경계에서 멈춰 승인받고
   ↓
결과가 원장에 남고, 답은 그 사실에서만 나오고
   ↓
다음 턴이 이어받는다
```

T5는 이 순환에서 모델의 **감각기관 · 손발 · 기억 · 양심(경계)**이다. 뇌가 아니다.
뇌를 흉내 내거나 뇌를 규칙으로 묶는 순간 모델은 멍청해진다(v3.1 §24, 실측 반복 확인).

---

## 순환의 칸 (보고 단위)

1. 원문 보존
2. 현재 맥락 / 대상
3. selfState / 가능한 손발
4. 외부 연결 현실
5. 위험 / 승인 경계
6. 실행 원장 / calledWith
7. 실제 한 일 / 못 한 일
8. 다음 턴 승계
9. 사용자에게 보이는 답변 — **이 칸만은 "채웠다"로 답하지 못한다. "도달을 확인함 / 안 함"으로만 답한다**
   (커널이 완벽해도 화면이 안 그리면 사용자에겐 없는 것이다)

### 칸이 비면 이렇게 된다 — 2026-07-27 하루의 실측 다섯

| 빈 칸 | 실제로 일어난 일 |
|---|---|
| ⑥ calledWith 없음 | 모델이 `메모5.md` 에 쓴 목록과 "이렇게 적었어" 보고가 **세 줄 전부 달랐다.** 원장·파일은 일치, 답변만 창작 |
| ③ "승인 후 T5 가 이어서 실행한다" 없음 | 파일 3개를 정확히 찾아 놓고 `cp` 명령 세 줄을 사용자에게 치라고 넘겼다 |
| ④ 바깥에 닿는 손 없음 | "너 내 노션 볼 수 있어?" 에 **브라우저 손이 실행 가능인데도** "내용을 복사해서 붙여주면"이라 답했다 |
| ④ 에 **틀린 사실** | "로그인해 둔 화면이면 그대로 보인다"(거짓 — 임시 프로필) → "브라우저에서 Google 로그인만 해주세요"라는 **불가능한 약속**이 나갔다 |
| ⑨ 도달 미확인 | 승인 카드 `previewOf` 가 커널까지 완벽했는데 UI 가 `scope` 를 안 그려 사용자에겐 **고치기 전보다 후퇴**였다 |

다섯 다 모델은 정상이었다. **빈 칸을 모델이 상상으로 메웠을 뿐이고, 가장 값싼 상상이
"사용자에게 시키기"였다.** 모델을 고치려 하지 말고 칸을 채워라.

---

## 모델의 판단 자리를 침범하는 것들 (금지)

- 키워드 분기 (서비스·상황별 if)
- 고정 문구 / 응답 템플릿
- 금지문 주입 ("~하지 마라"로 제어)
- 서비스별 처방 (노션이면 브라우저를 제안하라 식)
- 테스트용 문장 맞추기 (검사를 통과시키기 위한 문구)
- **사실을 분류기에 매달기** — 금지문도 템플릿도 아니지만 같은 병이다.
  실측: `fast_chat` 분류 턴에서 연결 현실을 뺐더니, 오너의 네 시나리오 중 셋이
  그 분류에 걸려 **자기 손을 모른 채** 답했다. 사실 공급 여부를 분류기가 정하면 안 된다
- **경로를 문장으로 처방하기** — "브라우저로 열어서 볼 수 있다" 같은 문장 목록을 줬더니
  라이브 답변이 그 목록과 거의 1:1 이었다. 모델이 판단한 게 아니라 번역한 것이다.
  사실(손 이름·상태)만 주고 길은 모델이 고른다

**긍정 주장도 침범이다.** 구현에 없는 능력을 현실이라고 넣으면("로그인해 둔 화면이 보인다")
모델은 그걸 근거로 못 지킬 약속을 한다. 능력 문장의 부정 주장만 검사하던 게이트의 구멍이었고,
검사로 고정했다(`connector-truth.test.js` — 능력 문장 ↔ 구현 대조).

---

## 보고 형식 (모든 작업 보고에 적용 — 오너·Codex 합의)

```
이번 변경은 T5-모델 운영 순환의 어느 칸을 채웠는가?
  (칸 번호와 무엇을 채웠는지 — "기능을 만들었다"가 아니라 "어느 칸이 더 정확해졌다")

모델의 판단 자리를 침범한 부분은 없는가?
  (위 금지 목록 대조 — 있었으면 있었다고 쓰고 어떻게 제거했는지)

⑨ 도달 확인: 했음(어느 표면) / 안 했음
```

완료 판정은 언제나 나눠서: 코드 기준 / 게이트 기준 / 실제 UI / 실제 대화 /
실제 외부 서비스 연결 / 라이브 미검증 / 잔여 있음.

**실사용자가 지적한 것은 최상위 증거다. 방어하지 말고, 어떤 불변식으로 바꿀지 먼저 생각한다.**

---

## 판단 기준 한 줄

> **이 작업은 순환의 어느 칸을 채우거나 더 정확하게 만드는가?
> 아니면 모델의 판단 자리를 침범하는가?**

전자면 T5답다. 후자면 기능이 늘어도 실패다.

---

## 말귀의 정의 (오너 원문, 2026-07-27 — 요약·재작성 금지)

> 말귀는 사용자의 말을 알아듣는 기능이 아니라, **사용자의 말이 실제 도움으로 이어지게
> 만드는 운영 능력**이다.

범위 열 가지(오너 원문 그대로):

1. **발화 이해** — 사용자가 실제로 뭘 원하는지. "정산 자료 봐줘"가 파일 찾기인지 요약인지,
   "노션 볼 수 있어?"가 능력 질문인지 연결 요청인지, "그거 다시 해줘"의 그것이 무엇인지.
   화난 말, 애매한 말, 생략된 말까지.
2. **대화 흐름 이해** — 방금 찾은 파일, 방금 막힌 연결, 방금 승인 기다리는 일, 방금 사용자가
   고른 방향, 이전에 실패한 방식, 사용자가 싫어한 응답 방식.
3. **현재 목적과 사용자 입장 이해** — 사용자는 경로가 아니라 자료를 보고 싶은 것. API 연결이
   아니라 자기 일을 맡기고 싶은 것. 오류 설명이 아니라 다음 길을 원하는 것. 개발자가 아니라
   답답해서 도움을 청하는 사람일 수 있음.
4. **가능한 손발 이해** — 웹·브라우저·터미널·파일·프로세스·채널·커넥터·MCP/CLI/API,
   준비 중인 손과 연결하면 가능한 손까지. 이걸 모르면 모델은 "못 해요"라고 하거나 떠넘긴다.
5. **연결/권한/위험 상태 이해** — 연결됨·연결 필요·설정 필요·권한 필요·planned·disabled,
   읽기·쓰기·전송·삭제·결제. 사용자는 권한 상태를 말하지 않고 그냥 "해줘"라고 말하기 때문이다.
6. **실행 판단 이해** — 읽기는 자연 진행, 쓰기/전송은 승인, 비밀/세션은 보호, 위험하면 멈춤.
   그래도 기능을 제거하지 않고 안전 경계로 다룸.
7. **실행 결과 이해** — 도구를 불렀는가, 성공했는가, 어디까지 봤는가, 어떤 인자로 실행했는가,
   답변이 원장과 일치하는가. 이게 없으면 "했다고 말했는데 안 함"이 나온다.
8. **복구와 다음 길 이해** — 다른 손으로 시도, 범위 바꾸기, 연결 필요 안내, 사람이 해야 하는
   최소 단계만 요청, 실패 이유와 다음 길을 같이 말하기.
9. **다음 턴 승계** — "거기서 다시 찾아봐", "아까 그 파일", "그럼 연결해줘". 이걸 못 하면
   사용자는 매번 처음부터 설명해야 한다.
10. **응답 태도** — 사용자를 탓하지 않기, 할 수 있는 걸 먼저 하기, 막다른 답으로 끝내지 않기,
    개발자 용어로 밀어붙이지 않기, 정직하지만 포기하지 않기, 필요한 순간만 묻기.

정의(오너 원문):

> **T5에서 말귀란, 사용자의 발화를 원문 그대로 보존한 뒤, 대화 흐름·현재 목적·가능한 손발·
> 연결 상태·위험 경계·실행 원장·다음 턴 맥락·응답 태도를 함께 구성해서, 모델이 사용자
> 편에서 가장 자연스럽고 정직하게 사고하고 행동하게 만드는 운영 능력이다.**
>
> 말귀는 L1 intent 만의 일이 아니다. **T5 전체 운영 알고리즘의 품질이다.**
> 모델이 더 똑똑해 보이는 이유는 모델을 억제해서가 아니라,
> T5가 모델 앞에 더 정확한 현실과 기준을 놓기 때문이다.

위 순환의 9칸은 이 정의의 부분집합이다 — 3항(목적·사용자 입장)은 사실로 공급할 것이 아니라
**모델의 판단 자리**이며, 우리 몫은 침범하지 않는 것과 1~9의 현실을 정확하게 하는 것이다.
말귀의 측정은 칸별 검사가 아니라 **실사용 문장 세트의 라이브 통과**로 한다.
