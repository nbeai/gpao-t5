# GPAO-T5 현재 세션 인수인계

- 갱신 시각: 2026-07-29 (Asia/Seoul) · T-cell TG-5A `b7ffa6a` Codex 독립 감사 판정 반영
- 성격: 새 세션이 현재 P-OP 작업을 바로 이어받기 위한 **고정 진입 문서**
- 정본 상태: 이 문서는 계획과 계약을 대체하지 않는다. 최신 실행 보드와 실제 Git 상태가 다르면
  실제 상태를 먼저 확인하고 이 문서를 갱신한다.
- 인계 전담 에이전트: `Linnaeus` (`019fa944-9508-7060-a24c-20fdf92b562b`)
  - 보조 수단이다. 새 세션의 연속성은 에이전트 기억이 아니라 이 문서와 저장소 증거가 보장한다.

## 0. 현재 진실 패킷 (Claude·Codex 공통)

이 절이 아래의 과거 P-OP 상세보다 우선한다. 아래 §5~10은 P-OP를 닫은 과정의 역사 기록이며,
현재 착수 위치를 판단하는 근거로 쓰지 않는다.

- 현재 브랜치: `claude/p-op-1-a-system-view`
- 현재 구현 HEAD: **§0-C 보강 커밋**(직전 감사 기준선 `b7ffa6a`)
- 현재 단계: **TG-5A shadow admission**
- 구현 상태: **종료 행렬 1~9 + §0-C 4건 보강 완료 · Claude 자체 검증**
- 독립 감사 상태: **재감사 대기** — §0-C 4건을 한 단위로 닫아 제출했다(자체 검증을 독립 통과로 쓰지 않는다).
- 현재 테스트 사실:
  - 구현자 자체 검증: **1,329건 통과 · 실패 0** (§0-C 검증 11건 포함)
  - §0-C 축소 재현 4건: 보강 전 `b7ffa6a` 에서 **4/4 실패**, 현재 4/4 통과(worktree 실측)
  - 공식 gate: **BLOCKED 1건** — CPU 53.8s/40s(부하 중). 기준선 무변경.
    이번 변경 A/B: `b7ffa6a` 52.21s ↔ 현재 53.42s — 차이는 새 검사 11건의 시험 자체 비용.
- TG-5B 진입: **금지**. 조용한 환경 공식 gate PASS + Codex 독립 재감사 뒤에만 진입한다.
- P-OP-7: 최종 PASS·오너 승인 완료. C·D·E-1 지정 외부계정 검증은
  `OUT_OF_SCOPE_BY_OWNER`이며 현재 차단으로 되살리지 않는다.

### TG-5A 종료 행렬 — 현재 상태

증거: `evidence/tcell/tg-5/TG-5A-CLOSURE-MATRIX-EVIDENCE-2026-07-29-ko.md`(행렬 1~9) ·
`evidence/tcell/tg-5/TG-5A-AUDIT-CLOSURE-EVIDENCE-2026-07-29-ko.md`(§0-C 4건)
배선 선언: `T5-TG-5A-CLOSURE-PRE-WIRING-DECLARATION-…` · `T5-TG-5A-AUDIT-CLOSURE-WIRING-…`

| # | 종료 조건 | 상태 |
|---|---|---|
| 1 | 누적 원장이 아니라 고정 시간창의 턴 사실만 | **닫힘**(자체 검증) |
| 2 | 지시 관계 3값 · 같은 지시는 충돌 아님 · **자연어 반대 뜻 포함** | **닫힘** — FACT_ATOMS 결합 + correction 지속(§0-C-2) |
| 3 | 모델 전/계획 뒤 역할 분리 · 정규식 추정 비권한 | **닫힘** |
| 4 | pending ≠ grant · **실제 버튼으로 부여·재사용·철회** | **닫힘** — [계속 허용]·`/grants`·철회(§0-C-3) |
| 5 | 웹·채널·승인·거절 공통 준비 경계 | **닫힘** |
| 6 | anchor 저장 · **project = 실제 확정 자리**(모르면 null) | **닫힘** — currentPlaceOf 단일 원천(§0-C-1) |
| 7 | registry 실행 전/후 바이트 불변 | **닫힘** |
| 8 | A2 무grant 는 정확히 권한 사유 | **닫힘** |
| 9 | 반대시험 묶음 · **저장 실패의 정직한 승계** | **닫힘** — ENOENT 만 부재, 그 외 degraded(§0-C-4) |
| 10 | 조용한 환경 공식 gate PASS + 문서 동기화 | **미완 — 유일한 잔여**(CPU, 기계 부하) |

§0-C 의 상세 판정·한계(반대 지시의 다음 턴 유효, 원자 8개 근거, persist 는 TG-5C 등)는
`TG-5A-AUDIT-CLOSURE-EVIDENCE` §6 에 있다 — 재감사는 그 절부터 보면 빠르다.

### 역할별 다음 행동

- **Claude 구현선**: §0-C 4건 제출 완료 — 재감사 결과를 기다리며 비중첩 준비만 한다(감사 대상
  파일 비수정). 남은 것은 조용한 환경 gate 재측정 하나. TG-5B·다른 T-cell 영향 단계 착수 금지.
- **Codex 감사선**: 같은 파일을 수정하지 않는다. 보강 전 전체 행렬을 확정하고, 제출 뒤 생산 경로·
  반대시험·공식 gate를 한 번에 판정한다. 새 인접 결함을 뒤늦게 나누어 전달하지 않으며,
  감사 응답에는 문제·재현·영향·위반 계약·종료 조건만 싣고 구현 해법은 제시하지 않는다.
- **오너**: 기술 범위를 중재하지 않는다. 제품 철학·우선순위·실제 권한 결정만 맡는다.
- **새 세션**: 먼저 실제 Git 상태를 확인한다. HEAD가 `b7ffa6a`보다 앞서면 새 diff와 증거를 읽고,
  이 절을 갱신하기 전에는 완료·봉인·TG-5B 진입을 선언하지 않는다.

### 0-C. Codex 독립 감사 판정 — `b7ffa6a` (2026-07-29)

**판정: BLOCKED.** 배선 자체와 shadow 영향 0은 확인됐지만, 시험의 좌표와 실제 인간 사용의
좌표가 아직 결합되지 않았다. 이 상태로 TG-5B를 열면 안전을 뚫는 쪽보다 **대부분 입장하지 못해
T5가 경직되는 쪽**으로 실패할 가능성이 높다.

#### 한 번에 닫아야 할 4개 종료 조건

1. **실제 project 신분**
   - 현재 `ctx.workspaceId`와 관찰 `anchor.project`가 모두 `SessionStore.dir`이다.
   - 기본값은 `~/.local/state/gpao-t5/sessions`이므로 서로 다른 실제 프로젝트도 같은 project로 보인다.
   - 파일·웹·서비스·대화 등 실제 활성 대상에서 안정적 project/workspace 신분을 만들고,
     서로 다른 프로젝트 두 개의 생산 관통으로 격리를 증명한다.

2. **자유문 경계와 현재 지시의 의미 결합**
   - 추출 모델은 `validWhen`/`invalidWhen`을 자유문으로 쓰지만 admission은 문자열 완전 일치만 본다.
   - 독립 재현: `파일 읽기에 실패했을 때`는 실제 사실 `실패 직후`와 의미가 같아도
     `boundary_not_satisfied`; `보낼 땐 확인하지 마`는 기존 원리의 반대여도 `unknown`.
   - 정규식 목록을 늘리지 않는다. 모델이 만든 경계를 OS가 아는 구조화 fact id/atom에 결합하거나,
     근거 참조가 있는 의미 판정을 단일 계약으로 둔다. 같은 뜻·반대 뜻·무관한 뜻을 실제 어댑터로 관통한다.

3. **실제 bounded grant 생산·철회·일반 대상 신분**
   - 모든 대화 승인 생산자는 현재 `grantScope.kind:'once'`만 만든다. `session/persist` 소비는
     helper 단위 시험에서만 만들어졌고 실제 사용자 경로에는 없다.
   - 대상도 `sendArgs[action].target` 중심이라 파일 경로·명령·기타 A2 행동은 grant key를 만들 수 없다.
   - 사용자 표면에서 범위 있는 부여 → 소비 → 다음 턴 재사용 → 철회/만료를 실제 버튼으로 관통하고,
     도구 종류가 아니라 공통 대상 신분 계약으로 묶는다.

4. **확인 원장 오류의 정직한 승계**
   - `ConfirmationStore.#all()`이 ENOENT뿐 아니라 권한 오류·디렉터리·손상 줄까지 전부 빈 원장으로 삼킨다.
   - 독립 재현에서 읽기 불능인데도 admission snapshot이 `status:"ok"`였다.
   - ENOENT만 “아직 없음”으로, 읽기/손상은 `degraded`로 승계하고 기존 바이트를 보존한다.

#### 독립 검증 사실

- 대상 T-cell 검사 48건: 통과.
- 전체 회귀 1,318건: 통과.
- 공식 gate: 기능 항목 전부 통과, CPU **42.0s/40s**로 BLOCKED.
- 제품 코드 수정: 0. 이 절은 감사 판정과 재개 지점만 기록한다.

### 0-B. 종료 행렬 보강 세션이 남기는 사실 (2026-07-29 후반)

§0-A는 **그 이전 세션**의 기록이다. 아래가 더 새로운 실측이며, 충돌하면 이 절이 우선한다.

#### 게이트 CPU — A/B 3쌍 교차 실측 (§0-A (1)을 갱신)

`git worktree add --detach /tmp/t5-ab-base 68463b7` 로 같은 머신에서 연속 교차 측정했다.

| 회차 | A = 기준선 `68463b7` (1,306건) | B = 현재 (1,318건) | B − A |
|---|---|---|---|
| 1 | 51.36s | 48.42s | −2.94 |
| 2 | 51.63s | 53.01s | +1.38 |
| 3 | 52.05s | 48.67s | −3.38 |
| **평균** | **51.68s** | **50.03s** | **−1.65** |

- **§0-A가 말한 내 몫 +4.7s는 회수됐다.** 검사를 12건 더 하면서도 기준선보다 평균 1.65s 빠르다.
  방법: registry 읽기 캐시(mtime+크기) · 세포 0건 조기 종료 · 확인/권한 원장 지연 공급.
- **절대값 ~50s는 기준선 코드도 같다** → 초과의 지배적 원인은 기계 부하다.
  같은 스위트가 실행 보드 동결 manifest(`6ce88bf`)에서는 **39.2s**였다.
- 회차 편차가 ±2s이므로 **1회 측정으로 방향을 말하지 않는다.** 3쌍을 교차한 이유다.
- 다음 세션은 이 표를 근거로 "코드가 게이트를 넘겼다"고 말하지 않는다. 조용한 환경 재측정이 종료 조건이다.

#### 이번 보강에서 구조적으로 확인한 것 (다음 세션이 다시 파지 않도록)

1. **admission이 도는 모든 자리는 실행 앞이다**(모델 앞 · 계획 뒤 · 승인 소비 뒤). 그래서
   "이번 턴이 만든 영수증"은 구조적으로 항상 0건이고, 그 창을 만들면 영원히 비는 죽은 코드가 된다.
   사실 창은 **직전 턴 하나**로 두었다. 실행 뒤 판정이 실제로 필요해지면 TG-5B에서 만든다.
2. **제품의 `grantScope.kind`는 `once|session|persist`다.** admission이 요구하던 `'bounded'`는
   제품에 없던 어휘였고, 그래서 어떤 실제 승인도 A2 계획 역할을 열 수 없었다. 지금은
   `session/persist`만 소비 시점에 bounded로 승격한다. **`once`는 소비돼도 권한이 아니다.**
3. **범위 미상(anchor 없음) 세포는 막지 않는다** — 의도된 계약이다(과잉 차단 금지).
   명세 §6이 막는 것은 "경계를 **넘는** 조회"이지 "경계가 없는 것"이 아니다. 이 줄이 뒤집히면
   legacy 이관분과 anchor 이전 세포가 전부 막힌다.
4. **`mail.send`는 demo에서 `executable:false`다.** A2 관통 시험에는 `slack.post`를 써야 실제
   A2 계획이 선다. 옛 시험이 사유 4개를 허용한 진짜 이유가 이것이었다 — A2 턴을 만든 적이 없었다.

#### 필독 문서에서 이번에 끌어온 제약 (놓치기 쉬운 것)

- **T-cell 명세 §19**: `turn hot path 추가 동기 CPU p95 5ms 이하`가 정본 수치로 이미 있었다.
- **v3.1 §19.1 L2**: 슬라이스 완료 조건이 셋이다 — 수정 전 실패 **건수** · 라이브 실측 ·
  **`deps.X ?? demoY()` fixture 폴백 전수 확인**.
- **v3.1 §19.1 L3**: TG-5A는 고위험 5종 중 **권한 경계·기억 승격** 둘에 걸린다(깊은 감사 대상).
- **작업지시서 §6**: TG-5A는 shadow라 사용자 문장이 다르게 끝나지 않는다 →
  **슬라이스 완료로 보고하지 않는다.** TG-5 단계의 구현 단위다.

### 0-A. 이전 세션(Claude)이 남긴 인계 사실 — 2026-07-29 전반

§0의 감사 판정과 10개 종료 행렬이 **정본**이다. 이 절은 그 행렬을 닫으러 들어오는 새 세션이
같은 함정을 다시 밟지 않도록 **실측 사실과 지도**만 남긴다. 판정을 바꾸지 않는다.

#### (1) 게이트 CPU — 정확한 실측과 원인 특정

같은 부하 조건에서 임시 worktree 로 A/B 측정했다(방법: `git worktree add --detach /tmp/… 1a84495`).

| 대상 | 테스트 | CPU |
|---|---|---|
| TG-4 봉인 기준선 `1a84495` | 1,284건 | **42.9s** |
| 현재 `6d214ad` | 1,306건 | **47.6s** |

- **기준선 코드도 40s 를 넘는다** → 초과의 지배적 원인은 기계 부하다(같은 스위트가 이날 오전
  조용한 상태에서 21.9~22.7s 였다).
- 그 위에 **내 몫 +4.7s 가 실재한다.** 새 시험 파일 자체는 CPU 0.34s 로 무시할 수준이므로,
  **+4.7s 는 모든 서버 시험의 매 턴마다 admission 준비가 도는 비용**이다
  (`registry.load()` + `confirmationStore.snapshot()` + `grantSnapshotFromSession`).
- **다음 조치(미착수)**: 세포가 0건인 세션에서 매 턴 저장소를 읽지 않도록 서버 수명 캐시나
  조기 종료를 넣는다. 기준선 40s 는 건드리지 않는다.
- 조용한 환경 재측정은 여전히 종료 조건이다.

#### (2) 이번 세션에서 내가 **잘못 보고한 것**(정정)

새 세션은 아래를 사실로 인용하지 말 것.

- ❌ "게이트 초과는 openclaw 93.6% 때문" — 순간 `ps` 값 하나로 원인을 단정했다. 오너 확인 결과
  openclaw 는 켜져 있지 않았다. **부하 주장은 반드시 A/B 측정으로만 한다.**
- ❌ "stash 로 봉인 기준선과 A/B 했다"(3차 보고) — 이미 커밋된 상태라 stash 가 비어
  **같은 코드를 두 번 측정**했다. 위 (1) 표가 유효한 측정이다.

#### (3) T-cell 코드 지도 (TG-0~5A)

| 파일 | 역할 |
|---|---|
| `src/kernel/l5-growth/tcell-core.js` | TCellCore 계약·3축 불변식·`validateTCell`(실패는 quarantine) |
| `src/kernel/l0-evidence/tcell-observation.js` | ObservationEvent 계약·§5.1 생성자(관찰자가 이 통로만 씀) |
| `src/kernel/l5-growth/tcell-replay.js` / `tcell-replay-engine.js` | replay 계약 / 판정·전이. 공개 통로는 **`transitionCell` 하나** |
| `src/kernel/l5-growth/t-sphere.js` | TSphere 계약(TG-6 에서 소비) |
| `src/runtime/tcell-extractor.js` | EvidenceBundle·모델 추출·의미 중복 수렴 |
| `src/runtime/model-provider.js` `buildExtractionMessages` | **추출 전용 메시지 경계**(일반 조립과 분리) |
| `src/surface/tcell-store.js` | 관찰자(JSONL·0600·중복 방지·`getByRefs`) · `TCellRegistry` · `ConfirmationStore` · `grantSnapshotFromSession` · `importLegacyMemory` |
| `src/kernel/l1-intent/turn-facts.js` | **턴 사실 조립기** — admission 의 판단 재료 생산(이번 세션 신규) |
| `src/kernel/l1-intent/tcell-admission.js` | 입장 판정 · `buildAdmissionSnapshot`(비동기 저장소→불변 동기 조회기) · `admitFromSnapshot` |
| `src/kernel/turn.js` | admission 호출 위치 = **intent 뒤 · 첫 모델 호출 앞**. 결과는 `result.principleTrace` 로만 |
| `src/surface/server.js` | `ctx.admissionSources`(registry·observer·confirmation·grant) 공급 |

검사: `test/tcell-{contracts,observer,registry,extractor,extraction-wire,replay-engine,admission,no-orphan-contracts}.test.js`
증거: `docs/03-verification/evidence/tcell/tg-0…tg-5/`

#### (4) 반드시 유지해야 할 계약(깨면 감사에서 즉시 잡힌다)

- **판정은 세 값**이다: 통과 / 실패 / `insufficient_evidence`. 자료 없음은 통과가 아니다.
- **내용은 조회로 확인**한다. 호출자가 준 객체·불리언(`confirmed:true`, `passed:true`)을 믿지 않는다.
- **상태는 한 계단씩**, `rolled_back`·`quarantined` 는 자동 부활 없음. 판정·적용 두 곳에서 막는다.
- **모르는 권한은 저위험이 아니다** — `tierKnown:false` 는 A0 가 아니라 `authority_tier_unknown`.
- **경계 판정 `unknown` 은 입장 근거도 단독 거절 근거도 아니다**(과잉 차단 금지).
- **trace 에는 사유 코드만** — 사용자 원문·비밀이 들어갈 자리를 만들지 않는다.
- TG-5A 는 **shadow**: 모델 메시지·도구 스키마·호출 수·실행·답·registry 가 admission 유무와 무관하게
  동일해야 한다. 다른 것은 `principleTrace` 뿐.

#### (5) 이 세션에서 반복된 내 실패 패턴 — 새 세션은 착수 전에 이걸 먼저 읽을 것

TG-0~5A 에서 `feat` 5건에 `fix` 12건이 났고, 감사 왕복이 단계마다 2~5회였다. 원인은 하나다:
**확인하지 않은 것을 확인된 것으로 취급했다.** 다섯 얼굴로 반복됐다.

1. **없음을 통과로 읽음** — 사례 0건 suite 통과, 안 돌린 사례 통과, baseline 없는 비교 통과.
2. **계약의 모양만 만들고 결합하지 않음** — `assertCompressionSafe` 를 통합 검증이 안 부름,
   wake/추출이 생산 경로 미연결, `turnId` 세는 계약을 만들고 `turnId` 를 안 채움,
   `importLegacyMemory` 를 아무도 안 부름.
3. **검사가 제품이 지나는 길을 안 지남** — 가짜 receipt id, `tcellExtract` 를 직접 읽는 가짜 모델
   (실제 어댑터엔 `user:""` 가 갔다), 그리고 최악: **"커널이 admission 을 부르지 않는다"를
   단언해 미배선을 성공 조건으로 고정**했다.
4. **지속 저장의 실패 모드를 안 봄** — append 실패 후 영구 소실, 재시작 중복, 손상 덮어쓰기,
   동시 저장 19/20 소실, 읽기 실패를 파일 없음으로.
5. **숫자·확신이 권한을 엶** — 관찰 수만으로 global 반경, 확인 표식을 성숙도로, 점수로 계단 건너뜀.

**대응 절차(반드시 지킬 것)**

- 착수 전에 세 줄을 적는다: `이 계약을 소비하는 지점은 ___ / 그 지점을 지나는 검사는 ___ /
  지금 배선하지 않는다면 이유와 시점은 ___`. 세 번째가 비면 착수하지 않는다.
- **지적된 인스턴스만 고치지 말고 같은 모양을 전부 훑는다.** 이번 세션의 마지막 두 왕복이
  정확히 이걸 안 해서 났다(확인은 조회로 바꾸고 옆의 `executions` 는 주장으로 뒀다).
- **제출 전 전수 점검 출력을 증거에 첨부한다** — 필드별 `조회/주장`, 같은 사실의 판정 지점 수.
- 규율 문서: `docs/03-verification/T5-IMPLEMENTER-SELF-CHECK-2026-07-29-ko.md`
- 기계 강제: `test/tcell-no-orphan-contracts.test.js` — T-cell export 는 소비되거나 **유예 원장에
  사유·시점**이 적혀야 한다. 유예 원장이 곧 "만들었지만 안 붙인 것"의 정직한 목록이다.

#### (6) 알려진 비차단 잔여

- `baseline`/`candidate` 측정값은 아직 호출자 공급이다(TG-4 감사에서 공개된 항목).
- `importLegacyMemory` 미배선 — **제품 공백**(기존 기억이 이관되지 않음). 유예 원장 기록됨,
  TG-2 통합 배선의 첫 항목.
- `explainAdmission` 미소비 — 사용자 표면은 TG-5C.
- `ConfirmationStore` 쓰기 경로는 구현돼 있으나 확인 UI 는 TG-5C 몫이라 현재 기록 0건일 수 있다.

#### (7) 새 세션 첫 명령

```bash
cd /Users/jyp/Developer/t5-p-op   # ← 작업대. gpao-t5 는 worktree 기반 폴더이니 건드리지 않는다
git log --oneline -3 && git status --short
npm test                          # 전체 회귀
node scripts/gate.mjs             # 조용한 상태에서만 의미 있는 측정
```

## 1. 새 세션의 첫 행동

1. `AGENTS.md`의 필수 문서 순서를 읽는다.
2. `GPAO-T5-INDEPENDENT-AUDIT-AND-COLLABORATION-CONTRACT-2026-07-29-ko.md`를 읽고
   현재 차단/지정 후속/관찰 분류와 재개봉 조건을 적용한다.
3. `docs/03-verification/T5-OPERATOR-HARNESS-EXECUTION-BOARD-2026-07-28-ko.md`와 해당 증거를 읽는다.
4. `git status`, 최신 커밋, 원격 동기화, 미커밋 파일과 현재 작업자를 확인한다.
5. 이 문서의 커밋 기준선보다 새 변경이 있으면 diff와 새 증거를 먼저 감사해 상태를 갱신한다.

## 2. 현재 기준선

- 작업 브랜치: `claude/p-op-1-a-system-view`
- 최신 기능 구현 기준선: `6d214ad`
- P-OP-7 최종 판정·오너 승인 기준선: `7c75cdc`
- 현재 `HEAD`는 시작 시 `git status`와 최신 커밋으로 확인
- 핵심 구현 커밋:
  - `5413e45`: 도구 실행 뒤 런타임 현실을 한 함수로 다시 조립
  - `85a503a`: 모델 입력과 사용자 화면의 다음 길을 같은 사실 경계로 통일
  - `3ea3b1b`: 한 대화에 살아 있는 승인 요청 하나, 이전 승인 중복 실행 차단
  - `2e71665`: 독립 감사·협업 절대 계약 연결
  - `88c5a79`: 대화를 한 함수로만 그린다 — 서버 `activePendingIds` 가 승인 상태의 단일 진실
  - `4350635`: 고른 자리와 나머지 후보를 함께 다음 턴으로(사실 손실 차단)
  - `32e7718`: 마지막 결과의 현재성과 승인 활성성을 분리
  - `ec0b176`: 아직 안 고른 자리는 자리가 아니다(`place_candidates`) · 새 발화가 이전 승인 무효화
  - `4523f36`: 끝난 일은 끝났다고 남긴다(`recentOutcome`) · 완료 시 목표 명시 해제 · **G 봉인**
  - `5180eb0`: 중단한 것은 끝난 것이 아니다(`멈춘이유` 를 완료 판정에 연결)
- 현재 검증 수치: 구현자 자체 검증 **1,306건 통과**. 공식 서버 gate는
  **BLOCKED**(CPU 53.3s/40s, 구현자 측 측정). Codex 표적 독립 재검증은 36건 통과.
  이 수치는 TG-5A 독립 PASS가 아니며, 조용한 환경의 공식 gate 재측정이 종료 조건이다.
- P-OP-7: **최종 PASS · 오너 JYP 윤 승인 완료**. 인간 사용자 테스트 진입 허용.
  설치 패키지 제작은 인간 사용자 테스트와 후속 보강 뒤까지 보류.
- `32e7718`의 `Dual-Role` 구간은 독립 감사와 반대시험을 마쳤고 G 봉인에 포함됐다.
- 게이트 CPU 기준선은 **40s 그대로**다. 한때 46s 로 올렸다가 되돌렸다 — 그 40.2s 측정은
  시험 서버를 띄운 채 잰 것이었고, 조용한 상태에서는 31~33s 다. 게이트가 자기 실패 때문에
  느슨해지는 모양을 만들지 않는다.

## 3. 현재 작업 소유권

- T-cell 구현자: Claude 단일 구현선
- T-cell 독립 감사자: Codex
- 그 밖의 영역: 공통 계약 확정 뒤 Codex가 에이전트 worktree 병렬 개발·감사·통합
- 오너: 제품 목적·철학·우선순위·실제 권한 판단
- 현재 Claude·T-cell 전용 파일:
  - `src/kernel/l0-evidence/tcell-observation.js`
  - `src/kernel/l1-intent/tcell-admission.js`
  - `src/kernel/l1-intent/turn-facts.js`
  - `src/kernel/l5-growth/tcell-core.js`
  - `src/kernel/l5-growth/tcell-replay.js`
  - `src/kernel/l5-growth/tcell-replay-engine.js`
  - `src/kernel/l5-growth/t-sphere.js`
  - `src/runtime/tcell-extractor.js`
  - `src/surface/tcell-store.js`
  - 위 계약의 관련 T-cell 테스트
- 공유 접착 파일 `src/kernel/turn.js`, `src/surface/server.js`, `src/runtime/model-provider.js`의
  T-cell 배선 구간도 TG-5A 종료 전에는 Claude가 구현하고 Codex가 읽기 전용으로 감사한다.
- 단계 상태: TG-0~TG-4 기반 구현이 존재하며 TG-4는 독립 감사 봉인. TG-5A는
  `6d214ad` 구현 뒤 **독립 감사 차단 상태**다.
- 현재 병렬 개발선: 자동화 AC-1 공통 계약·migration이 정본에 편입됐고 독립 감사 통과.
- AC-1 정본 편입: `a40a56b` → `c357cc7` → `3f23c93` → `357ae5d` → `c58bdea`.
- 독립 증거: `docs/03-verification/evidence/automation-core/ac-1/CODEX-INTEGRATION-AUDIT-2026-07-29-ko.md`
- 다음 병렬 작업: Claude의 T-cell TG-0 단일 구현선 + Codex가 통합하는 Automation Closure 후속 개발선.
- C·D·E-1 외부 계정 라이브는 OUT_OF_SCOPE_BY_OWNER이며 다시 현재 차단으로 올리지 않는다.
- T-cell은 에이전트 구현으로 분할하지 않는다. 다른 개발은 협업 계약 §13의 병렬 개발을 기본값으로 쓴다.

## 4. 제품 방향과 비감축 원칙

- T5는 일반 사용자가 개발자가 되지 않아도 되는 AI 운영체제다.
- 모델을 대본과 금지문으로 강제하기보다 사용자 원문, 현재 대상, 가능한 손발, 권한, 실행 사실,
  실패와 다음 길을 정확히 모델 앞에 놓는다.
- 안전은 능력 삭제가 아니라 승인, 원장, 보호 영역, 복구로 만든다.
- **복수 후보 계약**(2026-07-29): 조용히 확정하지 않는다. 짧게 선택을 요청하거나, 위험이 낮고
  근거가 충분하면 선택 근거와 다른 후보를 공개한다. **변경·전송 전에는 대상이 반드시 확정**돼야 한다.
- 서비스별 예외를 계속 붙이지 않고 하나의 공통 현실·권한·승계 구조가 여러 현상을 해결하게 한다.
- 사용자의 피로나 일정 때문에 완료 기준을 낮추지 않는다.
- 반대로 주변 문제로 통과 범위를 무한 재개봉하지 않는다.
- 감사는 관련 범위의 문제·재현·영향·위반 계약·종료 조건을 한 번에 제출하되 구현 해법은 제시하지 않는다.
  구현선이 전체 구조를 보고 해법을 도출하며, 감사선은 그 결과를 독립 검증한다.
- **감사 완결성 절대 원칙(2026-07-29 오너 지시)**: 대표 사례를 순차적으로 던져
  `수정 → 재감사 → 추가 수정`을 만들지 않는다. 보강 요청 전에 계약 전체의 필드·교차 불변식·참조 존재·
  승인 증거·권한 우회·손상 입력을 감사 행렬로 한 번에 완성한다. 같은 작업 단위에서 인접 결함을 두 차례
  뒤늦게 추가하면 감사 범위 설계 실패로 기록하고, 추가 수정 전에 전체 행렬부터 다시 작성한다.
- **감사 비정체 원칙(2026-07-29 보정)**: 감사는 구현 착수 허가제가 아니다. 재현 가능한 현재 차단만
  그 결함에 직접 의존하는 경로를 멈춘다. 자체 검증을 통과한 비중첩 다음 단계는 `독립 감사 대기`를 밝히고
  계속 진행한다. 지정 후속·관찰은 개발을 멈추지 않으며, 감사 대기가 병목이면 감사자가 즉시 범위를 줄여
  판정하거나 별도 검증선에서 병렬로 끝낸다.

## 5. P-OP A~H 현재 상태

| 시나리오 | 현재 상태 | 남은 핵심 |
|---|---|---|
| A. 로컬 PC 진단 | **통과·봉인** | — |
| B. 자료 찾기·정리 | **통과·봉인** | — |
| C. 낯선 외부 업무 | 부분 통과 | API 키 실데이터, 앱 등록 OAuth, 실패 변수 |
| D. OAuth/MCP | 부분 통과 | 전용 계정 실제 폐기·재인증, 해제, 동의 취소 UI |
| E. 범위 있는 API | **E-0 통과 · E-1 대기 · E-2 배포 과제** | E-1 실제 Google 연결(시험 계정 준비 시에만 재개) · E-2 T5 관리 OAuth 클라이언트(설치 패키지 전) |
| F. 외부 전달 | **통과(축소형 라이브)** | 채널 답장 재확인·반복 전송 → P-OP-4/6 |
| G. 중단·승계 | **통과·봉인** | — (재현 시에만 재개봉) |
| H. 기억·성장 | **통과(P-OP 사용자 통제)** | T-cell 엔진은 TG-0(P-OP 봉인 후) |

전체 상세 상태의 단일 관리처는 실행 보드다. 이 표만으로 완료를 선언하지 않는다.

## 6. G-1A와 G-1B에서 이미 닫힌 것

### G-1A

- 원래 업무 보존
- 승인 전 외부 효과 0
- 동적 MCP 연결과 도구 편입
- 같은 흐름에서 새 도구가 모델 현실에 나타남
- 새 도구 실제 호출
- `web.collect`로 우회하지 않음

증거:
`docs/03-verification/evidence/core-harness/p-op-1/G-1A-CAPABILITY-ACQUISITION-2026-07-28-ko.md`

### G-1B 핵심 실행

- 승인 전 원래 업무와 승인 경계만 보존, 비밀값 없음
- 재시작 뒤 대기 승인과 원업무 복원
- `아까 하던 거 이어줘`에서 사용자 원문 반복 불필요
- 한 대화의 활성 승인 하나
- 이전 승인 실행 0, 새 승인 실행 정확히 1회
- 외부 중복 실행 차단

증거:
`docs/03-verification/evidence/core-harness/p-op-1/G-1B-INTERRUPT-RESUME-2026-07-28-ko.md`

위 범위는 새 안전 위반이나 반대 증거가 없는 한 재개봉하지 않는다.

## 7. 직전 보강 — 닫힘 (`88c5a79`)

문제였던 것:
서버에는 활성 승인이 하나인데 같은 화면에는 이전 승인 카드가 계속 살아 보였다. 눌러도 실행은
되지 않았지만(`찾지 못했어요`), 누르기 전까지 사용자는 어느 버튼이 유효한지 알 수 없었다.

원인:
대화를 **다시 열면** `activePendingIds` 로 옛 카드를 지난 것으로 그렸는데, 실시간으로 받은 결과는
그 자리에 **덧붙이기만** 해서 같은 화면에서만 틀렸다.

고침:
대화 한 벌을 **한 함수로만** 그린다. 턴·승인·거절이 끝나면 서버에서 대화를 다시 받아 통째로
투영하고 화면은 승인 상태를 추측하지 않는다. 승인 구조나 상태 시스템을 새로 만들지 않았다.

함께 피한 함정: `historical` 은 비밀 입력면·기억 제안처럼 **지금 필요한 자리까지 숨긴다.**
전부 과거로 그리면 방금 열린 안전 입력창이 사라진다 — 그래서 **마지막 결과만 현재**로 그린다.

종료 조건 7단계 전부 통과:

```
① 카드 생성 → ② 재시작 → ③ `이어줘` → 옛 카드 즉시 `지난 승인 요청`(승인 1 · 지난 1)
→ ④ 새 카드만 승인 가능 → ⑤ 새로고침 뒤에도 동일 → ⑥ 승인 시 declare 1회
→ ⑦ 테스트 1090건 · 게이트 PASS
```

후속 독립 감사에서 마지막 결과를 현재로 둔 것이 마지막 만료 승인까지 활성으로 보이게 할 수 있는
교차점을 확인했다. 마지막 결과의 현재성은 비밀 입력면·제안에, 승인 활성성은 서버
`activePendingIds`에 각각 맡겼다. 세 반대검증과 전체 테스트·게이트를 통과했다.

**G-1B는 닫혔다.** 외부 중복 실행이나 승계 실패의 실제 반증이 없으면 다시 열지 않는다.

## 8. 현재 분류

### 현재 차단

- **TG-5A 종료 행렬 10번 하나** — 조용한 환경의 공식 gate 재측정. 1~9는 자체 검증으로 닫혔다.
  상세는 이 문서 §0·§0-B를 단일 진입점으로 쓴다.
- 공식 gate CPU 기준선 초과. 기준선을 완화하지 않는다(A/B 실측상 기준선 코드도 같은 수준이다).
- 실행 보드(`docs/03-verification/T5-OPERATOR-HARNESS-EXECUTION-BOARD-2026-07-28-ko.md`)의
  기준선 표기가 `5413e45`로 낡았다 — TG-5A 독립 봉인과 같은 흐름에서 갱신한다.

### 지정 후속

- G 는 **봉인**(2026-07-29). 최초 `4/5 통과` 보고는 틀렸고 감사로 정정된 뒤 전부 닫았다.
- **E 는 3층 분리로 기록**(오너 결정 2026-07-29): E-0 공통 실행기·격리 검증 통과 /
  E-1 실제 Google 연결·읽기·계정 전환 **외부 권한 대기**(시험 계정 준비 시에만 재개) /
  E-2 배포용 OAuth 앱 등록·검증은 설치 패키지 전 별도 배포 과제.
  완료 처리도, 네이버 대체도, E 대기 때문에 전체를 멈추는 것도 하지 않는다.
- **배포 원칙**: 일반 사용자가 Google Cloud 에서 앱을 만들게 하지 않는다. 배포판은 T5 관리
  OAuth 클라이언트 + 사용자는 동의만. 현재 콘솔 안내(issue.steps)는 E-1 개발 시험 전용.
- **F 통과·봉인**(축소형 라이브 2026-07-29): 실발송 정확 1건 · 4자 일치 · 거절/중복/실패/재전달은
  격리 인정. 1회차가 잡은 매듭 둘은 `c9dba32` 로 닫음(두 경로 확정 계약 통일 + 허용 대화 사실 공급).
- **H 통과·최종 봉인**(행렬 9단계 + 구조 보강 3건, 2026-07-29): 모델 후보 제출 · 화면 승격/철회 ·
  재시작 유지/복원 없음(`54ebb9b`) + 통제 채널 분리(실행 등록부 밖, splitModelControlCalls 단일 경계) ·
  기억 수명주기 원장(proposed/confirmed/rejected/rolled_back, 원문 없이 digest) ·
  승격 단일 통로(confirmCandidate)(`08c87fe`·`0572f05`). 재검증 5항목 라이브 통과.
- **바로 다음**: P-OP-4 복구 변수 → P-OP-6/7. E-1 은 환경이 준비되면 그때만 돌아온다.
- 전체 승인 카드 정보 구조와 반복 UX: P-OP-6
- OAuth 실제 폐기·재인증: 전용 계정 준비 시 D
- 실패 주입과 복구: P-OP-4
- 기억 입장·철회와 T-cell: H/P-OP-5 이후
- 모델 습관 비교: P-OP-7 최종 이중 모델 검증

### 원인 미분류 관찰

- **있는 사실을 쓰지 않고 진행**(2건, 아직 같은 뿌리인지 모름):
  ① ~~후보 5곳~~ — **원인 판명·해결**: 모델 습관이 아니라 런타임 투영 충돌이었다(확정 자리 승격).
  ② 자기 도구함 대신 웹으로 감: `노션에 붙어 있는 기능 중 …` 질문에 T5 가 자기 손 20개를 안 보고
  노션 도움말 센터를 웹 검색했다(원장 `web.collect` 2건, 답은 `홈` 한 단어). 그리고 `아무거나 3개`
  요청을 키워드로 번역했다. 두 관찰이 같은 뿌리인지 아직 모른다 — 원인 분리 시험 전까지 새 규칙이나
  `toolbox.query` 같은 손을 만들지 않는다. 상세: `D-NOTION-REAL-USE-2026-07-28-ko.md` 미분류 관찰 절.
- 현재 G 를 붙잡아야 하는 관찰은 없다.
- 새 관찰은 두 번째 재현이나 핵심 통과 무효화 전까지 코드·검사로 확대하지 않는다.

## 9. 가장 좋은 다음 작업 순서

1. ~~G-1B 마지막 승인 상태~~ — **닫힘(`32e7718`)**
2. ~~G 잔여 다섯 시나리오 행렬~~ — **닫힘(`ec0b176` · `4523f36` · `5180eb0`)**
3. ~~G 증거와 실행 보드 봉인~~ — **닫힘**
4. ~~A·B 미봉인 증거~~ — **닫힘(2026-07-29)**
5. ~~E 실행기·선언·격리 검증~~ — **E-0 통과로 기록. E-1 은 외부 권한 대기(시험 계정 준비 시에만 재개), E-2 는 배포 과제**
6. ~~F 실제 외부 전달 라이브~~ — **통과(축소형, 2026-07-29).** 1회차 매듭 둘(`c9dba32` 수정) 포함 증거 봉인
7. ~~H 기억·성장 P-OP 라이브~~ — **통과(행렬 9단계, 2026-07-29).** 1회차 결함 둘(`54ebb9b` 수정) 포함 증거 봉인
8. ~~P-OP-4·P-OP-6·P-OP-7~~ — **완료. P-OP-7 최종 PASS·오너 승인**
9. T-cell TG-0~TG-4 기반 구현·감사
10. **현재: TG-5A 종료 행렬 10건 폐쇄 → 공식 gate PASS → 독립 봉인**
11. TG-5B 이후 영향 단계
12. T-cell 완료와 자동화·스킬·에이전트 통합 뒤 인간 사용자 시험, 그 뒤 설치 패키지

G 잔여 변수는 한 건씩 발견·수정·재보고하지 않는다. 관련 범위를 먼저 끝까지 실행하고 종합 감사한다.

## 10. 재개봉 금지선

- 표현 개선, 더 좋은 추상화, 희귀 조합 상상만으로 통과한 G-1A/G-1B 실행 안전을 다시 열지 않는다.
- 새 서비스별 `if`, 키워드 분기, 답변 대본을 공통 해결책으로 추가하지 않는다.
- 검사 숫자 증가를 진전으로 취급하지 않는다.
- 같은 파일을 Claude와 Codex가 동시에 고치지 않는다.
- 실제 표면·원장·외부 상태 없이 코드와 테스트만으로 라이브 통과를 선언하지 않는다.
- P-OP-6 전체 UX 문제를 현재 G-1B 좁은 보강에 흡수하지 않는다.
- 일정 때문에 검증을 생략하지 않고, 검증 때문에 한 지점에서 종료 조건 없이 머물지도 않는다.

## 11. 새 세션 시작용 여섯 줄

```text
현재 작업: T-cell TG-5A shadow admission — 종료 행렬 1~9 + §0-C 감사 4건 보강 완료·제출.
역할: Claude 단일 구현선, Codex 독립 감사선. 같은 파일을 공동 수정하지 않는다.
현재 판정: 구현자 **자체 검증** 1,329건 통과·실패 0. §0-C 재현 4건 보강 전 4/4 실패 실측.
Codex 재감사 대기(자체 검증을 통과로 쓰지 않는다).
현재 차단: 종료 행렬 10번(조용한 환경 gate 재측정) + Codex 재감사. CPU 는 기계 부하가 지배적
(이번 변경 A/B: 52.21↔53.42s, 차이는 새 검사 11건의 시험 비용). 기준선 40s 무변경.
진입 금지: TG-5B. 조용한 gate PASS 뒤 Codex가 한 번에 재감사해 봉인할 때만 진행한다.
이미 닫힌 범위: P-OP-7 최종 PASS·오너 승인, C·D·E-1 지정 항목은 OUT_OF_SCOPE_BY_OWNER.
병렬선: Automation Closure와 Owner Ops 준비 패키지는 T-cell 파일을 건드리지 않는다.
다음 세션 첫 행동: Git HEAD·diff 확인 → 이 문서 §0·§0-C 증거 확인 → 역할에 맞는 작업만 수행.
```

## 12. 이 문서를 갱신하는 규칙

- **무엇이든 닫히면 그 커밋과 같은 흐름에서 이 문서를 갱신한다.** 낡은 인계 문서는 다음 세션에게
  이미 끝난 일을 다시 시키거나, 없는 차단을 쫓게 만든다.
- 갱신할 것: 기준선 커밋 · 현재 편집 중인 파일 · 현재 차단 · 지정 후속 · 미분류 관찰 · §11 여섯 줄.
- 구현자가 적는 검증 수치는 **자체 검증**으로 표시한다. 독립 감사 확인 전까지 독립 통과로 쓰지 않는다.
- 이 문서는 실행 보드를 대체하지 않는다. 상세 상태의 단일 관리처는 언제나 실행 보드다.
