# GPAO-T5 현재 세션 인수인계

- 갱신 시각: 2026-07-29 (Asia/Seoul)
- 성격: 새 세션이 현재 P-OP 작업을 바로 이어받기 위한 **고정 진입 문서**
- 정본 상태: 이 문서는 계획과 계약을 대체하지 않는다. 최신 실행 보드와 실제 Git 상태가 다르면
  실제 상태를 먼저 확인하고 이 문서를 갱신한다.
- 인계 전담 에이전트: `Linnaeus` (`019fa944-9508-7060-a24c-20fdf92b562b`)
  - 보조 수단이다. 새 세션의 연속성은 에이전트 기억이 아니라 이 문서와 저장소 증거가 보장한다.

## 1. 새 세션의 첫 행동

1. `AGENTS.md`의 필수 문서 순서를 읽는다.
2. `GPAO-T5-INDEPENDENT-AUDIT-AND-COLLABORATION-CONTRACT-2026-07-29-ko.md`를 읽고
   현재 차단/지정 후속/관찰 분류와 재개봉 조건을 적용한다.
3. `docs/03-verification/T5-OPERATOR-HARNESS-EXECUTION-BOARD-2026-07-28-ko.md`와 해당 증거를 읽는다.
4. `git status`, 최신 커밋, 원격 동기화, 미커밋 파일과 현재 작업자를 확인한다.
5. 이 문서의 커밋 기준선보다 새 변경이 있으면 diff와 새 증거를 먼저 감사해 상태를 갱신한다.

## 2. 현재 기준선

- 작업 브랜치: `claude/p-op-1-a-system-view`
- 마지막 원격 동기화 커밋: `2e71665`
- 핵심 구현 커밋:
  - `5413e45`: 도구 실행 뒤 런타임 현실을 한 함수로 다시 조립
  - `85a503a`: 모델 입력과 사용자 화면의 다음 길을 같은 사실 경계로 통일
  - `3ea3b1b`: 한 대화에 살아 있는 승인 요청 하나, 이전 승인 중복 실행 차단
  - `2e71665`: 독립 감사·협업 절대 계약 연결
- 독립 검증 기준선: 테스트 **1090건 통과**, 실제 서버 게이트 **PASS**
- 주의: 위 검증은 `2e71665` 기준이다. 이후 미커밋 변경은 별도 검증 전까지 통과로 간주하지 않는다.

## 3. 현재 작업 소유권

- 기본 구현자: Claude
- 기본 독립 감사자: Codex
- 오너: 제품 목적·철학·우선순위·실제 권한 판단
- 현재 Claude가 수정 중인 파일: `src/surface/web/index.html`
- 다른 세션과 에이전트는 위 파일을 동시에 편집하지 않는다.
- 구현자는 코드·테스트·라이브 증거를 만들고, 감사자는 전체 계획 정합성·증거·다음 순서를 판정한다.

## 4. 제품 방향과 비감축 원칙

- T5는 일반 사용자가 개발자가 되지 않아도 되는 AI 운영체제다.
- 모델을 대본과 금지문으로 강제하기보다 사용자 원문, 현재 대상, 가능한 손발, 권한, 실행 사실,
  실패와 다음 길을 정확히 모델 앞에 놓는다.
- 안전은 능력 삭제가 아니라 승인, 원장, 보호 영역, 복구로 만든다.
- 서비스별 예외를 계속 붙이지 않고 하나의 공통 현실·권한·승계 구조가 여러 현상을 해결하게 한다.
- 사용자의 피로나 일정 때문에 완료 기준을 낮추지 않는다.
- 반대로 주변 문제로 통과 범위를 무한 재개봉하지 않는다.
- 감사는 문제와 함께 권장 해결, 최소 충분 검증, 종료 조건, 최적 다음 순서를 제시한다.

## 5. P-OP A~H 현재 상태

| 시나리오 | 현재 상태 | 남은 핵심 |
|---|---|---|
| A. 로컬 PC 진단 | 통과에 가까움 | 최종 승인 실행 화면 증거 보강 |
| B. 자료 찾기·정리 | 부분 통과 | 여러 후보를 사용자에게 공개 |
| C. 낯선 외부 업무 | 부분 통과 | API 키 실데이터, 앱 등록 OAuth, 실패 변수 |
| D. OAuth/MCP | 부분 통과 | 전용 계정 실제 폐기·재인증, 해제, 동의 취소 UI |
| E. 범위 있는 API | 미검증 | scope, 거부, 계정 전환, rate limit |
| F. 외부 전달 | 코드 기반 | 현재 브랜치 실제 전송 라이브 |
| G. 중단·승계 | 부분 통과 | 지난 카드 표면, 후보 둘, 취소, 새 요청, 감쇠 |
| H. 기억·성장 | 최소 기반 | P-OP 라이브, 이후 T-cell 엔진 |

전체 상세 상태의 단일 관리처는 실행 보드다. 이 표만으로 완료를 선언하지 않는다.

## 6. G-1A와 G-1B에서 이미 닫힌 것

### G-1A

- 원래 업무 보존
- 승인 전 외부 효과 0
- 동적 MCP 연결과 도구 편입
- 같은 흐름에서 새 도구가 모델 현실에 나타남
- 새 도구 실제 호출
- `web.collect`로 우회하지 않음

증거:
`docs/03-verification/evidence/core-harness/p-op-1/G-1A-CAPABILITY-ACQUISITION-2026-07-28-ko.md`

### G-1B 핵심 실행

- 승인 전 원래 업무와 승인 경계만 보존, 비밀값 없음
- 재시작 뒤 대기 승인과 원업무 복원
- `아까 하던 거 이어줘`에서 사용자 원문 반복 불필요
- 한 대화의 활성 승인 하나
- 이전 승인 실행 0, 새 승인 실행 정확히 1회
- 외부 중복 실행 차단

증거:
`docs/03-verification/evidence/core-harness/p-op-1/G-1B-INTERRUPT-RESUME-2026-07-28-ko.md`

위 범위는 새 안전 위반이나 반대 증거가 없는 한 재개봉하지 않는다.

## 7. 단 하나의 현재 보강

문제:
서버에는 활성 승인이 하나지만 같은 화면에는 이전 승인 카드가 계속 살아 있는 것처럼 보인다.
새로고침하면 맞게 보이고, 이전 버튼을 눌러도 실행은 되지 않는다.

판정:
G-1B 실행 안전은 통과했다. 다만 실제 사용자 표면 통과 전 **좁은 현재 보강**이 필요하다.

권장 방향:
서버의 `transcript + activePendingIds`를 단일 진실로 삼아 턴·승인·거절 뒤 현재 대화를 한 공통 함수로
다시 투영한다. 승인 구조나 상태 시스템을 새로 만들지 않는다.

현재 구현 상태:
Claude가 `src/surface/web/index.html`에서 위 방향의 미커밋 수정을 진행 중이다. 인계 세션은 먼저
현재 diff와 작업자 상태를 확인하고, 미완료면 중복 구현하지 않는다.

최소 충분 검증과 종료 조건:

1. 첫 승인 카드 생성
2. 재시작 뒤 `이어줘`로 새 승인 생성
3. 이전 카드는 즉시 `지난 승인 요청`, 승인·거절 버튼 없음
4. 새 카드만 활성
5. 새로고침 뒤에도 동일
6. 이전 승인 실행 0, 새 승인 실행 1회
7. 전체 테스트와 실제 서버 게이트 PASS

이 조건이 나오면 G-1B를 완전히 닫고 다시 파지 않는다.

## 8. 현재 분류

### 현재 차단

- 위 G-1B 지난 승인 카드의 같은 화면 정합성 하나

### 지정 후속

- G 잔여 변수: 후보 둘, 완료된 일, 취소, 무관한 새 요청, 오래된 작업 감쇠
- 전체 승인 카드 정보 구조와 반복 UX: P-OP-6
- OAuth 실제 폐기·재인증: 전용 계정 준비 시 D
- 실패 주입과 복구: P-OP-4
- 기억 입장·철회와 T-cell: H/P-OP-5 이후
- 모델 습관 비교: P-OP-7 최종 이중 모델 검증

### 원인 미분류 관찰

- 현재 G-1B를 추가로 붙잡아야 하는 원인 미분류 관찰 없음
- 새 관찰은 두 번째 재현이나 핵심 통과 무효화 전까지 코드·검사로 확대하지 않는다.

## 9. 가장 좋은 다음 작업 순서

1. G-1B 지난 카드 표면을 위 종료 조건으로 한 번만 닫는다.
2. G의 잔여 다섯 변수를 **하나의 시나리오 행렬**로 실행한다.
3. 현재 차단만 수정하고 G 증거와 실행 보드를 봉인한다.
4. A와 B의 작은 미봉인 증거를 묶어 닫는다.
5. E 범위 있는 API 라이브
6. F 실제 외부 전달 라이브
7. H 기억·성장 P-OP 라이브
8. P-OP-4 복구 변수와 P-OP-6 전체 표면
9. P-OP-7 GPT-5.6sol/Opus 5 또는 Fable 5 이중 검증
10. 이중 검증 PASS 뒤 인간 사용자 시험, 그 뒤 설치 패키지

G 잔여 변수는 한 건씩 발견·수정·재보고하지 않는다. 관련 범위를 먼저 끝까지 실행하고 종합 감사한다.

## 10. 재개봉 금지선

- 표현 개선, 더 좋은 추상화, 희귀 조합 상상만으로 통과한 G-1A/G-1B 실행 안전을 다시 열지 않는다.
- 새 서비스별 `if`, 키워드 분기, 답변 대본을 공통 해결책으로 추가하지 않는다.
- 검사 숫자 증가를 진전으로 취급하지 않는다.
- 같은 파일을 Claude와 Codex가 동시에 고치지 않는다.
- 실제 표면·원장·외부 상태 없이 코드와 테스트만으로 라이브 통과를 선언하지 않는다.
- P-OP-6 전체 UX 문제를 현재 G-1B 좁은 보강에 흡수하지 않는다.
- 일정 때문에 검증을 생략하지 않고, 검증 때문에 한 지점에서 종료 조건 없이 머물지도 않는다.

## 11. 새 세션 시작용 여섯 줄

```text
현재 작업: G-1B의 지난 승인 카드 표면을 서버 activePendingIds 단일 진실로 좁게 보강 중.
이미 통과한 범위: G-1A 전 구간, G-1B 승계·재시작·승인 전 효과 0·중복 실행 방지.
현재 차단: 같은 화면의 이전 승인 카드가 활성처럼 보이는 문제 하나.
지정 후속: G 후보 둘/완료/취소/새 요청/감쇠, 이후 A·B 잔여와 E·F·H, P-OP-4/6/7.
원인 미분류 관찰: 현재 G-1B를 붙잡을 추가 관찰 없음.
다음 작업과 종료 조건: 이전 카드 비활성 표시·새 카드만 실행·재접속 동일·실행 1회·테스트/게이트 PASS 후 G 잔여 행렬로 이동.
```

