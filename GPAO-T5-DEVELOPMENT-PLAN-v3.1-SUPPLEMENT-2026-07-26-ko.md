# GPAO-T5 개발 계획서 v3.1 — 보강안

날짜: 2026-07-26 · 상태: **초안(오너 확정 대기)**

이 문서는 `GPAO-T5-DEVELOPMENT-PLAN-v3.0-2026-07-26-ko.md`를 **대체하지 않고 보강**한다.
v3.0의 §0~§14와 Phase 1~9는 그대로 유효하다. 여기서는 v3.0에 없어서 개발이 흔들렸던
**판정 기준·완료 정의·부채·순서**만 더한다.

상위 기준 순서(절대 원칙 §12)는 그대로다: 사용자 안전 → 오너의 현재 지시 → 최종 개발 계획서 →
개발 절대 원칙 → 세부 스펙 → 구현 편의. **이 보강안은 "세부 스펙" 층에 놓인다.**

---

## 왜 보강이 필요한가 (2026-07-26 실측 근거)

v3.0으로 개발을 시작하려던 시점에 오너가 두 가지를 지적했고, 둘 다 사실로 확인됐다.

**지적 1 — "있긴 한데 부족하다"가 반복된다.**

봉인 완료로 보고한 기능 여덟 개의 실제 상태:

| 봉인한 기능 | 실제 |
|---|---|
| 로컬 파일(`local.file`) | **스텁. 파일을 하나도 안 건드린다** — 그런데 능력 문서엔 "읽고 정리한다"고 적힌다 |
| 웹 자료 수집 | URL 주면 읽기만. 검색 없음, robots.txt 실제 확인 없음 |
| 스킬 lifecycle | 6-상태 기계 완성이나 **실행 경로가 없다**(`canAutoExecute` 항상 false) |
| 자동화(크론) | 돌지만 **실행할 도구가 2개뿐** |
| 채널 | 보내기는 실 HTTP, **받기는 반쪽**(inboundPolicy 미소비) |
| 모델 | 6 provider 지원이나 **스트리밍은 2계열** |
| 기억 | 문자열 매칭 |
| 배포 | tarball 검증까지 |

여덟 개 중 여덟 개가 반쪽이다. 원인은 셋이다.

1. **슬라이스 단위를 "계약"으로 잡았다.** §6.11~§6.30은 계약 봉인이다. 계약이 닫힌 것과 사용자가
   그 기능으로 일을 끝낼 수 있는 것은 다르다.
2. **"후속"을 탈출구로 남용했다.** 봉인마다 후속 목록이 붙었고, 하나씩은 합리적이었으나 누적되니
   전부 반쪽이 됐다. 그 후속들은 **사용자가 쓰다가 바로 부딪히는 것들**이었다.
3. **완료 기준이 "테스트 통과 + 라이브 1건"이었다.** 그건 경로가 한 번 통했다는 증거이지 기능이
   완성됐다는 증거가 아니다.

**지적 2 — 로컬 AI 운영체제의 범위를 좁게 그었다.**

"터미널·파일 조작은 개발자용"이라고 경계를 그었으나 틀렸다. 올바른 경계는
**개발 전문성(디버거·패치·git·샌드박스) vs 로컬 지배력(파일·앱·웹·프로세스)**이고,
로컬 AI 운영체제에게 후자는 선택이 아니라 **정의**다. v3.0 §0 후반이 이미 그렇게 적고 있었다:

> 핵심은 사용자의 요구에 따라 T5가 필요한 외부 도구와 스킬을 찾고, 후보화하고, 연결하고,
> 검증하고, 운용할 수 있는 **자기 구성 능력**이다.

---

## §15. 대체 목표 — 무엇이 되면 끝인가

**목표 문장(정본):**

> **T5 사용자는 T5와 대화하는 것만으로 자기 T5를 확장할 수 있다.**
> 필요한 도구를 찾아 연결하고, 반복 작업을 스킬·자동화로 만들고, 역할별로 다른 모델을 쓰고,
> 로컬 파일·앱·웹을 다루고, 잘못되면 되돌린다. 그 과정에서 설정을 공부하지 않는다.

이것이 되면 사용자가 OpenClaw·Hermes를 쓸 이유가 없다. **기능 수가 같아서가 아니라, 자기 일을
T5 안에서 끝내고 스스로 넓힐 수 있어서다.**

### 사용자 작업 목록 (완성도 판정의 단위)

각 항목은 "T5 안에서 끝나는가"로만 판정한다. 부분 점수는 없다.

| # | 사용자가 하려는 일 | 현재 | 필요한 것 |
|---|---|---|---|
| 1 | 자료를 찾아 정리해 **파일로 받는다** | ✗ | 검색 + 파일 쓰기 + 결과물 전달 |
| 2 | 내 컴퓨터의 파일을 정리·변환한다 | ✗ (스텁) | 파일 손발 + 범위 권한 + 되돌리기 |
| 3 | **메신저로 말 걸어** 일을 시킨다 | ◐ (보내기만) | 채널 수신 실경로 |
| 4 | 반복 작업을 **자동으로** 돌린다 | ◐ (도구 부족) | 손발이 붙은 자동화 |
| 5 | 자주 하는 일을 **스킬로 만들어** 쓴다 | ✗ (실행 경로 없음) | 스킬 실행 경로 |
| 6 | 일마다 **다른 모델**을 쓴다 | ◐ (기계만 있음) | 역할 실사용 + 전 provider 스트리밍 |
| 7 | 필요한 **외부 도구를 연결**한다 | ✗ | 도구 탐색·연결·검증 |
| 8 | **내 자료 기준**으로 답을 받는다 | ◐ (문자열 매칭) | 의미 검색 |
| 9 | 잘못되면 **되돌린다** | ◐ (기억만) | 파일·실행까지 되돌리기 |
| 10 | 설치하고 **그냥 쓴다** | ◐ (tarball) | 실행 파일 + 자동 업데이트 |

**판정 규칙**: ✗ 또는 ◐ 가 하나라도 남아 있으면 v3.x 는 끝나지 않았다.
"있긴 한데 부족"은 ◐ 이며, ◐ 는 **미완이다**.

---

## §16. 완료의 정의 (강화)

절대 원칙 §11("사용자가 실제로 겪는 경로가 동작하는 것")을 유지하되, 반쪽 완료를 막는 다섯 규칙을 더한다.

**A. 완료 = 사용자가 그 기능으로 일을 끝낼 수 있고, 비교 대상에 견줘 부족한 점을 댈 수 없을 때.**
부족한 점을 말할 수 있으면 미완이다. 보고서에 "다만 ~는 아직"이 붙으면 그 슬라이스는 안 닫힌 것이다.

**B. "후속"은 두 경우만 허용한다.**
(a) 명시적으로 다른 Phase의 범위 (b) 사용자가 체감하지 못하는 내부 최적화.
**사용자가 쓰다 부딪히는 것은 후속이 될 수 없다.** 못 끝낼 것 같으면 그 기능은 시작하지 않는다.

**C. 선언과 구현의 어긋남은 사고다.**
레지스트리에 등록된 도구는 반드시 실제로 동작해야 한다. 스텁 핸들러가 등록돼 있으면 **테스트가
실패한다**(게이트 신설). §6.30 의 문서 파생만으로는 이걸 못 잡는다는 것이 `local.file` 로 증명됐다.

**D. 기능 단위는 "능력 완결"이다.**
"웹"이면 검색 + 읽기 + 실패 처리 + 결과 전달까지가 한 단위다. 쪼개서 반쪽을 내보내지 않는다.

**E. 완료 보고는 비교로 답한다.**
"이 기능은 OpenClaw·Hermes 의 같은 기능 대비 무엇이 같고 무엇이 없는가"에 답하지 못하면 완료가 아니다.

---

## §17. 성능 기준선 (회귀 금지선)

v3.0 §1 이 "속도"를 절대 목표로 두었으나 숫자가 없었다. 2026-07-26 실측치를 기준선으로 고정한다.
**넓히면서 이 선을 넘기면 그 슬라이스는 미완이다.**

| 항목 | 기준선(2026-07-26) | 허용 |
|---|---|---|
| 커널 자체 처리(모델 호출 제외) | 0.001s | ≤ 0.01s |
| 콜드 부팅 → health 응답 | 93ms | ≤ 300ms |
| 첫 글자까지(스트리밍 provider) | 2.4s | ≤ 4s |
| 전체 테스트 | 0.86s / 421건 | ≤ 5s |
| 런타임 의존성 | 0 | **0 유지** |

의존성 0 은 특히 지킨다 — 벡터DB·프레임워크 도입은 v3.x 범위 밖이다(임베딩이 필요하면 **이미 연결된
provider 의 embeddings API** 를 쓰고 결과는 파일에 둔다).

---

## §18. 지속성 계약 (기존 사용자를 깨지 않는다)

이미 사용 중인 사용자가 있다(연결·SOUL.md·CAPABILITIES.md·세션·기억·자동화).

- 저장 구조를 바꿀 때는 **자동 이관**을 함께 낸다. 사용자에게 재설정을 요구하지 않는다.
  (선례: 모델 연결 v1→v2 이관, ChatGPT 모델 id 이관 — 둘 다 재로그인 없이 처리했다.)
- 이관은 **읽을 때 수행하고 결과를 저장**한다(다음 부팅에서 반복하지 않는다).
- 사용자가 직접 편집하는 파일(SOUL.md, CAPABILITIES.md 사람 구역)은 **덮어쓰지 않는다**.
- 되돌리기가 있는 기능은 되돌린 뒤 상태가 실제로 복구되는지 테스트로 고정한다.

---

## §19. 검증 리듬 (테스트로 대체 불가)

2026-07-26 하루에 결함 9건이 **전부 오너의 실사용에서** 나왔다. 그 시점 자동 테스트 421건은 전부
통과 중이었다(모델 id 거절·타임아웃 절단·웰컴 끼어듦·스크롤 정지·자기 모델명·능력 과장·정체성 4종).

- **각 Phase 는 오너가 실제로 눌러본 뒤에 닫는다.** 테스트 통과는 닫는 조건이 아니라 전제다.
- 구현자는 슬라이스마다 **오너가 눌러볼 경로**를 한 줄로 제시한다(무엇을 어디서 어떻게).
- 오너 실사용에서 나온 결함은 **회귀 테스트로 고정**한다(그 버그에서 실제로 실패하는 테스트, 절대 원칙 §8).
- 이 리듬은 자동화로 대체하지 않는다. 테스트를 늘려 대신하려는 시도가 곧 실패 신호다.

---

## §20. 전역 안 할 것

v3.0 은 Phase 별로 "새로 만들지 않을 것"을 뒀다. 흔들릴 때 볼 전역 목록을 둔다.

```text
OpenClaw·Hermes 기능 수 따라가기
provider 마켓플레이스 / OAuth 무분별 추가
플러그인 SDK / 대형 워크플로 빌더
벡터DB·프레임워크 도입(의존성 0 파기)
임의 명령 실행(터미널)을 손발 완결 전에 여는 것
대화 자연스러움을 템플릿 문구로 해결하려는 것
사용자가 모르게 바뀌는 기억·자동 승격
반쪽 기능을 "완료"로 봉인하는 것
```

---

## §21. Phase 0 — 부채 상환 (새 기능보다 먼저)

지금 상태에서 손발을 더 붙이면 "반쪽이 열여섯 개"가 된다. **선언한 것을 완결로 끌어올리는 것이 먼저다.**

우선순위는 "거짓말 → 사용자가 부딪히는 것 → 이미 만든 것의 구멍" 순이다.

| # | 부채 | 왜 이 순서 | 완결 기준 |
|---|---|---|---|
| 0-1 | **`local.file` 스텁** | **지금 이 순간 T5 가 사용자에게 거짓말한다** | 실제 파일 읽기·쓰기·정리 + 범위 권한 + 되돌리기. 스텁 등록 금지 게이트 신설 |
| 0-2 | **웹 검색 없음** | URL 없이는 웹 능력이 반쪽 | 검색 → 읽기 → 정리 → 결과 전달이 한 번에 |
| 0-3 | **전 provider 스트리밍** | 이미 만든 것의 구멍(§6.29) | gemini·anthropic 포함 전 provider 조각 흐름 |
| 0-4 | **스킬 실행 경로** | 구조는 완성인데 실행이 없다 | 승인된 스킬이 실제 손발을 실행(A2 경계 유지) |
| 0-5 | **채널 수신** | 보내기만으로는 반쪽 | inboundPolicy 실소비 + 허용 발신자만 + 실경로 1개 |

0-1 은 즉시 착수한다. 0-2·0-3 은 병렬 가능하다. 0-4 는 0-1 이후(실행할 손발이 있어야 한다).

---

## §22. 로컬 지배력 — 권한을 먼저 넓힌다

로컬 지배력은 T5 안전 계약의 최대 시험이다. 지금까지의 외부 효과는 "메시지 전송" 하나였고 범위가
좁았다. 로컬 실행은 성격이 다르다: **되돌리기 어렵고, 범위가 무한하고, 연쇄된다.**

지금 A0~A3 는 도구 **종류**(read/organize/send)로만 갈린다. 로컬 실행에는 **범위(scope)** 축이 하나 더
필요하다 — 어느 폴더까지, 어떤 작업까지, 되돌릴 수 있는가.

**순서(반드시 이 순서로):**

```text
0. 권한에 범위(scope) 축 추가        ← 이게 없으면 나머지가 전부 위험하다
1. 파일 손발 (읽기·쓰기·정리·변환 + 되돌리기)
2. 앱·웹 자유도 (검색, 앱 열기·연동)
3. 도구·모델 연결 확장 (외부 도구 탐색·연결·검증)
4. 자기 구성 능력 개방 (스킬·크론·에이전트 생성)
— 임의 명령 실행(터미널)은 4 이후에, 정해진 작업만 좁게. 임의 실행은 계속 유보.
```

4 는 **새로 만드는 것이 아니라 1~3 의 손발을 이미 있는 구조에 연결하는 것**이다.
(SkillCandidate lifecycle · Automation · 역할 바인딩 · CapabilityResolution · Personal Tool 게이트가
전부 완성돼 있고 손발이 없어서 비어 있다.)

v3.0 §0 의 불변식 8개는 이 확장에서도 그대로다:
추천≠설치 / 설치≠연결 / 연결≠실행 가능 / 실행 가능≠자동 실행 / 스킬 활성화≠외부 행동 승인 /
검색됨≠반영됨 / 해제≠삭제 / 실패≠망가짐.

---

## §23. 카파시 원칙과의 대응 (개발 절대 원칙 재확인)

`GPAO-T5-DEVELOPMENT-ABSOLUTE-PRINCIPLES` 12개는 카파시의 진단(*LLM은 과신한다 — 조용히 가정하고,
과잉설계하고, 이해 못 한 코드를 건드리고, 혼란스러워도 계속 간다*)에서 나왔다. 이번 보강은 그중
**어긴 것**을 되살리는 것이다.

| 절대 원칙 | 이번에 어긴 방식 | 보강 조항 |
|---|---|---|
| 1. 소스가 아니라 산출물을 검증 | 계약 봉인을 완료로 보고 | §16-A, §19 |
| 2. 검증 안 된 전제 위에 세우지 않음 | 계획서를 안 읽고 우선순위 제안 | §16-E |
| 3. 되돌리기 어려운 행동은 게이트 | (지킴) 로컬 확장에서 시험대 | §22-0 |
| 4. 사례에 맞추지 말고 일반화 | (지킴) | §20 |
| 5. 코딩 전에 생각하고 애매하면 멈춤 | **상의 요청에 코드부터 짬** | §19 |
| 6. 외과적 변경 | (지킴) | — |
| 7. 단순함이 먼저 | **단순함을 이유로 필요한 것을 뺌** | §15 |
| 8. 모든 수정은 실패하는 테스트 동반 | (지킴) | §19 |
| 9. 결과를 정직하게 보고 | 반쪽을 완료로 보고 | §16-A·B |
| 11. 완료의 정의 | 테스트+라이브 1건을 완료로 | §16 전체 |

**특히 §7(단순함)의 오용을 경계한다.** 단순함은 "필요한 것을 빼는" 이유가 될 수 없다. 단순함은
같은 능력을 더 적은 코드로 내는 것이지, 능력을 줄이는 것이 아니다.

---

## §24. 이 보강안의 성공 판정

```text
§15 작업 목록 10개가 전부 ○ 가 된다.
§17 성능 기준선을 넘지 않는다.
각 Phase 가 오너 실사용 확인으로 닫힌다.
완료 보고에 "다만 ~는 아직"이 붙지 않는다.
사용자가 T5 와 대화하는 것만으로 자기 T5 를 넓힐 수 있다.
```

이 다섯 줄을 실제 근거로 말할 수 없으면 v3.x 는 끝난 것이 아니다.
