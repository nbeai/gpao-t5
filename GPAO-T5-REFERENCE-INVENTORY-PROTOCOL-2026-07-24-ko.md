# GPAO-T5 Reference Inventory Protocol

- Status: `phase_0_inventory_protocol`
- Date: 2026-07-24
- Scope: GPAO-T5 Phase 0 `Final Research Seal`
- Authority: Historical Phase 0 protocol under the current top product authority. The originating plan is archived at
  `docs/archive/retired-plans/GPAO-T5-FINAL-DEVELOPMENT-PLAN-2026-07-24-ko.md`.
- Purpose: Define the schema, classification rules, coverage bar, and Codex audit criteria for the Phase 0 Reference Inventory.

## 0. 원칙

Reference Inventory 는 좋은 기능을 많이 모으는 목록이 아니다.

목적은 네 가지다.

1. 이미 존재하는 제품과 소스에서 배울 것을 빠짐없이 본다.
2. 그대로 가져올 것, 원리만 흡수할 것, 버릴 것, 더 검증할 것을 분리한다.
3. T5 의 독립 OS 정체성, 자기파악, BEAI5 모델 운용, 권한·원장·복구 구조를 흐리지 않는다.
4. Phase 5 첫 코드 착수 전에 "무엇을 흡수했고 무엇을 금지했는지"를 증거로 남긴다.

이 문서는 Claude Code 가 초안을 작성할 때의 작업 기준이며, Codex 가 그 초안을 감사할 때의 판정 기준이다.

## 1. 인벤토리 표 스키마

Reference Inventory 의 기본 단위는 "파일"이 아니라 "관찰 가능한 기능·기관·운영 패턴"이다.

| 컬럼 | 필수 | 정의 | 작성 규칙 |
| --- | --- | --- | --- |
| ID | 필수 | 고유 식별자 | `SRC-영역-번호` 형식. 예: `T3-LEDGER-001`, `OPENCLAW-TOOL-003`. |
| 대상 저장소/제품 | 필수 | 조사 대상 | `GPAO-T3`, `lab_un/OpenClaw`, `Codex`, `Claude Code`, `OpenHands`, `ChatGPT`, `기타` 중 하나. |
| 경로/근거 위치 | 필수 | 실제 근거 | 로컬 파일 경로, 문서 URL, 화면/명령 근거, 또는 "공식 문서명". 추정만 쓰지 않는다. |
| 기능·기관명 | 필수 | 관찰한 단위의 이름 | 예: `Tool Receipt`, `Permission Mode`, `Project Context`, `Follow-up Queue`. |
| 6층위 | 필수 | 흡수 단위 | `기능`, `성능`, `로직`, `알고리즘`, `디자인`, `구조` 중 하나 이상. |
| 사용자 기능 서술 | 필수 | 사용자가 실제로 할 수 있거나 느끼는 일 | 내부 구현 설명보다 사용자 체감 결과를 먼저 쓴다. |
| 관찰된 작동 방식 | 필수 | 실제 동작/구조 요약 | 상태 전이, 권한 흐름, UI 흐름, 데이터 흐름, 실패 처리 중 해당되는 것을 쓴다. |
| T5 매핑 | 필수 | T5 의 어느 기관으로 재구성할지 | `표면`, `커널`, `라우터`, `권한`, `원장`, `복구` 중 하나 이상. 필요하면 `자기파악`, `BEAI5`, `성장`을 보조 태그로 추가한다. |
| 관련 T5 7대 영역 | 필수 | 계획서의 7대 개발 영역 | 1~7번 영역 중 하나 이상. 번호와 이름을 함께 쓴다. |
| 관련 11개 기능군 | 필수 | 계획서 §3 기능군 | 프로젝트/작업공간, 기억/개인화, 도구/앱 연결, 로컬 PC 실행, 멀티 표면, 자동화/스케줄, 승인/권한, 세션 연속성, 개발 작업, 확장성, 투명성 중 하나 이상. |
| 4분류 판정 | 필수 | 흡수 판단 | `재사용 가능`, `원리만 흡수`, `폐기`, `추가 검증 필요` 중 하나. |
| 판정 이유 | 필수 | 왜 그렇게 분류했는가 | 라이선스, T5 정체성, 안정성, 사용자 가치, 구현 비용, 위험을 근거로 쓴다. |
| 라이선스·의존성 상태 | 필수 | 재사용 가능성 | `확인됨`, `확인 필요`, `재사용 금지`, `공식 원리 참조만`, `비공개/비복제` 중 하나. |
| 권한·프라이버시 영향 | 필수 | 사용자/외부세계 위험 | 읽기, 쓰기, 전송, 삭제, 비용, 공개, 장기기억 승격 여부를 표시한다. |
| Truth Ledger 필요성 | 필수 | 실행/증거 기록 필요 여부 | `필수`, `권장`, `불필요` 중 하나와 이유. |
| 복구/되돌리기 요구 | 필수 | 실패 시 회복 구조 | non-mutation 증거, rollback, retry, 사용자 확인 중 필요한 것을 쓴다. |
| 검증 필요 여부 | 필수 | 다음 조사 필요 | `봉인 가능`, `추가 소스 필요`, `실행 검증 필요`, `라이선스 확인 필요`, `사용자 판단 필요` 중 하나. |
| Codex 감사 메모 | 필수 | 감사자의 판정 | 초안 작성자는 비워 두고, Codex 가 감사 시 작성한다. |

## 2. 4분류 판정 기준

### 2.1 재사용 가능

다음 조건을 모두 만족할 때만 `재사용 가능`으로 분류한다.

- 로컬 소스 또는 공개 라이선스상 재사용이 허용된다.
- T5 의 정체성, 이름공간, 사용자 표면, 권한 체계를 오염시키지 않는다.
- 외부 서비스 고유 브랜드, 비공개 자산, 복제 금지 UI, 폐쇄형 구현을 포함하지 않는다.
- T5 의 7대 영역 중 하나 이상에 자연스럽게 들어간다.
- 실패/권한/원장/복구 요구를 T5 방식으로 감쌀 수 있다.

`재사용 가능`은 그대로 복사한다는 뜻이 아니다. 코드 재사용 후보라는 뜻이며, 실제 구현 전에는 라이선스와 의존성 확인이 다시 필요하다.

### 2.2 원리만 흡수

다음 중 하나라도 해당하면 `원리만 흡수`로 분류한다.

- 제품은 좋지만 코드·UI·브랜드·문구·화면을 직접 가져오면 안 된다.
- Codex, Claude Code, ChatGPT 처럼 비공개 구현이거나 고유 제품 경험에 속한다.
- OpenHands 같은 공개 제품이라도 T5 를 wrapper 로 만들 위험이 있다.
- OpenClaw/lab_un 의 기능군은 참고할 수 있으나 identity, runtime path, config schema, 사용자 표면을 T5 정본으로 삼으면 안 된다.
- 사용자 체감 가치는 높지만 T5 에서는 다른 기관명, 데이터 모델, 권한 계약으로 재작성해야 한다.

예: Claude Code 의 permission mode 는 원리만 흡수한다. T5 에서는 A0-A3 Authority Tier 와 Approval Preview 로 재구성한다.

### 2.3 폐기

다음 중 하나라도 해당하면 `폐기`한다.

- T3 에서 이미 사고를 만든 임시 구조, live 회귀, 누더기 사례 전용 로직이다.
- 사용자 목적 달성보다 내부 절차나 도구 한계를 앞세운다.
- T5 의 독립 OS 정체성을 OpenClaw, T3, 특정 모델, 특정 회사 제품의 wrapper 로 낮춘다.
- 권한·외부 전송·삭제·비용·기억 승격을 흐리게 만든다.
- 사용자는 매끄럽다고 느끼지 못하고, 모델 응답의 자연스러운 판단을 기계적으로 훼손한다.
- 라이선스, 의존성, 비공개 자산 위험이 해소되지 않는다.

### 2.4 추가 검증 필요

다음 경우는 `추가 검증 필요`로 둔다.

- 근거가 문서 설명뿐이고 실제 동작을 확인하지 못했다.
- 소스는 보았지만 빌드/실행/사용자 경로에서 동작을 확인하지 못했다.
- 라이선스 또는 transitive dependency 상태가 불확실하다.
- 성능·지연·복구율·권한 경계가 T5 기준에 맞는지 아직 모른다.
- 좋은 아이디어처럼 보이지만 T5 의 7대 영역 또는 11개 기능군에 매핑이 애매하다.

`추가 검증 필요`는 실패가 아니다. 근거가 부족한 상태를 정직하게 표시하는 것이다.

## 3. lab_un OpenClaw 경계 판정 규칙

`/Users/jyp/Developer/lab_un` OpenClaw 소스는 적극 해부 대상이다. 그러나 T5 정본이 아니다.

### 3.1 볼 것

- tool/runtime 의 기능군과 책임 분리
- adapter 패턴과 외부 도구 연결 흐름
- UX 동선, 대시보드 정보 구조, 작업 전환 방식
- sandbox, 실행 receipt, 실패 표시, 권한 요청 방식
- package boundary 와 plugin/extension 구조
- 사용자가 "할 수 있다"고 느끼게 만드는 표면 장치

### 3.2 정본으로 삼지 않을 것

- OpenClaw identity, 이름공간, 브랜드, 제품 문구
- runtime path, 상태 디렉터리, 설치/환경 전제
- config schema 를 T5 의 canonical schema 로 승격하는 것
- OpenClaw 사용자 표면을 T5 화면 정본으로 복제하는 것
- 특정 사례 전용 로직, 임시 adapter, 과거 호환성 때문에 남은 구조

### 3.3 판정 원칙

OpenClaw 에서 발견한 것은 기본적으로 `원리만 흡수`에서 시작한다.

`재사용 가능`으로 올리려면 다음 근거가 추가로 필요하다.

- 라이선스와 의존성 재사용 가능성이 확인됨
- T5 이름공간과 데이터 모델로 완전히 감쌀 수 있음
- T5 권한·원장·복구 구조에 들어가도 의미가 변하지 않음
- 사용자 표면을 복제하지 않고 T5 고유 UX 로 바꿀 수 있음

## 4. 커버리지 기준

Phase 0 Reference Inventory 는 아래 조건을 모두 만족해야 `봉인 가능`하다.

### 4.1 대상별 최소 조사 단위

| 대상 | 최소 커버리지 |
| --- | --- |
| GPAO-T3 | 커널, Work Chat/표면, Tool Execution Truth Ledger, Follow-up Queue, 권한/연결, 대화 흐름 회귀, live human scenario 교훈 |
| lab_un/OpenClaw | tool/runtime, adapter, UX 동선, config/env, sandbox/permission, package boundary, 실패/복구 표현 |
| Codex | 로컬 PC 실행, 파일/터미널/브라우저 작업감, 승인/sandbox, 진행 보고, 검증/리뷰 문법, task/thread 운영감 |
| Claude Code | permission mode, MCP, hooks, skills, agents/subagents, resume/continue, terminal/IDE/desktop/web 다중 표면 |
| OpenHands | CLI, web GUI, headless/SDK, sandbox, model-agnostic agent server, Bash/file/web/MCP tool contract |
| ChatGPT | Projects, Memory, Apps/Connectors, Tasks, Canvas, 대화 자연스러움, 사용자에게 보이는 상태/확인 흐름 |
| 기타 AI OS/agent 제품 | 기능 발견, UX 비교, 실패 방지 패턴, benchmark 기준. 출처 불명 구현은 원리만 흡수 또는 폐기 |

### 4.2 11개 기능군 커버리지

계획서 §3 의 11개 기능군은 모두 최소 1개 이상의 근거 행을 가져야 한다.

1. 프로젝트/작업공간
2. 기억/개인화
3. 도구/앱 연결
4. 로컬 PC 실행
5. 멀티 표면
6. 자동화/스케줄
7. 승인/권한
8. 세션 연속성
9. 개발 작업
10. 확장성
11. 투명성

### 4.3 6층위 커버리지

다음 6층위는 전체 인벤토리에서 모두 등장해야 한다.

1. 기능
2. 성능
3. 로직
4. 알고리즘
5. 디자인
6. 구조

단, 모든 대상이 6층위를 모두 채울 필요는 없다. 전체 인벤토리 기준으로 누락이 없어야 한다.

### 4.4 T5 매핑 커버리지

다음 T5 매핑 태그는 모두 최소 1회 이상 등장해야 한다.

- 표면
- 커널
- 라우터
- 권한
- 원장
- 복구

보조 태그 `자기파악`, `BEAI5`, `성장`은 해당 근거가 있을 때 사용하되, Phase 0 봉인 전 각각 최소 1회 이상 등장하는 것이 권장된다.

### 4.5 봉인 조건

Reference Inventory 는 다음 조건을 만족해야 Phase 0 산출물로 봉인할 수 있다.

- 대상별 최소 조사 단위가 모두 채워짐
- 11개 기능군이 모두 커버됨
- 6층위가 모두 등장함
- T5 매핑 태그 6개가 모두 등장함
- `폐기`와 `원리만 흡수` 항목이 충분히 존재함
- `재사용 가능` 항목은 라이선스·의존성 상태가 최소 `확인 필요` 이상으로 표시됨
- 근거 없는 칭찬, 제품 인상평, "좋아 보임" 같은 행이 없음
- Codex 감사 메모가 모든 행에 들어감

## 5. Codex 감사 기준

Codex 는 Claude Code 의 Reference Inventory 초안을 아래 기준으로 감사한다.

### 5.1 통과 조건

1. 각 행이 실제 근거를 가진다.
2. 사용자 기능 서술이 내부 구현 설명보다 앞선다.
3. 6층위, T5 매핑, 7대 영역, 11개 기능군이 비어 있지 않다.
4. OpenClaw/lab_un 의 identity, runtime path, config schema, 사용자 표면을 T5 정본으로 삼지 않는다.
5. Codex, Claude Code, ChatGPT 의 비공개 구현·브랜드·고유 화면을 복제 대상으로 삼지 않는다.
6. `재사용 가능` 판정이 라이선스·의존성 상태 없이 내려지지 않는다.
7. 권한·외부 전송·삭제·비용·공개·기억 승격 위험을 숨기지 않는다.
8. Truth Ledger 와 복구 요구가 필요한 항목을 빠뜨리지 않는다.
9. T5 의 Operational Selfhood, BEAI5 Model Operation, 사용자 목적 달성 철학과 충돌하지 않는다.
10. 그대로 복제하지 않고 T5식 기관으로 재구성한 설명이 있다.

### 5.2 계획서 §12 공정감시 적용

Phase 0 에 직접 적용되는 공정감시 항목은 다음이다.

| §12 기준 | Phase 0 적용 방식 |
| --- | --- |
| 1. 닫는 7대 영역 | 각 행에 관련 T5 7대 영역을 표시한다. |
| 2. 사용자 목적 달성 흐름에서의 위치 | 사용자 기능 서술과 T5 매핑으로 표시한다. |
| 3. 자기파악에 필요한 상태 정보 | `자기파악` 태그와 SelfState 관련 후보를 표시한다. |
| 5. OS가 직접 통제할 상태·권한·도구·원장 | T5 매핑과 권한·원장·복구 컬럼으로 표시한다. |
| 6. 모델에게 남길 판단·언어·산출물 영역 | BEAI5/모델 판단으로 넘길 항목은 `원리만 흡수` 또는 BEAI5 태그로 구분한다. |
| 7. 권한 경계와 승인 필요 여부 | 권한·프라이버시 영향 컬럼으로 표시한다. |
| 9. 실패·복구·non-mutation 증거 | 복구/되돌리기 요구 컬럼으로 표시한다. |
| 10. 자연스러움 훼손 여부 | 대화/UX/모델 운용 관련 행에서 별도 감사 메모로 판단한다. |
| 11. 참조한 레퍼런스와 흡수 방식 | 경로/근거 위치, 6층위, 4분류 판정으로 표시한다. |
| 12. 그대로 복제하지 않고 T5식으로 재구성한 증거 | 판정 이유와 Codex 감사 메모로 표시한다. |

Phase 0 에서는 실제 구현 산출물이 없으므로 §12 의 4번 Task Context Packet, 8번 실제 사용자 시나리오 검증은 "후속 Phase 검증 대상"으로 남긴다. 단, Reference Inventory 가 Phase 2~4 에서 검증 가능한 형태로 근거와 매핑을 남겨야 한다.

### 5.3 반려 조건

다음 중 하나라도 있으면 Codex 는 초안을 반려한다.

- 근거 없는 일반론이 행을 채움
- OpenClaw/T3/Codex/Claude Code 의 특정 구현을 T5 정본으로 그대로 승격
- 11개 기능군 중 누락 발생
- `재사용 가능` 판정이 남발됨
- 라이선스·의존성 상태가 빈 채로 코드 흡수 후보가 됨
- 권한, 외부 전송, 삭제, 비용, 공개, 장기기억 승격 경계를 흐림
- T5 의 목적 달성 중심 철학보다 도구 나열이 중심이 됨
- 사용자 체감 가치 없이 내부 구조만 수집함

## 6. Claude Code 작업 지시

Claude Code 는 이 문서를 기준으로 `GPAO-T5-REFERENCE-INVENTORY-2026-07-24-ko.md` 초안을 작성한다.

작업 순서:

1. 정본 문서 4개를 먼저 읽는다.
2. 이 프로토콜을 읽고 표 스키마를 그대로 사용한다.
3. 대상별 최소 조사 단위를 채운다.
4. 11개 기능군과 6층위 커버리지를 확인한다.
5. 각 행에 4분류 판정을 내린다.
6. Codex 감사 메모 컬럼은 비워 둔다.
7. 제품 코드는 작성하지 않는다.
8. 계획서, 절대원칙, 환경헌장을 수정하지 않는다. 수정 필요 의견은 별도 제안으로만 남긴다.

첫 초안은 완벽한 결론보다 "근거 있는 분류"가 중요하다. 모르면 `추가 검증 필요`로 표시한다.

## 7. Codex 감사 산출

Codex 는 Claude Code 초안을 받은 뒤 다음 결과 중 하나로 닫는다.

- `통과`: Phase 0 봉인 가능
- `조건부 통과`: 특정 누락 보완 후 봉인 가능
- `반려`: 근거, 경계, 커버리지, T5 정체성 중 핵심 결함 존재

감사 결과에는 반드시 다음을 포함한다.

- 커버리지 요약
- 고위험 판정 목록
- `재사용 가능` 후보의 라이선스·의존성 위험
- OpenClaw/lab_un 경계 위반 여부
- T5 7대 영역과 11개 기능군 누락 여부
- Phase 1~4 로 넘길 결정/질문

## 8. 완료 언어

이 문서 자체가 만들어졌다고 Phase 0 이 완료되는 것은 아니다.

완료라고 말할 수 있는 것은 다음 조건을 통과한 뒤다.

```text
+ Reference Inventory 초안 작성
+ 대상별 최소 커버리지 충족
+ 11개 기능군 커버
+ 6층위 커버
+ T5 매핑 커버
+ 라이선스·의존성 상태 표시
+ Codex 감사 메모 작성
+ 반려 조건 없음
+ Phase 1 로 넘길 결정/질문 정리
```

그 전에는 `프로토콜 작성 완료`, `초안 작성 완료`, `감사 전`, `조건부 통과`처럼 좁은 상태 언어만 쓴다.
