# GPAO-T5 개발 절대 원칙

- 날짜: 2026-07-24
- 상태: `top_authority_development_principles`
- 적용 범위: GPAO-T5 전체 개발
- 관계: `GPAO-T5-FINAL-DEVELOPMENT-PLAN-2026-07-24-ko.md`와 함께 GPAO-T5 개발의 최상위 기준 문서다.
- 목적: LLM 개발 중 과신, 조용한 가정, 과잉설계, 이해하지 못한 코드 수정, 혼란 상태 지속을 막는다.

카파시의 LLM 코딩 원칙을 뼈대로, **이 프로젝트에서 실제로 터진 사고**를 근거로 삼는다.
각 원칙 뒤의 "왜"는 훈계가 아니라 우리가 당한 일이다. 어기면 같은 사고가 다시 난다.

핵심 진단: *LLM은 과신한다. 조용히 가정하고, 과잉설계하고, 이해 못 한 코드를 건드리고, 혼란스러워도 계속 간다.* 아래 원칙은 이 네 실패를 막기 위한 것이다.

## 0. T5 성능 철학 — 이해가 먼저다 (2026-07-27 오너 지시, 절대 원칙)

아래는 **오너 원문**이다. 요약·재작성·정형 문구로 대체하지 않는다. 다른 문서와 구현 체크리스트는
이 원문의 해석일 뿐, 원문보다 우선하지 않는다.

> 티파이브 성능 철학
>
> 사용자는 AI OS도 그냥 AI라고 인식한다.
>
> 그렇기 때문에 가장 중요한건
> 사용자의 발화에 대한 이해이다.
> 그 다음 중요한건 대화의 흐름에 대한 이해다.
> 그 과정에서 필요한 도구와 기능이 적절하게 쓰이고 사용자의
> 현재 의도와 목적, 맥락에 맞는 성능을 발휘해야 하는 것이다.
>
> "모델 앞에 어떤 현실을 만들어서 넣어주는가"가 엘엘엠 기반 AI를 운용하는데 있어
> 가장 중요한 지점이다. 이게 지파오티의 진짜 숨겨진 코어, 즉 차별점을 만든다.
>
> 이게 안되면 아무리 대단한 코드를 짜서 기능을 구현해도 의미가 없다.
>
> AI OS에서 제일 중요한 건 모델이 아니라, 모델 앞뒤의 운영 구조다.
> - 앞에서는 사용자의 말을 정확히 해석하고
> - 중간에서는 현재 상태와 가능한 손발을 인식하고
> - 뒤에서는 실제로 한 일과 못 한 일을 구분하고
> - 다음 턴에서는 그 사실을 이어받아야 한다
>
> T5 런타임이 할 일은 사용자의 말을 대신 해석해서 닫아버리는 게 아니라:
>
> - 원문을 보존하고
> - 현재 대상과 맥락을 주고
> - 가능한 손발을 알려주고
> - 실제 한 일/못 한 일을 구분하고
> - 위험 실행은 경계에서 막는 것
>
> 그 위에서 해석과 판단은 모델이 하게 둬야 한다.

이 원칙이 나머지 모든 원칙 **위**에 있다. 아래 원칙들은 "잘못 만들지 않는 법"이고, 이것은
"무엇이 잘 만든 것인가"의 정의다.

**우선순위(개발 판단이 갈릴 때 이 순서로 본다)**

```text
1. 사용자 발화 이해      이 말이 무슨 뜻인가
2. 대화 흐름 이해        지금 무엇에 대해, 어디까지 온 이야기인가
3. 모델 앞 현실 구성     모델이 딛고 설 현재 상태·손발·근거가 정확한가
4. 실제 결과 구분        한 일·못 한 일·추정·대체를 섞지 않는가
5. 다음 턴 승계          이 사실이 이어지는가
6. 기능의 존재           비로소 기능·구조·성능
```

이 문장이 §0 의 우선순위에 **방법**을 준다. 우선순위는 "무엇이 중요한가"이고, 이건 "그래서 우리가
무엇을 만드는가"다. 우리는 모델을 만들지 않는다 — **모델이 딛고 설 현실**을 만든다.

```text
앞      원문을 보존하고, 모델이 사용자의 말을 정확히 해석할 수 있게 한다
중간    현재 상태와 지금 쓸 수 있는 손발을 인식한다
뒤      실제로 한 일과 못 한 일을 구분한다
다음 턴  그 사실을 이어받는다
```

네 자리 중 **하나라도 비면 모델이 그 빈칸을 지어낸다.** 실측이 매번 그랬다:
- 「뒤」가 비어서 → 검색으로 찾은 블로그를 "성공"으로만 기록 → 모델이 이유를 지어냄("수집이 제한돼서")
- 「다음 턴」이 비어서 → 팔식당을 읽고도 다음 턴에 "책 리뷰 쓰는 방법"을 검색
- 「중간」이 비어서 → 세션 검색 도구가 있는데 "그 기능은 없습니다"

**그래서 판정 질문은 늘 하나다: 이 슬라이스는 앞·중간·뒤·다음 턴 중 어디를 채우는가.**
어디도 안 채우면서 기능만 늘리는 슬라이스는 §0 위반이다.

**근거(실측 — 오너 실사용에서 나온 실패 전부가 1·2번이었다)**

| 실패 | 우리가 부른 이름 | 실제 |
| --- | --- | --- |
| "오늘 날씨"에 두 번 되묻고 1분 52초 | 프롬프트 문제 | **발화 이해** — 사실(시각·지역)이 없어 못 알아들었다 |
| 네이버 분석 요청에 빈 답 4회 | 루프 버그 | **흐름 이해** — 막힌 다음을 이어가지 못했다 |
| "리뷰 읽어봐"에 책 리뷰 작성법 요약 | 검색 오작동 | **흐름 이해** — 현재 대상(팔식당)을 놓쳤다 |
| 화면에 내부 계획 문자열 노출 | 유출 버그 | **사용자면 이해** — 누구에게 하는 말인지 몰랐다 |

기능 실패는 하나도 없었다. 그 시점 자동 테스트는 643건 전부 통과 중이었다 —
**안전·경계는 643개로 검사하면서 이해·흐름은 0개로 검사하고 있었기 때문이다.**

**규칙**

- 슬라이스 완료 판정에 **이해·흐름 검증**을 포함한다. 기능이 동작해도 대화가 어긋나면 미완이다.
- 이해·흐름의 회귀는 **멀티턴 시나리오 재생**으로 고정한다(`GPAO-T5-SCENARIO-REPLAY-SPEC`).
  단발 테스트로는 흐름 실패를 못 잡는다 — 실제로 못 잡아서 오너가 매번 먼저 발견했다.
- 런타임의 역할은 두 가지로 좁힌다: **① 이해에 필요한 사실을 빠짐없이 준다 ② 이해가 어긋났을 때
  드러나게 한다.** 이해와 판단 자체는 모델이 한다(§24 — 규칙으로 굳히면 모델이 멍청해진다).
- **런타임이 모델을 방해하지 않는다.** 대상과 무관한 자료를 "확인된 사실"로 올려 모델을 오염시킨
  일이 실제로 있었다. 사실을 주는 것과 **틀린 사실을 주는 것**은 정반대의 결과를 낳는다.
- 기능 확장(새 채널·새 도구)은 이해·흐름이 선 뒤에 한다. 이해가 약한 채로 표면을 늘리면
  **실패가 표면 수만큼 늘어난다**(텔레그램에서 같은 실패가 그대로 재현됐다).

---

## 1. 소스가 아니라 산출물을 검증한다

이 프로젝트의 제1원칙이다.

개발 트리가 멀쩡해도 **배포·설치·빌드 과정이 그것을 깨뜨린다.** 검증 대상은 항상 "사용자에게 실제로 도달하는 것"이어야 한다.

- 배포 치환이 디브랜딩 가드를 먹어 라이브에서만 디브랜딩이 죽었다. 개발 테스트 424개는 전부 통과였다.
- postinstall 인라인 JS의 `"\n"`이 생성 시점에 깨져 배포본이 SyntaxError로 죽었다. 소스는 멀쩡했다.
- `npm install`의 dist 정리가 제품 코드 146개를 지웠다. 저장소에서 클론하면 부팅 못 했다. 로컬은 멀쩡했다.

규칙:

- 치환·패키징·설치 결과를 펼쳐서 직접 실행해 검사한다.
- "테스트 통과 = 동작"이라고 말하지 않는다.
- T5 본체 개발에서도 소스 단위 통과가 아니라 사용자가 실제로 만나는 화면, 응답, 도구 실행, 원장, 복구 흐름을 검증한다.

## 2. 검증 안 된 전제 위에 아무것도 세우지 않는다

- `2>/dev/null`이 tar 오류를 삼켜 빈 출력을 "잔여 0건"으로 읽었다. 그 거짓 전제 위에 불변식을 만들어 정상 빌드 463개 파일을 막았다.

규칙:

- "확인했다"고 말하기 전에 근거를 눈으로 본다.
- 명령이 실패를 숨기지 않는지부터 본다.
- 전제가 흔들리면 그 위의 모든 결론을 버린다.
- T5에서 모델·도구·권한·연결 상태는 추정하지 않고 `SelfStateSnapshot`과 실제 receipt로 확인한다.

## 3. 되돌리기 어렵거나 밖으로 나가는 행동은 증거로 게이트한다

- 테스트 결과를 안 보고 커밋·푸시를 한 줄로 이어 붙였다. 실패를 보고도 진행할 수 있는 구조였다.
- 빌드 스크립트의 `rm` 로직을 안 읽고 서명 없이 빌드해 성공본 pkg를 덮어썼다.

규칙:

- 커밋·푸시·삭제·배포·재빌드 전에 **직전 검증 결과를 확인한다.**
- 명령을 무조건 연결하지 않는다.
- 삭제·덮어쓰기 전에 대상을 먼저 본다.
- 설명과 다르거나 내가 만들지 않은 것이면 멈추고 알린다.
- T5에서는 외부 전송, 삭제, 결제, 공개 게시, 장기 기억 승격, 자동화 활성화가 반드시 authority gate를 통과해야 한다.

## 4. 눈앞의 한 사례에 맞추지 말고 일반화한다

- 부오상회 대화 하나에 맞춰 `kind: review/blog`, 네이버 모바일 문구까지 깎아 넣은 모듈을 만들었다. 사용자가 "누더기"라고 정확히 지적했다. 그건 그 사례 전용 부품이지 설계가 아니었다.

규칙:

- 사례는 참고하되 어느 한쪽만 만족시키는 데 집중하지 않는다.
- 웹뿐 아니라 캘린더·노션·메신저까지 같은 계약으로 도는지 본다.
- 사례 전용으로 만든 것은 배포 전에 일반형으로 바꾸거나 들어낸다.
- T5의 모든 기능은 7대 영역과 공통 contract에 매핑되어야 한다.

## 5. 코딩 전에 생각하고, 애매하면 멈춘다

카파시 원칙 1.

규칙:

- 가정하지 않는다.
- 가정은 명시한다.
- 더 단순한 길이 있으면 먼저 반박한다.
- 혼란스러우면 멈추고 묻는다.
- 사용자의 결정인 것, 특히 범위·배포·서명·되돌리기는 임의로 정하지 않는다.
- 단, 안전을 위협하지 않는 준비는 먼저 하고, 실제 외부 실행 직전에만 확인한다.

T5 적용:

- `Operational Selfhood`와 `ActionPlan`이 불명확하면 실행하지 않는다.
- 단순 read-only 조사, 로컬 초안, 진단, 계획 정리는 먼저 진행한다.
- 외부 효과 직전에는 멈춘다.

## 6. 외과적 변경만 한다

카파시 원칙 3.

규칙:

- 꼭 필요한 것만 건드린다.
- 안 고장난 것을 "개선"하거나 인접 코드를 리팩터링하지 않는다.
- 다른 팀이 작업 중인 파일은 건드리지 않는다.
- 커밋은 내 변경만 담는다.
- 워킹트리에 남의 미커밋이 섞여 있으면 그것만 골라 커밋한다.

근거:

- billing 오분류는 **패턴 한 줄** 이동으로 고쳤다.
- 설치 실패는 seed 스크립트 분리와 배치 순서로 고쳤다.

T5 적용:

- 7대 영역 중 이번 작업이 닫는 영역만 수정한다.
- 레퍼런스 흡수 중에도 주변 구조를 무단 재작성하지 않는다.

## 7. 단순함이 먼저다

카파시 원칙 2.

규칙:

- 문제를 푸는 최소 코드로 시작한다.
- 요청 이상의 기능·추상화를 만들지 않는다.
- 1000줄보다 100줄을 우선한다.
- 죽은 코드는 배포 전에 들어낸다.
- 배포본에 죽은 코드·미사용 부품을 넣지 않는다.

근거:

- 사례 전용 web-evidence 모듈은 제거했다.

T5 적용:

- 첫 slice는 `Work Chat + SelfStateSnapshot + BEAI5 Task Context Packet + ActionPlan + Authority + Truth Ledger + Connection status + Follow-up Queue`에 집중한다.
- 7대 영역 전체를 얇게 깔지 않는다.

## 8. 모든 수정은 그 버그에서 실제로 실패하는 테스트를 동반한다

카파시 원칙 4.

규칙:

- "버그를 고쳐라"를 "버그를 재현하는 테스트를 쓰고 통과시켜라"로 바꾼다.
- 테스트가 진짜 버그를 잡는지 반대 테스트로 확인한다.
- 수정 전 코드 또는 라이브 배포본에서 그 테스트가 실패하는 것을 본다.
- 재발 방지 테스트는 목록을 검사하지 말고 불변식을 검사한다.
- 손으로 관리하는 목록은 다음에 또 어긋난다.

근거:

- 디브랜딩, SW 캐시, billing은 수정 전 실패를 확인하고 고정했다.
- dist 인벤토리 사고는 손관리 목록의 위험을 드러냈다.

T5 적용:

- 말귀, 권한, 도구 사실성, 자기파악, 자연스러움 회귀는 각각 실패 fixture를 가져야 한다.
- 단위 테스트만으로 완료하지 않고 human scenario replay를 통과해야 한다.

## 9. 결과를 정직하게 보고한다

규칙:

- 실패하면 실패라고, 출력과 함께 말한다.
- 건너뛴 단계는 건너뛰었다고 말한다.
- 검증 안 된 것은 "적용했으나 미검증"으로 남긴다.
- 완료·검증된 것만 완료라고 한다.
- 내가 틀렸으면 틀렸다고 먼저 말한다.

근거:

- 이 세션에서 "잔여 0건" 검증이 무효였던 것, 경로 불변식이 과했던 것, 테스트 기대값이 틀렸던 것을 그렇게 정정했다.

T5 적용:

- Truth Ledger와 RecoveryCell은 사용자에게도 정직한 결과 보고를 가능하게 해야 한다.
- T5 자체 개발 보고도 같은 기준을 따른다.

## 10. 이 프로젝트 고유의 함정

반드시 기억한다.

- **배포 치환이 가드를 먹는다.** 레거시 토큰을 탐지하는 코드는 통짜 리터럴로 쓰지 말고 조각으로 합성한다. 예: `["open","claw"].join("")`. 통짜로 두면 배포 치환이 매처 자체를 바꿔 배포본에서만 죽는다.
- **`gpaoT3`는 정당한 중간 철자다.** "gpaoT3가 남았는가"를 빌드 게이트로 쓰지 않는다. 위험한 것은 파일시스템 상태 경로인 `~/.gpaoT3`뿐이고 그건 repair가 이미 교정한다.
- **재설치 = 실행 중인 바이너리 교체.** 제자리 `cp`는 SIGKILL 137을 부른다. bootout 먼저, 그 다음 `mv` 원자적 교체.
- **pkg 성공본은 보관한다.** 빌더가 시작 시 기존 산출물을 전부 `rm` 한다.
- 상세 참조: `gpao-t-handoff/PKG-BUILD-HANDOFF.md`, 그리고 auto-memory의 관련 항목들.

현재 T5 본체 개발에서 설치·온보딩은 비범위다. 그래도 위 함정은 T3에서 얻은 회귀 방지 지식이며, T5가 packaging/release 영역으로 확장될 때 즉시 적용해야 한다.

## 11. 완료의 정의

파일 복사나 "테스트 통과"가 아니다.

**사용자가 실제로 겪는 경로가 동작하는 것**이 완료다.

예:

- 설치는 `health check passed`가 아니라 실제 설치 후 실행 경로가 통과해야 한다.
- 연결은 설정 저장이 아니라 실제 응답이 와야 한다.
- 화면은 개발 서버가 아니라 라이브 표면에서 렌더되어야 한다.
- T5 본체는 소스 함수가 아니라 실제 대화, 자기파악, 도구 운용, 권한 경계, Truth Ledger, 복구 흐름이 이어져야 한다.

검증 못 한 것은 완료가 아니다.

## 12. 최상위 적용 규칙

이 문서는 `GPAO-T5-FINAL-DEVELOPMENT-PLAN-2026-07-24-ko.md`와 함께 GPAO-T5 개발의 최상위 기준이다.

원칙이 충돌하면 다음 순서를 따른다.

1. 사용자 안전, 법적·윤리적 제한, 외부 권한 경계
2. 사용자의 현재 지시와 목적
3. GPAO-T5 최종 개발 계획서
4. 본 개발 절대 원칙
5. 세부 PRD, kernel spec, UI spec, scenario benchmark
6. 특정 구현 편의

어떤 상황에서도 이 문서는 "좋은 조언"이 아니라 개발 게이트다.
