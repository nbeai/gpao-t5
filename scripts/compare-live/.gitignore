# 자격은 어떤 경우에도 저장소에 넣지 않는다. 계측기는 값을 읽지 않고 자식 프로세스로만 주입한다.
secret-env.sh

# 회차 산출물·로컬 상태 — 증거는 docs/03-verification/evidence/** 로 선별해 옮긴다.
# 원본 회차 폴더에는 제품 transcript 가 그대로 남으므로 커밋하지 않는다.
preflight/
oc-run-*/
hm-run-*/
v2-run-*/
INVALID-*/
PARTIAL-*/
oc-state/
usage-openclaw/
run.lock
tty-run-*.jsonl
