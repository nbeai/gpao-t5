{
  "request": "Build the first useful product workflow",
  "mode": "beginner",
  "taskType": "unknown",
  "firstWorkflow": "Guided First Workflow",
  "v0Goal": "The first real user can route a vague request, see the plan, run checks, and understand the result without reading the whole codebase so that a user can move from vague request to safer implementation with visible gates and evidence.",
  "firstSuccessPath": [
    "User starts the Guided First Workflow from the first visible entry point.",
    "User creates, selects, or previews one request without needing technical setup knowledge.",
    "System shows the intended result in plain language.",
    "AI/developer verifies the result with a repeatable check before final user review."
  ],
  "emptyStatePath": [
    "Open the command surface before any real user data exists.",
    "Explain what Guided First Workflow is for without exposing implementation jargon.",
    "Show the next safe action instead of a blank or confusing state."
  ],
  "failureRecoveryPath": [
    "Trigger the most likely failed or missing-input state for Guided First Workflow.",
    "Show a recoverable explanation and next safe action.",
    "Do not claim completion while the failure path is unknown."
  ],
  "included": [
    "route contract",
    "plain-language plan",
    "verification evidence",
    "next action"
  ],
  "excluded": [
    "unapproved external activation",
    "unrelated refactor",
    "large rewrite"
  ],
  "approvalGates": [
    "User confirms the first workflow matches the real intention.",
    "User approves taste, business policy, or operating preference where there is no technical source of truth."
  ],
  "aiVerification": [
    "AI/developer verifies first success path for Guided First Workflow.",
    "AI/developer verifies empty or first-time state.",
    "AI/developer verifies failure or recovery state.",
    "AI/developer inspects the main user-facing surface when possible."
  ],
  "finalUserAuthority": [
    "User judges whether the result matches the real-world intention.",
    "User approves preferences and final lived-context fit."
  ],
  "source": {
    "intake": false,
    "shape": false,
    "plan": true
  }
}
