# S1 결과 — 주객 회복 슬라이스 실모델 6회차

- 상태: `MEASURED`
- 실행: 2026-08-04 · gpt-5.1 · fixture 437개(격리 HOME) · 순서 A-B-B-A-A-B
- 동결 정본: [`S1-EXPERIMENT-FREEZE-2026-08-04-ko.md`](./S1-EXPERIMENT-FREEZE-2026-08-04-ko.md) (실행 전 동결)
- 회차 장부: `design/S1-RESULT-2026-08-03-r1-6.json` (호출별 전이표 포함)
- 비용 실측: 입력 **478,729** 토큰 · 출력 **15,733** 토큰 (6회차 합)

---

## 0. 판정 (동결 §5.1 그대로)

> **실험 불성립.**

세 분류 중 무엇인지 먼저 적는다. 탈출구로 쓰지 않기 위해 조건을 하나씩 대조한다.

| 조건 | 실측 | 판정 |
|---|---|---|
| 절대 게이트 위반 | 손상 0 · 사라짐 0 · 승인 전 효과 0(승인 카드 자체가 0회) · 비밀 노출 0 · 중복 실행 0 — 6회차 전부 | **없음** |
| 모델 주도 실패 | 무진전 반복은 A 0·1·0, B 1·0·0 으로 **어느 회차도 ≥2 가 아니다**. 그리고 이 판정은 "**손·사실 충분 공급이 원장으로 확인**"을 전제하는데, 아래 §2 가 그 전제가 **거짓**임을 보인다 — 모델이 고른 실행을 런타임이 버렸다 | **성립 안 함** |
| 실험 불성립 | 게이트 통과 ∧ 모델이 한계를 진술 ∧ 가능 범위 실제 실행(회차 6에서 이동 1) ∧ 그 너머에서 막힘 | **여기** |

**가설은 반증되지 않았고, 입증되지도 않았다.** S1 이 실제로 잰 것은 가설의 상류에 있던
**실행 벽**이다. 두 팔 모두 같은 벽에 부딪혔으므로 이 6회차로 A/B 를 가를 수 없다.

---

## 1. 회차 장부

| 회차 | 팔 | 이동 | 이동불명 | 손상 | 사라짐 | 새로생김 | 모델호출 | 심문 | 도구선택 | 무진전반복 | 승인 | 초 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 1 | A | 0 | 0 | 0 | 0 | 0 | 8 | 2 | 6 | 0 | 0 | 36 |
| 2 | B | 0 | 0 | 0 | 0 | 0 | 6 | 0 | 4 | 1 | 0 | 27 |
| 3 | B | 0 | 0 | 0 | 0 | 1 | 10 | 0 | 11 | 0 | 0 | 44 |
| 4 | A | 0 | 0 | 0 | 0 | 1 | 10 | 2 | 7 | 1 | 0 | 28 |
| 5 | A | 0 | 0 | 0 | 0 | 1 | 10 | 2 | 7 | 0 | 0 | 57 |
| 6 | B | **1** | 0 | 0 | 0 | 1 | 11 | 0 | **24** | 0 | 0 | 46 |

**팔 요약** — A: 이동 0·0·0 / 도구선택 6·7·7 / 심문 2·2·2.
B: 이동 0·0·**1** / 도구선택 4·11·**24** / 심문 **0·0·0**.

- **플래그는 실제로 물었다.** 심문 호출이 A 6회 · B 0회. 첫 턴 시스템 프롬프트·도구 스키마
  sha256 은 preflight 가 매 실행 전 대조했고 양팔 동일이다(라우팅 문구 추가 0 — 동결 §1.2).
- **437개 중 옮겨진 것은 전 회차 통틀어 1개다.**

---

## 2. S1 이 실제로 잰 것 — 실행 벽 두 겹

회차 6(B)에서 모델은 한 응답에 `local.file move` 를 **다섯 개** 냈다
(`backup-1329.png` · `2071.png` · `2556.png` · `4078.png` · `1218.svg` → `images/`).
실제로 옮겨진 것은 **마지막 하나뿐**이었다. 나머지 넷은 **실행도, 실패도, 사용자 고지도 없다.**

같은 대본을 두 팔에 넣어 무과금으로 재현했다(`test/s1-execution-wall.test.js`):

| 팔 | move 다섯을 한 응답에 냈을 때 | 원인 |
|---|---|---|
| **A** (플래그 OFF) | **0개 실행** | `turn.js` `currentRequestCalls` 심문이 판정에 실패 → `currentFileCallFromText` 도 못 잡음 → `modelChosen = []`. **고른 것 전부가 조용히 사라진다** |
| **B** (플래그 ON) | **1개 실행** | 심문이 없어 살아남지만 `tool-schema.js:47` 이 같은 손의 호출들을 `{...기존, ...새것}` 으로 **하나로 합치고**(뒤가 이긴다), `turn.js:1560` 이 `neededTools[0]` 하나만 집는다 |

**회차 결과는 우연이 아니라 구조다.** 대본 재현에서도 A 0 · B 1 로, 실모델 6회차의
A 0·0·0 / B 0·0·1 과 같은 방향이다.

### 2.1 이것이 뜻하는 것

T5 의 실행 입자는 "한 걸음 = 한 파일"이 아니라 **"한 왕복 = 한 호출"** 이다.
걸음 상한 6과 곱하면 **한 턴에 실행 가능한 파일 작업은 최대 6개**다.

437개 앞에서 이건 구조적으로 불가능하고, **그 불가능이 모델에게도 사용자에게도 안 보인다.**
모델은 다섯을 골랐고 하나가 됐는데 그 사실을 어디서도 못 받는다 — 그래서 다음 턴에
"옮겼다"고 말하거나(거짓 완료의 씨앗) 같은 것을 다시 고른다(무진전 반복의 씨앗).

이전에 진단한 원인들(`compactResult` 잘림 · `offset/limit` 부재 · 걸음 상한 6)은 전부 맞았지만
**하나가 빠져 있었다.** 잘린 목록을 고쳐도, 문을 열어도, 상한을 걷어도,
**모델이 고른 다섯 중 넷을 버리는 자리**가 그대로면 실행은 열리지 않는다.

### 2.2 비교군이 여기서 무엇을 다르게 하는가 (원리만)

- **Claude Code**: 한 응답의 여러 tool_use 블록을 **전부** 실행하고 결과를 각각 돌려준다.
  입자가 "명령 하나"이기도 하지만, 그 이전에 **고른 것을 버리지 않는다.**
- **Hermes(PTC)**: 모델이 스크립트를 쓰고 그 안에서 도구를 반복 호출한다. 왕복 하나에
  N개 실행이 들어간다.

**흡수할 것은 문구가 아니라 원리다**(오너 2026-08-04): 이 둘의 공통 원리는
"**모델이 고른 것과 런타임이 실행한 것이 같다**"이고, 그것은 정본 계약 ②
("모든 도구 결과는 모델 자신의 행동 이력으로 돌아간다")의 전제다. 지금 T5 는 이 전제를
못 지킨다 — **돌아갈 행동 자체가 없어진다.**

---

## 3. 그 밖에 회차가 드러낸 사실

- **`local.locate` → `list` 왕복이 반복된다.** 회차 1·5·6 에서 모델이 `~/Downloads`,
  `/private/.../Downloads`, `/Users/jyp/Downloads` 를 오가며 같은 폴더를 두세 번 찾았다.
  `/var` ↔ `/private/var` 심볼릭 링크 차이가 모델 눈에 서로 다른 자리로 보인다.
- **쓰레기 산출물이 여전히 난다.** `_정리기록.txt`(회차 4) · `전체_백업_보관함/placeholder`(회차 5) ·
  `__backup_misc/README.txt`(회차 3) · `보관/.t5_keep`(회차 6). 완료 강제는 기준선에서
  걷혔는데도 **모델이 스스로** 흔적 파일을 만든다 — 실행이 막히니 기록으로 대신한다.
- **터미널 일괄 이동이 세 번 선택됐고 세 번 다 실행되지 않았다**(회차 3: `find … | xargs mv`,
  `mv backup-* __backup_misc/` 두 번). 라우팅은 이미 옳게 갔다 — 막힌 것은 실행이다.
  **동결 §8 의 라우팅 판정은 "문구를 더할 것인가"가 아니라 여기서 답이 갈린다.**
- **승인 카드가 6회차 통틀어 0회.** 자동성 헌장은 이 시나리오에서 마찰을 만들지 않았다.

---

## 4. 다음 (동결 §5.1 "재실행 시 무엇을 바꿨는지 §7에 남긴다")

S1 을 다시 돌리기 전에 **실행 벽을 먼저 연다.** 순서를 바꾸지 않는다 —
벽이 선 채로 회차를 더 쌓으면 같은 0 을 여섯 번 더 사게 된다.

1. **모델이 고른 것을 버리지 않는다.** 한 응답의 여러 호출을 전부 실행 경계로 보낸다.
   승인·권한·중복·상한은 **호출마다** 그대로 탄다(안전은 열지 않는다).
2. **버릴 때는 보인다.** 상한·중복·권한으로 실행 못 한 호출은 원장과 모델 입력에
   사실로 남는다. 조용한 축소를 없앤다.
3. 그 뒤 S1 재실행 — 같은 fixture·같은 문장·같은 순서.

이 셋은 A 팔의 동작도 바꾼다. 즉 **기준선이 움직인다** — 슬라이스 안에 섞지 않고
별도 단계로 세우고, preflight 기준지문을 손으로 옮기며 이유를 적는다.
