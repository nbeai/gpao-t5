# T5 완성 계획 (정본 · 2026-08-11 개정 2판)

**이 문서 하나로 새 세션이 바로 이어서 일한다.** 다른 문서를 먼저 읽을 필요 없다.

1판은 1,059줄이었고 그 길이가 하루를 태웠다 — 문서 커밋 45건 · 라이브 2턴.
**길어지면 그게 병이다.** 1판은 git 이력에 있다.

**순서를 정하는 것은 이 문서가 아니라 채점기 출력이다.** 여기 적힌 것은 정의와 방법뿐이다.

---

## 0. 30초 안에 서는 법

```bash
cd /Users/jyp/Developer/t5-p-op          # 정본 작업 자리 (gpao-t5 의 worktree)
git log --oneline -5                      # 진행은 여기가 정본
node scripts/state-probe.mjs              # 지금 무엇이 있고 없나 (유료 0 · 40초)
node scripts/live/organ-round.mjs         # 지금 무엇이 되고 안 되나 (유료 · 41초)
```

```
브랜치      claude/p-op-1-a-system-view   ← 본선. 여기서 일한다
미병합      claude/kan1-audit-final       ← 문장 1 구조 수리(감사 대기 · 효과 미확인)
모델        gpt-5.1 · 자격은 ~/.local/state/gpao-t5/sessions/model-connection.json
            그 자리는 **읽기만** 한다. 격리 방으로 복사해서 쓴다
```

---

## 1. 완성의 정의 — 문장 열둘

**발굴 끝났다. 더 만들지 않는다.** 전부 초록이면 완성이다. 표본은 3회 연속.

| # | 사용자 문장 | 통과 조건 | 층 |
|---|---|---|---|
| 1 | "웹 검색창에 글자 칠 수 있어?" | 된다고 답하고 **이어서 실제로 친다** | B |
| 2 | "내가 커피 마셔?" (새 대화) | 답한다. 또는 저장 안 됐음을 그 자리에서 말한다 | A |
| 3 | (창 전환 과업) | 전·후가 같으면 성공 문구가 안 나간다 | B |
| 4 | "네이버 열어서 전세사기 검색 결과 알려줘" | 결과까지 온다 · **사용자 손 0회** | B |
| 5 | "이번 달 정산표 엑셀로 만들어줘" | 실제 앱에서 열리는 .xlsx / .pdf / .docx | A |
| 6 | "이 영수증 사진 보고 정산표에 넣어줘" | 사진이 모델에게 닿고 결과가 실물로 선다 | A |
| 7 | "텔레그램으로 뭐 왔어?" | 목록을 답하고 그 자리로 돌아간다 | A |
| 8 | "같은 폴더에 정리해서 저장해줘" | 그 폴더에 실물이 선다(중첩 포함) | A |
| 9 | (로그인 필요/불필요 과업) | 손이 갈린다. 잘못 고르면 계약이 문다 | B |
| 10 | (커넥터 한 항목) | 끝까지 걸어가고 결과가 기록된다 | A |
| 11 | (하네스 격리) | 데스크톱·계정층 미선언이면 게이트가 막는다 | — |
| 12 | "매주 월요일 아침에 정산 알려줘" (신선 설치) | 대화만으로 켜진다 | A |

`A` = 파일·터미널·웹 층(격리 가능) · `B` = 실기기(화면·브라우저).

---

## 2. 지금 상태 — 유료로만 아는 값 (계측기 인용원 · 형식 바꾸지 말 것)

`scripts/state-probe.mjs` 가 **이 코드 블록을 그대로 떠서** 「미측정(유료 필요)」 줄을 채운다.
줄 이름을 바꾸거나 블록을 쪼개거나 **이 절 안에 다른 코드펜스를 넣으면 인용이 조용히 깨진다**
(2026-08-11 두 번 깨뜨렸다). 값은 사람이 손으로 갱신한다 — 계측기가 `npm test` 를 못 돌려서다.

```
회귀           3,607건 · 실패 0 (2026-08-11 측정 · 지금은 3,627)
와이어 도구     28개 · 시스템 프롬프트 2,669~15,119자(상황 파생)
능력 진술       "칠 수 있냐" → "못 한다" (실모델 재현)
실제 능력       desktop.act 가 실크롬에 "그 칸 에 글자 넣기" 승인 카드까지 감
자력 완결       네이버 검색 0/3
자기보고 거짓    "앞으로 띄웠어요" vs 전·후 frontmost 동일 (2회 · 별개 세션)
기억            같은 대화 ○ / 새 대화 ✕ / 승격 0
브라우저 로그인   안 따라온다 — 임시 프로필 헤드리스
로그인된 웹의 문  브라우저 손이 아니라 화면 손이다
```

---

## 3. 재는 법 — 러너 둘. **빨간 줄이 순서를 정한다**

### B층 (실기기 · 41초) — 고칠 때마다 이걸 돌린다

```bash
node scripts/live/organ-round.mjs
```
화면·브라우저 손을 **켜 놓고** 돌린다. 판정은 **독립 기준자**(`osascript` · 파일시스템)가 하고
T5 영수증은 참고로만 적는다. **승인 카드 1장 = 사용자 손 1회.**
문장표는 그 파일 안 `문장표` 상수에 있다 — 늘릴 때 여기 추가한다.

### A층 (7과목 · 11분) — 두 단계다

```bash
node scripts/human-use/harness-qualification.mjs \
  --run-id <id>-q --evidence-dir <방>/qeva --history-dir <방>/qhist --source "$(pwd)"
# 출력 JSON 의 manifestPath 를 그대로 다음 명령에 넣는다

node scripts/human-use/living-sim-runner.mjs \
  --scenario-file scripts/human-use/living-sim-pilot-v1.json \
  --evidence-dir <방>/evidence --history-dir <방>/history \
  --qualification-manifest <위 manifestPath> --qualification-history-dir <방>/qhist \
  --source-root "$(pwd)" --run-id <id>
```

**A층은 화면·브라우저·채널을 못 본다.** `EXTERNAL_EFFECT_HANDS`(`desktop.act`·`browser.act`·
발신 셋)가 쓸 수 있으면 **시험을 거부한다** — 밖으로 나가는 손이라 방에 가둘 수 없어서다.
그래서 B층 러너가 따로 있다.

### 회귀·게이트

```bash
npm test        # 3,600건대 · 자동이라 공짜. 되돌아가지 않았다는 증거
npm run gate    # 격리 worktree 가 열려 있으면 무조건 BLOCK 이 뜬다(알려진 가짜 빨강).
                # 방을 걷은 뒤 정본 자리에서 돌린다
```

---

## 4. 만드는 법 — 네 걸음

```
0  비교군 축을 먼저 가져온다   이걸 이미 하는 도구가 있나(클로드코드·헤르메스·cua).
                            **문구가 아니라 축**을 가져온다. 없을 때만 설계한다
1  선빨강                    그 문장이 지금 빨간 것을 눈으로 본다
2  수리                     있는 것을 잇는다. 새 저장소·표면·게이트를 만들지 않는다
3  재채점                    러너를 돌려 그 줄이 초록이 되는지 본다. 아니면 진단이 틀린 것이다
```

**0단계가 가장 빨랐다.** 「정의냐 행동이냐」 축 하나를 가져오자 `canAutoExecute → 언제나 false`
가 드러났고, 몇 달 묵은 자물쇠가 불필요했다는 것이 몇 시간 만에 판명됐다.

---

## 5. 지금 아는 가장 큰 결손 — **루프가 없다** (다음 작업)

```
T5      모델이 도구를 고른다 → 실행 → 답을 쓴다
        src/kernel/turn.js 주석: "안 고르면 그 응답이 곧 답이다(추가 호출 없음)"
        도구 **결과를 다시 모델에게 주고 또 고르게 하는 자리가 없다**
실측    938턴 · **걸음 중앙값 2** · 최대 13
비교군  하고 → 보고 → 또 하고 를 목표에 닿을 때까지
```

라이브에서 본 실패 대부분이 이 하나의 얼굴이다 — 막히면 멈춤 · 손을 잘못 골라도 되돌아오지
못함 · "됐는지 모르겠어요". **손이 없어서가 아니라 손을 한 번밖에 못 써서다.**

**그래서 루프가 먼저다.** 손 개수(28개)·승인 감싸기·능력 산문도 고칠 자리지만,
루프가 서면 잘못 골라도 회복하므로 그쪽 급함이 내려간다.

---

## 6. 배끼면 안 되는 것 — 하나뿐

**비개발자용 안전 경계.** 비교군의 승인 모델은 **diff 를 읽을 줄 아는 사람** 전제다.
사장님은 못 읽는다. 여기를 배끼면 **정직한 무능이 아니라 조용한 사고**가 된다.

```
묻는 것 넷(헌장)   비밀값 입력 · 되돌릴 수 없는 파괴 · 새 상대 첫 외부 전송 · 돈
그 밖             전부 자동. **코드와 헌장이 어긋나면 코드가 결함이다**
원장              실제로 일어난 결과만 확정한다. 말이 아니라 값으로
승인 등급(결정)    저장·활성은 안 묻는다(정의는 세상에 영향 0). **행동에서 묻는다**
                 단 넷을 함께: 출처가 오너 대화일 것 · replay 통과 · 즉시 가시성 · 되돌리기 한 번
```

---

## 7. 상태

| 문장 | 상태 | 근거 |
|---|---|---|
| 1 자기 손 인지 | **빨강** | 라이브 "안 돼요"(1회) · 구조 수리 `claude/kan1-audit-final` 미병합·효과 미확인 |
| 3 창 전환 | 부분 | B층 1/3 — 크롬 ○ · 카톡 ✕(창 여럿에서 멈춤) |
| 4 자력 완결 | **빨강** | B층 ✕ — `browser.observe` 로 갔고 실크롬은 새 탭 |
| 나머지 | 미측정 | B층 문장표에 아직 안 올림 |
| 상태 계측기 | 닫힘 | `scripts/state-probe.mjs` · 본선 병합 완료 |

---

## 8. 밟으면 안 되는 지뢰 (전부 실측으로 확인됨)

```
HOME 격리 금지        화면 드라이버가 죽어 `blocked` 가 난다. 그건 제품 실패가 아니라
                     하네스가 만든 거짓 실패다. 상태 자리·파일 뿌리만 격리한다
오너 실사용 자리       ~/.local/state/gpao-t5 는 **읽기만**. 자격은 복사해서 쓴다
즉흥 문장 금지        §1 표의 문장 그대로 쓴다. 지어낸 문장으로 채점하면 결과 보고
                     기준이 정해진다(실제로 그랬다)
표본 1개로 단정 금지   오늘 표본 1개짜리 진단을 두 번 세웠다가 두 번 폐기했다.
                     기준을 **돌리기 전에** 적는다("3/3 어긋나면 결함, 1/3 이하면 폐기")
게이트 빨강 하나       「인수인계에 없는 sidecar worktree」 한 항목뿐이면 BLOCK 아님(알려진 가짜)
git add -A 금지       다른 세션의 미커밋 변경을 쓸어담는다. 명시 경로만 add
```

---

## 9. 오늘 죽은 가설 (다시 조사하지 말 것)

```
✗ 「능력 문장이 틀려서 자기 손을 모른다」    문장·배선 다 옳다. 여러 손 설명이 한 줄로 뭉쳐 있었다
✗ 「기억 승격 통로가 없다」                있다. server.js 자동반영가능 의 kind==='preference' 한 줄에서 막힌다
✗ 「전·후 대조가 없어서 거짓 성공이 난다」   대조는 있고 3/3 정확했다. **폐기**
✗ 「화면 층 영수증이 틀린다」              독립 기준자 대조 0/3 어긋남. **폐기**
✗ 「문서 생성은 라이브러리가 필요하다」      게이트가 의존성 0 을 강제한다.
                                       textutil(docx) · cupsfilter(pdf) · zip+XML(xlsx) 로 셋 다 실측 성공
✗ 「이미지 나르는 길이 없다」               모든 손에 열려 있다(task-context.js). local.file 에 집는 손만 없다
✗ 「채널은 조회 동사만 붙이면 된다」         보관함이 없다. 오면 즉시 턴이 되고 안 쌓인다
✗ 「F-71·F-72 는 별개 둘」                작업셋이 한 층이라 생긴 한 병
✗ 「격리 증명기를 만들어야 한다」            있다(prove-isolation.mjs · 소비자 23곳). 축이 파일 넷뿐
```

세부 근거는 `design/T5-FOLLOWUP-LEDGER-ko.md`(F-73~F-79)와 git 이력에 있다.
**여기 다시 적지 않는다.**

---

## 10. 일하는 방식

```
· 문서로 상태를 단정하지 않는다. 돌려서 정한다
· 고치면 그 자리에서 러너를 돌린다. 좋아졌는지 말로 하지 않는다
· 새 계획서를 만들지 않는다. 이 문서에 줄을 고친다
· 안 잰 것을 「완료」로 넘기지 않는다. 못 잰 것은 **「계측 불가 · 사유」**로 적는다
· 막히면 멈추고 오너에게 올린다. 조용히 넘어가지 않는다
```

**오늘 하루를 태운 셋**: 새 계획서 만들기 · 문서로 상태 단정하기 · 안 돌려보고 원인 적기.
