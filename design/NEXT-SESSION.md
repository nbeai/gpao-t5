# 인수인계 — 아무것도 모르는 세션이 이 한 장으로 시작한다

> 갱신 2026-08-15 (심야~새벽 터미널 세션 종료 시점 · 인계 관리자 R 직접 실측)
>
> **① 지금 상태 (전부 이 세션에서 직접 센 값)**
> ```
> HEAD        b71f31f9 · 브랜치 claude/p-op-1-a-system-view · 작업트리 깨끗
> 미푸시      0  (origin/claude/p-op-1-a-system-view == HEAD · 원격 tip 대조 완료)
> 검사        4,237 통과 · 실패 0 · 러너 보류(skip) 0
> 게이트      **빨강 1건** — 프로젝트 진입 감사 (→ §9-1. 문구 상한 게이트는 982580c3 에서 은퇴했다)
> 병합 안 된 브랜치 4  codex/terminal-core(20) · codex/terminal-rebuild(7) ·
>                     codex/terminal-stage1(8) · lane/ux(12)  — 전부 판정 대기 (→ §6-판정)
> ```
> ⚠️ 미푸시는 `git log origin/claude/p-op-1-a-system-view..HEAD` 로 센다.
> `origin/HEAD..HEAD` 를 치면 `origin/HEAD=origin/main` 이라 **1,295** 가 나온다 — 사고가 아니다.
>
> 이 문서는 요약이 아니다. **다음 세션이 아무것도 다시 발견하지 않게 하는 자료다.**

---

# 0. 30초 — 지금 무슨 일이 벌어지고 있나

**T5(GPAO-T5)는 「한국형 독립 AI OS」다.** 모델이 아니라 모델을 부리는 커널이다.

**지금 개발의 중심은 터미널 손이다.** 오너 실측(2026-08-15)이 방향을 확정했다:

> 같은 질문("작업 폴더에서 제일 긴 문서가 뭐야?")에
> **클로드코드+소넷 4.6(최저 사양)** 은 셸 2개로 전부 재고 1·2·3위까지 답했다.
> **T5+gpt-5.1(더 강한 모델)** 은 local.file 9회로 두 개만 재고 멈췄다.
> **더 약한 모델이 더 좋은 답을 냈다. 부품 문제가 아니라 배선 문제다.**

이번 세션(2026-08-15 심야~새벽)이 그 배선을 측정으로 확정하고 5칸 나아갔다(F-116~F-120).
현재 라이브 실측: **터미널 참여 9/10 · 1위 정답 10/10 · 2위 오답 9/10**.
남은 표적은 **「끝까지 재는 깊이」 하나**다 — 명령이 `ls -l` 로 얕아 폴더 안을 안 잰다.

**② 진행 중인 슬라이스**: 정본 §5 순서의 **5번(원시 결과·실패 진단 공급 · §5-3)** 한복판.
이번 세션의 세부 기록 전부는 이 한 장에 있다 — **반드시 먼저 읽어라**:
```
docs/03-verification/evidence/terminal-2026-08-15/기준선과-남은자리.md   §3-b · §7~7-f
```
**다음 한 걸음**: 깊이 슬라이스 착수 — **선빨강은 이미 확보됐다**(2위 오답 9/10 · n=10 ·
원본 보존). 코드를 만지기 전에 **손 관리자(t5-hand-keeper)를 §7-2 형식으로 소집**한다.

---

# 1. 무엇을 읽어야 하나 — 순서가 정해져 있다

## 1-1. 저장소 규칙의 최상위는 `AGENTS.md` 다

**저장소 뿌리의 `AGENTS.md` 를 먼저 읽어라.** 그 문서가 정한 것 중 반드시 알아야 할 다섯:

```
① 제품 철학 단일 권위   docs/03-product-plan/GPAO-T5-VISION-AND-PERFORMANCE-PHILOSOPHY-2026-07-27-ko.md
② 계획은 하나뿐        design/T5-PLAN.md — "There is no second plan"
③ 최종 조립 정본       design/T5-FINAL-ASSEMBLY-ko.md  ← T5-PLAN 의 모든 노드보다 앞선다
④ 독립 감사 계약       종료 판정 조항은 2026-08-14 오너 선언으로 폐지 — 판정은 라이브가 한다
⑤ 고정 인계 문서       GPAO-T5-CURRENT-SESSION-HANDOFF-ko.md (2026-08-11 에 멈춤 · 역사용)
```

## 1-2. 오늘 착수하려면 실제로 읽을 것은 넷

```
1  이 문서 전체
2  docs/03-verification/evidence/terminal-2026-08-15/기준선과-남은자리.md   ← 이번 세션 기록 전부
3  design/T5-FINAL-ASSEMBLY-ko.md §1-0(손의 정의) · §3(라이브 회차 규격) · §5(순서)
4  design/T5-STATE-MAP-ko.md §12(결함 대장 — J12 줄은 낡았다, 아래 §3-보류 1 이 현재값)
```

⚠️ **장부(T5-FOLLOWUP-LEDGER-ko.md)의 꼬리는 F-106(2026-08-12)에서 멈춰 있다.**
F-107 이후(F-107~F-120)는 장부에 없다 — **정본은 커밋 메시지 · 증거 문서 ·
검사 파일(test/f118-*·f119-*·f120-*)이다.** 장부만 보고 "F-106 이 마지막"이라 믿지 마라.
(장부에 입양할지는 열린 판정이다 → §6-판정.)

## 1-3. 기억(메모리) 자리 — 놓치기 쉽다

오너와 쌓은 원칙이 여기 있다(이 저장소 경로 `-t5-p-op` 와 **다르다**):
```
~/.claude/projects/-Users-jyp-Developer-gpao-t5/memory/
```
이번 세션에 둘이 늘었다(둘 다 파일 확인 완료):
```
해법이-안먹히면-본질을-다시-더듬는다.md      즉각 반응 금지 · 전제 재조사·재설계 먼저 (오너 2026-08-15)
보고끝-쉬운설명-작업은-끊지말고-잇는다.md    끊을지 묻는 것 자체가 위반 (오너 2026-08-15)
```
셋째 원칙 — **"증상이 확실해지면 배선은 깔끔하게(덕지덕지 금지)"** — 도 이 세션에 오너가
줬다고 전달받았으나 **기억·저장소 어디에서도 검색 0건**이다(직접 grep). 다음 세션이
기억 파일로 저장해 두는 것이 안전하다.

---

# 1-b. ★★ 작업 루프 열 걸음 — **도구 무관 · 오너 승인 방식 (2026-08-15)**

> 오너: *"지금 이 세션에서 작업을 진행하는 방식이 너무나 맘에 든다. 클로드코드가 아닌
> 코덱스가 인계받아도 이 방법까지 모두 전수받게 하고 싶다."*
> 이 절은 그래서 **저장소 안**에 있다 — 특정 도구의 기억이 아니라 여기가 정본이다.
> 4차 실패하던 터미널을 하루에 수리 6개·라이브 참여 0→9/10 으로 끌어올린 그 루프다.

```
1  선빨강     기계 사실로 실패를 먼저 세운다 (라이브 원본 또는 빨간 검사). 없으면 안 고친다
2  착수 검문   구현자가 아닌 눈 둘에게 착수를 판정받는다 — 판정 전 제품 코드 금지.
             클로드코드는 .claude/agents/ 의 t5-hand-keeper·t5-process-supervisor 를 부른다.
             다른 도구는 그 두 파일(마크다운 계약서)을 읽고 같은 질문지로 자기 검문을 세운다:
             손 관리자 = "무엇을 세우나·요구 폭·닫는 문장·강제인가 유도인가"
             감시자   = "기준 커밋·범위·선빨강 실재·검증 계획·종료 조건" (§2 아홉 줄)
3  선등록     닫는 문장·판정선(숫자)·미달 시 행동·채점 규칙을 **결과 보기 전에** 문서로 커밋.
             결과를 본 뒤에는 자를 바꿀 수 없다(바꾸려면 다음 배치 전에 재선등록+검문)
4  수리       유도(사실 채움) 우선 — 규칙·강제 금지. 특례 없는 균일 배선. 오늘 닫을 최소 단위
5  반대시험   수리를 걷으면 다시 빨간지 — 이것 없이는 그 수리가 원인인지 모른다
6  전체 회귀   기존 검사를 무르지 않는다. 무르면 수리가 아니라 조작(C5)이다
7  라이브     n≥10 · 회차 원본(발화·손 순서·명령 원문·답 전문·재료 실측)을 저장소에 보존
             (scripts/terminal-qualification/그냥써본다.mjs 가 이 규격이다)
8  판정       선등록 자 그대로. **미달이면 수리를 더 얹지 않고 멈추고 본질 재조사**
             (가설 사다리 금지 — 안 먹힌 순간이 전제가 틀렸다는 신호다)
9  정직 봉인   미달도 그대로 커밋한다("판정선 미달 · 멈춤") — 제목에 등급어(완성·해결·소생) 금지
10 보고       결론 → 근거 → 다음 → **쉬운 설명(비유)** → 숫자. 오너는 비개발자다
```

**이 세션의 실측**: 검문 소집 15회 전부가 구현자의 실지적 오류(채점 관대화 2 · 근거 과대 1 ·
범위 누락 다수)를 잡았다. 2번(판정 전 코드 금지)과 8번(미달 시 멈춤)을 건너뛰고 싶은
순간이 정확히 그것이 필요한 순간이었다. 오너 원칙 넷도 같은 날 나왔다:
해법이 안 먹히면 본질 재조사 먼저 · 보고 끝 쉬운 설명 + 작업은 끊지 말고 잇기 ·
증상이 확실하면 배선은 깔끔하게(특례·복제 금지) · 사람 시간 은유 금지(기계 사실로 말하기).

# 2. 무엇을 기준으로 삼나 — 오너 절대 원칙 (변한 것 없음 · 핵심만)

```
존재이유          사용자 목적 달성. 그 위에 아무것도 없다
손의 정의         손 = 기능 × 성능. 특정 영역만 잘하면 로봇팔이다 (정본 §1-0)
검증 단위 다섯     배선·원장·관절·손·성능 — 앞의 셋이 초록이어도 넷째가 빨강이면 손은 빨강
강제가 아니라 유도  리트머스: "이걸 넣으면 모델이 더 잘 판단하게 되나, 판단을 안 하게 되나?"
만점 아니면 빵점   완료 선언에만. 진단 기록에는 어디서 멈췄는지 남긴다. 안 잰 회차는 「미측정」
판정은 라이브가    "유일한 감사는 실제 콘솔 라이브 결과다" (오너 2026-08-14)
말귀의 정의        파악 → 행동 → 목적까지의 루프. 파악 없이 묻는 것이 떠넘김 (오너 2026-08-14)
기본부터          오픈클로·헤르메스·클로드코드가 하는 것이 기본. 시험도 그들이 하는 것을
                 하느냐다 — **지어내지 마라** (오너 2026-08-15 · tasks.mjs 가 치른 값)
```

---

# 3. ★ ③ 못 닫고 남긴 것 — 보류 전량 (하나도 빠뜨리지 않았다)

**러너 수준의 skip 은 0 이다**(npm test 실측: skipped 0 · todo 0). 아래는 **문서·측정
수준의 보류** 전량이다. 각각 「왜 보류했나 · 남은 길」을 적는다.

## 3-1. J12 — 모델 답 갈아치움 4자리 (다음 순서 b)

```
무엇    모델 답을 런타임 문장으로 갈아치우는 자리가 4인데 자는 2만 센다
자리    turn.js:696 · 848 · 849-853 · 3584          ← 이 줄 번호가 현재값이다.
        지도 §12 J12 줄(687·3503·838·840)은 낡았다 — 베끼지 마라
자      test/answer-authorship-lanes.test.js:31 — /정직한답/g 낱말 세기라 2만 센다
왜 보류  깊이 슬라이스(2위 오답 9/10)가 사용자 영향이 더 커서 순서가 뒤로 갔다
라이브 표본  n=1 있음 — 한 턴에 답 생성 요청 3회(요청 6·7·8), 6의 정답(AGENTS.md 16,051B)이
        7·8에서 오답으로 퇴화. ⚠️ **그 캡처는 세션 스크래치에만 있었다(저장소 밖 · 소멸)**
남은 길  착수 때 **응답 저장까지 하는 캡처**(방법: 기준선 §3-b 모델입출력 캡처)로 표본을
        다시 잡고, 표본 위에서 선빨강을 세운 뒤 연다. n=1 을 결함 등급으로 승격하지 마라
```

## 3-2. F-118 잔여 — 기록만 하고 안 연 것 둘

```
① exit≠0 이면서 looksBlocked 아닌 probe 갈래도 probeChangedNothing 을 받는다.
   failureState 는 막지 않는다(감시자 재현: probe `ls /없는자리` → failureState none · confirmed).
   왜 안 열었나  실린 문장이 "실행했는데 오류로 끝났어요(코드 1)" 로 exit code 를 그대로 말해
   사용자면에서 거짓이 없고, 이전(추정 분류)보다 참에 가깝다.  남은 길  증상(사용자 거짓)이
   라이브에 나오면 그때 선빨강으로 연다
② 검사 ⑥ 은 스텁 구조상 ⑦ 과 거의 같은 것을 문다 — 실질 증명은 ⑧ 이 진다. 기록만
③ 어조 개선(단정형 5/5)의 인과는 **미측정**이다 — 1표본 가설이었고 그 5회는 진단 기록이다.
   통과 증거로 쓰지 마라
```

## 3-3. F-119 잔여 — 같은 기전(로케일)의 다른 문 셋 (안 열었다 · 기록만)

```
local-process.js:120     백그라운드 로그의 한국어 — 같은 기전. 증상 표본 없음
capsule.js:151-158       node 실행이라 증상 미측정
NFC/NFD 정규화           증상 없어 안 연다
남은 길   증상이 라이브에 나오면 그 표본으로 연다. 증상 없이 미리 고치지 마라
```
그리고 F-119 라이브 3/3 은 세 회차 모두 손 순서에 파일 손이 섞여 **이름의 출처를 라이브만으로
격리하지 못한다** — 격리 증거는 `test/f119-terminal-speaks-utf8.test.js` ①(터미널 단독 경로)이다.

## 3-4. F-120 잔여 — 미배선·미측정 셋

```
turn.js:2761 부분합 그물   copy 계열이 목록에 없다(미배선 · 사유 있음). 해가 나는 형상은
                         「복사+같은자리 read 혼합 턴」뿐이고 **표본 0**. 표본이 나오면 별도 선빨강
write 등급의 카드 0        코드 독해 + 라이브 1회라 **미측정 항목**이다
등급 판정 갈라짐           copy 를 특례 없이 write 차선에 뒀다(불변식 「바꾸는 동사는 전부 승인
                         판정을 지난다」 유지) — **감시자의 organize 처방과 갈라진 자리.
                         커밋 검문에 신고돼 있다.** 후속 판정 대상 (→ §6-판정)
```

## 3-5. 이전부터 열려 있는 것 (이번 세션이 안 건드림 · 지도 §12 가 원본)

```
H04 0/5     기억 철회(memory.withdraw)가 target 을 못 맞혀 조용히 지나감 — §5 순서 7번은
            H04 가 서기 전까지 안 닫힌다 (정본 §5 「2026-08-12 갱신」에 실측·기각 이력 전부)
S6          legacy-default-agent(A2·전 도구) 유일 활성이면 무조건 선택      server.js:1041
J5          desktop.act 거절 문구 낡음(실제 17동사)                        desktop-act-tool.js:508
J7          따옴표 든 CSV 는 표 안전망이 조용히 꺼진다                      local-file.js:126
J8          「승인 대기」 줄이 모델에게 영영 안 간다                        working-state.js:171
F3          비밀 이름 정규식이 일반 자료를 잡는다                          file-scope.js
F5          채널 계층이 모든 외부 전송을 A2 로 선언(헌장 ③ 충돌)
C1·X1·X2·X3  지도 §12 그대로 (X3: A층이 화면·브라우저·발신 손을 한 번도 검증 안 함)
카톡·화면 손  §5 순서 9번 — **오너가 후속으로 미룸(공수 과다)**. 자료는 정본 §5-9 계열에 남아 있다
```

## 3-6. 기각·폐기 확정 — **다시 파지 마라** (이번 세션이 값을 치르고 닫은 길)

```
와이어 재생으로 기각 셋   문구 재배치(정체 먼저·경계 앞으로) 첫 수 0/5 — 죽은 채
                        action enum 설명에 경계 한 줄        첫 수 0/5 — 죽은 채
                        선언 분할(읽는 손/정리 손)           7/10 — 집계줄 8/10 과 차이 없음
첫 수(터미널 첫 호출)     **대리지표로 폐기** — 라이브 누적 0/20 인데 참여 9/10 · 1위 정답 10/10.
                        전 회차가 list → terminal 순서 = 「보고 나서 잰다」는 판단이지 결함 아님
와이어 3/8 vs 라이브 0/20 괴리   사용자 영향이 소멸해 닫음
「라이브에서 터미널 원천 불가」 가설   기각 — 캡처 회차에서 첫 수 local_terminal 2연발
가설 1(원문이 안 가서) · 가설 2(능력 문장이 안 가서)   고쳤는데 호출 수 안 움직임 — 단독 범인 아님
가설 3(무서운 문구)      단독 범인 아님 — B팔·D팔이 같은 0. F-116 수리는 사실 정합으로 가치 유지
```

---

# 4. ④ 이번 세션이 밟은 사실 — 다시 재지 않아도 된다

## 4-1. 봉인·푸시된 커밋 8개 (전부 원격 확인 완료)

```
539322f8  죽은 세션의 프라임에이전트 오픈북 원본·포인터 입양
301672e9  F-116(터미널 선언 수리 입양) + F-117(파일 손↔터미널 경계 사실 양쪽) — 진단 기록
d7426110  F-118: 성공한 probe 관측이 원장에서 「추정」으로 분류되던 자리 — 확인으로
f3da0917  F-118 증거 정직: 라이브 원본 미보존 사실과 반대시험 출처를 봉인 문서에 그대로 적음
a12bea2b  하네스(그냥써본다.mjs)가 회차 원본을 저장소에 남김 + 기준선 10실행
2e767ccd  F-119: 터미널이 한국어 파일 이름을 읽는다 — spawn env 에 LC_CTYPE 한 칸(유도)
26609906  F-120: 파일 손이 복사를 안다 — copy·bulk_copy 를 bulk_move 와 한 갈래로
b71f31f9  ② 측정: 첫 수는 대리지표였다 — 남은 표적은 깊이 하나 (참여 9/10)
```

## 4-2. 원인 확정 — **범인은 파일 손의 선언이었다** (측정으로 · 기준선 §3-b)

네 번 실패한 방법(가설 하나 고르기→한 겹 고치기→라이브→안 움직임)을 버리고
**맨몸 대조군(A~E팔) + 겹 얹기 이분탐색**으로 바꿨다:
```
A  터미널만·중립 선언            2·2·2·2   부품 무죄
D  파일 손 + 터미널(중립 선언)    0·0·0·0   ★ 문구를 고쳐도 죽어 있다 — 범인은 문구가 아니다
E  파일 손이 자기 한계를 말함      1·2·3     ★ 전부 살아남 — 유도의 증거
```
죽이는 겹 = **파일 손 선언이 과대 선언하고 한계를 말하지 않는 것**
(`demo-context.js:774` capability · `:790` schema). 수리는 E팔 검증대로 빠진 사실 채우기(유도).
⚠️ **맨몸판 자로 쓸 수 있는 숫자는 터미널 호출 수 하나다**(손 관리자 정정) — 「답 정답」은 못 본다.

## 4-3. 현재 라이브 실측값 (HEAD 26609906 · 10실행 · 원본 보존 · 기준선 §7-f)

```
터미널 참여      9/10    (기준선 f3da0917 시점 4/10 에서 뜀 — F-120 동사 10개가 눌렀다는 긴장은 기우)
답 1위          10/10   AGENTS.md (바이트 기준 참)
답 2위          9/10 오답  ★ 다음 선빨강 — README(11,373B)를 2위라 말함. 실제 2위는
                        시작문서/현재상황.md(15,837B). 폴더 안을 안 쟀다
기제(명령 원문)  터미널 명령이 `ls -l` — 최상위만 보는 얕은 눈. 재귀(find|wc)로 안 간다.
                list 결과에서 시작문서는 「(폴더)」로만 보여 크기가 안 보인다
```
증거 원본(라이브 원문 · 전부 커밋됨):
```
docs/03-verification/evidence/terminal-2026-08-15/그냥써본다-원본/   33개 (터미널 명령 원문까지)
docs/03-verification/evidence/terminal-2026-08-15/맨몸대조군-*.json  6개
docs/03-verification/evidence/live5-2026-08-14/run-*/               9회차 (앞 세션 라이브 전량)
```

## 4-4. 측정 도구 셋 — **다음 세션은 가설 대신 이걸 쓴다**

```
scripts/terminal-qualification/그냥써본다.mjs   오너 말투 발화를 그대로 침 · 회차 원본
                                             (발화·턴별 손 순서·답 전문·터미널 명령 원문·
                                             HEAD·모델)을 그냥써본다-원본/ 에 남긴다
scripts/terminal-qualification/맨몸판.mjs      커널 겹 없는 대조군(A~E팔) · 유효 숫자는 호출 수 하나
모델입출력 캡처 → 절제 재생                     제품이 모델로 보낸 요청 원문을 그대로 다시 쏘며
                                             변수 하나씩 절제 — 방법은 기준선 §3-b
```
⚠️ `tasks.mjs`(지어낸 과업 다섯)는 개발자 시험이었다 — **폐기된 방법이다. 다시 쓰지 마라.**

## 4-5. 그 밖의 확정 사실

```
스키마 재동결        172d1402 (신고문 있음 · F-116 봉인 때)
캐시 지문           59d68451 로 2종 → 1종 (잡담↔일 전환 때 접두 5,118자+스키마 9,836자 재청구가 사라짐)
F-116 단독 무효     F-116 만 걸린 트리 실측: 터미널 0회 · local.file 6회 — D팔 예측과 일치
F-119 단일변수      wc -c 현재상황.md — LANG 없음 "15837 ????.md" · 로케일 있음 "15837 현재상황.md"
F-120 선빨강        복사 요구에 bulk_move — 시작문서가 통째로 비는 사고, 디스크 재현(표본 2)
게이트 문구 상한     982580c3 에서 은퇴 — 「후속/아직 표현 12→13」 빨강은 이제 없다
```

---

# 5. ⑤ 이번 세션이 틀렸던 것 — 같은 자로 또 재면 또 틀린다

```
① 가설을 순서로 태웠다 — 네 번 실패. 가설 1·2·3 전부 터미널 쪽만 봤는데 범인은 옆
   (파일 손 선언)이었다. **양 끝을 재는 도구(맨몸판·절제 재생)가 답이었다**
② 지어낸 과업 다섯(tasks.mjs)이 개발자 시험이었다 — 비개발자 제품에 개발자 시험지.
   하루를 잡아먹었다. 시험은 「오너가 클로드코드에게 하는 말 그대로」다
③ 첫 수를 표적으로 세웠다 — 대리지표였다(참여·정답이 진짜 값). 0/20 을 보고도
   n 을 쌓은 뒤에야 폐기했다
④ 맨몸판의 「답 정답처럼 보임」을 믿을 뻔했다 — 진값은 wc -l(줄 수)인데 모든 팔이
   바이트로 답했고 줄 수 1위 ≠ 바이트 1위. 손 관리자가 정정했다
⑤ 앞판의 "손 6회"류 숫자는 세션 누적이라 턴별 실측보다 부풀려 있었다 — 하네스를
   턴별로 갈라 고쳤다(a12bea2b)
⑥ F-118 라이브 5회를 원본 미보존(세션 콘솔 관측)으로 적을 뻔했다 — 감시자가 잡았고,
   같은 날 하네스 원본 보존으로 갚았다(§7-c)
⑦ 표본 1 승격 금지 · 자격 파일 단일 문(model-connection-facts.mjs) · 기존 검사 먼저 읽기 ·
   과거 게이트 숫자 재사용 금지 — **전부 이번에도 한 번씩 물렸다.** 규율이 문서에만 있으면
   또 밟는다. agent-start 와 검사가 무는 것만 안 밟았다
```

**규율 (이 세션이 재확인)**: 음성 결과가 나오면 자를 먼저 의심한다 · 해법이 안 먹히면
겹을 더 쌓지 말고 **본질 재조사부터**(오너 원칙 · 기억 저장됨) · 증상이 확실해지면 배선은
깔끔하게 — 덕지덕지 금지.

---

# 6. ⑥ 열려 있는 판정 — 오너·후속 결재 대기

```
브랜치 4개        codex/terminal-core(20 · 08-14 12:30 tip) · codex/terminal-rebuild(7) ·
                 codex/terminal-stage1(8) · lane/ux(12 · 08-13) — **읽고 살릴지 버릴지 판정하라.
                 자동 병합 금지**(agent-start 지시). lane/ux 는 앞 세션 기록상 충돌 없이 병합
                 가능하며 병합은 오너 판단이다. codex/terminal-* 셋은 08-14 병렬 터미널 작업 —
                 본선이 같은 자리를 측정 기반으로 다시 밟았으므로 **겹침 대조 후** 판정할 것
자리 밖 worktree  /private/tmp/t5-check-271 · /private/tmp/t5-terminal-core — 게이트 FAIL 의
                 직접 원인. 남의 세션 것일 수 있다 — **지우기 전에 소유를 확인하라**
F-120 등급       copy 를 write 차선에 둔 판정이 감시자의 organize 처방과 갈라졌다(커밋 검문에
                 신고됨) — 후속 판정 대상
장부 공백        F-107~F-120 이 장부(T5-FOLLOWUP-LEDGER)에 미기재 — 입양 여부 판정 대상
검사 ⑥/⑦ 겹침   F-118 검사 ⑥ 이 ⑦ 과 거의 같다 — 정리 여부는 후속
```

---

# 7. ⑦ 전체 완성까지 남은 지도

## 7-1. 정본 §5 순서 (오너 승인 2026-08-12 · 바꾸려면 물어라)

```
1  타이핑 과잉 승인 제거              ✅
2  행동 분류 관통                    ✅ (이음매 ⑨ 등재)
3  네이버 사용자 손 0회 재판정        ✅
4  원장 종결 범위 봉인               ◐ 함수 층 6/7 · ⑥ 은 선언된 경계(F-105 · 라이브 재현 없음)
5  원시 결과·실패 진단 공급 (§5-3)    ← ★ 지금 여기. 터미널 「기본 다섯 줄」이 이 칸이다
6  결과 기반 손 전환 확장 (§5-4)      8줄 중 2줄 (F-107 배선)
7  기억 승격 완결 (§5-5)             ◐ H01·H03·H07 5/5 · **H04 0/5**
8  채널 보관함 (§5-6)
9  범용 컴퓨터유즈 열 동작            ⛔ 오너가 후속으로 미룸(공수 과다)
10 7과목 + 4비교군 최종 대조
```

## 7-2. 문장 열둘 점수판

**이번 세션은 안 쟀다 — 미확인.** 앞 세션 판정(잴 수 있는 것 2·5·8·12 안팎 · 7·10·6 은
미측정(자격 필요)로 확정하고 뺀다)이 마지막 기록이다. 손 관리자 경고 유지:
**문장 열둘은 「손 점수판」이 아니다**(영역당 1~2칸 · 터미널 0칸).

## 7-3. 다음 순서 셋 (이 세션 종료 시점의 합의)

```
(a) 깊이 슬라이스     선빨강 확보됨(2위 오답 9/10 · n=10 · 원본 33개). **손 관리자 소집부터.**
                     기제까지 좁혀져 있다 — 명령이 ls -l 로 얕음, 폴더 크기가 안 보임
(b) J12 4자리        turn.js:696·848·849-853·3584. 라이브 표본은 소멸 — **응답 저장 캡처로
                     다시 잡은 뒤** 착수 (→ §3-1)
(c) ④ 정면 대조 라이브  닫는 문장 판정 — "오너가 클로드코드에게 하는 말을 T5 에 그대로
                     넣었을 때 같은 결과가 나온다"(기준선 §6). 클로드코드(소넷 4.6) 답과
                     나란히 놓고 판정. 멈춤 조건: 고쳤는데 답이 안 달라지면 멈추고 다시 잰다
```

---

# 8. ⑧ 착수하면 바로 부를 것 — 에이전트

**이 세션에서 손 관리자 3회 · 감시자 6회를 소집했고 전부 실지적을 냈다** — 소집을 건너뛰지
마라. 실제로 문 것: 맨몸판 자의 한계(손 관리자) · 라이브 원본 미보존(감시자) · copied 소비자
전수 열거(감시자 재검문) · F-118 라벨·가용성은 발동 조건이 아님(둘 동시) · LC_CTYPE 값(손
관리자 실측) · exit≠0 probe 갈래 재현(감시자).

```
t5-hand-keeper        ★ 착수 직전·봉인 직전. 깊이 슬라이스는 이 소집부터 시작한다
t5-process-supervisor 착수 · 수정 전 실패 확인 · 구현 직후 · 공유파일 통합 전 · 회귀 직전 ·
                      커밋 직전 · 봉인 보고 직전 (일곱 시점)
t5-map-keeper         착수 때 "지도의 어느 줄에서 나오나" — J12 줄 번호 갱신도 이 손이다
t5-handoff-keeper     포화 신호가 보이면 즉시. 이 문서를 다시 쓴다
```
소집 형식(감시자 ①~⑨ 아홉 줄 — **① 기준 커밋을 빠뜨리면 무조건 「조정」 판정**) ·
맥락 승계 금지(매번 밟은 사실을 다시 실어 보낸다) · 손 관리자 다섯 물음(0번 「몇 개를
낫게 하나」부터)은 `.claude/agents/*.md` 원문이 정본이다.

---

# 9. ⑨ 밟으면 안 되는 것 — 이 세션과 앞 세션이 데인 자리

```
가설을 순서로 태우지 마라            맨몸판·절제 재생으로 양 끝을 재라. 가설 셋 = 네 번의 실패
개발자 시험을 지어내지 마라           오너 말투 발화 그대로 (그냥써본다.mjs). tasks.mjs 는 폐기
첫 수를 표적으로 다시 세우지 마라      대리지표로 폐기됐다 (0/20 인데 참여 9/10 · 1위 10/10)
기각 확정 셋을 다시 파지 마라         문구 재배치 · enum 경계 한 줄 · 선언 분할 (→ §3-6)
자격 파일을 열지 마라                model-connection.json 에 API 키 원문.
                                  문은 하나: node scripts/model-connection-facts.mjs
                                  (test/credential-file-single-door.test.js 가 문다)
기존 검사가 초록인 자리를 고칠 때      그 검사부터 읽어라 — 이번에도 물렸다
표본 1 을 승격하지 마라              어조 5/5 인과 미측정 · B팔 1표본 단정 금지가 그 사례
과거 게이트·검사 숫자를 베끼지 마라    직접 세라. 이 문서의 숫자도 다음 세션엔 낡는다
/tmp 와일드카드 삭제 금지            자기가 만든 경로 변수만. 남의 worktree 가 /tmp 에 있다
서버는 --no-open · process.exit 금지  오너 크롬 탭 · 좀비 서버 사고 이력 (agent-start 원문)
git add -A 금지 · 명시 경로만        다른 세션의 미커밋을 쓸어담는다
새 계획서를 만들지 마라              계획은 T5-PLAN 하나
낡은 J12 줄 번호(지도 §12) 금지      현재값은 turn.js:696·848·849-853·3584 (→ §3-1)
```

## 9-1. 게이트가 빨간 이유 — 지금은 **1건**이다 (2026-08-15 실측)

```
BLOCKED — 프로젝트 진입 감사 1건
  · 자리 밖 worktree 둘: /private/tmp/t5-check-271 · /private/tmp/t5-terminal-core
  · 방치된 방: ../t5-lanes/ux (48시간 · 미병합 12커밋)
  · (작업 중 방 둘 — terminal-rebuild·terminal-stage1 — 은 실패 아님 · 아직 안 잊힘 표시)
문구 상한(「후속/아직 12→13」) 빨강은 982580c3 에서 은퇴 — **앞판의 「빨강 2건」은 낡았다**
```
푸는 길: 자리 밖 worktree 소유 확인 후 정리 + lane/ux 판정(→ §6). **자동 병합 금지.**

---

# 10. 다음 세션이 첫 30분에 할 일

```
1  node scripts/agent-start.mjs                            착수 점검 (어긋난 칸 그대로 옮겨 적기)
2  docs/03-verification/evidence/terminal-2026-08-15/기준선과-남은자리.md  §3-b·§7~7-f
3  design/T5-FINAL-ASSEMBLY-ko.md §1-0 · §3               손의 정의 · 라이브 회차 규격
4  t5-hand-keeper 소집                                    깊이 슬라이스 착수 판정 —
                                                          선빨강(2위 오답 9/10 · n=10 · 원본
                                                          그냥써본다-원본/)과 기제(ls -l 얕음)를
                                                          §7-2 다섯 물음 형식으로 실어 보낸다
5  판정을 받은 뒤에 코드를 만진다                            판정 전에 고치지 마라
```

**다음 세션 첫 3분**: `node scripts/agent-start.mjs` 를 돌리고, 기준선 문서 §7-f(깊이 선빨강
2위 오답 9/10)를 연 뒤, 손 관리자 소집문 작성부터 시작한다 — 코드는 그 다음이다.
