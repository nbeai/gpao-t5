# 다음 세션이 첫 줄에서 읽는 것 (2026-08-13 · 8시간 세션 인계)

> 오너 지시: *"작업 전체가 끊임없이 이어질 수 있도록 완벽하게 세팅해라. **부분이 아니라
> 전체 완성이다.**"* — 이 문서는 요약이 아니다. **다시 발견하지 않기 위한 자료**다.

---

## ① 지금 상태

```
HEAD        1e14eaf   (원격과 같음 · 미푸시 0)
미커밋      0
검사        4093 통과 · 실패 0 · **보류 1**
게이트      `npm run gate` 는 lane/ux 미병합 때문에 빨갛다 — 내 작업 아님(§⑨ 참조)
브랜치      claude/p-op-1-a-system-view
```

**착수 첫 명령**: `node scripts/agent-start.mjs`

---

## ② ★ 개발의 틀이 이번 세션에 바뀌었다 — 이것부터 읽어라

코드보다 이것이 먼저다. 정본 `design/T5-FINAL-ASSEMBLY-ko.md` **§1-0** 에 실려 있다.

### 손의 정의 (오너 2026-08-13)

```
손 = 기능 × 성능        더하기가 아니라 곱하기이고 함수다
```

*"터미널 손은 터미널을 만지는 기능과 **터미널을 만져서 할 수 있는 일들의 성능과 수준**을
가리킨다. **특정 영역의 일만 잘 할 수 있다면 그건 손이 아니라 로봇팔이다.**"*

**도구 수를 세지 마라.** `web.search`·`browser.observe`·`desktop.screen` 은 각각 손이 아니라
**웹 손이라는 한 함수가 쓰는 눈과 관절**이다.

### 검증 단위가 다섯이다

```
배선 검사   도구가 호출됐다
원장 검사   영수증이 생겼다
관절 검사   동작이 실행됐다
손  검사   **사용자 목적이 정확한 결과로 끝났다**    ← 완성의 기준
성능 검사   비교군보다 품질·시간·개입이 낫다
```

**앞의 셋이 초록이어도 넷째가 빨강이면 손은 빨강이다.**

### 만점 아니면 빵점 (오너 2026-08-13)

> *"목적대로 정상 작동하면 그게 만점인거지. **개발에는 만점 외에는 점수가 없어.
> 백점만점이 아니면 다 빵점인거야.**"*

`5/8`·`2/3` 같은 부분 점수를 결과로 적지 마라. 목적이 끝났거나 아니거나다.

### 강제가 아니라 유도 (오너 2026-08-13)

> *"모델 다루는 법을 절대 잊지마. **엘엘엠을 깡통으로 만드는건 한순간**이니까."*

수리 전 리트머스: *"이걸 넣으면 모델이 **더 잘 판단하게** 되나, **판단을 안 하게** 되나?"*
어법 넷과 경계선(*"결과가 같고 방법만 다르면 코드가, 결과가 달라지면 모델이"*)은 §1-0 에 있다.

---

## ③ 못 닫고 남긴 것 — **보류 1건. 잊으면 영영 안 열린다**

### `test/f110-…test.js` ③ (`test.skip`)

**무엇을 재려 했나**: 예약 봉투가 `allowedKinds` 에 `read` 를 담아야 한다. 손을 여럿 줘도
kind 가 한 칸이면 「목록 본 뒤 그 파일을 읽기」조차 `agent_tool_kind_outside_scope` 로 막힌다.
**손 관리자 원문**: *"도구를 넓히고 kind 를 안 넓히면 넓힌 손은 장식이다."*

**왜 보류했나**: 봉투(`allowedTools`/`allowedKinds`)를 넓혔더니
`agent-runner.js:217` 의 등가 검사가 `boundedChildToolAllowlist`(= 실재하는 손으로 좁힌 결과)와
안 맞아 **성장 자동화 tick 셋이 통째로 죽었다.** 되던 것을 깨뜨렸으므로 되돌렸다.

**남은 길 둘** — 어느 쪽이든 `src/kernel/l5-growth/agent-run.js` 를 함께 봐야 한다:
1. 등가 검사가 「좁혀진 결과」를 인정하게 한다(봉투 ⊇ 실행 목록을 허용).
2. 봉투를 담을 때 `selfState` 로 한 번 걸러서 실재하는 손만 넣는다.

**재현**: `node --test test/f110-a-scheduled-hand-can-change-its-means.test.js`
(skip 을 풀면 빨강) · 깨지는 것 확인: `node --test test/automation.test.js`

---

## ④ 이번 세션이 밟은 사실 — **다시 재지 마라**

### 닫은 것 여섯

| | 무엇 | 라이브 증거 |
|---|---|---|
| F-104 | 계측기가 「0건」을 「없다」로 읽었다 — 부재는 양성 대조 없이 선언 못 한다 | 검사 3건 |
| F-105 | 안 잰 0 을 0 으로 읽었다(계열 넷째) + 조항 ⑥ 라이브 판정 | 10회차 · 거짓실패 0/10 |
| F-106 | 만들어 놓고 어디에 뒀는지 안 말했다 | **6/8 → 0/8** |
| F-107 | 빈손으로 돌아온 걸음도 막힌 걸음이다(손이 로봇팔처럼 움직였다) | 웹 영역 10요구 통과 |
| F-108 | 옮긴 것을 한 일로 안 셌다 + 그것을 재던 검사가 손을 안 보고 있었다 | 검사 6건 |
| F-109 | 예약이 사용자의 집을 못 찾아 조용히 죽었다 | **만점**(아래) |
| F-110 | 예약된 손이 갈아탈 관측 손을 쥔다 (봉투는 보류) | 검사 4건 |

### 자동화가 이제 만점을 낸다 (F-109 라이브)

```
--양성   격리 집에 PDF 3개를 심고 예약을 돌렸다
        → "오늘 새로 생긴 PDF는 3개입니다. - 영수증-1.pdf - 영수증-2.pdf - 영수증-3.pdf"
        심은 3 = 답한 3. 파일 이름까지 맞다.
--실패   첫 수단을 막았다(폴더를 없앰)
        → "제가 다루는 폴더 안에서 ~/Downloads 를 찾지 못했어요."
        침묵이 아니라 **원인이 붙은 통지**다.
```

**이전 5/5 는 「돌았다·도착했다」였다**(관절·원장 검사). 이제 목적을 잰다.

### 원장 반대시험 일곱 — 함수 층 **6/7**

⑥(거짓 실패)만 열려 있고, 그것은 **선언된 경계**다(정의역 `실제 > 0`).
라이브 10회차에서 사용자 피해 재현 0. **「7/7」로 적지 마라** — 네 문서가 그 숫자로 어긋난 적 있다.

### 웹 영역 10요구 실측 (F-107 뒤)

`node scripts/live/web-hand.mjs --run` 으로 재현한다(양성 대조를 먼저 돌리고 어긋나면 회차를
아예 안 돈다). 대부분 목적을 달성하며, 남은 구멍은 **동적·차단면에서 화면 손으로 넘어가는 것**이다.
`desktop.screen` 은 라이브에서 아직 한 번도 안 쓰였다.

---

## ⑤ ★ 내 자가 이 세션에서 여러 번 틀렸다 — **같은 자로 또 재면 또 틀린다**

```
① 기존 검사가 손이 안 내는 칸(`{path}`)을 지어내 넣고 있었다 — 실물이 안 맞는데 내내 초록
   (F-108. 손은 `{from,to}` 를 낸다)
② 브라우저를 안 물린 채 재고 「브라우저로 안 넘어간다」고 적을 뻔했다 — 손이 아예 없었다
   (F-107. 지금 대본은 findBrowserSync 로 진짜 크롬을 문다)
③ PDF 를 심고 1 이 나오는지 본 적이 없다 — 「0개」가 「0이 맞다」인지 「이 자로는 못 본다」인지
   구분 못 했다 (F-109 계측기. 손 관리자가 잡았다)
④ 답 끝의 "더 필요하면 말해줘" 한 줄을 떠넘김으로 세서 **달성한 회차 둘을 미달로** 찍었다
⑤ 「안 멈췄다」를 「해냈다」로 읽었다
```

**규율**: 음성 결과(0건·없음·안 됨)가 나오면 **자를 먼저 의심하라.** 양성 대조를 세워
그 자가 실재하는 것을 잡는지 보인 다음에 0 을 적어라(F-104·F-105).

---

## ⑥ 열려 있는 판정 · 지켜야 할 조건

- **손 관리자가 준 남은 칸 여섯**(자동화 봉투 계열):
  ```
  ① 도구표 절단     프로필은 넓혔다 · **봉투가 남았다**(=③ 보류 건)
  ② kind 한 칸 고정  ①과 한 몸
  ③ 맥락 공백       runtime:491-496 이 memory:{}·recentTurns:[]·activeGoal:null
                   ※ 「목적을 모른다」는 **틀린 진단**이다 — skill.purpose 는 사용자 원문 그대로 간다
  ④ 목적↔결과 대조   자동화 run 안에 「미달」 판정이 없다. status 만 있다
  ⑤ 실패의 배달     **이번에 섰다**(원인 붙은 통지 실측)
  ⑥ 회차 간 상태     이번 회차 결과가 다음 회차에 안 남는다
  ```
- **봉투를 넓힐 때 승인면도 같이 고칠 것**(손 관리자 조건). 안 하면 「몰래 넓히기」다.
  담당 이름이 승인 카드에 그대로 나간다.
- **오너 결재 대기**: F-97(갈아치움 목표 0 ↔ 절대 게이트 상충) — 장부에 있다.
- **정본 §5 순서는 오너 승인 사안**이다. 바꾸려면 물어라.

---

## ⑦ 전체 완성까지의 지도

```
§5      여섯 칸    5-1 원장 종결자 · 5-2 네 경계 단일 권한 · 5-3 입력/출력 분리
                  5-4 결과가 다음 손을 고르게 · 5-5 기억 완결 · 5-6 채널 보관함
   ↓
§1-1    문장 열둘   각각 라이브 초록 3회 연속 = 바닥 통과
   ↓
§1-2    바닥 결승선  비교군과 같은 과업에서 목적 달성률·시간·개입·복구율·연속성
   ↓
§1-2    우위 축     원장이 사용자 대신 확인하는 제품
   ↓
완성 3   오너 하루 실사용
```

### ★ 가장 큰 빈칸 — **문장 열둘의 점수가 어디에도 없다**

정본 §1-1 이 그것을 「점수판」이라 부르는데 **정본에도 지도에도 장부에도 점수가 안 적혀 있다.**
즉 **「지금 몇 개가 서는가」를 아무도 모른다.** 그래서 개발이 「눈에 띄는 결함」 단위로 흘렀다.

**이것을 재는 것이 큰 남은 일이다.** 웹 손 검사(`web-hand.mjs`)가 그중 두어 개를 덮는다.
나머지를 덮는 손 검사를 세우고, 재고, 그 결과로 순서를 정해야 한다.

### §5-4 만 봐도 여덟 줄 중 한 줄이다

```
파일을 못 찾으면        검색 범위를 바꿈
웹 수집이 막히면        브라우저로 전환          ← F-107 이 배선 하나
브라우저 정밀이 막히면   화면 손으로 전환          ← 아직
화면 접근성이 비면      이미지 관찰로 전환        ← 아직
결과가 잘렸으면         다음 페이지
같은 호출 반복이면      무진전 감지 (있음)
A→B→A→B 이면          ping-pong 감지
사용자가 방향을 바꾸면   현재 목표를 갱신
```

---

## ⑧ 착수하면 바로 부를 것 — 에이전트 넷

```
t5-hand-keeper       손 관리자 H  — **착수 직전·봉인 직전**. "이 영역에서 뭘 요구하든 해내나"
                                  닫는 문장이 「사건」이면 지적한다. 신설(2026-08-13)
t5-process-supervisor 공정감시자 S — 착수·수정 전 실패 확인·구현 직후·공유파일 통합 전·커밋 전
t5-map-keeper        지도 관리자 M — 착수 때 "지도의 어느 줄에서 나오나" · 봉인 때 "어느 줄을 고치나"
t5-handoff-keeper    인계 관리자 R — **포화 신호가 보이면 즉시.** 이 문서를 다시 쓴다. 신설
```

**손 관리자를 안 부르고 착수하지 마라.** 이번 세션에서 그가 내 오진 하나를 잡았고
(「목적을 모른 채」는 틀렸다 — purpose 는 간다), 착수 문장이 「능력 선언」임을 지적해
반증 가능한 문장으로 바꿔 줬다.

---

## ⑨ 밟으면 안 되는 것

- **되던 것을 깨뜨린 자리**: F-110 봉투 확장 → `agent-runner.js:217` 등가 검사 ↔
  `boundedChildToolAllowlist`. 넓히려면 그 둘을 함께 봐라.
- **게이트 빨강은 `lane/ux` 미병합**(커밋 11개)이다. 내 작업이 아니고, 그 라인과
  겹치는 파일도 0이다. 병합은 그쪽 결정이다.
- **자격 파일(`model-connection.json`)은 열지 마라.** API 키 원문이 있다. 복사(`cp`)도
  차단된다 — 라이브가 필요하면 대본의 `연결옮기기()` 경로를 쓴다.
- **오너 자리(`~/.local/state/gpao-t5/sessions/`)는 읽기만.** 라이브는 `GPAO_T5_HOME` 으로
  집을 격리해서 돈다(F-109 로 그 경로가 실제로 살아났다).
- **「7/7」·「5/5」 같은 옛 숫자를 베끼지 마라.** 직접 세라.

### 라이브 대본 쓰는 법

```bash
node scripts/live/web-hand.mjs --run                       # 웹 손 검사(양성 대조 먼저)
node scripts/live/automation-close.mjs "" "<문장>" 4360 1 --양성   # 자동화: PDF 심고 개수 맞히나
node scripts/live/automation-close.mjs "" "<문장>" 4362 1 --실패   # 자동화: 막히면 무엇이 오나
node scripts/live/h04-memory-round.mjs --run --rounds 5     # 기억 회차(방하나 를 export 한다)
```
실패한 회차의 방은 **남는다** — 경로가 출력된다. 치우면 왜 죽었는지 아무도 못 본다.

---

## 다음 세션이 첫 3분에 할 일

`node scripts/agent-start.mjs` → 정본 §1-0 을 읽어 **손의 정의**를 머리에 넣고 →
**t5-hand-keeper 를 불러** 「지금 어느 손이 몇 칸 남았나」를 받은 뒤 그 답으로 착수 대상을 고른다.
결함이 눈에 띈다고 바로 고치지 마라 — 그것이 이 세션이 오너에게 지적받은 병이다.
