# 다음 세션 지시문 (새 대화에 이 파일 경로만 주면 된다)

> 새 대화를 열고 이렇게만 치면 됩니다:
> **`/Users/jyp/Developer/t5-p-op/design/NEXT-SESSION.md 읽고 그대로 진행해`**

---

저장소 `/Users/jyp/Developer/t5-p-op` (한국형 AI OS "T5"). 브랜치 `claude/p-op-1-a-system-view`.
**보고는 전부 한국어로.** 오너는 윤님이다. 응답 후반에 **오너용 쉬운 설명**을 붙인다
(전문 용어 없이, 사용자가 무엇을 겪게 되는지로).

## ★ 첫 줄 — 이 둘부터
```
node scripts/agent-start.mjs
cat design/T5-HANDOFF-2026-08-12-ko.md
```
착수 점검이 어긋난 칸을 내면 **고치고 시작한다.** 2026-08-12 에 이 점검이 네 회차에서 **네 번 다**
낡은 커밋을 잡았다(한 번은 임무가 지목한 파일이 아예 없는 커밋이었다).
인계 문서에 좌표와 **그날 여섯 번 데인 계측 자리**가 다 있다.

## 임무 — 손 이름 하나. 이것으로 자동화가 진짜 닫힌다

**증상**: 예약이 라이브 26회차 중 **18회만** 선다. 원인은 트리거가 아니라 **손 이름**이다. 상관 6/6:
```
tool=functions.local_file → job 0건 (실패 전부)
tool=local.file           → job 1건 (성공 전부)
실패 회차에 daily 도 있었다 — `once` 만의 문제가 아니다
```
**모델은 도구를 `local_file` 로 본다**(점이 안 되는 공급자 때문에 `wireToolName` 이 바꾼다).
자동화 예약은 `local.file` 만 받는다.

**★ 이 저장소가 같은 자리를 이미 겪고 풀었다** — `src/runtime/capsule.js:186-190` 을 열어 읽어라:
> *"모델은 도구를 `local_file` 로 본다 … 캡슐만 `local.file` 을 요구하면 모델이 매번 헛손질하고,
>  실측(2026-08-04 라이브)에서 정확히 그랬다: 다섯 번 재시도했고 매번 호출 0이었다.
>  **이름이 두 벌인 것은 우리 사정이지 모델 잘못이 아니다.**"*

캡슐은 두 이름을 다 받는다. **자동화만 안 받는다.** 고치면 `once`·`daily`·`weekly` 가 한꺼번에 닫힌다.

## ⚠️ 어디서 정규화하느냐가 판정이다 — 이게 이 과업의 알맹이

관대하게 받으면 2026-08-12 에 닫은 **「아무 스킬에나 묶는 자리」**가 다시 열린다:
후보에 `skillRef` 가 없을 때 **활성 스킬 배열의 첫 항목**을 집던 결함이었다
(재현: "새 PDF 개수"가 `skill-invoice`(청구서 정리)에 묶임). 지금은 `skill_binding_required` 로 거절한다.
**그 봉인을 깨지 마라 — 반대시험으로 세워라.**

후보 자리: 입구에서 한 번 펴기 / 대조하는 자리에서 두 이름을 다 보기 / 캡슐처럼 손 목록에서 되찾기.
**어느 층이 옳은지 재서 정하고 이유를 적어라.** 두 벌이 되면 언젠가 갈린다(이 저장소의 반복 흉터).

## 반대시험 (전부 검사로 · 선빨강 먼저)
```
① functions.local_file 로 부른 예약이 선다 — 밟은 그 자리
② local.file 은 예전 그대로
③ 아는 이름 밖(functions.없는손)은 안 선다 — 그물이 안 넓어졌다
④ 「아무 스킬에나 묶는 자리」가 안 열린다 — skillRef 없으면 여전히 거절
⑤ 실행 시점 헌장 판정은 그대로 — 자동화라고 승인을 안 건너뛴다
⑥ 이름이 두 벌로 살지 않는다 — 정규화가 한 자리에서만 돈다
```

## 라이브 (문서 초록으로 안 닫는다)
```
node scripts/live/automation-close.mjs
```
삼중 금고다(친 문장 / 나온 답 / `automation.json` 실물). **표본은 세 종류 이상**:
`매일 아침 9시에 …` · `내일 아침 9시에 …` · `매주 금요일 저녁 6시에 …`. **각 5회 이상.**
2026-08-12 에 한 종류 3회로 「닫혔다」고 했다가 26회차에서 18/26 이 나왔다. **같은 실수를 하지 마라.**

## 규율
- npm 의존성 0 · 헌장 넷 불변 · 안전 바닥 불변 · **판정 자를 고쳐 초록 만들기 금지**(C5)
- 새 저장소·표면·게이트 금지 · 한국어 주석 · 주변 관습 유지 · **선빨강 먼저**
- `git add -A` 금지(명시 경로만) · 커밋 전 `git branch --show-current` · 끝나면 원격에 푸시
- `scripts/s1/preflight.mjs:35` 기준지문 동결 · 허용파일 목록은 **두 벌**
  (`preflight.mjs` 와 `test/s1-preflight.test.js`) — 움직이면 **양쪽에** 손으로 옮기고 이유를 적어라
- 장부 `design/T5-FOLLOWUP-LEDGER-ko.md` 에 다음 번호로 등재(최신은 F-92)
- 에이전트를 쓰면 브리핑 첫 줄에 `node scripts/agent-start.mjs` 를 넣어라

## ⚠️ 계측 규율 — 2026-08-12 에 여섯 번 데였다
```
서버는 node bin/gpao-t5.mjs --no-open      안 그러면 오너 크롬에 탭이 열린다
서버가 stdout 으로 알려준 포트를 써라        진입점은 포트가 막히면 조용히 옮긴다.
                                          포트를 박으면 남의 서버와 말하게 되고,
                                          그 때문에 5회 연속 거짓 측정을 냈다
process.exit 금지                          finally 를 건너뛰어 좀비가 남는다
회차마다 새 방·새 서버                      방을 재사용하면 중복에 걸려 「실패」로 읽힌다
응답과 저장은 다른 시각이다                  파일이 멎을 때까지 기다린 뒤 읽는다
시각을 옮길 때 아무 과거를 쓰지 마라          레코드의 다음 시각에서 한 주기를 뺀다
오너 자리(~/.local/state/gpao-t5)는 읽기만   자격 사본 금지 · 끝나면 서버를 죽여라
/tmp 에서 와일드카드로 지우지 마라           mkdtemp 가 돌려준 경로만
```
**음성 결과가 나오면 자를 먼저 의심하고 다른 방법으로 한 번 더 재라.**
그날 T5 가 *"이미 후보가 5개 떠 있어요"* 라고 했고 나는 환각으로 의심했다. **T5 가 맞았다.**

## 증명 문장
`npm test` 전체 초록(3,988+신규 · 실패 0) + 반대시험 여섯 초록 + **선빨강 원문** +
**라이브 판정표**(세 발화 × 5회 이상 · `automation.json` 실물로) + 커밋 해시.

## 이것이 닫히면 다음
`design/T5-ASSET-SCORECARD-ko.md` 가 「①②③은 섰는데 ④가 안 되는 것」 셋을 좌표까지 적어 뒀다:
```
local.terminal 이 답을 손에 쥐고도 안 준다   3/3 재현 · local-terminal.js:207·292-294
agent.delegate 가 한 번도 돈 적이 없다      0/3 · 오너 원장 315건 중 0건
local.process·system·discovery             각 0/3
```
그다음은 계획서 `design/T5-FINAL-ASSEMBLY-ko.md` §5 순서(5·6·8·9·10)와
`design/T5-MEMORY-GRAPH-CLOSE-ko.md`(착수 대기 · 닫는 문장 적혀 있음).
