# 인수인계 — 아무것도 모르는 세션이 이 한 장으로 시작한다

> 갱신 2026-08-15 (J12 3차 봉인 직후 · 인계 관리자 R 이 **직접 실행해 센 값만** 적는다)
>
> **① 지금 상태**
> ```
> HEAD        55c284c3  fix(J12 3차): 파악을 마친 물음은 빈손이 아니다
> 브랜치      claude/p-op-1-a-system-view · 작업트리 **깨끗**(git status --short 빈 출력)
> 미푸시      **0**   origin/claude/p-op-1-a-system-view == HEAD (git log @{u}..HEAD = 0)
> 본선 대비    origin/main 기준으로는 **1,309** — 이 브랜치가 한 번도 본선에 안 들어갔다.
>             `git log origin/HEAD..HEAD` 를 치면 이 숫자가 나온다. **사고가 아니다.**
>             다만 「본선 미병합 1,309」 자체는 열린 위험이다 (→ §6)
> 검사        **4,246 통과 · 실패 0 · 러너 보류(skip) 0 · todo 0**  (npm test 실행값)
> 게이트      **빨강 1건** — 프로젝트 진입 감사 (npm run gate 실행값 · 내역 §9-1)
> 병합 안 된 브랜치 4   codex/terminal-core(20) · codex/terminal-rebuild(7) ·
>                     codex/terminal-stage1(8) · lane/ux(12)   — 전부 판정 대기 (→ §6)
> ```
> 이 문서는 요약이 아니다. **다음 세션이 아무것도 다시 발견하지 않게 하는 자료다.**
> 앞판(b71f31f9 시점)의 숫자·표적·「다음 한 걸음」은 전부 낡았다 — 이 판이 정본이다.

---

# 0. 30초 — 지금 무슨 일이 벌어지고 있나

**T5(GPAO-T5)는 「한국형 독립 AI OS」다.** 모델이 아니라 모델을 부리는 커널이다.

**지금 개발의 중심은 터미널 손이다.** 오너 실측(2026-08-15)이 방향을 확정했다:

> 같은 질문("작업 폴더에서 제일 긴 문서가 뭐야?")에
> **클로드코드+소넷 4.6(더 낮은 사양)** 은 셸 2개로 전부 재고 1·2·3위까지 답했다.
> **T5+gpt-5.1(더 강한 모델)** 은 local.file 9회로 두 개만 재고 멈췄다.
> **더 약한 모델이 더 좋은 답을 냈다. 부품 문제가 아니라 배선 문제다.**

이번 세션은 그 배선을 **가설이 아니라 측정**으로 팠다. 하루 동안 닫은 것:
F-116~F-120(앞 구간) → **F-121(깊이 · childCount)** → **J12 1차·2차·3차**.

**② 진행 중인 슬라이스 — 지금 무엇을 하고 있었나**

```
직전에 닫은 것   J12 3차 (55c284c3) — 「파악을 마친 물음은 빈손이 아니다」.
                신규 12사슬 발동 0 · 선등록 예측 4셀 전부 적중 · 양성 대조 16 검출 · 회귀 4,246
열려 있는 슬라이스 **F-121(깊이)** — 코드는 봉인됐으나 **판정선이 두 배치 연속 미달**이라
                슬라이스는 **열린 채**다. 재판 조건은 「J12 해소 후」였고 **이제 충족됐다**
다음 한 걸음     ~~F-121 깊이 재판 재개~~ → **채점기 판별력 수리가 선행한다** (§7-q).
                착수 검문 둘을 밟은 결과 **재는 자가 양방향으로 새는 것이 실측 반례로 확인**됐다
                (L1 거짓 정답 · L2 단정 무늬 누수 · 채점기를 무는 검사 0개). 「무근거 단정 0」은
                자의 오검출 0 을 보이기 전엔 못 쓴다. 그리고 **선등록과 같은 구성의 배치가
                이미 있다**(19-48-44~19-50-04 · 정답 3 · 한정 0 · 무근거 5 · 판정불가 2)
                ⚠️ **착수 직전 감시자(t5-process-supervisor) 검문 필수** — 앞 세션이
                J12 3차에서 검문을 한 차례 건너뛴 위반이 커밋 본문에 기록돼 있다(§5-⑦)
그 다음         ④ 정면 대조 라이브 (클로드코드와 나란히 · 「터미널 정상 가동」 판정)
```

**세부 기록의 정본은 이 한 장이다 — 반드시 먼저 읽어라**:
```
docs/03-verification/evidence/terminal-2026-08-15/기준선과-남은자리.md
  §3-b(범인 확정 측정) · §7~§7-f(F-116~F-120) · §7-g~§7-j(F-121 깊이 · 채점표 v2 · 미달)
  · §7-k~§7-o(J12 1·2·3차)                              ← 이번 세션 후반 전부
```

---

# 1. 무엇을 읽어야 하나 — 순서가 정해져 있다

## 1-1. 저장소 규칙의 최상위는 `AGENTS.md` 다

```
① 제품 철학 단일 권위   docs/03-product-plan/GPAO-T5-VISION-AND-PERFORMANCE-PHILOSOPHY-2026-07-27-ko.md
② 계획은 하나뿐        design/T5-PLAN.md — "There is no second plan"
③ 최종 조립 정본       design/T5-FINAL-ASSEMBLY-ko.md  ← T5-PLAN 의 모든 노드보다 앞선다
④ 독립 감사 계약       종료 판정 조항은 2026-08-14 오너 선언으로 폐지 — 판정은 라이브가 한다
⑤ 고정 인계 문서       GPAO-T5-CURRENT-SESSION-HANDOFF-ko.md (2026-08-11 에 멈춤 · 역사용)
```

## 1-2. 오늘 착수하려면 실제로 읽을 것은 넷

```
1  이 문서 전체
2  docs/03-verification/evidence/terminal-2026-08-15/기준선과-남은자리.md   ← 이번 세션 기록 전부
3  design/T5-FINAL-ASSEMBLY-ko.md §1-0(손의 정의) · §3(라이브 회차 규격) ·
                                  **§4-b(작업 루프 열 걸음 · 영구 정본)** · §5(순서)
4  design/T5-STATE-MAP-ko.md §12(결함 대장)
   ⚠️ **§12 의 J12 줄(543행)은 낡았다** — 줄 번호가 `turn.js:687·3503·838·840` 으로 적혀 있고
   현재값과 다르다. 현재값은 §3-1 에 있다. **베끼지 마라.** 지도 갱신은 t5-map-keeper 소관
```

⚠️ **장부(T5-FOLLOWUP-LEDGER-ko.md)의 꼬리는 F-106(2026-08-12)에서 멈춰 있다**(직접 grep 확인 —
꼬리 세 항목은 F-100·F-102·F-101 열림 블록 + F-105 + F-106 닫힘).
**F-107~F-121 과 J12 는 장부에 한 줄도 없다.** 정본은 커밋 메시지 · 위 증거 문서 ·
검사 파일(`test/f118-*` `f119-*` `f120-*` `f121-*` `j12-*` `answer-authorship-lanes.test.js`)이다.
장부만 보고 "F-106 이 마지막"이라 믿지 마라. (입양 여부는 열린 판정 → §6)

## 1-3. 기억(메모리) 자리 — 놓치기 쉽다

오너와 쌓은 원칙이 여기 있다(이 저장소 경로 `-t5-p-op` 와 **다르다**):
```
~/.claude/projects/-Users-jyp-Developer-gpao-t5/memory/
```
**이번 세션(2026-08-15)에 여섯이 늘었다** — 전부 파일 존재 확인함(ls -lat 실측):
```
04:12  채점은-기계가-나는-주석만.md              같은 관대화 3회 → 지배 숫자는 기계 실행값만
03:48  슬라이스-작업-루프-선등록-검문-멈춤.md      루프 열 걸음 (정본은 §4-b)
03:43  사람-시간-은유로-말하지-않는다.md          "10분이면" 류 금지 · 기계 사실로
03:15  증상이-확실해지면-배선은-깔끔하게.md        덕지덕지·특례·복제 금지
02:07  보고끝-쉬운설명-작업은-끊지말고-잇는다.md   끊을지 묻는 것 자체가 위반
01:18  해법이-안먹히면-본질을-다시-더듬는다.md     즉각 반응 금지 · 전제 재조사·재설계 먼저
```
(앞판이 「검색 0건」이라 적었던 "배선은 깔끔하게"는 **이제 파일로 있다.** 그 줄은 해소됐다.)

---

# 1-b. ★★ 작업 루프 열 걸음 — 정본은 여기가 아니다

**영구 정본: `design/T5-FINAL-ASSEMBLY-ko.md` §4-b (610행)** — 오너 승인 2026-08-15 · 도구 무관.
이 인계서는 교대마다 통째로 다시 쓰는 장부라 여기 옮겨 적지 않는다 — **열어서 읽어라.**
한 줄 요약: 선빨강 → 착수 검문(판정 전 제품 코드 금지) → 선등록 → 수리(유도·균일 배선) →
반대시험 → 전체 회귀 → 라이브 원본 보존 → 판정(미달 시 **멈춤**) → 정직 봉인 → 쉬운 설명 보고.
검문 계약서 둘(`.claude/agents/t5-hand-keeper.md` · `t5-process-supervisor.md`)은 일반
마크다운이라 클로드가 아닌 도구도 그 두 장으로 같은 질문지를 세운다(83d9e147 에서 추적에 올림).
**인계 관리자에게: 이 절(포인터)을 다시 쓸 때도 유지하라 — 오너 지시다.**

# 2. 무엇을 기준으로 삼나 — 오너 절대 원칙 (핵심만)

```
존재이유          사용자 목적 달성. 그 위에 아무것도 없다
손의 정의         손 = 기능 × 성능. 특정 영역만 잘하면 로봇팔이다 (정본 §1-0)
검증 단위 다섯     배선·원장·관절·손·성능 — 앞의 셋이 초록이어도 넷째가 빨강이면 손은 빨강
강제가 아니라 유도  리트머스: "이걸 넣으면 모델이 더 잘 판단하게 되나, 판단을 안 하게 되나?"
만점 아니면 빵점   완료 선언에만. 진단 기록에는 어디서 멈췄는지 남긴다. 안 잰 회차는 「미측정」
판정은 라이브가    "유일한 감사는 실제 콘솔 라이브 결과다" (오너 2026-08-14)
말귀의 정의        파악 → 행동 → 목적까지의 루프. **되물음은 감점이 아니다.
                 파악 없이 묻는 것이 떠넘김** (오너 2026-08-14) ← J12 3차 수리의 근거
채점은 기계가      지배 숫자는 채점기 실행값만. 사람 읽기는 회차 주석 전용 (오너 2026-08-15)
배선은 깔끔하게    특례·복제 금지 (오너 2026-08-15)
사람 시간 은유 금지 "몇 분이면" 류로 말하지 않는다. 기계 사실로 (오너 2026-08-15)
작업은 끊지 말고    "여기서 끊을까요?"를 묻는 것 자체가 위반 (오너 2026-08-15)
보고 끝에 쉬운 설명  결론 → 근거 → 다음 → **비유로 쉬운 설명** → 숫자. 오너는 비개발자다
```

---

# 3. ★ ③ 못 닫고 남긴 것 — 보류 전량 (하나도 빠뜨리지 않았다)

**러너 skip 은 실행값 0 이다.** 다만 코드에는 **조건부 보류 문이 3개** 있다(직접 grep) —
이번 실행에서 셋 다 조건이 안 걸려 안 열렸다. 환경이 바뀌면 열린다:
```
test/local-protection.test.js:252              !sandboxAvailable() → t.skip('샌드박스 없음')
test/lane-guard-one-hand-per-room.test.js:157  core.hooksPath 없음 → t.skip (설치는 환경 사실)
test/f54-screen-is-a-place.test.js:56          동봉/설치 드라이버 없음 → t.skip (정의역 밖)
남은 길  전부 「기계 환경이 없으면 잴 수 없는 것」이다. 결함이 아니다.
        다만 **다른 기계에서 돌리면 4,246 이 안 나온다** — 숫자가 다르면 여기부터 의심하라
```

아래가 **문서·측정 수준의 보류 전량**이다. 각각 「왜 보류했나 · 남은 길」을 적는다.

## 3-1. ★ J12 — 이번에 닫은 것 1, 남긴 것 5 (손 관리자 열거 5 중 1 닫음)

J12 3차가 닫은 것: **되물음을 빈손으로 읽던 규칙**(§7-o). 남은 다섯은 **전부 열려 있다**:

```
① 되부름 산출은 앞답이 빌 때만 되살아난다 — **일반 명제로 쓰면 안 된다**
   무엇   "재생성은 버려진다"는 이번 세션의 서술은 **감시자 정정으로 틀렸다.**
          앞답이 비어 있지 않은 턴에 한해서만 참이다. **앞답이 빈 턴은
          F-84 `되살리기()`(turn.js:2949 · 호출 2955·2960)가 되부름 글로 갈아친다**
   왜 보류  J12 3차 표적(사실을 댄 답 + 물음표)은 앞답이 안 비어 해당 없음. 범위 밖
   남은 길  「앞답이 빈 턴」의 표본을 잡아야 한다. **이 명제를 조건 없이 인용하지 마라 —
          다음 슬라이스가 틀린 전제 위에서 출발한다**
② 빈손 걸음이 `막힌것` 에 안 들어옴 (F-107 가족의 문)
   왜 보류  3차 표적이 아니었다. 수리는 최소 단위로 (루프 4)
   남은 길  F-107 계열 자리부터 읽고, 선빨강(라이브 원본 또는 빨간 검사) 확보 후 착수
③ 걸음 0 턴과 걸음>0 턴이 **서로 다른 자로** 판정됨
   자리   걸음 0 = `한걸음도안뗐다`(turn.js:2822 계열 · 원장 인자라 말투에 안 닿음) ·
          걸음>0 = `걸음이말하는미달`(turn.js:2828 · 말투 갈래)
   왜 보류  3차는 :2828 한 갈래만 건드린다고 선등록에 못 박았다(:2740·:2822 불가침)
   남은 길  두 자를 한 자로 합칠지 판정. 합치면 회귀가 크다 — 검문부터
④ **사슬로는 정오를 안 잰다** (사슬분석기 ↔ 채점기 이음 없음)
   왜 보류  일부러다. 정오 규칙은 `채점기.mjs` 하나뿐이어야 한다(두 번째 자 금지 · 감시자 조정 5)
   남은 길  이으려면 채점기 쪽을 확장하고, **배치 돌리기 전 + 검문**을 지나야 한다
⑤ `안써본손` 게이트가 사실상 **항상 열림**
   근거   재생성 요청 시스템에 매번 "아직 이 턴에 한 번도 안 써 본 손: … (10개)"가 붙는다
   왜 보류  3차 표적 아님
   남은 길  게이트가 실제로 무엇을 가르는지부터 측정(사슬분석기). 규칙을 박지 마라
```

**그리고 J12 의 「자」는 이제 6자리를 문다** — 앞판의 「4자리(turn.js:696·848·849-853·3584)」는
**낡았다.** 31170dc9(J12 1차)가 자를 낱말 세기 → 기제별 정의역 열거로 바꿨고, 그 뒤 3차
수리로 줄 번호도 밀렸다. 현재값(직접 grep 실측):
```
기제 A · 정직한답 (갈아치움 칸 · 목표 0)      turn.js:696 · turn.js:3597
기제 B · fallbackReplyFrom 호출부 3          turn.js:708(빈 답 안전망) · :848 · :852
기제 C · 런타임 작성 리터럴 1                 "방금 요청은 아직 완료하지 못했어요"
자                                        test/answer-authorship-lanes.test.js (합계 단언 6)
무는 범위    A·B 의 **인스턴스 증가**(새 호출부가 생기면 계수가 어긋나 빨개짐)
못 무는 범위  **새 리터럴 문장**(C 는 등록 문자열 자체가 정의역) ·
            모델이 말하기 전에 끝나는 경로의 런타임 답(승인 못찾음/만료 리터럴 둘 — 정의역 밖)
```

## 3-2. ★ F-121(깊이) — **슬라이스가 열린 채다. 코드만 봉인됐다**

```
코드      c9e8477d 로 봉인 — 목록의 폴더가 안의 항목 수를 말한다(childCount)
검사      test/f121-folder-tells-what-is-inside.test.js 4/4 (① 폴더가 안 개수 말함 ② 파일엔
          그 칸 없음 ③ 상한에서 끊고 끊었다고 말함 ④ 반례: 턴 머리 관측엔 안 실림 — F-65 경계)
판정      **두 배치 연속 미달** → 선등록대로 슬라이스를 안 닫았다
  1차 §7-h  정답 2 · 한정·고지 2 · 무근거 단정 6   (판정선: 무근거 단정 0 && 정답 ≥7)
  2차 §7-j  정답 1 · 한정·고지 0 · 무근거 단정 9   ← 지배 숫자 흐름 기준선 8 → 6 → 9
효과 관측  childCount 효과의 직접 관측은 18-30-03 **1건(n=1)** — 시작문서 안 네 개를 이름으로
          나열했다. **n=1 을 효과로 승격하지 마라**
왜 보류    "재판은 J12 해소 후" — J12 가 측정→최종답 구간의 결함 후보였기 때문. **이제 충족**
남은 길    **봉인 재판 구성이 §7-o 에 이미 고정돼 있다**(선등록 복원):
            원발화 2턴 ×6 + 처음 보는 발화 ×4(제일 큰 파일 ×2 · md 개수 ×2) = n 10.
            **무근거 단정 0 은 열 회차 전체에 걸린다**(원발화만 아님 · 1건이라도 나오면 미달).
            정답 ≥7 도 전체 기준. 자는 **채점기.mjs(v2)** 실행값.
            미달이면 수리를 더 얹지 말고 **멈추고 본질 재조사**
```

**이 재판을 열기 전에 반드시 결정할 것 둘 (다음 선등록에 올려야 한다)**:
```
(가) 「기계 판정 불가 2」의 분류 규칙 — J12 3차 회차에서 새로 생긴 칸이다.
     즉석 분류를 금지하고 넘겼다. **결과를 본 뒤에 정하면 그게 관대화다**
(나) 정오 조항을 **결정적 경로(고정 원본 재투입)**로 다시 설계 —
     기존 선등록의 "채점기로 턴별 1:1 대조 · 한 턴이라도 바뀌면 실패"는
     **쓰던 순간에 이미 원리적으로 불가능**했다(라이브는 비결정적이라 같은 회차를 못 돌린다).
     이건 실행 실패가 아니라 **선등록의 설계 결함**이다 (감시자 정정)
```

## 3-3. J12 3차의 「미귀속」 — 숫자가 있는데 효과로 못 쓴다

```
수리 후 n=10 총계   정답 3 · 한정·고지 0 · 무근거 단정 5 · **기계 판정 불가 2**
수리 전(§7-j)      정답 1 · 한정·고지 0 · 무근거 단정 9 · 기계 판정 불가 0
왜 보류   **다른 회차의 값이다.** 수리 효과로 귀속하지 않는다 — 좋아 보여도 쓰지 마라
남은 길   §3-2 (나) 의 결정적 경로 설계가 서면 그때 잰다
```

## 3-4. J12 2차가 남긴 관측 — 닫지 않은 것

```
재생성 채택 0/35   재생성 호출 16회가 **한 번도 최종답이 되지 않았다**(22사슬 35턴).
                 현재 피해는 **버려지는 왕복(지연·토큰)**이고, 답의 진실을 바꾼 사례는
                 이 22사슬에 없다. → 3차가 발동을 껐으니 왕복은 줄었을 것이나 **미측정**
퇴화 표본(n=1)     한 턴에 답 생성 요청 3회(요청 6·7·8) — 6의 정답(AGENTS.md 16,051B)이
                 7·8에서 오답(15,837B)으로 퇴화. **그 캡처는 세션 스크래치에만 있었고
                 소멸했다**(저장소 밖). 요청사슬캡처.mjs 10사슬에서 **재현 0/10**
남은 길           n=1 을 결함 등급으로 승격하지 마라. 재현되면 그때 연다
```

## 3-5. 무효 판정된 도구 — **결과를 쓰지 마라**

```
scripts/terminal-qualification/귀속표.mjs   ← **무효**
왜   호출부 귀속을 «[이번 턴 실행 사실] 없음» 문자열로 가르려 했는데, 그 줄은
     **도구 결과를 받은 요청 60건 전부에 나타나는 상수**였다(정합 0건). 판별력 0.
     그 표(«16회 전부 :2822»)는 버렸다. 파일은 남기되 **결과는 인용 금지**
대신 쓸 것  손 관리자의 **같은-발화 대조**(같은 발화·같은 걸음 순서·답글만 다름)가
     유일하게 선 귀속 증거다 — 답 끝 물음표 유무로 발동이 갈렸다(§7-n)
```

## 3-6. F-118 잔여 — 기록만 하고 안 연 것 셋

```
① exit≠0 이면서 looksBlocked 아닌 probe 갈래도 probeChangedNothing 을 받는다.
   failureState 는 막지 않는다(감시자 재현: probe `ls /없는자리` → failureState none · confirmed)
   왜 안 열었나  실린 문장이 "실행했는데 오류로 끝났어요(코드 1)" 로 exit code 를 그대로 말해
   사용자면에서 거짓이 없고, 이전(추정 분류)보다 참에 가깝다
   남은 길  증상(사용자 거짓)이 라이브에 나오면 그때 선빨강으로 연다
② 검사 ⑥ 은 스텁 구조상 ⑦ 과 거의 같은 것을 문다 — 실질 증명은 ⑧ 이 진다. 기록만
③ 어조 개선(단정형 5/5)의 인과는 **미측정** — 1표본 가설이었다. 통과 증거로 쓰지 마라
```

## 3-7. F-119 잔여 — 같은 기전(로케일)의 다른 문 셋 (안 열었다 · 기록만)

```
local-process.js:120     백그라운드 로그의 한국어 — 같은 기전. 증상 표본 없음
capsule.js:151-158       node 실행이라 증상 미측정
NFC/NFD 정규화           증상 없어 안 연다
남은 길   증상이 라이브에 나오면 그 표본으로 연다. **증상 없이 미리 고치지 마라**
```
그리고 F-119 라이브 3/3 은 세 회차 모두 손 순서에 파일 손이 섞여 **이름의 출처를 라이브만으로
격리하지 못한다** — 격리 증거는 `test/f119-terminal-speaks-utf8.test.js` ①(터미널 단독 경로)이다.

## 3-8. F-120 잔여 — 미배선·미측정 셋

```
turn.js:2761 부분합 그물   copy 계열이 목록에 없다(미배선 · 사유 있음). 해가 나는 형상은
                         「복사+같은자리 read 혼합 턴」뿐이고 **표본 0**. 표본이 나오면 별도 선빨강
write 등급의 카드 0        코드 독해 + 라이브 1회라 **미측정 항목**
등급 판정 갈라짐           copy 를 특례 없이 write 차선에 뒀다(불변식 「바꾸는 동사는 전부 승인
                         판정을 지난다」 유지) — **감시자의 organize 처방과 갈라진 자리.
                         커밋 검문에 신고돼 있다.** 후속 판정 대상 (→ §6)
```

## 3-9. 이전부터 열려 있는 것 (이번 세션이 안 건드림 · 지도 §12 가 원본)

```
H04 0/5     기억 철회(memory.withdraw)가 target 을 못 맞혀 조용히 지나감 — §5 순서 7번은
            H04 가 서기 전까지 안 닫힌다 (정본 §5 「2026-08-12 갱신」에 실측·기각 이력 전부)
S6          legacy-default-agent(A2·전 도구) 유일 활성이면 무조건 선택      server.js:1041
J5          desktop.act 거절 문구 낡음(실제 17동사)                        desktop-act-tool.js:508
J7          따옴표 든 CSV 는 표 안전망이 조용히 꺼진다                      local-file.js:126
J8          「승인 대기」 줄이 모델에게 영영 안 간다                        working-state.js:171
F3          비밀 이름 정규식이 일반 자료를 잡는다                          file-scope.js
            ★ 이 계열이 §7-j 원6 에서 실물로 튀었다 — 최종답에 T5 의 가림 문자열
              (src/kernel/l0-evidence/sensitive-text.js:103 MASK)이 «[가림].md» 로 개입했다
F5          채널 계층이 모든 외부 전송을 A2 로 선언(헌장 ③ 충돌)
C1·X1·X2·X3  지도 §12 그대로 (X3: A층이 화면·브라우저·발신 손을 한 번도 검증 안 함)
카톡·화면 손  §5 순서 9번 — **오너가 후속으로 미룸(공수 과다)**. 자료는 정본 §5-9 계열에 남아 있다
```

## 3-10. 미설명 수치 하나 — 합치지 않고 따로 둔다

```
§7-j 원6   "[가림].md — 144,027바이트" 를 답했다. **방 전수 수색으로 실물 부재 확인**
          (14방 · 픽스처 6파일뿐). 두 줄로 가른다 — 합치지 마라:
          (i) [가림] 은 모델 환각이 아니라 **T5 의 가림 문자열이 최종답에 개입**한 것(F3 계열)
          (ii) 144,027 은 **어떤 잰 값과도 안 맞는 미설명 수치**
남은 길   합치는 판단은 요청 사슬 재확보 후. n=1 승격 금지
```

## 3-11. 기각·폐기 확정 — **다시 파지 마라** (값을 치르고 닫은 길)

```
와이어 재생으로 기각 셋   문구 재배치(정체 먼저·경계 앞으로) 첫 수 0/5 — 죽은 채
                        action enum 설명에 경계 한 줄        첫 수 0/5 — 죽은 채
                        선언 분할(읽는 손/정리 손)           7/10 — 집계줄 8/10 과 차이 없음
첫 수(터미널 첫 호출)     **대리지표로 폐기** — 라이브 누적 0/20 인데 참여 9/10 · 1위 정답 10/10.
                        전 회차가 list → terminal 순서 = 「보고 나서 잰다」는 판단이지 결함 아님
「라이브에서 터미널 원천 불가」 가설   기각 — 캡처 회차에서 첫 수 local_terminal 2연발
가설 1(원문이 안 가서) · 가설 2(능력 문장이 안 가서)   고쳤는데 호출 수 안 움직임 — 단독 범인 아님
가설 3(무서운 문구)      단독 범인 아님 — B팔·D팔이 같은 0. F-116 수리는 사실 정합으로 가치 유지
채점표 v2 **무조건형** 초안  감시자 검문에서 **기각**. 조건을 정직하게 달면 근거 관측 3건이 전부
                        그대로 무근거 단정이라, 무조건형은 조작화 교정이 아니라 **관대화**였다.
                        같은 초안을 다시 쓰지 마라 (§7-i 에 기각 이유까지 있다)
내 원발화×10 봉인 재판안   기각 — 선등록의 「처음 보는 발화 ≥4」 조건 탈락. 6+4 로 복원됨
```

---

# 4. ④ 이번 세션이 밟은 사실 — 다시 재지 않아도 된다

## 4-1. 앞 갱신(b71f31f9) 이후 봉인된 커밋 10개

```
9ce31edd  깊이 채점표 선등록 + 계측기 둘 (재료실측 · 명령 캡처 확인)
c9e8477d  F-121: 목록의 폴더가 안의 항목 수를 말한다 — 판정선 미달 · 멈춤 · 재조사 측정 지정
18d1272f  깊이 재조사: 분리 배치 **정수만** 기록 — 선등록 문턱 미충족, 결론 문장 없음
5c43c5df  인계·선등록: 작업 루프 열 걸음 저장소 정본화 + 채점표 v2 조건형 재선등록
8f3e3ac2  선등록: v2 에 해상도 무단정 · 정답 분자 규칙 — 봉인 재판 직전 고정
83d9e147  정본·검문·전수: 루프 영구 자리(§4-b) + 폐기 감사 조항 수리 +
          **.claude/ 가 통째로 gitignore 라 검문 계약서 4장이 추적 밖이던 것**을 올림
85a7efc4  §7-j 정정: 지배 숫자 무근거 단정 **9** — 셈 관대화 3차를 검문이 잡음
1dc10a7d  J12 준비: 요청사슬캡처.mjs + 10사슬 증거 — 뒤집힘 **0/10** 정직 기록
e1441854  채점기: 지배 숫자를 기계 실행값으로 — 같은 관대화 3회의 문을 **코드로** 닫는다
31170dc9  J12 1차: 답 저작권 자를 낱말 세기 → **기제별 정의역 열거**(A2·B3·C1 = 6)
dd5bc67c  J12 2차 선등록: 표본 22사슬 · 정오 미채점 둘 · 분석기 정오 칸 금지 — 돌리기 전 고정
6ffb7a58  J12 2차: 사슬분석기 + 22사슬 실측표 — 재생성 채택 **0/35**, 오판 규칙 확정
8e983289  J12 3차 선등록: 내 귀속표 무효 자백 + 표적·설계·예측 고정
55c284c3  J12 3차: 파악을 마친 물음은 빈손이 아니다 — 신규 12사슬 발동 **0**
```

## 4-2. J12 오판 기제 — **코드까지 밟았다** (다시 안 파도 된다)

```
경로   turn.js:2828 걸음이말하는미달 → 빈손으로끝났나 (exit-verification.js:67)
범인   if (/\?|까요|할까/.test(t)) return true   ← 답에 물음표가 하나라도 있으면 「빈손」
실물   발동한 답  "시작문서 폴더 안에는 지금 네 개가 있어요. - README.md …
                이 중에서 먼저 같이 보고 싶은 파일 있어?"   ← 손을 불러 정확히 답한 뒤 후속 제안
      미발동 답  "…가장 큰 단일 항목은 시작문서 폴더(60K)입니다."  ← 물음표 없음
모순   **같은 파일 안에서 두 자가 반대 방향이다** — 대조대상인가(:83)는
      "물음·제안은 되묻는 중 … 원장 대조를 물리면 심문을 벌하는 개입"이라며 물음을 빼주는데,
      빈손으로끝났나는 같은 물음을 빈손으로 잡았다. 오너 말귀 정의와도 충돌
수리   빈손으로끝났나에 **끄는 문(옵션 게이트)** 하나 — 규칙 자체·기본값 불변.
      turn.js:2828 이 「사실을 가져온 걸음」을 그 문에 넘긴다.
      자는 새로 안 만들었다 — **F-107 의 `빈손으로돌아왔나` 한 벌 재사용**
```

## 4-3. J12 3차 실측 (55c284c3 · 선등록 8e983289 대조)

```
셀별 발동 (예측 → 실측 · 원자수 · 비율 금지 — 종별 n=3)
  "시작문서 폴더 안에 뭐가 있어?"       3사슬  2 → 0   예측 적중
  "작업 폴더에 뭐가 들어있는지…"(2턴쌍)  3사슬  3 → 0   예측은 「내려감(숫자 미예측)」
  "작업 폴더에서 제일 큰 파일…"         3사슬  0 → 0   0 유지(내려가면 안 되는 쪽) 적중
  "여기 md 파일 전부 몇 개야?"          3사슬  0 → 0   0 유지 적중
양성 대조  기존 22사슬 재분석에서 **여전히 16 검출** — 주입 서명이 살아 있다.
          신규 0 이 「수리됨」이지 「서명 불일치」가 아님이 이것으로 갈린다
검사      test/j12-asking-after-facts-is-not-empty-handed.test.js 5/5
          (① 사실 가져온 걸음의 물음은 빈손 아님 ② 반대시험: 껍데기 걸음이면 여전히 빈손
           ③ 반대시험: 약속 갈래("이어서 해볼게요")는 안 꺼짐 ④ 기본값 불변 ⑤ turn.js 가 실제로 넘김)
반대시험   수리를 걷으면 ①⑤ 빨강
회귀      4,246/4,246 · 실패 0 · 보류 0
```

## 4-4. 원인 확정 — **범인은 파일 손의 선언이었다** (앞 구간 · 측정으로 · §3-b)

```
A  터미널만·중립 선언            2·2·2·2   부품 무죄
D  파일 손 + 터미널(중립 선언)    0·0·0·0   ★ 문구를 고쳐도 죽어 있다 — 범인은 문구가 아니다
E  파일 손이 자기 한계를 말함      1·2·3     ★ 전부 살아남 — 유도의 증거
```
죽이는 겹 = **파일 손 선언이 과대 선언하고 한계를 말하지 않는 것**
(`demo-context.js:774` capability · `:790` schema). 수리는 E팔 검증대로 빠진 사실 채우기(유도).
⚠️ **맨몸판 자로 쓸 수 있는 숫자는 터미널 호출 수 하나다** — 「답 정답」은 못 본다(손 관리자 정정).

## 4-5. 라이브 실측값 (HEAD 26609906 · 10실행 · §7-f)

```
터미널 참여      9/10   (기준선 f3da0917 시점 4/10 에서 뜀)
답 1위          10/10   AGENTS.md (바이트 기준 참)
답 2위          9/10 오답  README(11,373B)를 2위라 말함. 실제 2위는 시작문서/현재상황.md(15,837B)
```
그 뒤 F-121 이 들어갔고 §7-h·§7-j 두 배치는 위 표와 **다른 자(3분류)**로 잰 값이다 — 섞지 마라.

**측정 축은 섰다**(§7-j 확정): 전수 wc · 재귀 du · find 를 모델이 **자발적으로** 골랐다.
남은 결함의 무게중심은 **측정 → 최종답 구간**이다. 그 후보 실물 둘:
```
§7-j 원4  wc 두 번으로 16,051 · 15,837 을 **둘 다 재고** 작은 쪽을 1위로 답함 — 잰값 모순.
         "동일 서명 2건째"는 과장(감시자) — **같은 픽스처에서 같은 오답 값 재현(독립성 없음)**이 정확
§7-j 원6  «[가림].md — 144,027바이트» (→ §3-10)
```

## 4-6. 증거 원본 — 전부 저장소 안이다 (경로까지 적는다)

```
docs/03-verification/evidence/terminal-2026-08-15/기준선과-남은자리.md   714줄 · 정본
docs/03-verification/evidence/terminal-2026-08-15/그냥써본다-원본/       회차 원본 (라이브 전량)
docs/03-verification/evidence/terminal-2026-08-15/J12-요청사슬/          요청·응답 사슬 원본
docs/03-verification/evidence/terminal-2026-08-15/맨몸대조군-*.json      6개
docs/03-verification/evidence/terminal-2026-08-15/조사/ · 초기-S1S2-라이브/
docs/03-verification/evidence/live5-2026-08-14/                        54파일 (앞 세션 라이브)
docs/03-verification/evidence/prime-agent-openbook-2026-08-13/          죽은 세션 입양분
→ terminal-2026-08-15 아래 **추적 파일 133개** (git ls-files 실측). 작업트리에 미커밋 0
```

## 4-7. 측정 도구 — **다음 세션은 가설 대신 이걸 쓴다**

`scripts/terminal-qualification/` (전부 추적됨 · git ls-files 확인):
```
채점기.mjs        ★ **지배 숫자는 이 실행값만 쓴다.** 사람 분류 금지 — 사람 읽기는 회차 주석 전용.
                 v2(§7-g + §7-i) 의 기계화. 판정 못 하는 회차는 「기계 판정 불가」로 그대로 보고.
                 사용: node scripts/terminal-qualification/채점기.mjs <부터 HH-MM-SS> <까지 HH-MM-SS>
                 (그냥써본다-원본/ 을 읽는다 · **저장소 뿌리에서 실행**)
사슬분석기.mjs     발동 실측 전용 — 정오는 **안 잰다**(두 번째 자 금지). 사람 분류 0 · 전부 문자열 대조.
                 세는 것 셋: 재생성 발동 수/턴 · 주입 계약말 원문(요청 diff 전량) · 최종답 출처
요청사슬캡처.mjs   모델 요청·응답 사슬 전체를 저장소 증거로 남긴다(J12-요청사슬/).
                 ⚠️ **저장소 절대경로가 하드코딩돼 있다**(`/Users/jyp/Developer/t5-p-op/src/...`)
                 — 다른 worktree 에서 돌리면 **본선 소스를 읽는다.** 쓰기 전에 경로를 확인하라
그냥써본다.mjs     오너 말투 발화를 그대로 침 · 회차 원본(발화·손 순서·답 전문·터미널 명령 원문·
                 HEAD·모델·재료실측)을 그냥써본다-원본/ 에 남긴다
맨몸판.mjs        커널 겹 없는 대조군(A~E팔) · 유효 숫자는 **터미널 호출 수 하나**
귀속표.mjs        ★ **무효 — 결과 쓰지 마라** (→ §3-5)
```
⚠️ `tasks.mjs`(지어낸 과업 다섯)는 개발자 시험이었다 — **폐기된 방법이다. 다시 쓰지 마라.**

## 4-8. 그 밖의 확정 사실

```
스키마 재동결        172d1402 (신고문 있음) · 배치 사이 스키마 지문(391d7758) 변경 금지
캐시 지문           59d68451 로 2종 → 1종
F-116 단독 무효     F-116 만 걸린 트리 실측: 터미널 0회 · local.file 6회 — D팔 예측과 일치
F-119 단일변수      wc -c 현재상황.md — LANG 없음 "15837 ????.md" · 로케일 있음 "15837 현재상황.md"
F-120 선빨강        복사 요구에 bulk_move — 시작문서가 통째로 비는 사고, 디스크 재현(표본 2)
게이트 문구 상한     982580c3 에서 **은퇴** — 「후속/아직 표현 12→13」 빨강은 이제 없다
.claude/ gitignore  83d9e147 이 검문 계약서 4장을 추적에 올렸다. 그전엔 통째로 추적 밖이었다
같은 빌드 증명       배치를 합칠 자격은 `git diff --stat <기준>..HEAD -- src/` 빈 출력으로 세운다
```

---

# 5. ⑤ 이번 세션이 틀렸던 것 — 같은 자로 또 재면 또 틀린다

```
① **셈 관대화가 세 번 났다.** 공통 기제는 「결과를 본 뒤 사람 손으로 분류하며 선등록에
   없던 칸을 만든 것」이다 — 1차는 「경계 사례·측정-해상도」 칸, 3차는 「선등록-밖」 칸.
   칸 이름만 안 늘렸지 **셈을 관대하게 만든 효과**였고 감시자가 매번 잡았다.
   → 오너 판정: **채점을 기계로.** e1441854 채점기 커밋이 그 문이다.
   **지배 숫자는 채점기 실행값만. 다시 사람 손으로 세지 마라**
② **채점표 v2 무조건형 초안이 관대화였다** — 이득 방향도 내가 반대로 짚었고(봉인은 다음
   배치 숫자로 나므로 **전향 적용**이 이득 자리), 관측 횟수도 「세 배치 3회」가 아니라
   **두 배치 3회**였다. 근거 3건이 조건을 달면 전부 그대로 무근거 단정이었다
③ **내 귀속표가 무효였다** — 판별 문자열이 요청 60건 전부에 있는 **상수**였다(정합 0건).
   판별력 0인 자로 «16회 전부 :2822» 표를 만들 뻔했다 (→ §3-5)
④ **"재생성은 매 턴 2회 · 사용자는 둘째 것을 받는다"가 과장·거짓이었다** — 손 관리자
   재측정으로 뒤집혔다. 실측은 재생성 11/20턴, 최종답은 그 10사슬 전부 **첫 생성**
⑤ **"재생성은 버려진다"도 조건 없는 단정이라 틀렸다** — 감시자 정정. **앞답이 빈 턴은
   F-84 되살리기가 답을 갈아친다**(→ §3-1 ①). 일반 명제로 쓰면 다음 슬라이스가 틀린다
⑥ **선등록의 정오 조항이 원리적으로 실행 불가능했다** — "턴별 1:1 대조"는 라이브
   비결정성 때문에 **쓰던 순간 이미** 불가능. 돌려보고 어려웠던 게 아니다 (→ §3-2 (나))
⑦ **감시자 착수 검문을 한 차례 건너뛰고 J12 3차에 착수했다** — 커밋 본문에 적어 뒀다.
   손해는 없었고(diff 가 조건 셋 안) 커밋은 무르지 않되, **그 구간의 선빨강 증거는
   「감시자 미확인」으로 남는다.** 다음 착수(F-121 재판)는 **검문부터 밟아라**
⑨ **측정 도구를 만들 때 양성 대조만 하고 오검출(음성) 반례를 한 번도 안 찾았다.**
   귀속표(무효 · 판별 문자열이 상수)와 채점기(누수 · §7-q L1·L2) **둘의 원인이 같다.**
   채점기는 §7-j 구간 합치 하나만 보고 통과했고 **그 자를 무는 검사도 안 만들었다.**
   → 규율: 계측기는 **양성 대조 + 오검출 반례 + 그것을 무는 검사** 셋이 한 세트다.
   「채점을 기계로 옮겼다」는 문장은 자의 판별력을 안 보이면 **절반만 참이다**
⑧ 이전 세션분: 가설을 순서로 태웠다(네 번 실패) · 지어낸 과업 다섯(tasks.mjs)이
   개발자 시험이었다 · 첫 수를 표적으로 세웠다(대리지표) · 맨몸판의 「답 정답처럼 보임」을
   믿을 뻔했다 · 라이브 5회를 원본 미보존으로 적을 뻔했다
```

**규율 (이 세션이 값을 치르고 재확인)**: 음성 결과가 나오면 **자를 먼저 의심**한다 ·
해법이 안 먹히면 겹을 더 쌓지 말고 **본질 재조사부터** · 배선은 **특례·복제 없이 깔끔하게** ·
사람 시간 은유로 말하지 않는다 · **끊을지 묻지 말고 잇는다**.

---

# 6. ⑥ 열려 있는 판정 — 오너·후속 결재 대기

```
본선 미병합 1,309   claude/p-op-1-a-system-view 가 origin/main 에 한 번도 안 들어갔다.
                  하루치 수리(F-116~F-121·J12)가 전부 이 브랜치에만 있다. **병합 시점은 오너 판단**
브랜치 4개         codex/terminal-core(20) · codex/terminal-rebuild(7 · 마지막 커밋 13시간 전) ·
                  codex/terminal-stage1(8 · 9시간 전) · lane/ux(12 · 50시간 전).
                  ⚠️ **어느 것을 병합해도 `scripts/terminal-qualification/` 전량과
                  `그냥써본다-원본/` 75개(이 재판 기록 전체)가 삭제된다** — 그 브랜치들이
                  이 파일들 이전 기준선이기 때문이다(감시자 기계 확인 · 2026-08-15).
                  **오너 결재 안건에 이 문장이 반드시 붙어야 한다.**
                  **읽고 살릴지 버릴지 판정하라. 자동 병합 금지**(agent-start 지시).
                  ⚠️ terminal-rebuild·stage1 은 **지금도 커밋이 늘고 있다 — 남이 일하는 방일 수 있다**
자리 밖 worktree   /private/tmp/t5-check-271 · /private/tmp/t5-terminal-core — 게이트 FAIL 의
                  직접 원인. 남의 세션 것일 수 있다 — **지우기 전에 소유를 확인하라**
「기계 판정 불가 2」 분류 규칙 미정 — 다음 선등록에 올려야 F-121 재판을 열 수 있다 (§3-2 가)
정오 조항 재설계    결정적 경로(고정 원본 재투입)로 (§3-2 나)
F-120 등급        copy 를 write 차선에 둔 판정이 감시자의 organize 처방과 갈라졌다(커밋 검문에 신고됨)
장부 공백         F-107~F-121 · J12 가 T5-FOLLOWUP-LEDGER 에 미기재 — 입양 여부 판정 대상
지도 §12 갱신      J12 줄(543행) 줄 번호가 낡음 — t5-map-keeper 소관
검사 ⑥/⑦ 겹침    F-118 검사 ⑥ 이 ⑦ 과 거의 같다 — 정리 여부는 후속
```

---

# 7. ⑦ 전체 완성까지 남은 지도

## 7-1. 정본 §5 순서 (오너 승인 2026-08-12 · 바꾸려면 물어라 · §5 676행)

```
1  타이핑 과잉 승인 제거              ✅ (라이브 절반 미측정 — AX 권한 없어 계측불가)
2  행동 분류 관통                    ✅ (이음매 ⑨ 등재)
3  네이버 사용자 손 0회 재판정        ✅
4  원장 종결 범위 봉인               ◐ 함수 층 6/7 · ⑥ 은 **선언된 경계**(F-105 · 라이브 재현 없음)
5  원시 결과·실패 진단 공급 (§5-3)    ← ★ **지금 여기.** 터미널 「기본 다섯 줄」이 이 칸이다
6  결과 기반 손 전환 확장 (§5-4)      8줄 중 2줄 (F-107 배선)
7  기억 승격 완결 (§5-5)             ◐ H01·H03·H07 5/5 · **H04 0/5**
8  채널 보관함 (§5-6)
9  범용 컴퓨터유즈 열 동작            ⛔ 오너가 후속으로 미룸(공수 과다 · 2026-08-13)
10 7과목 + 4비교군 최종 대조
```

## 7-2. 문장 열둘 점수판

**이번 세션은 안 쟀다 — 미확인.** 앞 세션 판정(잴 수 있는 것 2·5·8·12 안팎 · 7·10·6 은
미측정(자격 필요)으로 확정하고 뺀다)이 마지막 기록이다. 손 관리자 경고 유지:
**문장 열둘은 「손 점수판」이 아니다**(영역당 1~2칸 · 터미널 0칸).

## 7-3. 손별 남은 칸 (터미널 손 기준)

```
측정 축      **섰다** — 전수 wc · 재귀 du · find 를 모델이 자발적으로 고른다(§7-j 확정)
측정→최종답   **여기가 결함의 무게중심이다.** 잰 값을 두고 반대로 답하거나(§7-j 원4),
             해상도를 안 밝히고 순위를 단정하거나(du 블록으로 "공동 1등"),
             가림 문자열이 최종답에 개입한다(§7-j 원6 · F3 계열)
되부름 루프    **한 갈래 닫음**(J12 3차 · 물음표 오판) · 다섯 남음 (→ §3-1)
깊이(childCount) 코드 봉인 · **판정 미달로 슬라이스 열림** (→ §3-2)
로케일        F-119 로 한 문 닫음 · 같은 기전 셋 미개봉 (→ §3-7)
파일 손 경계   F-116·F-117·F-118·F-120 으로 넷 닫음 · 잔여는 §3-6·§3-8
```

## 7-4. 다음 순서 (이 세션 종료 시점의 확정)

```
(a) ★ F-121 깊이 **재판** — 조건(J12 해소) 충족됨. **감시자 검문 → 두 결정(§3-2 가·나)을
    선등록에 올림 → 봉인 재판 6+4 → 채점기 실행 → 판정.** 미달이면 멈추고 본질 재조사
(b) ④ 정면 대조 라이브 — 닫는 문장 판정: "오너가 클로드코드에게 하는 말을 T5 에 그대로
    넣었을 때 같은 결과가 나온다"(§6). 클로드코드(소넷 4.6) 답과 **나란히 놓고** 판정.
    이것이 「터미널 정상 가동」의 판정선이다
(c) J12 남은 다섯 (→ §3-1) — 표본이 서는 것부터
```

---

# 8. ⑧ 착수하면 바로 부를 것 — 에이전트

**소집을 건너뛰지 마라 — 이번 세션이 한 번 건너뛰었고 그 구간은 「감시자 미확인」으로 남았다.**
이번 세션에서 검문이 실제로 문 것: 셈 관대화 3회 · 근거 과대 1회(«동일 서명 2건째») ·
v2 무조건형 기각 · 봉인 재판 구성의 선등록 위반 · 「재생성은 버려진다」 무조건 단정 ·
정오 조항 실행 불가 · 표본 합칠 자격(같은 빌드 증명) · 맨몸판 자의 한계 · 라이브 원본 미보존.

```
t5-hand-keeper        ★ 착수 직전 · 봉인 직전. F-121 재판은 이 소집부터 시작한다
t5-process-supervisor 착수 · 수정 전 실패 확인 · 구현 직후 · 공유파일 통합 전 · 회귀 직전 ·
                      커밋 직전 · 봉인 보고 직전 (일곱 시점)
t5-map-keeper         착수 때 "지도의 어느 줄에서 나오나" — **J12 줄(§12 543행) 갱신도 이 손**
t5-handoff-keeper     포화 신호가 보이면 즉시. 이 문서를 다시 쓴다
```
소집 형식(감시자 ①~⑨ 아홉 줄 — **① 기준 커밋을 빠뜨리면 무조건 「조정」 판정**) ·
맥락 승계 금지(매번 밟은 사실을 다시 실어 보낸다) · 손 관리자 다섯 물음(0번 「몇 개를
낫게 하나」부터)은 `.claude/agents/*.md` 원문이 정본이다(83d9e147 이후 추적됨).

---

# 9. ⑨ 밟으면 안 되는 것 — 이 세션과 앞 세션이 데인 자리

```
지배 숫자를 사람 손으로 세지 마라      **채점기.mjs 실행값만.** 같은 관대화 3회가 이 문을 닫았다
채점기 실행값을 무조건 믿지도 마라     자가 **양방향으로 샌다**(§7-q L1·L2 · 실측 반례 셋).
                                   기계라는 사실은 판별력을 보장하지 않는다 — 자 수리 전이다
선등록에 없던 칸을 결과 본 뒤 만들지 마라 이름만 안 늘려도 셈이 관대해지면 같은 조작이다
귀속표.mjs 결과를 쓰지 마라           판별 문자열이 상수 — 무효 (→ §3-5)
"재생성은 버려진다"를 조건 없이 쓰지 마라 앞답이 빈 턴은 F-84 되살리기가 갈아친다 (→ §3-1 ①)
J12 줄 번호를 지도 §12 에서 베끼지 마라 현재값은 §3-1 (696·708·848·852·3597 + 리터럴 1)
검문을 건너뛰지 마라                  건너뛴 구간은 「감시자 미확인」으로 영영 남는다
정오를 사슬분석기에 구현하지 마라       **두 번째 자 금지.** 정오는 채점기 하나뿐
가설을 순서로 태우지 마라             맨몸판·절제 재생으로 양 끝을 재라. 가설 셋 = 네 번의 실패
개발자 시험을 지어내지 마라            오너 말투 발화 그대로(그냥써본다.mjs). tasks.mjs 는 폐기
첫 수를 표적으로 다시 세우지 마라       대리지표로 폐기됐다
기각 확정된 것을 다시 파지 마라        → §3-11 전량
n=1 을 결함 등급으로 승격하지 마라      퇴화 표본 · childCount 효과 관측 둘 다 n=1
자격 파일을 열지 마라                 model-connection.json 에 API 키 원문.
                                   문은 하나: node scripts/model-connection-facts.mjs
                                   (test/credential-file-single-door.test.js 가 문다)
요청사슬캡처.mjs 를 다른 방에서 돌리지 마라 절대경로 하드코딩 — 본선 소스를 읽는다 (→ §4-7)
기존 검사가 초록인 자리를 고칠 때       **그 검사부터 읽어라** — 이번에도 물렸다
과거 게이트·검사 숫자를 베끼지 마라     직접 세라. 이 문서의 숫자도 다음 세션엔 낡는다
/tmp 와일드카드 삭제 금지             자기가 만든 경로 변수만. 남의 worktree 가 /tmp 에 있다
서버는 --no-open · process.exit 금지  오너 크롬 탭 · 좀비 서버 사고 이력 (agent-start 원문)
git add -A 금지 · 명시 경로만         다른 세션의 미커밋을 쓸어담는다
배치 사이 스키마 지문(391d7758) 변경 금지  표본을 합칠 자격이 사라진다
새 계획서를 만들지 마라               계획은 T5-PLAN 하나
```

## 9-1. 게이트가 빨간 이유 — **1건**이다 (2026-08-15 `npm run gate` 실행값)

```
[gate] BLOCKED — 1건 : 프로젝트 진입 감사 FAIL
  · 자리 밖 worktree 둘   /private/tmp/t5-check-271 · /private/tmp/t5-terminal-core
  · 방치된 작업 방        ../t5-lanes/ux (마지막 커밋 50시간 전 · 본선 미병합 12커밋)
  · (작업 중인 방 둘 — ../t5-lanes/terminal-rebuild 13시간 · terminal-stage1 9시간 —
     은 「실패 아님 · 아직 안 잊힘」 경고로만 나온다)
그 외 게이트 칸은 전부 초록: 안전 바닥 · 터미널 우회 5갈래 · 프로세스 · 위생 · 작업 대상 찾기 ·
프롬프트 5,011자(고정 4,899 + 가변 112) · 능력 설명 정합 · 커넥터 진실층 · previewOf 3개 ·
한 사실 한 층 · subjectOf · 서비스 이름 0건 · 말귀 층 12건(기준선 이하) · descriptor 단일 진실 ·
테스트 4,246 · 테스트 CPU 99.4s(기준선 106.15s) · 런타임 의존성 0 · 조용한 절단 없음
```
푸는 길: 자리 밖 worktree **소유 확인 후** 정리 + lane/ux 판정(→ §6). **자동 병합 금지.**

---

# 10. 다음 세션이 첫 30분에 할 일

```
1  node scripts/agent-start.mjs                            착수 점검 (어긋난 칸 그대로 옮겨 적기)
2  docs/03-verification/evidence/terminal-2026-08-15/기준선과-남은자리.md  §7-g~§7-o
3  design/T5-FINAL-ASSEMBLY-ko.md §4-b(루프 열 걸음) · §1-0 · §3
4  §3-2 의 두 결정(가·나)을 **선등록 초안**으로 적는다        「기계 판정 불가」 분류 규칙 ·
                                                          정오를 결정적 경로로
5  t5-hand-keeper + t5-process-supervisor 소집             F-121 깊이 **재판** 착수 판정.
                                                          선등록 초안을 실어 보낸다
6  판정을 받은 뒤에 라이브를 돌린다                            판정 전에 코드도 배치도 만지지 마라
```

**다음 세션 첫 3분**: `node scripts/agent-start.mjs` 를 돌리고, `기준선과-남은자리.md` §7-o
(F-121 슬라이스가 열린 채라는 줄 + 봉인 재판 구성 6+4)를 연 뒤, **감시자 소집문 작성부터**
시작한다 — 이번 세션이 검문을 한 번 건너뛴 자리가 바로 여기다.
