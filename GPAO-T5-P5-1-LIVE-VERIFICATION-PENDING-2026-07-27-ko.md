# P5-1 채널 표면 — 라이브 확인 상태와 남은 목록

- 상태: **부분 라이브 확인됨 / 오늘 수정분 채널 미확인**
- 완료 아님.
- 코드 기준: `npm test` 846건 통과 · `npm run gate` PASS (4.2~4.6초, 기준선 5초 — 여유 얇음)

> **이 문서의 첫 판(`b2cbf59`)은 틀렸다.** "텔레그램에서 한 번도 안 봤다"고 썼는데, 원장을 읽으니
> 실제 방 대화가 있었다. 내 문서에 "원장을 읽어라"고 써놓고 안 읽고 문서만 보고 썼다.
> 아래는 원장으로 대조한 뒤 다시 쓴 것이다.

---

## 실제로 라이브가 돌았다 (원장 대조 완료)

```
세션 56a6ae67-e74e-4962-ba34-5a85fb5b4b08
origin: { channel: "telegram", chatId: "8601204821" }
2026-07-26 23:46 → 07-27 03:13 (KST) · 26턴 · 원장 3건
```

| 항목 | 상태 | 근거 |
|---|---|---|
| 채널 수신 | ✅ 확인 | 발화 8건에 `channel: "telegram"` 태그 |
| 방↔대화 연결·허용목록·봇 자격 | ✅ 확인 | 한 방이 한 대화에 3.5시간 누적 |
| 채널에서 웹 도구 | ⚠️ 갈렸다 | 턴6 "도구 호출이 연결되어 있지 않아서" / 턴11~13 `web.collect` 실제 성공 |
| 채널 발신 | ⚠️ **직접 증거 없음** | 아래 참조 |

### 발신을 왜 "정황만"이라고 쓰는가

당시 전달 사실이 **어디에도 저장되지 않았다** — transcript 의 `channelDelivery` 13턴 전부 `null`,
전달 원장에 그 세션 건 0건. 코드가 그 값을 복사본에 쓰고 저장하지 않았기 때문이다(`fb96133` 에서 수정).

즉 **"안 보냈다"는 증거가 아니라 "확인할 수단이 없었다"** 는 뜻이다. 오너가 3.5시간 동안
답변에 반응하며("이거 너무 답답한데…") 대화를 이어간 정황은 강하다.

## 그 대화에서 드러난 결함 (오늘 고친 것들의 출처)

| 발화 | 그때 답 | 오늘 상태 |
|---|---|---|
| "웹으로 확인해서 알려줘" | "도구 호출이 연결되어 있지 않아서" | 같은 세션 후반엔 성공 — **채널 재확인 필요** |
| "그 세션 찾을 수 있어?"(턴8, telegram) | "직접 찾아볼 수는 없어요" | `session.search` 는 있다 — **있는 것을 없다고 말함** |
| "디벨로퍼 폴더에 뭐있지?" | **답변이 빈 문자열**, 원장 `local.file [blocked]` | 터미널·locate·이어받기가 이후에 생김 |

**오늘 하루 고친 것들이 전부 이 대화의 증상에서 나왔다.** 남은 일은 그 수정들이 채널에서도
도는지 보는 것이다.

---

## 라이브에서 확인해야 할 것

전제: 실제 텔레그램 방, 등록된 봇, 허용 목록에 오너.
**답변만 보지 말고 원장을 함께 읽는다** — `~/.local/state/gpao-t5/sessions/<id>.json` ·
`deliveries.json`.

### 오늘 수정분이 채널에서 도는가 (우선)

- **C1** "디벨로퍼 폴더에 뭐있지?" — 빈 답변이 아니라 실제로 찾아 답하는가.
  터미널·locate 가 채널에서도 붙는지. 원장에 `actualCall` 이 남는지.
- **C2** "그 세션 찾을 수 있어?" — `session.search` 를 실제로 부르는가.
  **있는 것을 없다고 말하면 실패**(인수인계 §3-④).
- **C3** 웹 조사 — 채널에서 `web.collect` 가 붙는가("도구가 연결되어 있지 않다"가 다시 나오면 실패).
- **C4** 한 손이 막혀도 다른 손으로 이어가는가 — "옮겨 주세요 / 경로를 알려주세요"가 나오면 실패.

### 채널 표면 계약

- **L1** 승인 알림에 도구 id·원시 명령·내부 문구가 없다 (`e0033e6`)
- **L2** 방에서 "네" 한 마디로 외부 효과가 실행되지 않는다 — 승인은 T5 화면에서만
- **L3** 화면에서 승인하면 이어 실행되고, 원장 인자가 카드에 보인 것과 같다
- **L4** 웹과 방에서 말투·사실이 같다
- **L5** 허용 안 된 발신자는 조용히 무시 — **설명도 하지 않는다**(봇 존재 탐지 통로)
- **L6** 못 보낸 것을 보냈다고 하지 않는다 — **이제 `deliveries.json` 으로 대조 가능**(`fb96133`).
  첫 판에서는 기록이 없어 검증 자체가 불가능했다.
- **L7** 연달아 온 두 마디가 사라지지 않는다(세션 큐 회귀)

## 실패로 볼 것

- 있는 도구를 없다고 말한다
- 답변에 도구 id·원시 명령·내부 계약 문구가 보인다
- 방에서의 한 마디로 외부 효과가 실행된다
- 못 한 일을 했다고 말한다 / 못 보낸 것을 보냈다고 한다
- 사용자에게 T5 가 할 수 있는 일을 시킨다
- 웹과 방에서 사실이 다르다

## 해제 조건

C1~C4 와 L1~L7 을 실제 방에서 확인하고 원장으로 대조했을 때. 그 전까지 P5-1 은 완료가 아니다.
