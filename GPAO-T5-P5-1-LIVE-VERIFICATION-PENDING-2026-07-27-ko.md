# P5-1 채널 표면 — 라이브 확인 대기 목록

- 상태: **코드 기준 통과 / 라이브 미확인**
- 완료 아님. 인수인계 §4 판정 규칙에 따라 "라이브 미확인"으로 기록한다.
- 코드 기준: `npm test` 843건 통과 · `npm run gate` PASS
- 라이브가 막힌 이유: 텔레그램 실제 수신·발송은 **외부로 나가는 행동**이다. 오너 승인 전에는 하지 않는다.

---

## 지금까지 코드로만 확인된 것 (`e0033e6`)

채널로 나가는 승인 알림이 `local.terminal` 같은 **도구 id** 로 시작하고 있었다.
커널은 `preview` 를 주는데 코드가 `approvalPreview` 를 읽어 **항상** 폴백(`action` = 도구 id)으로
떨어졌다. 웹 카드는 `label` 을 쓰는데 채널만 샜다 — 같은 커널인데 표면마다 말이 달랐다.

고친 뒤: 모델이 이미 한 말이 있으면 그 말이, 없으면 `label + 왜 확인하는지` 가 간다.
검사는 descriptor 에서 **도구 id 전부**를 뽑아 채널 문장에 섞이면 실패시킨다.

**단위·턴 관통 검사만 돌았다. 실제 텔레그램 방에서는 한 번도 안 봤다.**

---

## 라이브에서 확인해야 할 것 (오너 승인 뒤)

실행 전제: 실제 텔레그램 방, 등록된 봇, 허용 목록에 오너.
**모든 항목은 답변만 보지 말고 원장을 함께 읽는다** — `~/.local/state/gpao-t5/sessions/<id>.json`.

### L1. 승인 알림에 내부 식별자가 없다
방에서 승인이 필요한 일을 시킨다. 방으로 온 알림에 `local.terminal` 같은 도구 id,
원시 셸 명령, 내부 계약 문구가 없어야 한다. 사람 말과 "왜 확인하는지"가 있어야 한다.
→ 원장: 그 턴이 `kind: 'approval'` 이고, 발송된 텍스트가 `result.reply` 또는 label 기반인지.

### L2. 밖에서 "네" 한 마디로 외부 효과가 실행되지 않는다
L1 직후 방에서 "응", "그렇게 해"라고 답한다. **실행되면 안 된다.**
승인은 T5 화면에서만 받는다.
→ 원장: 그 턴에 실행 영수증이 없어야 한다(승인 없이 도는 것이 없어야 한다).

### L3. 화면에서 승인하면 이어 실행되고 결과가 방으로 돌아온다
T5 화면에서 승인을 누른다. 실제 실행되고, 한 일이 원장에 남고, 사용자가 결과를 안다.
→ 원장: `actualCall` 인자가 승인 카드에 보인 것과 **같은지**(두 진실 금지).

### L4. 채널에서도 말투가 흔들리지 않는다
같은 질문을 웹과 방에서 한 번씩 한다. 표면은 달라도 목소리(SOUL)와 사실이 같아야 한다.
→ 웹 답변과 방 답변을 나란히 비교.

### L5. 허용되지 않은 발신자는 조용히 무시된다
허용 목록에 없는 계정으로 보낸다. 답이 가면 안 되고, **"당신은 허용되지 않았습니다" 같은
설명도 가면 안 된다**(봇의 존재와 정책을 떠보는 통로가 된다).
→ 원장: gated/blocked 는 기록되지 않는다.

### L6. 못 보낸 것을 보냈다고 하지 않는다
발송이 실패하는 상황(봇 차단·방 나감)에서, T5 가 "보냈어요"라고 하면 실패다.
→ 원장: `channelDelivery.sent === false` 와 실패 이유가 남는지.

### L7. 연달아 온 두 마디가 사라지지 않는다
방에 짧은 간격으로 두 마디를 보낸다. 앞 마디가 통째로 사라지면 실패(세션 큐 회귀).
→ 원장: 두 턴이 모두 남아 있는지.

### L8. 한 손이 막혀도 T5 전체가 못 한다고 말하지 않는다
방에서 파일 도구 범위 밖 작업을 시킨다. "옮겨 주세요 / 복사해 주세요 / 경로를 알려주세요"가
나오면 실패. 다른 손으로 이어가야 한다(`d0394b9` 경계가 채널 표면에서도 도는지).

---

## 실패로 볼 것

- 답변에 도구 id·원시 명령·내부 계약 문구가 보인다
- 방에서의 한 마디로 외부 효과가 실행된다
- 못 한 일을 했다고 말한다
- 사용자에게 T5 가 할 수 있는 일을 시킨다
- 웹과 방에서 사실이 다르다

## 이 문서의 해제 조건

L1~L8 을 실제 방에서 확인하고 원장으로 대조했을 때. 그 전까지 P5-1 은 **완료가 아니다.**
