# P2-4 작업 지시서 — 대화 목록 정리성

> 오너 지시서(2026-07-26) + 구현자 검토 반영본. 본문은 오너 지시가 정본이고,
> **[보강]** 표시된 항목은 코드 대조로 드러난 사고 지점을 오너 승인 아래 추가한 것이다.

**목표**: 좌측 대화목록을 "쌓이는 로그"가 아니라 사용자가 자연스럽게 정리하고 다시 찾는
**작업 기억 공간**으로 만든다. 범위는 P2 대화 품질 보강에 한정한다. P5 채널, P7 의미 검색,
P9 캔버스, 완성형 프로젝트 시스템과 겹치지 않는다.

## 왜 필요한가

실사용 대화가 늘수록 좌측 목록이 무한히 쌓인다. 이는 T5를 오래 쓰는 데 직접적인 피로를 만든다.
T5가 추구하는 AI OS는 대화를 단순 기록으로 쌓는 것이 아니라, 사용자의 작업 기억을
**정리·회수·폐기**할 수 있어야 한다.

## 흡수 기준

- Hermes: 세션 제목, 아카이브, 삭제, 다중 삭제, 프로젝트/그룹의 방향성
- OpenClaw: 세션 생명주기, 삭제 시 복구성, 작업공간적 사고
- T5식 재구성: 무거운 프로젝트 시스템이 아니라 **좌측 목록 관리의 최소 완성**

## 명시적 비범위

```text
완성형 프로젝트/워크스페이스 시스템 금지
벡터 검색/의미 검색 금지 (P7-1)
채널/외부도구/자동화 연결과 결합 금지 (P5/P5-B/P8)
대화 요약·압축·장기기억 승격 금지
UI를 설정판처럼 무겁게 만들지 말 것
```

## 슬라이스 분할 [보강]

1~5를 먼저 라이브에 착지시키고, 그룹은 뒤로 뺀다 — 그룹이 프로젝트 시스템으로 번지면
비범위 첫 줄과 충돌한다.

| 슬라이스 | 내용 |
| --- | --- |
| **P2-4a** | 데이터 계약 + API + 개별 조작(이름 변경·고정·아카이브·삭제) + 좌측 UI |
| **P2-4b** | 다중 선택 · 정리 메뉴(빈 대화 정리 · 오래된 대화 아카이브) |
| **P2-4c** | 가벼운 수동 그룹 |

## 필수 기능

### 1. 대화명 변경 (4a)
- 사용자가 직접 제목을 바꿀 수 있다.
- 첫 발화 자동 제목은 유지하되, **수동 제목은 이후 자동 제목이 덮어쓰지 않는다.**
  (근거: 지금 자동 제목은 "첫 user 발화가 없을 때만" 붙는다 — 첫 턴 전에 이름을 바꾸면 덮어쓴다.)
- 제목은 공백 정리·제어문자 제거·최대 길이 제한.
- 빈 제목은 "새 대화"로 보인다.

### 2. 고정 (4a)
- 개별 대화 고정/해제.
- 정렬: `고정 먼저 → 그 안에서 최근순 → 나머지 최근순`.
- 고정은 모델 입력·기억·승인·자동화에 영향을 주지 않는 **순수 목록 메타데이터**다.

### 3. 아카이브 (4a)
- 기본 목록에서 숨긴다. 별도 보기에서 다시 보고 복원할 수 있다.
- **"정리"의 기본 동작은 삭제보다 아카이브가 우선이다.**

### 4. 삭제 (4a)
- 바로 영구 삭제하지 않는다(soft delete).
- 삭제된 대화는 기본 목록과 모델 이력에서 제외된다.
- 현재 열려 있는 대화를 삭제하면 안전하게 새 대화로 이동한다.

### 5. 선택 정리 (4b)
- 좌측 목록 다중 선택 모드. 선택분 아카이브/삭제.
- "전체 삭제"는 바로 노출하지 않고 정리 메뉴 안에서 확인 절차를 둔다.
- 빈 대화 정리, 오래된 대화 아카이브는 1차 옵션으로 허용.

### 6. 가벼운 그룹 (4c)
- 수동 그룹만. `groupId/groupName` 수준.
- 그룹 생성·이름 변경·대화 이동·해제만. **자동 분류·프로젝트 추론 없음.**
- **[보강]** `groupId` 는 4a 의 데이터 계약에 미리 넣어 둔다(나중에 마이그레이션하지 않게).
  UI·API 는 4c 에서.

## 권한 등급 [보강] — 먼저 못박고 시작한다

자연어로도 조작할 수 있게 하는 순간 이건 **T5가 사용자 데이터를 지우는 도구**를 갖는다는 뜻이다.
Phase 0 에서 두 번 터진 사고(도구 단위 판정 → 삭제 누수)를 세 번째로 반복하지 않으려면
행동 종류를 먼저 정한다. 판정은 `toolActionKind` 하나를 쓴다(층마다 다른 답 금지).

| 동작 | 권한 종류 | 근거 |
| --- | --- | --- |
| 이름 변경 · 고정 · 그룹 이동 | `organize`(자동 진행) | 데이터 손실 없음, 되돌리기 쉬움 |
| 아카이브 · 복원 | `organize`(자동 진행) | 숨김일 뿐 |
| 삭제(개별) | **`delete`(승인 필요)** | 안전 바닥 |
| 다중 삭제 · 전체 삭제 | **`delete`(승인 + 대상·개수 명시)** | 무인 대량 실행 금지 |

## 사용자 체감 기준

버튼으로도, 자연어로도 관리할 수 있다.

```text
"이 대화 제목 T5 개발 검수로 바꿔줘"
"이 대화 고정해줘"
"최근 빈 대화들 정리해줘"
"이 대화는 숨겨줘"
"선택한 대화 삭제해줘"
```

위험 동작은 자연스럽게 확인한다. **"정말 삭제할까요?" 같은 딱딱한 정책문이 아니라, 무엇이
사라지고 무엇이 복구 가능한지 짧게 알려준다** — P2-3에서 만드는 문구 계약을 그대로 쓴다.

## 데이터 계약

```text
title
manualTitle: boolean
pinned: boolean
archivedAt: number | null
deletedAt: number | null
groupId: string | null      # 4a 에서 필드만, 사용은 4c
```

`groupName` 은 세션마다 중복 저장하지 않고 별도 가벼운 그룹 저장소/메타 파일로 관리한다.

**[보강] 휴지통 보존 정책**: soft delete 만 두면 "지웠는데 디스크는 안 준다"가 된다.
파일 손발이 이미 쓰는 원리(`.trash` 개수 상한)와 같게 **보관 기한 또는 개수 상한**을 둔다(§18).

## API 범위

- 세션 메타 업데이트: 제목·고정·그룹 이동
- 아카이브/복원, soft delete
- 다중 아카이브/삭제
- 그룹 생성·변경·삭제·목록 (4c)

`/sessions` 는 기본적으로 삭제·아카이브 제외 목록을 반환한다. `?archived=1`, `?deleted=1` 같은
조회만 허용한다.

**[보강] 직접 접근 규칙**: `GET /sessions/:id` 는 **삭제=404, 아카이브=열람 가능**.
목록에서만 막으면 URL 로 들어온다.

## 검증 기준

- 새 대화 생성 기존 흐름이 깨지지 않는다.
- 첫 발화 자동 제목은 계속 되고, **수동 제목은 다음 턴에도 유지된다.**
- 고정 정렬이 유지된다.
- 아카이브하면 기본 목록에서 사라지고 복원하면 돌아온다.
- 삭제하면 기본 목록과 대화 로드에서 제외된다.
- **[보강] 지운 대화는 세션 검색·회수 후보에도 나오지 않는다.**
  (`/search` 는 `store.loadAll()` 로 세션 경계를 넘어 전체를 뒤진다 — 목록만 막으면
  사용자가 지운 대화가 다음 턴 검색 후보로 되살아난다.)
- 현재 대화 삭제 후 UI 가 빈 화면에 갇히지 않는다.
- 다중 선택 삭제/아카이브가 일부 실패해도 나머지 결과를 정직하게 보여준다.
- 테스트는 세션 저장소·서버 API·좌측 UI 동작 중심으로, 과하지 않게.

## 완료 조건

- 좌측 목록에서 개별 대화를 이름 변경·고정·아카이브·삭제할 수 있다.
- 여러 대화를 선택해서 정리할 수 있다.
- 가벼운 수동 그룹으로 묶을 수 있다(4c).
- 삭제·아카이브·복원 후 새로고침해도 상태가 유지된다.
- 기존 대화·모델 응답·승인·기억·자동화 경로를 건드리지 않는다.
- 전체 테스트 통과 + **실사용 화면에서 직접 조작 경로 확인**(§19).

---

핵심: T5가 무거운 관리툴이 되는 게 아니라, **오래 써도 대화 공간이 지저분해지지 않는 기본 체력**을
갖추는 것.
