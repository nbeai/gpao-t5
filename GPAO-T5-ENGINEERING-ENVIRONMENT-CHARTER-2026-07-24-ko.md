# GPAO-T5 개발 환경 헌장

- 날짜: 2026-07-24
- 상태: `top_authority_engineering_environment`
- 관계: `GPAO-T5-FINAL-DEVELOPMENT-PLAN-2026-07-24-ko.md`의 착수 전후 필수 산출물 8번째.
  `GPAO-T5-DEVELOPMENT-ABSOLUTE-PRINCIPLES-2026-07-24-ko.md`와 짝을 이룬다.
- 역할 분담: **절대 원칙이 "왜"를 정한다. 이 문서는 개발 환경에서 "어떻게 지킬지"를 정한다.**

## 0. 이 문서가 존재하는 이유

절대 원칙은 게이트라고 선언했지만, 게이트를 강제할 장치가 없으면 반쪽이다. 그렇다고 아직
존재하지 않는 코드·빌드를 기준으로 hook과 CI를 미리 잠그면, 그것 자체가 T3 못지않은 방해가
되고 추정 위에 구조를 세우는 일이 된다(절대 원칙 2).

그래서 이 헌장의 방침은 하나다.

```text
마찰 0인 것은 지금 심는다.
느린 게이트는 대상이 실재할 때 붙인다.
```

- **지금 적용(마찰 0):** .gitignore, 폴더 구조 원칙, 협업 기본 원칙, 결정적 빌드 원칙.
- **Phase 5 이후 적용:** pre-commit/pre-push hook, CI, 테스트 게이트, 산출물 검사 자동화.
  실제 `src/`·빌드 파이프라인이 생긴 뒤, 그 형태에 맞춰 붙인다.

일상 작업(코딩·로컬 커밋·초안·조사)은 지금도 나중에도 느려지지 않는다. 게이트는 되돌리기
어렵거나 밖으로 나가는 순간(push·릴리스·배포)에만 걸린다 — 절대 원칙 3의 게이트 대상과 같다.

---

## 1. 빌드 산출물은 소스 트리 밖에만 쓴다 (지금 적용)

T3 최악의 사고: 빌드 스테이지 경로를 `cwd`/`version` 같은 변할 수 있는 문자열로 파생시켰고,
디브랜딩 치환이 그 문자열까지 바꿔 스테이지가 엉뚱한 형제 트리에 떨어졌다. 그 트리가 12GB까지
자랐고, 두 번 지워도 패키징을 돌릴 때마다 다시 생겼다.

규칙:

- 빌드·스테이지·릴리스 산출물은 **소스 트리 밖 고정 절대경로**에만 쓴다.
  기준 위치: `~/Library/Caches/gpao-t5/build/` (또는 OS별 캐시 루트).
- **경로를 변할 수 있는 문자열로 파생시키지 않는다.** 스테이지 위치는 하드코딩 절대값이다.
  버전·브랜드·cwd 를 경로에 끼워 넣지 않는다.
- 소스 폴더 안에 상태 디렉터리(`.gpao-t5/`, `.gpaoT5/`)를 두지 않는다.
- 정리는 성공 경로에만 두지 않는다. `finally` + 시그널 트랩 + 시작 시 고아 스윕, 3중.
- `.gitignore` 가 산출물·스테이지·릴리스 파일을 이미 배제한다. 혹시 새더라도 커밋되지 않는다.

## 2. 소스와 산출물을 물리적으로 분리한다 (원칙 지금 확정, 구조는 Phase 5)

T3는 컴파일된 `dist/` 7,863개를 git 에 커밋하고 직접 편집했다. 그래서:

- 빌드할 때마다 수백 개 파일이 diff 에 떠 "내 변경"과 "빌드 산출물"을 매번 구분해야 했다.
- "소스는 멀쩡한데 배포본만 깨지는" 부류가 여기서 나왔다 — 커밋된 dist 가 곧 "소스"였기 때문.

원칙(지금 확정):

- 사람이 쓰는 소스와 기계가 만든 산출물은 물리적으로 분리한다.
- 산출물(`build/`, `dist/`, `out/`)은 git 에 커밋하지 않는다. 릴리스 때 생성한다.
- 그러면 빌드는 트리를 절대 더럽히지 않는다. `git status` 에는 항상 "사람이 의도한 변경"만 뜬다.

실제 폴더 구조(`src/`, `test/`, 레이어 배치 등)는 **Phase 5에서 첫 코드가 생길 때** 계획서
6.2 아키텍처(L0~L5)에 맞춰 만든다. 지금 비어 있는 구조를 미리 파 두지 않는다(추정 금지).

## 3. 결정적 빌드 (원칙 지금 확정)

T3 빌드 스크립트는 시각·난수를 7곳에서 썼다. 같은 소스인데 매빌드 산출물이 달라져 의미 없는
diff 가 생겼고, SW 캐시 스탬프가 실제 배포 바이트와 어긋나 수정이 사용자에게 도달하지 않았다.

규칙:

- 빌드 산출물에 `Date.now()`·`new Date()`·`Math.random()` 을 넣지 않는다.
- 버전·캐시 키·스탬프는 **콘텐츠 해시**로 만든다.
- 시각이 꼭 필요하면 빌드 입력으로 주입하고 산출물에 박지 않는다.
- 목표: 같은 소스를 다시 빌드하면 동일 산출물 → spurious diff 0.

캐시 스탬프·인벤토리 같은 파생값은 **변환·치환이 끝난 최종 산출물** 기준으로 계산한다
(T3: 변환 전 기준으로 계산해 스탬프가 배포 바이트와 어긋났다). — 절대 원칙 1과 같은 뿌리.

## 4. 에이전트 협업: 격리는 기본 원칙, 강제는 아니다 (지금 확정)

T5는 Reference-First 로 T3·lab_un·Codex 등 여러 소스를 병렬 흡수한다. 여러 에이전트가 동시에
붙을 때, T3처럼 하나의 워킹카피를 공유하면 오염된다. 실제로 T3에서 다른 팀 미커밋 변경이 작업
중 계속 나타났고, 테스트가 다른 에이전트의 파일 쓰기 중에 일시적으로 깨졌다.

기본 원칙(여러 에이전트가 동시에 작업할 때):

- 에이전트마다 격리된 작업 공간을 쓴다: `git worktree add ../t5-<작업명> <브랜치>`.
  하나의 `.git` 을 공유하되 디렉터리는 분리된다(디스크 저렴).
- 하나의 파일을 둘이 동시에 편집하지 않는다.
- 합류는 편집이 아니라 병합(PR)으로 한다. `main` 은 항상 통과 상태를 유지하고, 작업은 브랜치에서.
- 커밋은 내 변경만 담는다. 워킹트리에 남의 미커밋이 섞여 있으면 그것만 골라 커밋한다(절대 원칙 6).

강제가 아니다:

- 혼자 작업할 때는 worktree 를 쓰지 않아도 된다. 격리는 동시 작업의 오염을 막는 장치이지,
  단독 작업까지 복잡하게 만드는 규칙이 아니다.

### 4.1 도구 역할 분리 (지금 확정)

T5 개발은 Claude Code 와 Codex 를 함께 쓴다. 역할의 핵심은 "누가 손인가"가 아니라
**만든 자와 의심하는 자가 분리된다**는 것이다. 만든 주체가 자기 산출물을 감사하면 같은 맹점을
공유한다 — T3에서 "잔여 0건"을 잘못 검증하고 그 위에 불변식을 세운 사고가 정확히 이것이다.

한 문장: **Claude Code 는 손, Codex 는 눈과 판단계, 사용자는 최종 의지와 방향.**

기본 소유권:

| 영역 | 주체 |
| --- | --- |
| 코드 구현·리팩터링·테스트 코드·모듈 작업·성능·버그 수정 | Claude Code |
| 문서·설계·UX 판단·시나리오 벤치마크·로드맵·원장 관리 | Codex |
| 지시서 정제(작업 전) | Codex |
| 산출물 감사(작업 후)·공정감시·릴리스 판단 | Codex |

기본 운영 흐름:

```text
1. 사용자가 목표를 말한다.
2. Codex 가 작업 지시서를 정제한다.
3. Claude Code 가 별도 브랜치/worktree 에서 구현한다.
4. Codex 가 정본 문서·절대 원칙·사용자 시나리오 기준으로 감사한다.
5. 통과한 것만 병합한다.
6. 릴리스/라이브 반영은 별도 승인 후 진행한다.
```

접점은 한 방향이다: Codex 가 쓴 시나리오·설계를 Claude Code 가 코드로 구현한다. 소유가
겹치지 않으므로 "한 파일 동시편집 금지"가 자동으로 지켜진다.

비타협 조건(감사자에게도 적용되는 절대 원칙 1):

- 감사는 **실행 감사**다. 코드·문서를 읽는 리뷰에서 멈추지 않는다. 산출물을 펼쳐 실제로
  돌려서 검증한다. 리뷰만으로는 T3에서 테스트 424개가 통과하는데도 배포본이 죽던 상황을
  잡지 못한다. Codex 의 로컬 실행력이 여기에 쓰인다.

### 4.2 예외: 구현자를 쓸 수 없을 때 (지금 확정)

역할 분리는 원칙이지 족쇄가 아니다. Claude Code 를 쓸 수 없는 상황에서 "구현자가 없으니
멈춘다"가 되면 그것 자체가 개발을 방해하는 구조가 된다. 그래서 예외를 연다.

원칙: **분리가 기본, 겸임은 예외, 겸임할 땐 검증을 더 조인다.**

- Codex 가 구현을 겸할 수 있다. 단 그 순간 손과 눈이 한 주체가 됨을 **명시적으로 인지**하고,
  절대 원칙 1·8(산출물 실행 검증, 실패 재현 테스트)을 평소보다 **더 엄격히** 적용한다.
- 겸임 중 만든 구간은, 나중에 Claude Code 가 복귀하면 **독립 감사에 다시 태운다.**
  겸임 상태에서 생긴 맹점을 뒤에서 잡는 안전망이다.
- 되돌리기 어렵거나 밖으로 나가는 것(릴리스·라이브·외부 전송)은 겸임 상태에서 단독으로
  강행하지 않는다. 이때는 사용자의 판단이 분리된 감사자를 대신하는 게이트가 된다.
- 반대 방향도 같다. Codex 를 쓸 수 없으면 Claude Code 가 감사·판단을 겸하되, 같은 조건을
  적용한다(자기 산출물 감사의 맹점을 인지하고, 실행 감사로 보완하며, 밖으로 나가는 것은
  사용자 승인으로 게이트).

겸임 구간 표시(의무):

- 한 주체가 구현과 감사를 겸한 작업은 **작업 기록에 겸임 표시를 남긴다.** 표시가 없으면
  나중에 어디를 독립 감사에 다시 태워야 하는지 찾을 수 없어, 위의 안전망이 작동할 자리를
  잃는다.
- 커밋 메시지 trailer 에 `Dual-Role: <주체> (구현+감사 겸임 — 독립 감사 필요)` 를 남긴다.
  예: `Dual-Role: Codex (구현+감사 겸임 — 독립 감사 필요)`.
- 원장·작업 노트에도 해당 구간을 `겸임 구현` 으로 표시한다.
- 복귀한 독립 감사자는 이 표시를 기준으로 겸임 구간을 우선 재검토하고, 통과하면 표시를 해소한다.

## 5. 지금 하지 않는 것 (명시적 비범위)

아래는 대상이 실재하기 전에는 하지 않는다. 하면 추정 위에 구조를 세우는 일이다.

- pre-commit 에서 전체 테스트 실행 (커밋을 느리게 만들고 `--no-verify` 우회를 부른다)
- 실제 빌드 방식이 정해지기 전 hook/CI 를 과하게 고정
- 비어 있는 폴더 구조를 미리 많이 만들기
- 아직 존재하지 않는 위험을 기준으로 개발 흐름을 잠그기

## 6. Phase 5 이후 붙일 게이트 (예고, 지금은 문서로만)

첫 코드와 빌드 파이프라인이 생기면 그 형태에 맞춰 아래를 붙인다.

- pre-commit(빠른 것만): staged 파일에 산출물·시크릿·스테이지 패턴이 섞였는지 grep 검사(밀리초).
  전체 테스트는 여기 넣지 않는다.
- pre-push 또는 CI: 관련 테스트 게이트. 밖으로 나가기 전에만 검증.
- CI: **산출물을 펼쳐 검사**한다(절대 원칙 1). T3 pkg 빌더에 넣은 6개 게이트가 그 선례 —
  소스가 아니라 배포될 파일을 `--check` 하고 실제로 실행해 본다.
- 모든 게이트에 명시적 우회구를 두되, 우회하면 로그에 남긴다(절대 원칙 9: 정직한 보고).

이 게이트들의 구체 형태는 Phase 5 착수 시 별도로 확정한다. 이 문서는 "언제·무엇을 붙일지"의
기준만 정한다.

---

## 7. 최상위 적용 규칙

이 헌장은 개발 환경에 대한 게이트다. 절대 원칙 12의 충돌 해소 순서를 따른다.

원칙이 개발을 방해한다고 느껴지면, 그것은 이 헌장이 잘못 적용된 것이다. 이 헌장의 성공 기준은
"T3에서 터진 사고가 다시 나지 않으면서, 일상 개발 속도는 건드리지 않는 것"이다. 마찰이 생기면
게이트를 강화하지 말고 이 기준으로 되돌아온다.
