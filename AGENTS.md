# T5 Refoundation Engineering Contract

이 저장소의 제품 방향은 오너가 정한다. 작업자는 기존 코드·테스트·문서를 제품의 진리로
간주하지 않는다. 현재 T5의 재창립 목적과 진행 상태는 아래 두 문서만 먼저 읽는다.

1. `T5-PRODUCT.md` — 무엇을 만드는가
2. `T5-REFOUNDATION.md` — 지금 어느 Gate를 통과하는가

하위 디렉터리에 `AGENTS.md`가 있으면 그 디렉터리 작업에 한해 더 좁은 규칙이 우선한다.

## 1. 제품 한 문장

> 사용자는 T5를 배우지 않는다. T5가 사용자를 배우고, 사용자가 평소 말로 목적을 말하면
> T5는 컴퓨터와 외부 자원을 활용해 실제로 끝낸다.

모델은 의도·계획·도구 선택·복구·사용자 문장을 판단한다. 런타임은 현재 현실·도구·권한·실행·
영수증·취소를 제공한다. 런타임이 정규식과 사후 문장 교정으로 모델의 판단을 대신하지 않는다.

## 2. 현재 개발선

- 기존 `src/`, `test/`, `scripts/`는 동결된 legacy 증거선이다.
- 새 코어는 `refoundation/` 아래에서 독립적으로 만든다.
- 새 코어는 legacy `src/`를 import하지 않는다.
- 현재 콘솔 디자인과 대화 UX는 R4에서 재사용한다. 새 코어 개발을 이유로 UI를 다시 디자인하지 않는다.
- legacy 제품 수정은 새 코어 비교에 필요한 계측 또는 즉시 차단해야 할 실제 P0 위험일 때만
  오너에게 범위를 설명하고 별도 작업으로 연다.
- Memory, Skills, Learning, Automation, Channels, Multi-agent는 `T5-REFOUNDATION.md`의 현재
  Gate가 열기 전에는 구현하지 않는다.

## 3. 한 작업의 시작

코드를 만지기 전에 다음 일곱 줄을 확인한다.

1. T5의 제품 약속
2. 현재 Gate
3. Gate의 사용자 완료 문장
4. 이미 선 실제 증거
5. 현재 가장 큰 미달
6. 이번 변경이 그 미달을 줄이는 방식
7. 이번 변경의 non-goals

이 일곱 줄을 Git과 실제 실행에서 확인할 수 없으면 구현하지 않는다.

## 4. 개발 순환

```
사용자 목적 → 실패 원본 → 실제 모델 입력 → 원인 확정 → 작은 변경
→ 반대시험 → 실제 모델 과업 → legacy·비교군 대조 → 통과/폐기/재설계
```

- 추론만으로 원인을 확정하지 않는다.
- 모델 입력 문제는 실제 prompt dump를 읽는다.
- 작업 단위는 작게 유지하되 제품 판정은 사용자 목적 전체로 한다.
- 도구 호출 수, 테스트 수, 코드 줄 수를 제품 성공으로 보고하지 않는다.
- 도구 하나가 성공해도 과업 영역이 서지 않았으면 완료가 아니다.

## 5. 실행과 진실

다음 사실을 하나의 `success` 값으로 합치지 않는다.

- 모델이 요청했다.
- 런타임이 허용했다.
- 실제 실행됐다.
- 로컬 상태가 변했다.
- 외부 효과가 발생했다.
- 사용자 목적이 달성됐다.

실행하지 않은 일을 실행했다고 기록하지 않는다. 실행한 일을 실행하지 않았다고 기록하지 않는다.
로컬 효과가 없다는 사실은 외부 효과가 없다는 뜻이 아니다. 모델의 주장은 영수증이 아니며,
영수증도 목적 달성 판단을 대신하지 않는다.

사용자에게 보이는 최종 답은 모델이 작성한다. 런타임은 내부 회계·고정 문구·사후 판정을 답에
덧붙이거나 답을 갈아치우지 않는다.

## 6. 권한과 자동성

사용자에게 멈춰 묻는 기본 경계는 오너가 정한 네 가지다.

1. 비밀값 입력
2. 백업 없는 파괴
3. 새 상대에게 첫 외부 전송
4. 돈이 나가는 일

그 밖의 행동은 사용자의 명시적 목적과 허용 범위 안에서 자동성을 우선한다. 안전을 반복 승인으로
사지 않는다. 범위, 격리, 백업, 영수증, 실행 전후 관측, 취소와 복구로 만든다.

실제 홈·계정·자격증명으로 자동 시험하지 않는다. 개발과 테스트는 격리 HOME·DATA·WORKSPACE를
사용한다. 외부 효과 시험은 루프백 수신 서버로만 한다.

## 7. 테스트 규율

새 코어의 검사는 세 종류뿐이다.

- 핵심 불변식: 실행·권한·영수증·취소·답 저작권
- 과업 영역: 입력 표현이 달라도 같은 목적을 완수하는가
- 비교 시험: 새 T5·legacy T5·비교군의 실제 사용자 성과

문자열 일치를 자연어 품질의 정답으로 만들지 않는다. 테스트는 실제 행동을 제거하면 빨개져야 한다.
기존 검사를 지워 초록을 만들지 않는다. legacy 검사가 현재 제품 계약과 충돌하면 숨기지 말고 별도
baseline으로 보고하고 오너 결정 없이 의미를 바꾸지 않는다.

`npm test`와 `npm run gate`를 동시에 실행하지 않는다. 새 레인 일상 검사는
`npm run refoundation:check`를 사용한다.

사용자에게 보이는 기능은 내부 호출 성공만으로 완료하지 않는다. 실제 콘솔에서 사용자의 시작점부터
`진행 중 → 성공 또는 실패 → 현재 적용 상태`가 이해되는지 확인하고, 설정 화면·대화 입력 영역처럼
같은 사실을 보여주는 표면들이 서버의 단일 상태와 함께 갱신되는지 검증한다.
진행 문구는 실제 런타임 사건과 일치하는 짧은 사용자 언어로 표현한다. 내부 사고 원문·검색어·경로·
명령·비밀값을 노출하지 않으며, `판단`처럼 책임과 확정을 암시하는 말 대신 상황에 맞는 생각·확인·
검토·파악·연구·작업 언어를 사용한다.

## 8. 중단선

다음 중 하나가 발생하면 코드를 더 얹지 않고 구조를 재판정한다.

- 같은 결함 가족에 세 번째 패치를 붙이려 한다.
- 새 코어가 legacy `turn.js`, Intent, ActionPlan, T-cell을 import하려 한다.
- 사용자 흐름 없이 타입·저장소·관리 화면부터 만든다.
- 모델이 판단할 내용을 런타임 정규식으로 옮긴다.
- 테스트는 통과하지만 실제 모델 과업이 실패한다.
- 현재 Gate 밖 기능을 편의상 구현한다.
- 같은 내용을 설명하는 새 정본·인계서·봉인문을 만든다.
- 변경 이유를 사용자 목적 한 문장으로 설명할 수 없다.

## 9. 저장소와 Git

- 기존 변경과 미추적 파일은 사용자 소유다. 관련 없는 파일을 수정·정리·스테이지하지 않는다.
- `git add -A`, `git add -u`를 쓰지 않고 경로를 명시한다.
- 공유 브랜치 amend 금지. 새 커밋만 만든다.
- 빌드·상태·비밀은 소스 트리에 두지 않는다.
- 한 단계의 증거는 Git 커밋과 `refoundation/evidence/`의 작은 기계 자료로 가리킨다.
  같은 사실을 새 문서로 반복 기록하지 않는다.

## 10. 작업 종료 보고

매 작업은 다음만 보고한다.

- 가능해진 사용자 행동
- 변경한 경계와 파일
- 실행한 검사와 실제 결과
- 실패·미달·확인하지 않은 것
- 현재 Gate에 미친 영향
- 다음 한 작업

불리한 결과를 먼저 숨기지 않는다. 다음 Gate로 갈지 여부는 오너가 사용자 경험을 보고 결정한다.
