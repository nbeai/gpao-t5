# GPAO-T5 UI/UX Reference Seal

- Status: `초안 작성 완료 · 감사 전`
- Date: 2026-07-24
- Author: Claude Code (구현자)
- Auditor: Codex (감사 대기)
- Phase: Phase 5 진행 전 삽입 — 사용자 표면 정본 봉인(오너 지시)
- 근거: UX Architecture(봉인) §1.2 안티 대시보드 / Kernel Contract(봉인) / Product Constitution §3.1
  Reference-First Absorption / First Build Slice 감사(조건부 통과) / T3 실제 화면(기준 샘플)
- 위상: UX Architecture 아래, 실제 표면 구현 위. Phase 5 이후 모든 Work Chat·표면 작업을 구속하는
  디자인 정본이다. 계약을 화면으로 번역할 때 "어떤 형태로" 번역할지를 고정한다.

## 0. 작성 규율 + UI Reference-First 경계

- 이 문서는 시각 디자인 명세가 아니라 **표면 구조·상호작용 정본**이다. 픽셀·컬러 토큰·타이포 스케일은
  구현에서 정하되 이 정본의 규칙을 어길 수 없다.
- **Reference-First 는 UI에도 적용된다(헌법 §3.1).** 참조 표면의 구조적 DNA를 흡수하되, 브랜드·정확한
  화면·컴포넌트·config·UI 문자열을 정본으로 삼거나 복제하지 않는다.
- **T3 실제 화면은 기준 레퍼런스(샘플)이지 복제 대상이 아니다.** 계승하는 것은 배치·상호작용 원리이지
  T3의 브랜딩·메뉴 문구·정확한 레이아웃이 아니다.
- 최상위 판정선(감사가 못박음, UX §1.2): **사용자는 "시스템을 조작한다"가 아니라 "말하면 일이
  이어진다"고 느껴야 한다.** 내부 계약이 늘수록 첫 화면은 더 단순해야 한다.

---

## 1. 참조 표면 해부 (무엇을 취하고 무엇을 버리나)

여섯 표면을 사용자 경험 관점으로 해부한다. 각각에서 취할 DNA와 버릴 것을 분리한다.

| 참조 표면 | 잘하는 것(취할 DNA) | T5가 버릴 것 |
| --- | --- | --- |
| **T3 실제 화면**(기준 샘플) | 조용한 좌측 사이드바 + 넓은 채팅 + 하단 고정 입력, 고기능(작업 기록·도구)을 대화 안에서 접어 노출, 작은 모델·상태 표시, 생활형 따뜻한 톤 | T3 브랜딩·정확한 메뉴 문구, 상태 표시에 내부 id 노출 |
| **OpenClaw**(런타임 계보 — T3 표면의 상위 계보) | 실행·도구·권한을 대화 흐름에 통합하는 계보적 발상 | 런타임 정체성·내부 경로·비공개 구현(정본 금지, 인벤토리 폐기/추가검증) |
| **ChatGPT** | 대중적으로 친숙한 중앙 채팅, 최소 크롬, 모델 선택의 가벼움, 진입 장벽 낮음 | 기능을 "GPTs/도구 선반"처럼 나열, 설정 과다 노출 |
| **Claude** | 차분한 미니멀 톤, 대화 옆 산출물(아티팩트/캔버스) 병치, 프로젝트 공간 | 특정 브랜드 비주얼, 사이드 패널 과밀 |
| **Codex** | 작업(Task) 중심 실행 표면, 계획·진행·결과·diff의 가시화, 고기능을 정면에 | 개발자 콘솔 밀도, diff/PR 중심 정보구조(일반 사용자엔 과함) |
| **Claude Code** | 강력한 도구 운용 + **권한 게이트를 흐름 안에서** 처리, 계획/할일 가시화, 실행 사실성 | 터미널 밀도, 내부 로그·스택·스키마의 상시 노출 |

핵심 종합: **ChatGPT/Claude의 친숙한 자연 웹챗 껍질** 위에 **Codex/Claude Code의 고기능(도구·권한·계획·
원장)**을, **T3 실제 화면의 "접어서 조용히"** 방식으로 얹는다. 이것이 오너가 처음부터 요청한
"챗지피티 인터페이스 + 코덱스 기능 / 클로드 인터페이스 + 클로드코드 기능"의 표면적 실체다.

### 1.1 CSS/레이아웃 craft 참고원 — OpenClaw `ui/` (craft만, IA·identity 아님)

이 제품군은 이미지가 무거운 게 아니라 **CSS·레이아웃 감각**이 자산이다(오너 지시). 그 감각의 참고원을
못박는다:

- **참고 대상(craft)**: OpenClaw `ui/docs/design-system/`(문서화된 디자인 시스템 —
  `color-tokens.md·motion.md·glass-surfaces.md·accessibility.md`) + `ui/src/styles/`의
  `base.css·layout.css·layout.mobile.css·components.css·chat.css(+chat/)·option-card.css·approval.css` +
  `theme.ts`. 여기서 **간격·타입 스케일·밀도·motion 타이밍·컴포넌트 형태·모바일 브레이크포인트·접근성**을
  감각으로 흡수한다. (`layout.mobile.css`는 T5 모바일 브레이크포인트 게이트의 직접 참고원.)
- **금지(§5와 동일)**: `--oc-*`/`<openclaw-*>` 토큰·클래스·요소 접두어의 그대로 수입 금지. OpenClaw의
  40-라우트 콘솔 **정보구조(IA)는 반면교사**(§1 표·§5). CSS 장인정신은 배우되 화면 구성은 안 따른다.
- **규율**: 값은 영감으로 보고 **T5 자기 토큰으로 재구성**한다("그들이 어떻게 여백·크기·움직임을 다루는지
  읽어서 배우고, T5는 자기 언어로 다시 쓴다"). Hermes는 `apps/desktop`(Tauri) 정도라 웹챗 craft 참고는
  OpenClaw `ui/`가 주력이다.
- **추적**: 실제로 반영한 지점은 design evidence에 **출처와 함께 기록**해 다음 감사에서 craft-흡수 vs
  복제를 판별할 수 있게 한다.

---

## 2. 계승할 구조 (INHERIT)

1. **3영역 골격** — 조용한 좌측 사이드바 · 넓은 중앙 채팅 · 하단 고정 입력.
2. **고기능은 접어서 대화 안에** — 작업 기록/도구/근거를 별도 콘솔이 아니라 대화 흐름 안 접힌 요소로.
3. **작은 보조 상태** — 모델·연결·컨텍스트는 크게 두지 않고 모서리에 작게(상태칩·하단 상태행).
4. **사이드바에 고기능 은닉** — 스킬·연결·자동화·배운 작업을 접힌 진입점으로(정면 나열 금지).
5. **하단 입력 우선** — 입력창이 항상 손에 닿는 곳에, 넓고 방해 없이.
6. **생활형 톤** — 개발자 콘솔이 아니라 따뜻하고 조용한 도구. 색·여백·문장 모두 그 방향.
7. **세션 목록의 좌측 상주** — 대화 이어가기를 한 클릭 거리에(단, 실제 세션만; 가짜 목록 금지).

## 3. 버릴 구조 (DISCARD)

1. **대시보드 우선** — 상태·연결·원장 패널이 첫 화면을 채우는 배치.
2. **기능 선반(feature shelf)** — 도구/플러그인/GPTs를 정면에 나열해 사용자를 고르게 만드는 방식.
3. **개발자 콘솔 밀도** — diff/로그/스키마/터미널 밀도의 정보구조.
4. **모달 과다** — 흐름을 끊는 팝업 중심 승인·설정.
5. **설정 과노출** — 토글·옵션을 상시 펼쳐 사용자를 관리자로 만드는 배치.
6. **참조 브랜딩 잔재** — T3/OpenClaw/외부 제품의 이름·정체성·정확한 화면 복제.

## 4. T5 고유화할 구조 (MAKE T5-UNIQUE)

계승·폐기를 넘어, T5만의 표면 정체성. 계약이 그대로 화면 언어가 된다.

1. **Operational Selfhood 칩** — SelfStateSnapshot 을 대화 점유 없이 접힌 칩으로. "지금 가능/막힘/다음
   안전 행동"이 한 번의 클릭으로 열린다. (참조 표면에 없는 T5 고유 요소.)
2. **Truth Ledger 근거 토글** — 모든 실행 뒤 "확인/미확인/추정"을 대화 안 접힌 "작업 기록"으로.
   답과 근거가 분리되지만 한자리에(ToolReceipt 번역).
3. **Authority 인라인 승인** — 외부 행동은 모달이 아니라 대화 안 카드로 멈춘다. 승인/거부가 흐름 안에서
   (AuthorityGrant 번역, Claude Code 계승의 T5화).
4. **막다른 답 금지 표면** — 실패·차단에도 "다음 안전 행동"이 답 끝에 붙는다(nextSafeAction 번역).
5. **안티 대시보드 강도** — 계약이 많아질수록 첫 화면은 더 조용해진다는 역방향 규칙을 T5 정체성으로.

## 5. 금지할 디자인 (FORBIDDEN)

1. **내부용어·id·스키마·raw path·스택·provider 오류의 기본 화면 노출**(UX §1.2, S43). 사용자면은
   userSafeSummary·라벨만. 진단은 diagnosticTrace로 분리.
2. **죽은 버튼**(헌법 §4.2) — 목록에 올린 것은 끝까지 붙여주거나, 아직이면 "곧" 정직하게 안내. 눌러서
   아무 일도 안 나는 요소 금지.
3. **가짜 데이터** — 없는 세션·연결·기록을 채워 넣기 금지(사례 전용 누더기, 절대원칙 4).
4. **대시보드 첫 화면** — 상태/설정/원장이 채팅보다 앞서는 배치.
5. **흐름을 끊는 모달 승인** — 승인은 대화 안 인라인으로.
6. **상태 표시의 대화 점유** — 자기파악이 기본 대화 흐름을 밀어내는 배치(헌법 §5).

## 6. 첫 Work Chat 화면 기준 (BASELINE)

첫 슬라이스가 만족해야 할 표면 계약. (이 문서와 함께 커밋된 slice-1 구현이 이 기준의 첫 충족본이다.)

```text
┌───────────┬─────────────────────────────────────────────┐
│ 사이드바   │ 브레드크럼: GPAO-T5 › Work Chat      [상태칩] │
│ (조용)    ├─────────────────────────────────────────────┤
│ 브랜드     │                                             │
│ 검색       │            넓고 조용한 채팅                   │
│ ─접힌 나브─ │   사용자 발화(우) / GPAO-T5 답(좌)           │
│  스킬/연결/ │   └ 접힌 "작업 기록 · 도구 N개"(근거)         │
│  자동화 …  │   └ 인라인 승인 카드(A2·A3)                  │
│ +새 대화   │                                             │
│ 세션 목록  │                                             │
│ ─하단유틸─ ├─────────────────────────────────────────────┤
│ 상태·설정  │  [+] GPAO-T5에게 메시지 입력        [보내기]  │
│ ·테마     │  모델·상태(작게)          말하면 일이 이어짐   │
└───────────┴─────────────────────────────────────────────┘
```

- 기본 화면 = 채팅. 상태·원장·설정은 접힘/보조.
- 데스크톱 3영역, 모바일은 사이드바가 햄버거로 접힘(반응형 필수).
- 라이트/다크 모두 지원, 따뜻한 톤.

## 7. 컴포넌트 기준 (계약 번역 규칙)

각 컴포넌트는 특정 Kernel Contract 필드의 번역이다. 발명 금지.

### 7.1 사이드바
- 역할: 고기능을 조용히 은닉하고 대화 이어가기를 한 클릭 거리에.
- 규칙: 브랜드·검색·접힌 고기능 진입점(스킬/배운 작업/연결/자동화)·새 대화·세션·하단 유틸.
- 금지: 정면 기능 나열, 가짜 세션, 죽은 진입점(미구축은 "곧" 정직 안내).

### 7.2 입력창
- 역할: 항상 손에 닿는 발화 지점. "말하면 일이 이어진다"의 물리적 구현.
- 규칙: 하단 고정, 넓게, 방해 없이. 아래에 모델·상태를 **작게** 보조 표시. Enter 전송·Shift+Enter 줄바꿈.
- 금지: 입력 주변에 도구·옵션 과노출.

### 7.3 상태칩 (SelfStateSnapshot 번역)
- 역할: 자기파악을 대화 점유 없이. 접힌 채, 필요 시 열림.
- 규칙: 평소 "준비됨"; 문제 시 상태어(billing_blocked/rate_limited…)와 색으로. 열면 모델·바로 쓰는 연결
  (**라벨만**)·막힌 것·다음 안전 행동.
- 금지: 내부 도구 id 노출(감사 지적 지점), 기본 펼침, 대화 흐름 점유.

### 7.4 승인 카드 (AuthorityGrant.approvalPreview 번역)
- 역할: 외부 행동을 흐름 안에서 멈춰 승인받는다(모달 아님).
- 규칙: 행동 **라벨**·등급(A2/A3)·영향·되돌리기 여부·[승인]/[보내지 마]. 승인은 재해석이 아니라 보관된
  봉인 계획을 이어받는다(감사 지적 수정 반영). 거부는 안전 정지(초안 보존).
- 금지: 내부 id 표시, 승인 없는 외부 실행, 되돌리기 불가를 숨김.

### 7.5 근거 토글 (ToolReceipt / Truth Ledger 번역)
- 역할: 답과 근거를 분리하되 한자리에. 대화 안 접힌 "작업 기록 · 도구 N개".
- 규칙: 펼치면 확인/미확인/추정. userSafeSummary만. 실패는 미확인 + 다음 안전 행동.
- 금지: 진단면(diagnosticTrace) 노출, 미확인을 확인으로 뭉뚱그림.

## 8. 현재 구현 대비 (정직한 현황)

이 Seal과 함께 커밋된 slice-1 Work Chat 구현의 충족·미충족:

- 충족: 3영역 골격, 접힌 작업 기록, 인라인 승인(계획 이어받기), 상태칩(라벨만), 하단 입력+작은 상태,
  반응형(모바일 햄버거), 라이트/다크, 죽은 버튼 금지("곧" 안내), 라이브 렌더 확인.
- 미충족/이월: 실제 세션 다건·검색, 스킬/연결/자동화 실화면(P1), 캔버스/아티팩트 병치(P1),
  실제 BEAI5/LLM(스텁), 시각 디자인 정교화(컬러·타이포 스케일).

## 9. 정본 수정 제안 (감사·다음 Phase 판단용, 정본 수정 아님)

1. 컬러·타이포·간격 토큰 체계는 Phase 5 표면 밀도화에서 확정하고, 이 Seal은 구조·상호작용까지만 고정.
2. 시각 증거는 실제 PNG로 저장한다(`design/evidence/2026-07-24-slice1/`). 캡처는 Chrome headless +
   DevTools Protocol(`design/evidence/capture.mjs`, 저장소 의존성 0)로 재현 가능하다. (앞선 판에서
   "스크린샷 커밋 불가"라 적은 것은 오류였고 정정했다 — 감사 지적 반영.)

---

*이 문서는 초안이다. Codex 감사 후 UI/UX Reference Seal 로 봉인한다. 다음 단계는 감사다.*
