# GPAO-T5 P2-7 작업 지시서 — 운영 상태 단일 진실화

작성: 2026-07-27 · 오너 지시서 + 검토 의견 4건 반영(전부 수용) · **P2 최우선**

---

## 목적

이 작업의 목적은 T5를 더 복잡한 에이전트로 만드는 것이 아니다. **모델이 T5의 현재 손발·기억·
맥락·한계를 정확히 알고 행동하게** 만드는 것이다.

T5는 AI 모델을 만드는 프로젝트가 아니라, **모델의 한계를 보완하고 능력을 최대치로 쓰게 하는
AI OS**다. 따라서 이번 작업은 프롬프트 문구 추가가 아니라, **실제 운영 상태를 모델 앞에 허위 없이
공급하는 구조 정리**다.

과잉 설계하지 않는다. 기존 descriptor·receipt·selfState·TaskContext·원장을 살려 최소한으로 닫는다.

## 문제 정의

두 축이 아직 닫히지 않았다. **둘은 같은 문제다** — OS가 모델 앞에 넣어야 할 현재 사실을
**단일 진실로 조립하지 못하는 것**이다. 모델이 똑똑하지 않아서가 아니다.

1. **지금 무엇을 할 수 있는가** — 도구 설명이 수동 맵 네 곳에 흩어져 있다(실측):
   `tool-labels.js:LABELS`, `tool-schema.js:SCHEMAS`, `action-plan.js:TOOL_KIND`, `live-context.js`.
   도구가 사라져도 설명이 남고, 새 도구가 들어와도 모델이 모른다.
2. **지금 이 대화에서 어디까지 왔는가** — 도구 결과가 이번 턴에만 들어가고 다음 턴엔 사라진다.
   `recentTurns`는 사람 발화·산문 답변만 본다. 그래서 "방금 그 페이지", "그 리뷰"에서 대상을 놓친다.

## 개발 원칙

- 과잉 설계 금지. 새 대형 컨텍스트 엔진 금지. 안전 경계 변경 금지.
- 기존 receipt·descriptor·selfState·TaskContext를 살린다.
- 모델 입력을 무겁게 만들지 않는다. **원문 전체는 원장에, 모델 입력에는 압축 사실만.**
- 사용자에게 내부 구조를 드러내지 않는다.
- 완료 기준은 테스트 초록이 아니라 **실제 멀티턴 체감**이다.

---

## 0순위 — 세션 경합·원자적 저장 (맥락 원장화보다 먼저)

채널 수신기·웹 UI·자동화 tick이 같은 세션을 동시에 만질 수 있다. 맥락을 원장화하기 전에 저장이
안전하지 않으면 데이터 유실·JSON 깨짐이 생긴다.

**검토 정정 ①(수용)**: "write 직렬화"로는 안 닫힌다. 실측한 `server.js` 턴 경로의 모양은

```text
세션 로드 → transcript.push(user) → await runTurn(...)  ← 30초 이상
          → transcript.push(assistant) → 원장·workingState 대입 → 저장
```

**변경 구간이 모델 호출 전체를 가로지른다.** 쓰기만 직렬화하면, 턴 B가 턴 A의 저장 **전에**
세션을 읽어버려 A의 transcript·원장·workingState를 통째로 덮는다.

작업:

- 같은 세션의 **턴 전체(로드·실행·저장)** 를 직렬화한다.
- 대기는 **거절이 아니라 큐**다. T5가 생각하는 동안 텔레그램으로 들어온 다음 메시지는 버려지지
  않고 순서대로 처리된다(OpenClaw 세션 큐와 같은 원리).
- 세션 저장은 원자적으로 한다(임시 파일 + rename).
- 크게 만들지 않는다: 세션 단위 큐 + atomic write 정도로 닫는다.
- **경계 명시**: 인프로세스 큐는 한 프로세스 안에서만 유효하다. 지금은 수신기·UI·tick이 모두 같은
  프로세스라 충분하다. 다중 프로세스(CLI 병행)가 생기면 부족하다 — **지금 만들지 말고 주석으로 남긴다.**

완료 기준:

- 같은 세션 동시 턴에서 transcript·receipt·workingState 유실 없음
- **턴 A 실행 중 도착한 턴 B가 유실되지도, 거절되지도 않는다**(큐 확인 — 이 줄이 없으면 뒤 요청을
  튕겨도 통과한다)
- 저장 중 크래시 가정에서 깨진 JSON이 남지 않음

---

## 1축 — 도구 자기파악 단일 진실화

**검토 정정 ②(수용)**: **2축을 1축보다 먼저 한다.** 근거 —

- 1축은 지금 실패를 만들고 있지 않다(근거였던 "세션 못 찾아요"는 손으로 닫혔다). 재발 방지다.
- 2축은 지금 실패다. 오너가 두 번 지적했고 핵심 완료 판정이 거기 있다.
- 1축은 네 표면 + 게이트를 건드리는 **광범위 리팩터**다. "개발 트리는 멀쩡한데 라이브가 깨지는"
  유형(절대원칙 1). 2축이 안 끝난 채로 하면 어긋났을 때 **원인 분리가 안 된다** — 0순위를 앞세운
  것과 같은 논리다.
- 두 축의 접점은 `toolLabel()` 정도로 작다.

목표: 도구가 삭제되면 모델 입력·도구함·능력 문서·tool schema에서 **함께** 사라지고, 추가되면
같은 기준으로 **함께** 나타난다.

핵심 원칙: **`ToolDescriptor`가 단일 진실이다. 나머지는 전부 파생물이다.**

작업:

1. `ToolDescriptor`에 `label`·`capabilityLine`·`schema`·`toolKind`·`needsApproval`·`availability`·
   `reversible`·`reversibleNote`를 모은다.
2. `toolCapabilityLine()`은 별도 맵이 아니라 descriptor/selfState에서 파생한다.
3. `toolSchemasFor(selfState)`는 executable descriptor의 `schema`만 노출한다.
4. 실행 불가능한 도구는 모델에게 노출하지 않는다(이미 `executable` 필터가 있다 — 1축은 생각보다 작다).
5. `CAPABILITIES.md` 파생 구역을 현재 selfState/descriptor에서 재생성한다.
6. 게이트가 **손 없는 선언·선언 없는 schema·설명만 남은 유령 도구**를 막는다.

완료 기준:

- descriptor에서 도구 제거 시 tool schema·도구함·capability line·CAPABILITIES 파생 구역에서 모두 사라짐
- 새 테스트 도구 descriptor + handler 추가 시 네 표면에 함께 나타남
- handler 없는 descriptor는 게이트 차단 / descriptor 없는 schema·문구는 게이트 차단
- 연결 안 된 도구는 "할 수 있다"가 아니라 "연결하면 가능/현재 막힘"으로 표시

---

## 2축 — TurnContext/Receipt 기반 현재 대화 상태 보존 (0순위 다음)

목표: 도구 결과가 산문 답변에 녹아 사라지지 않고, 다음 턴에서도 **1급 맥락**으로 쓰인다.

핵심 원칙: **receipt가 진실이다.** `workingState`는 별도 추정 저장소가 아니라 receipt에서
파생되는 **얇은 뷰**다(지금은 별도 저장소처럼 굴고 있다 — 교정 대상).

작업:

1. receipt에서 다음 턴에 필요한 최소 사실을 뽑는다: tool · target · source URL/title ·
   result summary · available next links · failure state · pending approval.
2. 이 사실을 다음 턴 TaskContext에 구조적으로 전달한다.
3. 긴 원문 전체를 모델 입력에 넣지 않는다.
4. 현재 대상·방금 읽은 자료·이어갈 링크·막힌 경로를 모델이 알게 한다.
5. **모델이 이번 턴에 실제로 무엇을 봤는지** 확인할 수 있는 최소 창구를 둔다.
   **검토 정정 ④(수용)**: 개발용 환경변수가 아니라 **턴 결과의 필드 하나**로 둔다. 압축 사실은
   어차피 2축이 만드는 물건이니 얹기만 하면 된다 — **엔진이 아니라 필드 하나다.** 나중에
   "무엇을 보고 그렇게 판단했나"(거버넌스·자가학습)가 공짜로 답해진다. 지금 그 기능을 만들자는
   게 아니라 **나중에 만들 자리를 막지 말자**는 것이다. 사용자 화면 노출은 없다.
6. `recentTurns`의 산문 답변만 믿지 않는다.

**검토 정정 ③(수용) — 대상을 잇는 것만큼 푸는 것이 중요하다.**
지시서는 잇기만 말했다. 현재 `updateWorkingState`는 앞에 쌓고 중복만 제거할 뿐 **아무것도 지우지
않는다.** 그래서 모델이 엉뚱한 페이지를 읽으면 그게 현재 대상으로 **고착되고**, 이후 모든 턴이
그 오염을 물려받는다. 절대원칙 §0: *틀린 사실을 주면 모델을 오염시킨다.*

완료 기준:

- 웹페이지를 읽은 뒤 "그 페이지 리뷰 봐줘"가 새 일반 검색으로 도망가지 않고 현재 대상에서 이어짐
- 파일을 읽은 뒤 "그거 정리해줘"가 방금 파일을 대상으로 이어짐
- 지난 대화를 찾은 뒤 "그 세션 내용 기준으로"가 검색 결과를 이어받음
- 막힌 도구는 다음 턴에서도 **막힌 이유와 다음 길**을 기억함
- **사용자가 새 대상을 말하면 이전 대상이 현재 대상 자리에서 물러난다**
- **사용자가 "아니 그거 말고"라고 하면 대상이 풀린다(고집하지 않는다)**
- **실패한 실행은 "못 했다"로만 남고, 그 결과 내용은 사실로 올라가지 않는다**
  (지금 코드는 지키고 있으나, 작업 1번이 `failure state`를 넘기라고 해서 어긋날 수 있는 자리다)
- 모델 입력이 과도하게 커지지 않음 —
  **검토 정정 ④(수용)**: 이대로는 판정 불가다. **작업 시작 시 라이브 프롬프트를 실측**하고, 그
  값에 여유를 얹은 상한을 시나리오 재생 게이트로 건다. 숫자를 미리 고르지 않는다(절대원칙 2).
- 시나리오 replay와 **실제 라이브 대화 둘 다** 통과

---

## 3축 — 채널 사실 전달 (작고 체감이 큼)

TaskContext에 **현재 응답 채널**이 사실로 들어가야 한다(실측: 지금 없다. 전사에는 `channel`을
저장하면서 프롬프트엔 안 보낸다 — 텔레그램 답장이 웹처럼 긴 마크다운으로 나온 원인).

작업:

- 현재 턴의 채널(웹 UI·텔레그램·슬랙)을 모델 입력에 사실로 전달한다.
- **정형 문구를 만들지 않는다.** 모델이 자연스럽게 조절하게 한다(§24).

완료 기준:

- 텔레그램 답장이 웹 UI용 긴 마크다운처럼 나오지 않음
- 웹 UI에서는 필요한 만큼 충분히 설명함
- 같은 내용이라도 채널에 맞게 자연스럽게 압축/확장됨

---

## 3축-b — 대화 품질: 답변 렌더링 (오너 추가 지시, 2026-07-27)

결함: 웹 UI가 assistant 답변을 `textContent` 로 그려 `#`, `*`, `**`, 코드 울타리가 화면에 그대로
찍혔다. 사람이 읽는 글이 아니라 **원문 소스**가 보였다.

Hermes 는 Streamdown 기반 CompactMarkdown + 스트리밍 블록 파서를 둔다. **그 구조를 복제하지 않는다** —
원리만 가져온다(§24.1): *스트리밍 미리보기와 최종 답변이 같은 렌더 경로를 쓴다.*

작업:

- 최소 안전 마크다운 렌더러(`src/surface/web/markdown.js`). 라이브러리·빌드 단계 없음(§17 의존성 0).
- 지원: 제목 · 굵게 · 기울임 · 코드(인라인·블록) · 목록 · 링크 · 인용 · 구분선. **표·이미지·HTML 통과는
  일부러 안 한다.**
- 안전 계약: ① 먼저 전부 escape, 그 뒤 우리가 만든 태그만 ② 링크는 http/https 만
  (javascript:·data: 는 글자로 남긴다) ③ 속성은 href·target·rel 뿐.
- **스트리밍 미리보기와 최종 답변이 같은 함수를 쓴다** — 갈라지면 흐르는 동안과 끝난 뒤 화면이 달라진다.
- 모델 출력 습관도 함께: 헌장 `<말>` 에 "제목·구분선은 문서를 만들 때만" 한 줄(정형 문구 금지, §24).

완료 기준:

- `#`, `**`, 코드블록, 목록, 링크가 글자가 아니라 서식으로 보인다
- 코드 블록 **안**의 `#`·`*` 는 서식이 되지 않는다(코드는 코드다)
- HTML injection 이 통과하지 않는다(수집한 페이지 내용이 답변에 섞여도 글자로 보인다)
- 스트리밍 중 안 닫힌 코드 울타리도 깨지지 않는다
- 짧은 답이 보고서처럼 꾸며지지 않는다

---

## 이번 작업에서 하지 말 것

- MCP·외부 도구 마켓 확장 / 스킬 자동 설치
- 새 대형 메모리 엔진 / 벡터DB 도입
- 모든 도구 결과 원문을 프롬프트에 넣기
- 안전 승인 경계 변경
- **정규식 규칙을 더 늘려서 때우기**
- 사용자 화면에 내부 디버그 정보 노출

## 최종 사용자 체감 기준

사용자는 내부 구조를 몰라도 된다. 다만 T5는 다음을 자연스럽게 해야 한다.

- 지금 할 수 있는 일을 **현재 상태 기준으로** 말한다.
- **없는 기능을 있다고 하지 않고, 되는 기능을 못 한다고 하지 않는다.**
- 방금 읽은 것·찾은 것·실행한 것을 다음 턴에서 이어받는다.
- 막혔을 때 "안 됩니다"가 아니라 **다음 가능한 길**을 제시한다.
- 채널에 맞게 답한다.
- 도구 사용·기억/맥락·자가학습이 **한 흐름**으로 이어진다.

## 핵심 완료 판정

이 작업은 코드 구조 정리가 아니라 **P2 체감 성능 작업**이다.
팔식당/리뷰 같은 **오너 실사용 멀티턴이 통과해야 한다.**

**"테스트 통과"만으로 완료 보고하지 않는다.**

---

## 실행 순서 (확정)

```text
0순위  세션 경합·원자적 저장      ← 데이터 유실은 되돌릴 수 없다
2축    TurnContext/Receipt 보존   ← 지금의 실패, 핵심 완료 판정
3축    채널 사실                  ← 작고 체감 큼
1축    도구 단일 진실화           ← 재발 방지, 광범위 리팩터라 마지막
```
