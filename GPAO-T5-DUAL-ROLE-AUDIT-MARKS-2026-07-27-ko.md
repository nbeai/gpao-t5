# 겸임 구현 감사 표식 — 2026-07-27

- 상태: `needs_independent_audit`
- 근거: `AGENTS.md` · Working-environment rules · **Dual-role marking (mandatory)**
- 표식자: Claude Code (본인 신고)

---

## 무엇을 표시하는가

아래 커밋 구간은 **Claude Code가 구현과 자체검증을 겸임**했다. 만든 손과 의심하는 눈이 같았다.
AGENTS.md는 이런 구간을 나중에 독립 감사자가 **찾아내 다시 볼 수 있게** 표시하라고 요구한다.

커밋 메시지에 `Dual-Role:` 트레일러를 넣었어야 했는데 **작업 당시 넣지 않았다.**
소급 삽입은 히스토리 재작성이라 오너가 금지했다(더 흔들지 말 것). 그래서 이 파일이 그 표식이다.

## 대상 구간

`1f58e28` … `06c7fe2` (Claude Code 커밋 10건. 중간의 `2d95eb8` 은 다른 작업자의 문서 커밋으로 대상 아님)

| 커밋 | 무엇을 건드렸나 |
|---|---|
| `1f58e28` | `sandbox.js` · `terminal-run.js` — probe 에 1회용 임시 자리 부여(heredoc 오판정) |
| `aa922ae` | 적대적 검사 보강(`;` 가 쓰기 실패를 묻던 약한 검사) |
| `a4d6cf8` | `local-locate.js` — 이름→자리 승계, 도구 스키마 문구 |
| `4de189d` | `local-locate.js` — 한글 이름 NFC 정규화 |
| `ce5957d` | `file-scope.js` · `recovery-ladder.js` · `turn.js` — 떠넘김 제거·다음 길 우선순위 |
| `a5aeccd` | 위 수정을 실제로 잡는 검사 보강 |
| `64a7634` | `turn.js` · `web/index.html` — 승인으로 멈출 때도 말한다 |
| `d0394b9` | `turn.js` — 막힌 손의 "네가 해라"가 턴의 다음 길이 되지 못하게 |
| `fd9ce1a` | `working-state.js` — 찾은 자리 승계(뒤에 `06c7fe2` 가 계약으로 대체) |
| `06c7fe2` | **구조 변경** — 이어받기를 `subjectOf` 계약으로. 커널이 도구 이름을 모른다 |

## 독립 감사자가 먼저 볼 것 (내가 스스로 의심하는 순서)

1. **`06c7fe2` 계약 전환** — 가장 넓게 퍼진 변경이다. 도구 6개 + `tool-runner` + 커널 + 데모
   fixture + 검사 헬퍼가 함께 움직였다. 계약을 안 낸 도구가 **조용히** 안 이어지는 것이
   설계상 맞지만, 그게 "있는 것을 없다고 말하는" 쪽으로 새는 자리가 없는지 봐야 한다.
2. **`d0394b9` 의 `시키는말` 정규식** — 우리 문구를 우리가 검열하는 장치다. 한글 활용형을
   놓치면 경계가 장식이 된다(실제로 `옮기` 만 넣었다가 "옮겨 주세요"가 샜다).
3. **`ce5957d` 의 우선순위 규칙** — "사다리가 실패 종류를 알아본 계단이면 도구 말보다 먼저"가
   `from !== 'blocked'` 라는 한 줄에 걸려 있다. 새 실패 종류가 늘 때 이 판단이 맞는지.
4. **`1f58e28` 의 임시 자리** — 경계를 넓힌 유일한 변경이다. 반대 검증으로 `$TMPDIR` 전체를
   열면 미끼 4건이 죽는 것은 확인했지만, 독립적으로 다시 봐야 한다.
5. **`4de189d` 정규화 위치** — 비교 시점에만 NFC 로 맞춘다. 저장·표시 경로에서 섞이지 않는지.

## 이 구간에서 내가 실제로 저지른 것 (감사자가 알아야 함)

- 반대 검증 중 **`git checkout` 으로 미커밋 작업을 두 번 날렸다.** 두 번 다 다시 넣었지만,
  그 사이 파일 상태가 흔들렸다. 저장소 기록상 같은 사고가 이제 4회다.
- 검사를 쓰고 나서 **반대 검증으로 그 검사가 약한 것을 두 번 발견했다**(`aa922ae`, `a5aeccd`).
  즉 이 구간의 검사는 "통과했다"만으로 신뢰하면 안 된다.
- 판정에 `node --test` 를 쓰고 출력을 파이프로 받았다(인수인계 §4 위반). 최종 숫자는
  `npm test` / `npm run gate` 로 다시 확인했지만, 중간 판단은 그 규칙 밖에서 내려졌다.

## 해제 조건

독립 감사자가 위 5개를 직접 실행 감사(문서 검토 아님)하고 통과시키면 이 표식을 해제한다.
