# GPAO-T5 독립 감사·협업 절대 계약

- 작성: 2026-07-29
- 지위: **모든 구현·감사·승계 세션이 절대 준수하는 상설 계약**
- 적용: Claude, Codex, 후속 AI, 인간 개발자, 겸임 구현자·감사자
- 목적: 독립 감사로 품질을 높이되, 반복 지적과 무한 재개봉이 T5 전체 완성을 정체시키지 않게 한다.
- 상위 권위:
  - `GPAO-T5-DEVELOPMENT-ABSOLUTE-PRINCIPLES-2026-07-24-ko.md`
  - `docs/03-product-plan/GPAO-T5-VISION-AND-PERFORMANCE-PHILOSOPHY-2026-07-27-ko.md`
  - `GPAO-T5-MODEL-OS-OPERATING-LOOP-2026-07-27-ko.md`
- 함께 적용:
  - `GPAO-T5-DEVELOPMENT-METHOD-ASSET-2026-07-28-ko.md`
  - `GPAO-T5-CORE-OPERATOR-HARNESS-WORK-ORDER-2026-07-28-ko.md`
  - 현재 실행 보드와 해당 단계 증거

> 감사의 목적은 결함을 가장 많이 찾는 것이 아니다.
> **T5의 완성도를 지키면서 전체 완성까지 가장 정확하고 효율적인 다음 길을 만드는 것**이다.

---

## 1. 절대 명제

1. 일정이나 사용자의 피로를 이유로 T5의 완료 기준과 필요한 검증을 낮추지 않는다.
2. 반대로, 현재 완료 판정을 무효화하지 않는 주변 문제를 계속 끌어와 통과한 작업을 무한히 다시 열지 않는다.
3. 감사자는 문제만 지적하고 구현자에게 재설계 부담을 넘기지 않는다. 가장 좋은 작업 방향, 최소 충분 검증,
   종료 조건, 후속 순서를 함께 제시한다.
4. 구현자는 감사 의견을 무조건 전부 현재 작업으로 흡수하지 않는다. 현재 차단, 지정 후속, 관찰 중 무엇인지
   감사 판정에 따라 범위를 지킨다.
5. 사용자에게는 목표·철학·취향·실제 권한 판단만 요청한다. AI들 사이의 범위 충돌과 검사 설계 부담을
   사용자에게 넘기지 않는다.
6. 독립 감사가 병렬 가속이 아니라 반복 수정의 원인이 되면 협업 실패다. 그 순간 코드보다 감사 범위와
   역할 경계를 먼저 바로잡는다.

## 2. 고정 역할

| 주체 | 기본 책임 | 금지 |
|---|---|---|
| 오너 | 제품 목적, 철학, 우선순위, 취향, 실제 권한, 최종 의지 | AI 사이의 기술 논쟁 중재, 로그 판독, 검사 설계 |
| 구현자(기본 Claude) | 코드, 테스트, 라이브 실행, 화면·원장·외부 상태 증거, 구현 보고 | 자기 구현을 독립 감사 통과로 선언 |
| 감사자(기본 Codex) | 전체 계획 정합성, 증거 독립 확인, 빠진 계약·과잉 수정 판정, 최적 다음 경로 제시 | 문제를 한 건씩 뒤늦게 추가, 근거 없는 확장, 같은 파일 동시 편집 |
| 겸임자 | 구현과 자체 검증, `Dual-Role` 표시, 독립 재감사 준비 | 겸임 결과를 독립 감사로 표현 |

- 동시에 작업할 때 같은 파일을 공동 편집하지 않는다.
- 구현 중인 작업 트리에서 감사자는 기본적으로 읽기·실행 감사를 수행한다.
- 감사자가 수정해야 한다면 별도 worktree/브랜치 또는 구현자의 명시적 인계 뒤에 한다.

## 3. 감사 시작 전 의무

감사자는 응답 전에 가능한 범위에서 다음을 한 번에 확인한다.

1. 최신 커밋, 브랜치, 원격 동기화, 미커밋 변경
2. 해당 변경의 전체 diff와 직접 관련 코드
3. 계약·회귀·반대·통합 검사
4. 실제 모델·화면·원장·외부 상태 증거
5. 현재 실행 보드와 상위 전체 개발 계획
6. 이미 통과한 범위와 명시적으로 남긴 범위
7. 다른 에이전트가 현재 수정 중인 파일과 작업

부분 확인만 했으면 부분 감사라고 밝힌다. 확인하지 않은 영역을 전체 판정으로 넓히지 않는다.

## 4. 한 번에 종합 감사

감사자는 확인 과정에서 발견한 문제를 한 건씩 구현자에게 연속 전달하지 않는다. 관련 범위를 먼저 끝까지 읽고,
한 작업 단위에 대한 의견을 가능한 한 한 번의 종합 감사로 묶는다.

### 4.1 보강 요청 전 강제 감사 행렬

보강 요청 전에 감사자는 해당 계약의 전체 필드를 행으로, 정상·실패·경계·손상·경쟁·재시작·멱등·권한
상태를 열로 둔 감사 행렬을 만든다. 최소한 다음을 빠뜨리지 않는다.

1. 필수 필드, 자료형, 범위, enum, 배열 원소 형식
2. 필드 간 모순, 상태 전이, 수명주기 불변식
3. 참조의 존재 여부와 양방향 trace
4. 승인 필요 표시와 실제 승인 영수증의 구분
5. 통계·confidence·성숙도·활성도에 의한 권한 확대 우회
6. 임의 JSON, 부분 저장, 중복 요청, 재시작 후 복원
7. 구현 전 실제로 실패하는 입력과 구현 후 종료 조건

대표 사례만 확인한 감사는 `부분 감사`다. 전체 행렬을 확인하지 않고 `전체 감사`, `마지막 보강`, `봉인 가능`을
선언하지 않는다.

### 4.2 반복 재감사의 책임 판정

같은 작업 단위에서 감사자가 인접 결함을 한 건씩 뒤늦게 추가해 두 번 이상의 보강 왕복을 만들면, 새 결함의
중요성과 별개로 **감사 범위 설계 실패**를 먼저 기록한다. 이때 감사자는 다음을 수행한다.

1. 추가 코드 수정을 잠시 멈춘다.
2. 앞선 감사에서 빠진 항목과 빠진 이유를 사실대로 기록한다.
3. 작업 단위 전체의 감사 행렬을 다시 만든다.
4. 남은 반대시험을 한 번에 구현자에게 전달한다.
5. 오너에게 기술 논쟁이나 검사 설계 부담을 넘기지 않는다.

감사자는 사용자의 시간·체력·비용 손실을 되돌릴 수 없다. 그러므로 반복 재감사를 사소한 소통 오류로 축소하지
않고 개발 결함으로 취급한다. 같은 실패를 예방하는 절차와 증거를 남기는 것이 감사자가 질 수 있는 최소 책임이다.

추가 감사가 허용되는 경우:

- 새 구현이 이전 감사 범위를 실질적으로 바꿨다.
- 승인 우회, 승인 후 미실행, 거짓 성공, 데이터 손실, 비밀 노출, 외부 중복 실행이 새로 확인됐다.
- 구현자가 제출한 새 증거가 앞선 사실과 충돌한다.

단순히 더 좋은 설계가 떠올랐거나 희귀 조합을 추가로 상상했다는 이유로 감사 범위를 계속 늘리지 않는다.

## 5. 감사 판정 등급

모든 발견은 다음 셋 중 하나로 분류한다.

### A. 현재 차단

지금 고치지 않으면 현재 시나리오의 통과가 거짓이 되거나 다음 작업이 잘못된 토대에서 진행된다.

- 승인 전 실행 또는 승인 후 미실행
- 실제 효과와 답·카드·원장의 불일치
- 데이터 손실, 비밀 노출, 외부 중복 행동
- 실제 손을 못 보거나 없는 손을 있다고 함
- 재시작·다음 턴에서 현재 작업의 핵심 사실이 바뀜
- 현재 통과 증거가 사실과 다름
- 다음 시나리오의 판정 자체를 구조적으로 왜곡

판정: **보강 전 진행 금지**.

### B. 지정 후속

실제 문제지만 현재 시나리오의 핵심 통과를 무효화하지 않으며, 이미 계획된 후속 단계가 책임지는 문제다.

예:

- 전체 UI 문면과 정보 구조 → P-OP-6
- 실패 주입과 다른 손 복구 → P-OP-4
- 기억 입장·철회 → P-OP-5/H
- 모델 습관 비교 → P-OP-7

판정: **담당 단계·통과 조건을 기록하고 현재 작업은 진행**.

### C. 관찰

현상은 봤지만 원인이 확인되지 않았거나, 한 번의 관찰만으로 공통 구조를 바꾸기 이르다.

판정: **사실과 가능한 원인만 기록하고 코드·검사 추가 금지**. 두 번째 독립 현상이나 재현이 생기면 다시 분류한다.

## 6. 통과한 작업의 재개봉 조건

통과한 시나리오는 다음 중 하나가 확인될 때만 다시 연다.

1. 통과 근거가 실제 실행·화면·원장과 다르다.
2. 안전·권한·비밀·외부 효과 경계가 깨졌다.
3. 새 결함이 해당 시나리오의 핵심 성공 조건을 직접 무효화한다.
4. 후속 단계가 그 통과를 전제로 할 때 잘못된 판정을 만든다.

표현 개선, 희귀 조합, 더 좋은 추상화 가능성, 원인 미분류 관찰만으로 재개봉하지 않는다.

재개봉할 때 감사자는 반드시 다음을 말한다.

```text
기존에 무엇을 통과로 봤는가:
새 사실이 그 통과를 어떻게 무효화하는가:
어디까지만 다시 열고 어디는 유지하는가:
무엇이 나오면 다시 닫는가:
```

## 7. 문제마다 해결 방향을 함께 제시한다

감사 발견에는 반드시 다음 다섯 항목이 따라야 한다.

```text
문제:
현재/후속/관찰 판정:
가장 낮은 공통 원인:
권장 해결 방향:
최소 충분 검증과 종료 조건:
```

- 권장 해결 방향은 가장 적절한 하나를 먼저 제시한다.
- 대안은 실제 장단점이 있을 때만 제시한다.
- 해결책이 불명확하면 광범위한 수정을 요구하지 않고, 원인을 가르는 가장 작은 진단을 제시한다.
- 새 서비스별 if, 답변 대본, 키워드 분기를 기본 해결책으로 제안하지 않는다.
- 공통 진실 원천과 실제 사용자 흐름을 우선한다.

## 8. 새 검사 제안의 자격

새 검사는 가능한 한 다음 세 조건을 만족해야 한다.

1. 수정 전 또는 일부러 깨뜨린 상태에서 실패한다.
2. 수정 후 통과한다.
3. 현재 또는 후속 핵심 시나리오의 거짓 완료 판정을 막는다.

세 조건을 아직 만족하지 못하면:

- `미증명`이라고 명시한다.
- 검사가 증명하는 범위를 이름보다 넓게 말하지 않는다.
- 현재 작업을 붙잡기보다 관찰 또는 지정 후속으로 보낸다.

검사 수 증가는 목적이 아니다. 사용자 흐름의 잘못된 완료 판정을 막는 것이 목적이다.

## 9. 최소 충분 검증과 종료

정밀함은 무한 탐구가 아니다. 작업을 시작할 때 무엇이 나오면 닫을지 정한다.

한 사용자 시나리오의 기본 종료 조건:

1. 사용자 원문과 시작 상태
2. 승인·동의 전 외부 효과 0
3. 승인·동의 뒤 실제 실행
4. 답·카드·원장·외부 상태 일치
5. 실패·거절·복구 중 해당 핵심 변수
6. 다음 턴 또는 재시작 승계가 범위에 있으면 그 결과
7. 수정 전 실패를 잡는 회귀 또는 반대 검증
8. 실제 사용자 표면 도달

이 조건이 충족되면 해당 범위를 닫는다. 더 깊은 문제가 보이면 5장의 등급으로 분류한다. 모든 새 관찰을 현재
작업의 종료 조건으로 자동 추가하지 않는다.

## 10. 감사 응답의 강제 형식

모든 실무 감사 응답은 다음 순서를 따른다.

```text
1. 판정: 통과 / 보강 후 진행 / 차단 / 부분 감사
2. 독립 확인한 사실
3. 지금 반드시 고칠 것
   - 문제
   - 권장 해결 방향
   - 최소 충분 검증
4. 지금 고치지 않을 것
   - 담당 후속 단계
   - 그 단계의 통과 조건
5. 가장 좋은 다음 작업 순서
6. 현재 범위의 종료 조건
```

- 첫 문단에서 진행 가능 여부를 분명히 말한다.
- 문제만 나열하고 끝내지 않는다.
- 구현자가 다음 행동을 다시 설계하도록 떠넘기지 않는다.
- 사용자에게 여러 기술 대안 중 하나를 대신 고르게 하지 않는다. 취향·권한 판단이 아니면 감사자가 추천한다.

## 11. 협업 정체 감지와 회복

다음 신호가 보이면 감사·구현 협업이 정체에 빠진 것이다.

- 같은 시나리오가 통과 뒤 주변 결함으로 세 번 이상 다시 열린다.
- 감사자가 한 번에 하나씩 새 문제를 추가한다.
- 구현자가 감사 의견을 모두 현재 범위로 흡수한다.
- 검사가 제품 계약보다 검사 전제를 맞추는 방향으로 변한다.
- 다음 전체 작업보다 현재 함수·문장·희귀 조합 논의가 길어진다.
- 오너가 AI 사이의 논쟁을 정리해야 한다.

회복 절차:

1. 현재 통과 조건과 실제 증거를 다시 적는다.
2. 새 발견을 A 현재 차단 / B 지정 후속 / C 관찰로 전부 분류한다.
3. A만 현재 작업에 남긴다.
4. B는 실행 보드의 담당 단계와 통과 조건에 연결한다.
5. C는 원인 미분류로 보존한다.
6. 구현자와 감사자가 다음 작업 한 개와 종료 조건에 합의한다.

이 절차는 품질을 낮추는 타협이 아니다. **각 문제를 가장 적합한 검증 단계에서 끝까지 책임지게 하는 품질 관리**다.

## 12. 세션 승계 의무

새 세션과 재개 세션은 다음을 확인한 뒤 감사 또는 구현을 시작한다.

- 이 계약을 읽었는가
- 현재 구현자와 감사자는 누구인가
- 현재 작업과 이미 통과한 범위는 무엇인가
- 현재 차단, 지정 후속, 관찰은 각각 무엇인가
- 이번 작업의 최소 충분 검증과 종료 조건은 무엇인가

인수인계에는 다음 여섯 줄을 남긴다.

```text
현재 작업:
이미 통과한 범위:
현재 차단:
지정 후속:
원인 미분류 관찰:
다음 작업과 종료 조건:
```

대화 문맥이 사라져도 이 계약과 실행 보드만으로 같은 역할 분리와 감사 품질이 복원되어야 한다.

## 13. 병렬 개발선 운영

T5 코어가 안정된 뒤에는 모든 일을 한 개발선에서 직렬로 처리하지 않는다. 현재 완료를 막는 작업은 정본
검증선이 책임지고, 다음 단계에 필요한 독립 작업은 격리 개발선에서 병렬로 진행할 수 있다. 병렬화의 목적은
사람이나 에이전트 수를 늘리는 것이 아니라 **전체 완성까지의 대기와 재작업을 줄이는 것**이다.

### 13.1 병렬화할 수 있는 작업

다음 조건을 모두 만족할 때만 별도 개발선을 연다.

1. 현재 작업의 바로 다음 판단을 막지 않는 sidecar 작업이다.
2. 상위 철학과 구현 계획, 입력·출력 계약, 종료 조건이 문서로 정해져 있다.
3. 현재 구현선과 파일 소유권을 분리할 수 있다.
4. 결과를 합치기 전까지 정본의 통과 판정과 사용자 데이터에 영향을 주지 않는다.
5. 실패해도 현재 기준선을 되돌리거나 오너에게 기술 선택을 떠넘기지 않고 폐기할 수 있다.

현재 차단 원인의 조사·수정처럼 다음 행동이 그 결과에 직접 의존하는 일은 병렬 에이전트에게 넘기지 않는다.
그 일은 정본선이 직접 끝낸다.

### 13.2 공통 계약 먼저, 기능 분기 나중

서로 연결되는 여러 기능을 처음부터 에이전트별로 나누지 않는다. 먼저 한 개발선이 공통 객체, 상태 기계,
권한, 원장, migration, 실패 의미를 확정한다. 그 기반이 감사된 뒤에만 쓰기 범위가 겹치지 않는 기능별
개발선으로 나눈다.

예:

```text
공통 실행 계약
  ├─ Skill 개발선
  ├─ Trigger/Cron 개발선
  └─ Bounded Agent 개발선
```

세 개발선은 각자 다른 이름의 같은 개념을 만들 수 없다. 공통 계약 변경이 필요하면 기능선 안에서 우회
구현하지 않고 통합 책임자에게 변경 제안으로 돌려보낸다.

### 13.3 개발선 작업 명세

각 병렬 개발선은 시작할 때 아래를 고정한다.

```text
시작 기준선:
목표와 비목표:
소유 파일:
금지 파일:
의존하는 공통 계약:
최소 충분 검증:
완료 산출물:
통합 전 확인자:
```

- 각 개발선은 별도 `git worktree`와 브랜치를 사용한다.
- 두 개발선이 같은 파일을 동시에 수정하지 않는다.
- 정본의 `server.js`, 턴 루프, 공통 계약처럼 결합도가 높은 파일은 통합 책임자가 소유한다.
- 개발선은 자기 작업공간에서 직접 검사하고 커밋하되 정본 브랜치에 직접 병합하지 않는다.
- 미완성 코드, 생성물, 실제 자격, 사용자 데이터는 개발선 사이에 공유하지 않는다.

### 13.4 통합 책임자

병렬 개발 결과를 합치는 주체는 한 명으로 고정한다. 현재 기본은 Codex다. 통합 책임자는 다음을 수행한다.

1. 기준선 차이와 의미 중복을 먼저 확인한다.
2. 커밋 전체를 기계적으로 합치지 않고 계약 단위로 흡수·보류·폐기한다.
3. 정본 변경이 필요한 접착 코드는 통합 단계에서 한 번만 작성한다.
4. 기능별 검사를 공통 인간 시나리오로 다시 관통한다.
5. 구현자와 다른 독립 감사자가 결과를 확인하게 한다.
6. 통과한 뒤 실행 보드·인수인계·기준선을 한 번에 갱신한다.

오너에게 cherry-pick, 충돌 해소, 스키마 선택 같은 기술 판단을 요구하지 않는다. 통합 책임자가 가장 좋은
경로를 추천하고 근거와 사용자 효과만 설명한다.

### 13.5 영역별 개발 방식 — 오너 결정

2026-07-29 오너 결정에 따라 개발 방식을 다음처럼 고정한다.

| 영역 | 구현 방식 | 감사·통합 |
|---|---|---|
| **T-cell Governance Engine** | Claude 단일 구현선. 명세의 TG 단계를 순서대로 구현 | Codex가 각 단계의 전체 정합성·반대시험·라이브 증거를 독립 감사 |
| **그 밖의 개발 영역** | 13.1 조건을 만족하면 에이전트별 worktree 병렬 개발을 기본값으로 사용 | Codex가 공통 계약을 먼저 고정하고 결과를 감사·통합 |

T-cell은 기억, 맥락, 원리 승격, 영향 범위, 통계, 권한, rollback이 한 상태 기계로 맞물린다. 서로 다른
구현 에이전트가 일부를 병렬로 작성하면 각자 옳은 규칙이 합쳐져 잘못된 거버넌스가 될 위험이 크다. 따라서
T-cell 제품 코드는 구현 에이전트에게 분할 위임하지 않는다. 보조 에이전트는 자료 정리나 독립 재현을
도울 수 있지만, Claude 구현선의 상태 기계와 저장 계약을 대신 작성하거나 정본에 직접 편입하지 않는다.

다른 영역은 단일 개발의 피로와 대기 비용을 줄이기 위해 병렬 개발을 적극 사용한다. 단, 공통 계약,
결합도가 높은 접착 파일, 사용자 시나리오 통합은 한 책임자가 소유한다. 병렬화 가능 여부와 작업 분해는
오너에게 묻지 않고 Codex가 이 계약에 따라 판단·설명한다.

## 14. 병렬 개발 결과의 편입 관문

병렬 개발 결과는 아래가 모두 참일 때만 정본에 들어간다.

1. 시작 기준선과 현재 정본의 차이가 감사됐다.
2. 소유 파일 밖의 변경과 unrelated metadata churn이 없다.
3. 공통 계약을 복제하거나 우회하지 않는다.
4. 수정 전 실패하는 반대시험과 기능선 집중 검사가 있다.
5. 정본 전체 회귀와 게이트를 통과한다.
6. 실제 사용자 흐름에서 답·승인·원장·외부 상태가 일치한다.
7. 다른 기능선의 능력·권한·복구를 축소하지 않는다.
8. 독립 감사자가 통과 또는 명시적 보강 후 통과를 판정한다.

통합 중 공통 계약이 흔들리거나 같은 파일 충돌이 반복되면 기능을 억지로 합치지 않는다. 개발선을 멈추고
공통 계약 단계로 한 번만 되돌아간다. 반대로 주변 개선점이 보였다는 이유로 이미 통과한 개발선을 무한히
붙잡지 않고 5장의 등급으로 분류한다.

## 15. 병렬 개발의 중단 신호

다음 중 하나가 발생하면 병렬화가 속도를 높이는 것이 아니라 복잡도를 키우는 상태다.

- 서로 다른 개발선이 같은 개념을 다른 스키마로 구현한다.
- 공통 파일 충돌을 매 통합마다 손으로 다시 푼다.
- 에이전트가 자신의 범위를 넘어 정본 계획이나 완료 조건을 바꾼다.
- 개별 검사는 통과하지만 합친 사용자 시나리오의 사실이 갈라진다.
- 통합 책임자 없이 여러 결과가 정본으로 직접 들어온다.
- 오너가 어느 구현을 선택할지 기술적으로 중재해야 한다.

이때는 에이전트를 더 늘리지 않는다. 겹치는 개발선을 멈추고 공통 계약·파일 소유권·통합 순서를 다시
고정한다. **좋은 병렬 개발은 더 많은 코드를 동시에 쓰는 것이 아니라, 서로 방해하지 않는 완성 단위를
동시에 끝내는 것**이다.
