# GPAO-T5 구조 개발 원칙 — 나비·프랙탈·매듭

- 날짜: 2026-07-28
- 상태: `CURRENT`(오너의 반복 적용 지시로 확정). `GPAO-T5-DEVELOPMENT-ABSOLUTE-PRINCIPLES-2026-07-24-ko.md`의 하위 정본이다.
- 강제 수단: `scripts/gate.mjs` ③-b2. 이 문서의 §2-C 를 이 문서 자신이 먼저 지킨 뒤에 올렸다.
- 적용 범위: GPAO-T5 의 모든 수정·보강·신규 개발
- 관계: 최상위 제품 원칙이 **무엇을 만드는가**를 정한다면, 이 문서는 **어떻게 고치는가**를 정한다.
- 목적: 문제 하나마다 패치를 붙이는 개발을 막는다. 그리고 원칙이 문서로만 남아 사라지는 것을 막는다.

---

## 0. 오너 원문 (2026-07-28)

아래는 오너가 제시한 원문이다. 요약·재작성하지 않는다. 이 문서의 나머지는 전부 이 원문의
해석과 운용 지침일 뿐이며, 원문보다 우선하지 않는다.

> **티파이브의 개발/수정/보강 철학.**
>
> 아래의 이론을 적용한 개발/수정/보강 철학을 지향한다.
>
> **나비효과** — 작은 현실 오류 하나가 모델 판단 전체를 틀게 만든다. 예: 후보 폴더 하나를
> 조용히 고르는 문제는 작은 UX 문제가 아니라, 이후 파일 변경·승인·기억·다음 턴까지
> 오염시키는 시작점이 될 수 있어. 그래서 작은 사실 불일치를 가볍게 보면 안 된다.
>
> **프랙탈** — 한 턴의 구조가 전체 OS 구조와 닮아야 한다. 사용자 말 → 현실 확인 → 모델 판단 →
> 실행/승인 → 원장 → 다음 턴. 이 구조가 로컬 파일, 외부 연결, 자동화, 기억, 채널, 스킬
> 어디서든 반복되어야 해. 기능마다 다른 방식으로 해결하면 티파이브는 커질수록 약해진다.
>
> **매듭이론** — 기능을 많이 붙이는 것보다, 어떻게 얽히게 하느냐가 핵심이다. 외부 연결, 승인,
> 파일, 기억, 자동화, 채널이 따로 놀면 복잡도만 늘어난다. 반대로 "후보는 후보, 승인 전 실행
> 없음, 실제 한 일만 원장, 다음 턴은 원장 기반" 같은 공통 매듭이 있으면 여러 기능이 한 번에
> 정리된다.
>
> 그래서 개발 원칙은 이거야.
> 티파이브는 문제 하나마다 패치를 붙이는 방식으로 개발하지 않는다. 작은 결함이 전체 판단을
> 바꾸는 지점을 찾고, 한 턴의 해결 구조가 전체 제품 구조와 닮게 만들며, 기능들을 공통 운영
> 매듭으로 묶어 하나의 구조가 여러 현상을 해결하게 한다.

---

## 1. 판정 기준 — 세 축을 질문 하나씩으로

원칙은 훈계로는 안 지켜진다. 코드를 만졌을 때 **답할 수 있는 질문**이어야 한다.

| 축 | 물어볼 것 | 답이 "예"면 |
|---|---|---|
| 나비 | 이 불일치가 **다음 턴의 입력**이 되는가 | 작아 보여도 크다. 지금 고친다 |
| 프랙탈 | 같은 사실을 **두 층이 따로** 계산하는가 | 이미 결함이다. 아는 층으로 내린다 |
| 매듭 | 같은 병이 **두 번째 현상**에서도 나왔는가 | 그때 매듭으로 올린다 |

### 1-A. 나비 — 무엇이 "작은가"

작다·크다를 감으로 정하지 않는다. **다음 턴의 입력이 되는가**로 가른다.

- 원장에 남는 문장 → 다음 턴 맥락이 된다 → **크다**
- 승인 카드 문구 → 사용자의 결정을 바꾼다 → **크다**
- 도구 id → 손을 고르는 열쇠다 → **크다**
- 서버 로그 형식 → 모델도 사용자도 안 읽는다 → 작다

실측(2026-07-28): `mcp.undefined.ask_question` 은 로그 미관 문제로 보였다. 이 질문을 던져
들여다보니 **id 충돌**이었다 — 주소로 붙은 서비스가 둘이면 나중 것이 앞의 손을 조용히
덮어쓴다. 사용자가 A 를 불렀는데 B 가 도는 상황이었다.

### 1-B. 프랙탈 — 검사 가능한 문장으로

"구조가 닮아야 한다"는 그대로는 검사할 수 없다. 검사할 수 있는 형태는 이것이다.

> **같은 질문의 답이 두 층에서 따로 계산되면 그건 이미 결함이다.**
> 그리고 그때는 대개 **덜 아는 쪽이 이긴다.**

실측(2026-07-28, 같은 날 다섯 번):

| 무엇 | 아는 층 | 덮어쓴 층 | 사용자가 본 것 |
|---|---|---|---|
| 되돌릴 수 있나 | 도구 `previewOf().cancel` | 도구 전체 `reversibleNote` | 없던 파일에 "원본은 휴지통에 남아요" |
| 무엇을 거절했나 | 도구가 카드에 쓴 말 | 커널의 고정 문구 | 파일 저장에 "보내지 않았어요" |
| 무슨 종류의 일인가 | 도구 `toolKind` | 계획 층의 `kind = 'send'` | 조회에 "메시지를 밖으로 보내는 일이라" |
| 어디에 붙었나 | 커넥터 라벨 | MCP 서버 이름 | `undefined 서버에서` · `notion 에서` |
| 결과가 무엇인가 | 사람 말 자리 | MCP 가 준 JSON | 원장에 `{"results":[],...}` |

다섯 건 모두 **문구를 고치면 사라지는 것처럼 보였다.** 문구를 고치면 다음 도구에서 또 난다.

### 1-C. 매듭 — 언제 올릴 것인가

매듭은 강력한 만큼 **너무 일찍 만들면 추측**이 된다. 현상이 하나뿐일 때 구조를 먼저 지으면
그건 매듭이 아니라 설계 취향이다.

> **매듭은 사후에 발견되는 것이지 사전에 설계되는 것이 아니다.**
> 두 번째 현상에서 확인되기 전에는 그냥 고친다.

실측(2026-07-28): 되돌리기 결함 하나만 봤을 때는 `local-file.js` 문제로 보였다. 거절 문구까지
보고 나서야 "위층 고정 문구가 아래층 사실을 덮는다"는 매듭이 보였다. 첫 번째에서 매듭을
주장했다면 엉뚱한 구조를 지었을 것이다.

---

## 2. 현실에서 확인된 한계 — 단서 조항

세 축은 오늘 실제로 작동했다. 동시에 세 가지 한계가 드러났다. 원칙에 이 단서가 없으면
같은 원칙이 반대 방향으로 해를 끼친다.

### 2-A. 철학은 결함을 **찾지 않는다**

원칙은 "찾은 뒤 어디를 고칠지"에 강했고, "무엇이 잘못됐는지"에는 아무 말도 하지 않았다.

실측: 오늘 가장 큰 두 결함 — 승인 카드가 네 번 뜨고 답이 안 나온 것, 낯선 서비스에서 일을
사용자에게 떠넘긴 것 — 은 원칙에서 예측된 것이 아니라 **웹 화면과 원장에서 나왔다.**
그리고 화면 문장만 봤을 때는 "MCP 로 조회했다"고 판단했는데, 원장을 보니 `web.collect` 였다.

> **이 철학은 라이브 검증과 짝일 때만 값이 있다.**
> 화면에서 돌리고, 원장을 읽고, 그 다음에 이 문서를 편다. 순서를 바꾸면 예쁜 구조를
> 지어 놓고 사용자 문장은 그대로 끝난다.

### 2-B. 매듭 사고는 **없는 연결을 만들어 보이게** 한다

모든 것을 하나로 묶어 보는 시선은 실제로 안 묶인 것도 묶어 본다.

실측: "정규식 제거와 실행기 배선은 하나의 매듭"이라고 논리적으로 강하게 주장했다.
코드를 보니 둘은 서로 다른 경로였고 — 정규식은 개인 도구 등록에만 걸렸고 새 커넥터 선언은
모델이 도구 목록에서 직접 골랐다 — 주장이 틀렸다.

> **매듭은 코드로 확인한 뒤에 주장한다.** 논리로 먼저 주장하면 없는 결합을 만들어 낸다.
> 확인 방법은 하나다: 두 자리가 실제로 같은 함수·같은 사실을 지나는지 읽는다.

### 2-C. 원칙은 **게이트로만** 살아남는다

문서에 적힌 원칙은 다음 사람이 안 읽으면 없는 것과 같다. 회귀 테스트도 **그 자리만** 지킨다.
새로 생기는 코드에는 적용되지 않는다.

실측: 2026-07-28 하루에 회귀 테스트 31 건을 붙였지만(1038 → 1069) **게이트 계약은 한 줄도
늘리지 않았다.** 오늘 다섯 번 만난 병은 내일 새 파일에서 다시 날 수 있다.

> **새 매듭이 두 번째 현상에서 확인되면, 같은 날 `scripts/gate.mjs` 에 검사 한 줄을 늘린다.**
> 테스트는 그 자리를, 게이트는 그 종류를 지킨다. 둘은 서로를 대신하지 못한다.

### 2-D. **문서는 코드의 또 다른 층이다 — 그리고 항상 덜 아는 쪽이다** (2026-08-06 추가)

§1-B 가 프랙탈을 검사 가능한 문장으로 이렇게 적었다:

> **같은 질문의 답이 두 층에서 따로 계산되면 그건 이미 결함이다. 그리고 그때는 대개 덜 아는 쪽이 이긴다.**

**그 규칙이 문서에도 그대로 걸린다.** 문서가 "지금 무엇이 되는가"를 주장하면 그것은 코드와
같은 질문에 답하는 **두 번째 층**이다. 그리고 문서는 스스로 갱신되지 않으므로 **언제나 덜 아는
쪽**이고, 사람은 코드보다 문서를 먼저 읽으므로 **덜 아는 쪽이 이긴다.**

실측(2026-08-06 하루, 네 건 · 세 사람이 각각 걸렸다):

| 문서가 적은 것 | 실제 | 그래서 무슨 일이 났나 |
|---|---|---|
| 화면 기록 권한 `denied` | `granted` | 없는 벽 앞에서 CU 계획이 멈춰 있었다 |
| "기본값이 홈 전체가 되지 않는다" | 홈 전체가 강제 경계 | 읽은 사람이 **T5 가 거짓말한다고 오판**했다(F-46) |
| "임의 명령 실행은 계속 유보" | 그런 유보가 코드에 없다 | 터미널이 반쪽인 줄 알고 아무도 안 열었다 |
| "3층은 약관이 먼저" | 막아야 할 것은 층이 아니라 **쥐는 것** | 사장님이 자기 화면 보는 것까지 같이 막혔다 |

넷 다 **코드가 문서를 어긴 게 아니다.** 코드가 옳게 움직였고 문서만 안 따라왔다. 그런데도
개발이 멈췄다 — 잘못 도는 것보다 **잘못된 지도를 보고 아무도 안 가는 길**이 더 비쌌다.

> **낡은 문서는 안 고친 것보다 나쁘다.** 손댄 흔적이 없으니 최신으로 읽힌다.
> 이 규칙은 인수인계 문서가 진행 상태에 대해 이미 적어 둔 것이고, **모든 문서로 넓힌다.**

#### 그래서 지킬 것 셋

**① 변하는 사실은 문서에 안 적는다.** 권한 상태·손 목록·범위·유보 여부·진행 상태는 코드에서
파생하거나 아예 안 적는다. 선례가 이미 있다 — `CAPABILITIES.md` 는 레지스트리에서 매번 다시
만든다. 그 방식이 맞았고, **안 쓴 자리에서 위 넷이 터졌다.**

**② 문서에 남기는 것은 변하면 안 되는 것뿐이다.** 오너가 정한 것(헌장·정본·절대원칙)은 코드가
따라가야 하므로 낡지 않는다. 우리가 적은 것(계획·주석·설계 메모)은 사실이 바뀌면 **문서를
고친다.** 권위 등급(A~E)이 이 선을 이미 그어 놨다 — 안 쓰이고 있었을 뿐이다.

**③ 그래도 적어야 하면 재는 법을 옆에 적는다.** *"화면 기록 권한 denied"* 옆에 그것을 재는
명령 한 줄이 있었으면 셋 다 몇 초 만에 알았다. 없어서 셋 다 믿고 멈췄다.

#### 판정 질문

기존 셋에 하나를 더한다.

| 축 | 물어볼 것 | 답이 "예"면 |
|---|---|---|
| **문서** | 내가 지금 쓰는 이 문장이 **다음 달에도 참인가** | 아니면 적지 말고 파생하거나, 재는 법을 옆에 단다 |

§2-C 가 요구한 대로, 이 원칙도 **두 번째 현상에서 확인되면 게이트로 내린다.** 지금은 하루에
네 건이 한꺼번에 나온 첫 확인이라 규율로 먼저 세운다 — 그리고 넷을 고치는 것부터 한다
(원칙을 적고 대상을 안 고치면 그 자체가 이 항목의 위반이다).

---

## 3. 작업 순서

1. **화면에서 돌린다.** 실제 사용자 문장으로. 테스트로 대신하지 않는다.
2. **원장을 읽는다.** 화면 문장은 무엇이 실행됐는지 말해 주지 않는다.
3. **나비 질문**을 던진다 — 이게 다음 턴의 입력이 되는가.
4. **프랙탈 질문**을 던진다 — 같은 사실을 두 층이 계산하는가. 그러면 아는 층으로 내린다.
5. **두 번째 현상인지** 본다. 맞으면 매듭으로 올리고, **코드로 확인한 뒤** 주장한다.
6. 매듭을 올렸으면 **게이트에 한 줄**을 늘린다.
7. 다시 화면에서 돌려 **사용자 문장 하나가 다르게 끝나는지** 확인한다.

7 번이 완료 기준이다. "도구 하나를 만들었는가"는 기준이 아니다.

---

## 4. 이미 게이트가 지키는 매듭

새 매듭을 만들기 전에 여기부터 본다. 이미 있는 매듭에 안 묶인 자리를 찾는 것이 먼저다.

- 커널은 도구 이름을 모른다 — `subjectOf` 계약
- 승인이 필요한 도구는 모두 `previewOf` 를 낸다
- 연결 실행기·상태 해석층에 서비스 이름 0 건
- 도구 이름·설명은 descriptor 단일 진실
- 선언 ⊆ 실행 가능 ⊆ 손

실측: 오늘 거절 문구를 고칠 때 새 매듭이 필요하지 않았다. **이미 있던 매듭**(커널은 도구를
모른다)에 안 묶여 있던 자리를 찾은 것이었다.

## 5. 2026-07-28 에 새로 못 박은 매듭

하루에 다섯 번 만난 병 — **위층이 아래층의 사실을 자기 말로 덮고, 매번 덜 아는 쪽이 이긴다** —
을 게이트 `③-b2` 로 못 박았다. 넷 다 예/아니오로 끝나므로 기계가 본다.

| 못 박은 것 | 검사 방법 |
|---|---|
| 도구가 이 작업에 대해 낸 문장이 층 전체 고정 문구를 이긴다 | `explainAuthority` 에 둘을 함께 넣고 어느 쪽이 나오는지 본다 |
| 위층은 아래층의 종류를 바꿔 부르지 않는다 | `grantFor({kind:'read', needsApproval:true})` 의 종류·이유를 본다 + `kind = 'send'` 대입 금지 |
| 사람 말 자리에 기계 말을 넣지 않는다 | 카드·원장 문구를 만드는 자리에 `JSON.stringify` 금지 |
| 한 요청에서 같은 질문을 두 번 하지 않는다 | 승인을 만드는 자리 수 = 허락을 이어받는 자리 수 |

**검사가 실제로 잡는지 확인했다.** 넷을 하나씩 되돌려 넣어 보고 넷 다 `✗` 로 걸리는 것을
본 뒤에 이 줄을 적었다 — 통과만 하는 게이트는 게이트가 아니다.

> 새 매듭을 못 박을 때도 같은 순서를 지킨다: 검사를 넣고, **일부러 깨뜨려 걸리는지 보고**,
> 원상 복구한 뒤 통과를 확인한다.

---

## 6. 이 문서를 고치는 법

원칙을 늘리지 않는다. **실측이 원칙을 반박하면 원칙을 고친다.** 오늘 세 축에 붙은 단서
조항(§2)은 전부 그렇게 생겼다 — 원칙이 틀려서가 아니라, 원칙만으로는 부족한 자리가
현실에서 드러났기 때문이다. 그 자리를 적는 것이 이 문서의 일이다.
