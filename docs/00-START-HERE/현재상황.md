# 현재 상황 — 2026-08-05

기준 커밋 `8a9a26e`(S0 착지) · 브랜치 `claude/p-op-1-a-system-view`

> 이 문서는 **밟은 기계 사실만** 적는다. 숫자가 지금 돌린 것과 다르면 이 문서가 낡은 것이다.
> 다시 재는 법은 [README](README.md) §2 에 있다.

---

## 1. 기계 사실

```
회귀        2,380 / 2,380      npm test
돌연변이      306 / 306         npm run audit:mutation
게이트       PASS              npm run gate  (CPU 36.5s · 기준선 59.3s)
계획·문서·작업장·패키지  PASS   audit:plan · audit:docs · audit:workspace · verify:package
라이브 헌장   9건 PASS          npm run live:charter  (전송 1건은 자격 없어 건너뜀)
```

---

## 2. 실제로 되는 것 — 라이브에서 밟은 것만

| 일 | 실측 |
|---|---|
| 다운로드 437개 정리 | **376개 이동 · 44초 · 손상 0 · 사라짐 0 · 승인 0 · 보고=실물** |
| 두 벌 중 최신본 판별 → 요약 파일 생성 | 시각(0.006초 차)·내용 두 근거 · 원본 해시 불변 |
| 서버 재시작 뒤 이어가기 | 대화·근거·파일 전부 이어짐 |
| 파일 두 개 비교 분석 | 숫자 4개 + 문구 차이 정확 |
| 웹 최신 정보 | 출처 3개 실제로 읽고 URL·날짜가 원장과 일치 |
| 차단된 페이지 | 내용 안 지어내고 이유를 밝힘 |
| 민감값 | 카드번호 **디스크 0건** · 일반 숫자는 보존 |
| 승인 거절 | 실행 0 (crontab 0건 · 스크립트 0개, 실물로 확인) |
| 대상 불명 `rm -rf` | 보류 |
| 자기 상태 질문 | 실제 쓸 수 있는 손·못 쓰는 손·**실제 폴더 범위**를 정확히 말함 |

**절대 게이트 P0 0건.**

---

## 3. 안 되는 것 — 열려 있는 결함

전부 [후속 장부](../../design/T5-FOLLOWUP-LEDGER-ko.md) 에 원인·다음 절차까지 적혀 있다.

| 장부 | 무엇 | 상태 |
|---|---|---|
| **신규** | **짧은 발화에 대규모 파일 이동이 승인 없이 실행 가능** | 미해결 · 아래 §5 |
| F-9 | 화면이 **표를 못 그린다**(파이프 문자로 보임) | 미해결 |
| F-11 | 자동화가 **스킬이 없으면 못 켜진다** | 사슬 4곳 중 4곳 이었으나 마지막 한 칸 남음 |
| F-12 | 하지 않은 일을 "이미 해 뒀다"고 말한 사례 | **관측**(재현 1/4) · 넓힐 방법이 문구 목록이라 보류 |
| F-10 | 게이트 CPU 기준선 초과 | **해소** — 부하가 걷히자 36.5s 로 통과. 환경이었음이 확정됐다 |

---

## 4. 비교군 — 정직하게

동결 규약(`docs/03-verification/T5-MALGUI-COMPARISON-PROTOCOL-2026-08-03-ko.md`),
같은 모델 `gpt-5.5`, 첫 턴만, 8문항 1회씩.

| | 총점 | 둘 다 실패 없이 답한 6문항 |
|---|---|---|
| T5 (08-04) | 51 | **46** |
| OpenClaw (08-03) | 55 | **47** |

- 총점 차이의 대부분은 **각자의 실패 1건**에서 온다 — T5 7번은 공급자 오류(0점),
  OpenClaw 5번은 302초 만에 실패(0점).
- **둘 다 정상 답한 6문항으로 보면 사실상 동급이고 OpenClaw 가 1점 앞선다.**
- T5 가 확실히 앞선 자리는 **5번(지난 대화 검색)** 하나다 — 실제로 검색해 구체 사실을 냈고
  OpenClaw 는 실패했다. 이건 기억·맥락 축의 실제 차이다.
- **Hermes 는 자격이 없어 한 번도 못 쟀다.** 셀프후드·도구 실행·자동화는 **비교군을 아예 안 쟀다.**

원문·채점: `docs/03-verification/evidence/malgui-2026-08-04/`

**그러니 "우리가 앞선다"도 "망했다"도 근거가 없다. 대부분 비교 자체를 안 했다.**

---

## 5. 가장 중요한 열린 구멍

> **되돌릴 수 있는 대량 이동은 승인 카드 없이 실행된다.**

자동성 헌장이 그렇게 정했다 — 되돌릴 수 있으면 묻지 않는다. 그 자체는 옳다.
문제는 **짧은 발화 하나가 수백 개를 옮기는 실행으로 이어질 수 있고, 사용자가 막을 자리가 없다**는 것이다.

실제로 일어났다: 2026-08-05 00:03, 격리를 안 건 채 실모델을 돌렸더니 **"아이기스" 한 단어에
파일 443개가 옮겨졌다.** 되돌렸고 손실은 0이지만, **카드는 한 번도 뜨지 않았다.**

오늘 세운 그물 어느 것도 못 막는다 — 이월 보호는 도구 지문만 보고, 발화밖 파괴는 `delete` 만 본다.
`bulk_move` 는 되돌릴 수 있어 전부 통과한다.

**여전히 열려 있다.** 전환(§8)이 우선순위를 가져갔고, 이건 기능을 더하는 일이라
전환에 안 들어간다. 막는 방법은 마찰을 늘리는 쪽이라 **오너 판단이 먼저다.**

---

## 6. 사고 기록 — 숨기지 않는다

두 건 다 **격리를 증명 없이 선언한** 같은 원인이다.

| 언제 | 무엇 | 결과 |
|---|---|---|
| 08-04 오후 | `GPAO_T5_HOME` 만 바꾸고 `HOME` 을 안 바꿈 | 오너의 실제 `~/Documents` 파일 **이름**이 모델 공급자로 갔다. **되돌릴 수 없다** |
| 08-05 00:03 | 재현 스크립트를 고치며 `HOME` 한 줄을 뺌 | 오너 다운로드 **파일 443개 이동**. 원장 영수증으로 전량 복구 · 손실 0 |

그 뒤 `scripts/human-use/prove-isolation.mjs` 가 서고, **증명을 통과해야 서버가 뜬다.**
증명기 자체도 두 번 고쳤다 — 마지막은 폴더 **이름** 대신 **표식**으로 `~` 가 격리 방임을 확인한다.

---

## 7. 사람 사용시험 13계열

`docs/03-verification/evidence/human-use-2026-08-04/evidence.json` · 검증기 `ok: true`

```
pass    5   selfhood · surface_basics · project_file_delivery · agent_delegation · approval_lifecycle
fail    2   automation_surface · time_automation   (둘 다 F-11)
blocked 6   내가 그 턴을 안 돌렸다 — 귀속 test_agent
P0      0
```

**blocked 6 은 결함이 아니라 미측정이다.** 통과로 덮지 않고 그대로 적었다.

---

## 8. 지금 하는 일 — **AI 운영체제 전환**

기능은 설치파일 직전까지 와 있다. 그런데 오너가 새 대화창에서 16턴을 나누자 골격이 무너졌다.
원인은 기능 부족이 아니라 **운영체제의 기본 원리를 모른 채 칸 안쪽 기능만 정교하게 만든 것**이었다.
그래서 설치파일 앞에 전환을 넣었다.

**정본**: [`design/T5-AI-OS-TRANSITION-PLAN-2026-08-05-ko.md`](../../design/T5-AI-OS-TRANSITION-PLAN-2026-08-05-ko.md)
원리 여섯 · 불변식 넷 · 열 칸(S0~S9) · 컴퓨터 유즈 엇물리기. **진행 상태는 그 문서 §12 와 git log 로 본다** —
여기에 단계별 상태를 복사하지 않는다(복사하는 순간 낡는다).

**컴퓨터 유즈**는 전환 계획 §7 에 붙는 순서가 정해져 있다(CU C 는 S6 뒤 — 협상 대상 아님).
받는 문서: `design/CU0-*` 넷 · `design/T5-COMPUTER-USE-DEVELOPMENT-PLAN-2026-08-04-ko.md`.
소스 고정본은 저장소 밖 `/Users/jyp/Developer/t5-cu0-staging/`.

### 전환에 안 들어가는, 여전히 열린 것

전환은 **기능을 더하지 않는다.** 아래는 그대로 열려 있고 [후속 장부](../../design/T5-FOLLOWUP-LEDGER-ko.md)가 받는다.

1. §5 의 구멍 — 짧은 발화 ↔ 대규모 이동 사이에 사용자가 설 자리 (**오너 판단 필요**)
2. F-11 마지막 한 칸 — 스킬 없이도 자동화를 켤 수 있는 길
3. F-9 표 렌더
4. Hermes 자격을 받아 **세 제품 같은 과업 비교** (**오너만 할 수 있음**) — 이것 없이는 우위도 열세도 말할 수 없다

**오너만 할 수 있는 것**: Hermes 자격 입력 · §5 의 최종 제품 판단(마찰을 늘리는 일이라 자동성 헌장과
정면으로 맞닿는다) · 전환 계획 §7 의 CU 미결 4건.
