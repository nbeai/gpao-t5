# F-58 (가-2) 격리 리허설 — 회차 정리 (개발팀 · 2026-08-09 밤 ~ 08-10)

닫는 문장(PM ⑤): 같은 세션 연속 두 발신 — 첫 카드 O · **두 번째 카드 X** · 두 번 다 발신.
구성: 실서버(HTTP·세션 지속) + 실모델(gpt-5.1 저장된연결) + 격리 방(격리증명 전 회차 통과)
+ 화면만 리허설 드라이버(제품 슬롯 경계 주입 — `scripts/live/f58-rehearsal.mjs`).

## 결과 한 줄

**type(글자 넣기)은 무음 재전송이 선다(수리 뒤 3/3). 남은 빨강은 한 칸 — 미신고 엔터 카드.**
그리고 리허설이 **P0급 결함 하나를 실물로 잡아 수리했다**(걸음 증발 — 아래 ②).

## 회차와 발견 (원본: 리허설기록-*.json · 시각 역순 아님, 진행 순)

| 판 | 결과(턴1 카드/발신 · 턴2 카드/발신) | 발견 |
|---|---|---|
| 1~2 | 빨강 | **하네스 결함 둘**: 정적 화면(넣어도 안 보임 → 모델이 정직하게 "안 들어간다"로 접음) · 기록 파일 덮임(1~2판 원본 소실 — 러너 결함, 이후 시각 파일명으로 수리. 요지는 이 표와 커밋에 보존) |
| 3(14-42-20) | 2/1 · 0/0 | 카드에 방 이름이 안 실림 — **실질의 창이 모델 신고(`창제목`)에 달려 있었다**. 모델이 그 칸을 비우는 회차 실재 → 수리 ① |
| 4(14-43-59) | 2/1 · 2/1 | 수리 ① 뒤에도 탐침이 요소를 못 찾은 턴은 본창을 버려 앱 이름이 `카카오톡`/`KakaoTalk` 로 갈림 → 본창을 모든 반환에 싣는 수리 |
| 5~6(14-45~46) | 2/1 · 0/0 | 턴 2에서 모델이 손을 냈는데 **원장·카드·영수증 0으로 증발** — 덤프(prompts)로 확정 → ② |
| 7(14-50-23) | 2/1 · 0/0 | 턴 전문 저장. 증발 자리 확정: 경계(승인 필요) vs 걸음plan(자동)의 어긋남 → 수리 ② |
| 8(수리 뒤) | 1/1 · **0/1** | **초록** — 닫는 문장 성립 회차 실재 |
| 9~10(15-01~02) | 2/1 · **1/1** | type 무음 재전송 성립(수리가 문다). 남은 카드는 **미신고 `return 누르기`** 하나 |

## 수리 둘 (봉인 포함)

① **실질의 창은 모델 신고가 아니라 탐침이 본 창이다.** probe 가 실제로 본 창(`본창`)을
기계 사실로 반환(못 찾은 반환 포함)하고, previewOf 가 신고 부재 시 그것으로 열쇠를 세운다.
press_key·hotkey 는 창 신분만 탐침(요소 탐색 0 · 등급 불변). 봉인 4(본창 열쇠 · 엔터 본창 ·
좌표 금지 유지 · 탐침 창신분).

② **걸음 증발(F-20 계열 세 번째 · P0급).** 걸음 경로의 `승인면제`가 헌장 ③을 채널
손(target)으로만 알아서, 화면 손의 아는 상대는 경계="승인 필요" vs 분기 안 계획="자동"으로
어긋나 grants 빈손 break — **카드도 실행도 원장도 없이** 걸음이 사라졌다. 모델은 영수증
없이 answerOnly 로 몰리자 *"이 환경에선 막혀 있어서"* 를 지어냈다(거짓 실패 서사의 제조
자리 — dae9273 계열. 앞서 "모델이 재전송을 거부한다"로 관측한 것의 뿌리가 **제품**이었다).
수리: 면제에 ②-b(화면 실질 열쇠) — 선빨강(실물 모양: 관찰→신고 없는 type, 걸음 경로) →
수리 → 초록 · 절단 시 그 봉인만 빨강 확인.

## (a) 이행 뒤 — 2026-08-10 추기 (PM 판정 조건 넷 이행)

- 수리 ③ **엔터의 실질은 기계다**: 탐침이 press_key·hotkey 에서 그 창의 글자칸 내용을
  읽는다(정확히 한 칸·보안(Secure) 칸 낀 창은 내용 자체를 안 세움·못 읽으면 없음 —
  조건 ② fail-closed). 종류: 칸 내용이 선 엔터는 미상이 아니라 `field_input`(조건의 그
  읽기 — "탐침이 읽으면 더 이상 미상이 아니다"). 열쇠 재료는 신고(`기대.값`) 불사용
  (조건 ① — 신고 분기의 엔터도 동일. 표시는 기계 우선·못 읽으면 신고값을 보여 주되
  그때는 열쇠가 없어 카드가 반드시 뜬다). 봉인: 실경로 ③ 선빨강→초록 · 안전 대차(엔터)
  4행 · 신고 갈래 3형(빈 신고값·다른 신고값·못 읽음) · 절단 2회 각자 빨강(조건 ③).
- 리허설 판별: R12 초록(엔터 경로) → R13 빨강(**click 전송 갈래** — 아래) · R14 빨강
  (신고+빈값 엔터 — 단위로 재현 확정 후 수리) → **수리 뒤 R15~R19 5연속 초록**
  (턴1 카드 1~2 · 발신 1 / 턴2 카드 0 · 발신 1). 부수 효과: 턴 1 카드가 4→1로 줄었다 —
  한 장이 방·내용을 보여 주고 그 승인이 엔터까지 덮는다(보여 준 것 = 기억한 것).
- **남은 모양 하나 — click 「전송」 갈래**(R13 · 1회 관측): 모델이 엔터 대신 단추를
  누르면 값 없는 단추는 미상이라 카드가 남는다. 단추의 의미(전송인가 삭제인가)는 기계가
  모르고, 이름으로 가르면 문구 목록(A10 위반)이다 — **fail-closed 유지가 규율에 맞다고
  판단**하고 넓히지 않았다. 사용자 체감은 "가끔 한 장 더 묻는다"이지 발신 실패가 아니다.
  더 열려면(창+칸내용+단추 신분의 걸음 기억 등) PM 판정 자리다.

## (구) 남은 빨강 한 칸 — PM 판정 요청 → **(a) 채택·이행 완료 (위 추기)**

**미신고 `press_key`(엔터).** 미상 등급이라 조건 없이 카드다(규율: 모름은 확인 쪽).
기계는 창(본창)을 알고, 칸의 내용도 알 수 있다(탐침이 요소 값을 읽으면). 그러나 미상
걸음에 「기계 확인 실질 + 아는 상대」 면제를 여는 것은 **"미상은 항상 카드" 규율을 넘는
정의역 확장**이라 개발 선에서 넓히지 않았다(같은 자리 세 번째 갈림 — 멈추고 PM).
갈래: (a) 탐침이 칸 내용까지 읽어 실질을 세우고 면제 허용 (b) 닫는 문장을 "type 무음 +
엔터 1카드"로 재정의 (c) 다른 길. 어느 쪽이든 안전 대차표 선행.

## 정직 부기

- 회차 1~2 원본은 러너 결함(같은 파일명 덮음)으로 소실. 거기에 더해 고정 이름에 남아 있던
  마지막 판 원본 1건을 **정리 중 내가 오삭제**했다(rm — 되돌릴 수 없음). 합계 소실 3판이고,
  그 판들의 판정 수치·카드 문안은 이 표와 세션 기록에 남아 있다. 이후 전량 시각 파일명 보존.
- 모델 모양 변동 실측: 같은 발화에 신고 유/무·창제목 유/무·거부/시도가 회차마다 갈렸다 —
  "모델은 같은 답을 낼 수 없다"의 원장 실증. 열쇠를 기계 사실로 옮긴 근거다.
- 리허설 드라이버는 화면만 가짜다(상태 있는 카톡 창 흉내). MCP·실물 층은 이 리허설의
  정의역 밖 — 실물 확인은 창 요청(오너 자리)이 한다.
