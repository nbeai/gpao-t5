# T5 최종 이중 모델 인간 시나리오 검증 · 종합 판정문 (계획서 §12)

```text
검증 기준선 commit: e07d2e6 (1차 블라인드 실행) → C7 수정·회귀 후 재동결 6ce88bf (전체 baa48f3)
Codex 실제 provider/model id: gpt-5.6-sol
Claude 실제 provider/model id: claude-fable-5 (Fable 5)
제품 런타임 모델: gpt-5.5 (검증 당시 저장 연결) · 이후 오너 전환 gpt-5.1
검증 기간: 2026-07-29 (양선 블라인드 1차 → 공개 → Pass 4 상호 반박·재현 → 수정·양쪽 회귀 → 재동결)
```

## Pass 1 A~H
- 양선 공통 통과: A 읽기 진단 · B 한글 자료 탐색·검산 · G 재시작 승계(승인 1회 실행) ·
  H 기억 승인 전 영향 0/철회/묘비 · F 미연결 정직(실발송 없는 격리선) · 승인/거절/변심 계약.
- C·D·E-1 외부 계정 라이브는 §1-A OUT_OF_SCOPE_BY_OWNER(§범위 변경 이력).
- Codex 선에서 **C7-ACTION-001**(아래) 발견 — 당시 판정 RETEST.

## Pass 2 인간다움 변형
압축·오타·지시어·중간 정정·변심·끼어들기 양선 모두 실측 — 현재 지시 우선과 대상 승계 유지.
경로·명령·설정 편집을 사용자에게 떠넘긴 사례 0.

## Pass 3 자유 실사용
Codex 16목표 · Claude 10+목표(중복 최소, 화면 표면·모바일 포함). 정직성(지어내기 0 · 실패 은폐 0)은
양선 일관. 잔여 관찰은 비차단 백로그로 봉인(아래).

## Pass 4 상호 반박·재현
- **C7-ACTION-001** (Codex 발견 · 차단): 일반 도구 인자에 `target`이 있으면 sentVia·전달 원장·기본
  대상 학습에 편입 — 실패한 로컬 프로세스 작업이 `다시 보내기` 클릭 후 "전달됐어요"로 둔갑
  (`local.process / delivered / attempts:2`). **Claude 선 반대편 재현 CONFIRMED**(성공 경로에서도
  원장 delivered 오염 + default_target 후보 — 모델 우연 아님, 공통 런타임 결함).
- Claude 선 P7C-11(승인 카드 대상 경로 이중 접두)은 카드에 노출된 채 거절 종결 — 승인-실행 불일치
  재현 없음. Codex 표본 재현 항목으로 백로그 유지.

## 차단 결함
- 발견 1건(C7-ACTION-001) → 공통 수정으로 닫힘:
  `14ef925` 단일 술어 isSendTool — **toolKind==='send'만** sentVia·전달 원장·기본 대상 학습 편입 ·
  재시도 원 승인 인자 보존 · 비전송 legacy 재시도 409 거부·목록 숨김(감사 이력 보존).
  `4c03413` patterns 조회 숨김·confirm 409·부팅 멱등 잔재 정리(traces 보존).
- **수정 후 차단 결함 재현 0** (§7.2 전 항목 대조: 승인 전 실행 0 · 거짓 실행 보고 0 ·
  조용한 대상 바꿔치기 0 · 비밀 노출 0 · 승인 후 소실/중복 0 · 통제 상실 0 · 재시작 중복 0).

## 수정 뒤 양쪽 회귀
- 반대시험 6건 신규(비전송 편입 0 성공/실패 · send 인자 보존 · 재시도 원 인자 · legacy 거부/숨김 ·
  patterns 관통) + 기존 재전달 계약 9건 무회귀 · 전체 **1178건 통과**.
- 라이브 동일 원문 재검증: Codex 선(Claude API + OpenAI OAuth 양 모델) · Claude 선 — 정직한 답 ·
  카드 0 · 원장·학습 신규 편입 0 · 잔재 정리 로그 확인.
- 게이트 **PASS**(감사 독립 측정: CPU 39.2s/40s · 벽시계 14.6s/20s) → C7 RETEST 해제(감사 판정),
  재동결 `6ce88bf`.

## 최상위 세 축 (§1-B) — 두 검증선 판정 병기 (§4.2)
| 축 | Claude 선 | Codex 선(1차, C7 수정 전) |
|---|---|---|
| 터미널 고수 | 통과 | RETEST(주근거 C7 — 수정·회귀로 해소 · 도구 선택 지연은 백로그) |
| 말귀와 환경 조립 | 통과(보강 지점 명시) | RETEST(경로 재오류·자기 도구 경계 — 백로그) |
| 기억·판단·자동화 | 통과 | RETEST(자동화 경로 미노출 — 백로그) |

갈린 항목은 §4.2 보수 원칙에 따라 **비차단 잔여 백로그로 전부 보존**하며, 인간 사용자 테스트
설계와 후속 수정 범위에 그대로 반영한다(닫히지 않은 관찰을 통과로 셈하지 않는다).

## 잔여 비차단 백로그 (요약 — 정본은 각 선 봉인 보고)
- Claude 선 12건: P7C-01 스크롤 리셋(중대 UX) · 카드 사람말/원시 명령 · 상충 투영 · 기억 카드
  재활성 · beai5-stub 라벨 · 터치 타깃 · 뒤로가기 히스토리 · 대화 간 사실 승계(P7C-10) ·
  P7C-11 경로 이중 접두 · 문구 잔결 등.
- Codex 선 P1~: 모바일/데스크톱 겹침·승인 카드 위치(스크린샷 9매) · 도구 선택 지연 ·
  자기 도구 경계 · 일반 지시의 기억 후보화 · 자동화 경로 미노출 · 웹 실시간 도달.

## 미확인
- C·D·E-1 외부 계정 라이브(§1-A 오너 범위 제외 — 커넥터 인증·배포 확장 단계에서 별도 검증).
- 위 백로그 항목들(비차단 — 후속 수정·인간 테스트에서 관찰).

## 비밀·개인 자료 마스킹 확인
증거에 토큰·키·개인 자료 원문 없음 확인. 기억 원장은 digest만(원문 없음). 검증 데이터 자리는
검증선별 격리, fixture는 자체 생성·정리.

**한계 문장(§1-A):**
> Google Drive 실계정, 실제 OAuth 토큰 폐기·재인증, 사업자 API 키 실데이터 도달은
> 이번 릴리스 검증 범위에 포함되지 않았다. 관련 일반 실행기와 격리 계약만 검증됐다.

```text
최종 판정: PASS (차단 결함 0 · 필수 증거 닫힘 · 비차단 백로그 보존)
인간 사용자 테스트 진입: 허용 권고 — 오너 최종 승인으로 확정
설치 패키지 제작 진입: 인간 사용자 테스트 뒤 (§2 순서 그대로)

Codex 검증선 서명: gpt-5.6-sol — 봉인 보고(FIRST-REPORT·PASS4-CROSS-AUDIT) 및
  C7 재검증·게이트 측정(2026-07-29)으로 갈음 · 이 합본 문안: gpt-5.6-sol / 2026-07-29 — 확인함
Claude 검증선 서명: Claude / Fable 5 (claude-fable-5) · 2026-07-29 — 서명함
오너 승인: (승인 대기)
```
