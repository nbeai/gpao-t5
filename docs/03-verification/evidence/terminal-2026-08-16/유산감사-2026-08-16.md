# 유산 감사 — 2026-08-16 (HEAD 23f41537 · 읽기 전용)

**물음**: 이전 작업들이 만든 모순·결핍·문제가 「터미널 완성 3걸음」(§7-au-4: ① (나) 쓰기 자동화 —
봉인됨 · ② 집계 범위 · ③ 완성 재판)을 막는가. **방법**: 전 항목 직접 밟음(grep·실행·대조).
못 밟은 것은 「미확인」. 이 문서의 숫자는 전부 이 감사가 직접 센 값이다.

이 감사가 밟은 기계 사실 기준선: `npm test` 단독 실행 **4,285 통과 · 실패 0 · 보류 0** ·
`npm run gate` **BLOCKED 1건(원인 5)** · origin/main ⊂ HEAD (fast-forward 관계 · 미병합 **30**).

---

## 1. [지금 막음] — 3걸음을 직접 막는 것

| # | 항목 | 기계 근거 (직접 밟음) | 막는 걸음 |
|---|---|---|---|
| 1 | **`design/NEXT-SESSION.md` 가 다음 걸음을 반대로 가리킨다** — §0-②는 「다음 한 걸음 = 결과 기반 손 전환(§7-ah 선빨강)」, §7-4(a)는 「F-121 재판 6+4 착수」를 지시한다. 둘 다 그 뒤 세션에서 **완료·기각·정정됐다**: 6+4 재판은 `aa38aaac` 로 이미 열려 미달했고, 기준선 §7-u 가 「재개봉 면제는 여기서 끝난다」, §7-ay-2 가 「같은 라이브 재판을 네 번째로 열면 중단」을 못 박았다. §7-ah 의 선빨강 서술(`failureState undefined`)은 §7-ak-2 에서 **계측 오류로 정정**됐다(handler 직접 호출은 영수증이 아니다 — `tools.run` 경유 실측은 `blocked`). 인계서만 읽고 착수하면 **금지된 배치를 열거나 반박된 진단을 다시 판다** | NEXT-SESSION.md:50-58 · :592-594 ↔ 기준선 §7-u(:1142-1144) · §7-ac(`aa38aaac`) · §7-ay-2(:2748) · §7-ak-2(:1946-1955). 현재 확정 다음 걸음은 **② 집계 범위 착수**(선등록 `61b8ab8f`+`23f41537` 완료) | **걸음 2** (착수 오도) |
| 2 | **§7-ad-3 자 결정 둘이 미결인 채 걸음 2 앞에 서 있다** — ①파일 손으로 잰 것을 「쟀다」로 셀 것인가 ②한정 무늬 동결(`/안 봤\|확인된 것 중/`) 유지할 것인가. §7-ay-3 손 관리자 판정: 「**② 재판보다 반 걸음 먼저**」. 결정 없이 재판을 돌리면 §7-ad(자가 손을 과소평가 — 6 중 4가 자의 맹점)가 재발한다 | `채점기.mjs:63` — `r.터미널명령`만 읽음(파일 손 캡처 0 · grep `local` 0건) · `채점기.mjs` 상단 `무늬_한정고지 = /안 봤\|확인된 것 중/` 동결 그대로 | **걸음 2** |
| 3 | **걸음 2 의 표적 실물 넷** (선등록 완료 · 착수 대기 — 유산이 곧 표적이다): (ㄱ)목록에 크기 칸이 없다 (ㄴ)「반만 읽고 총」 게이트 어사가 순위 단정을 안 문다 (ㄷ)childCount 가 게이트에 안 닿는다 (ㄹ)터미널 영수증에 `같은자리파일` 대응물이 없다 — 자(터미널만)·게이트(파일손만) **대칭 맹점** | (ㄱ) `local-file.js:729` — items 는 `{name, kind, modifiedAt}` 뿐(실측 확인 · 폴더만 childCount) (ㄴ) `exit-verification.js:714` `/총\|전체\|합계/` (ㄷ)(ㄹ) `exit-verification.js:705-724` 는 `local.file`+`action==='read'` 영수증만 필터(실측 확인) | **걸음 2** (표적) |
| 4 | **조건 대칭 하네스가 없다** — 완성 재판 조건은 「양쪽 다 **한 세션 연속** + 카드는 눌러 준다」(§7-au-2)인데, `승인까지써본다.mjs` 는 **발화 1개 / 새 방**(argv[2] 하나 · mkdtemp), `그냥써본다.mjs` 는 **승인 0줄**(grep approve·grant = 0). 멀티 발화·단일 세션·승인 동반 하네스가 저장소에 없다 | `승인까지써본다.mjs:35-42,114` · `그냥써본다.mjs` grep 실측 0 · §7-af-0(조건 비대칭 신고)이 재판정 때 맞추라고 이미 명시 | **걸음 3** |
| 5 | **⑤⑥⑧ 발화가 한 번도 없었다** — 실행·빌드·테스트 / 설치·환경 / 장기 실행·프로세스 칸은 발화 자체가 0이라 빨간지 초록인지 모른다. 완성 판정 자격(§7-au-2 「발화 확장」) 미충족 | §7-au-3 성적표(전부 실측 근거 명기) — 세 칸 「미측정 · 발화 자체가 없었다」 | **걸음 3** |

---

## 2. [잠복] — 건드리면 터지는 것 (전부 직접 밟음 · 순서는 폭발 반경 순)

**계약·배선 모순**
- **codex/terminal-* 3 브랜치가 오늘 수리한 그 파일들을 고친다** — 세 브랜치 모두
  `terminal-run.js`·`local-terminal.js`·`tool-boundary.js`·`turn.js` 를 변경(diff --name-only 실측).
  merge-tree 실측 **충돌 있음**(3/3). 판정 없이 병합하면 오늘의 granted·창조만한다·막음자국있나
  수리와 정면 충돌한다. lane/ux 는 충돌 0 · lane/ux-integration 은 충돌 0 에 HEAD밖 커밋 **0**(이미 포함).
- **reversible 통째 선언 잔존** — `demo-context.js:661` `reversible: false`(실측 확인). 창조만
  갈래는 행동 사실이 이기지만(`tool-boundary.js:128`), 그 밖 명령의 카드 「되돌리기 어려워요」
  줄은 여전히 이 정적 선언에서 나온다(§7-w-3 별건 등록 그대로 · 미수리).
- **`빈손으로끝났나` 기본값 ↔ `대조대상인가` 모순은 반만 화해** — 옵션 게이트(`심문은빈손아님`)는
  `turn.js:2828` 계열 한 호출부만 탄다. `:2740` 기본값 호출부는 의도적으로 불변(실측:
  `exit-verification.js:82` 게이트 실물 확인). J12 남은 ③(걸음 0/걸음>0 두 자)이 이 모순의 잔여다.
- **sqlite3 미탐 + 어법 재설계 정지** — `terminal-run.js:437-440` 주석이 스스로 신고(실측 확인).
  사본 재실행 재설계는 §7-ap-3 급소(기본 cwd 가 홈이라 상한 걸면 거의 항상 「모른다」) +
  §7-aq-3 절반 이행 갈라짐에서 멈춰 **오너 결정 대기** 상태다.

**열린 채 잊힐 위험**
- **J12 남은 5**(§3-1 ①~⑤) — ⑤ `안써본손` 게이트 존치 실측(`turn.js:2843·2892·3448`).
  ① 「재생성은 버려진다」는 **조건부 명제**다(앞답 빈 턴은 F-84 되살리기가 갈아침) — 무조건 인용 금지.
- **F-118 잔여 ①** — exit≠0·비막힘 probe 도 `probeChangedNothing`(`local-terminal.js:417` —
  조건이 `sandboxed === true` 뿐임을 실측). 라이브 거짓 표본이 나오면 연다(문서 사유 그대로).
- **F-119 잔여 3문** — `local-process.js:120` 등 미개봉 + **옛 원본 36/75 mojibake 불가침 잔존**
  (`재료실측` 키 깨진 채 — 옛 원본을 진값으로 인용하면 거짓이 된다).
- **F-120 잔여** — `turn.js:2761` 부분합 그물에 copy 계열 미배선(실측: `['write','move','bulk_move','delete']`
  만). 표본 0 사유부 보류 그대로.
- **F3 실물화** — `sensitive-text.js:103 MASK='[가림]'`(실측)이 §7-j 원6 최종답에 개입 + 미설명
  수치 144,027(§3-10) — n=1 로 열지 않은 채. **H04 0/5** · S6(아래 지도 낡음 참조) · J7(`local-file.js:126`
  따옴표 CSV 안전망 침묵 — 실측 확인) · J8(`deriveWorkingState` 호출 8곳 전수 — `pendingApprovals`
  넘기는 호출자 **0** 실측) · F5 · C1 · X2 · X3 — 전부 열림 유지.
- **X1** — `scripts/compare-live/secret-env.sh` 잔존(600 권한 · 실측 ls).

**계측기·문서의 인용 함정**
- **`귀속표.mjs` 파일 안에 무효 경고가 없다** — grep `무효` **0건**(실측). 무효 판정은
  NEXT-SESSION §3-5 와 기준선 §7-n 에만 있다. 파일만 연 세션은 「16회 전부 :2822」류를 다시
  만들 수 있다. `tasks.mjs`(폐기된 개발자 시험)도 경고 없이 잔존.
- **지도 §12 낡음 3건** — ① J12 줄(:543) `turn.js:687·3503·838·840` ↔ 현재 실측
  `696·3597`(정직한답) · `708·848·852`(fallbackReplyFrom) — 이미 경고돼 있으나 미갱신.
  ② S6 자리 `server.js:1041` 낡음 — 현재 그 줄은 자동화 승인 코드이고 `legacy-default-agent` 는
  `automation-contracts.js` 로 이동(실측). **「유일 활성이면 무조건 선택」 행동의 존속 여부는 미확인**.
  ③ J5 는 코드가 이미 동사 목록을 **동적으로 나열**한다(`desktop-act-tool.js:527`
  `DESKTOP_ACT_ACTIONS.join` · 17동사 실측 · 수리 커밋 `bce1a42f`) — 지도는 열림으로 유지 중.
- **장부 꼬리 F-106 정지** — grep 실측 F-100~F-106 뿐. F-107~F-121 · J12 **한 줄도 없음**(기존 경고 유지).
- **local `main` 이 3주 낡았다** — origin/main 보다 **1,320 커밋 뒤**(실측). `main..브랜치` 로 세면
  거짓값이 나온다(이 감사가 실제로 밟은 함정: `main..codex/terminal-core` = 1,292 vs
  `HEAD..` = **20**). 브랜치 비교는 HEAD 나 origin/main 기준으로만.
- **NEXT-SESSION §6 「본선 미병합 1,309 · 한 번도 안 들어감」은 이제 거짓** — origin/main 이
  16시간 전 `79c02db6`(§7-v 진단 커밋)까지 전진했다(fast-forward 관계 기계 확인). 남은 미병합
  **30 커밋**(§7-w 이후 ~ HEAD). 미푸시 0.
- **기준선 문서 자기 서술 낡음** — §4-6 「714줄 · 정본」 ↔ 실측 **2,771줄**. 회귀 기준선 숫자도
  문서 위치마다 4,219~4,285 로 다르다(각 시점 값 — 베끼지 말고 직접 재라는 자기 경고 유효).
- **f101 검사가 병렬 실행에 안전하지 않다** — 이 감사가 `npm run gate` 와 `npm test` 를 **동시에**
  돌리자 `f101-lane-rooms-are-not-defects.test.js` 가 `fatal: a branch named 'lane-test/일하는방'
  already exists` 로 거짓 빨강(재현 1회). 단독 재실행은 4,285/4,285 초록. 두 검사 러너가 같은
  픽스처 이름을 공유하는 것이 원인 후보 — 동시 실행만 피하면 안 터진다.

**게이트·자리 밖 유산 (실행값)**
- **게이트 BLOCKED 1건 · 원인 5** (이 감사 실행값 — NEXT-SESSION 시점의 3원인에서 늘었다):
  ```
  ① worktree 자리 밖  /private/tmp/t5-check-271     — 주 저장소 gpao-t5 의 worktree(gitdir 실측) · 소유 jyp · 8-14 이후 유휴
  ② worktree 자리 밖  /private/tmp/t5-terminal-core — 같음(codex/terminal-core 체크아웃)
  ③ 방치된 방  ../t5-lanes/terminal-rebuild  46시간 · 본선 밖 7커밋   ← 전엔 「작업 중」 경고였다 — 방치로 승격
  ④ 방치된 방  ../t5-lanes/terminal-stage1   42시간 · 본선 밖 8커밋   ← 같음. 「지금도 커밋이 는다」는 이제 거짓(마지막 커밋 2일 전)
  ⑤ 방치된 방  ../t5-lanes/ux                83시간 · 본선 밖 12커밋
  ```
  그 외 게이트 칸 전부 초록(테스트 4,285 · CPU 89.5s · 프롬프트 5,011자 등 — 실행값).
- **/tmp 추가 잔재 2** (게이트 밖 · 소유 jyp): `t5-f114-git-audit`(독립 git 저장소) ·
  `t5-terminal-write-effect`(**죽은 worktree** — gitdir 이 소멸한 `/private/tmp/t5-home-terminal-integration/…` 를 가리킴 · 실측 fatal).
- **미병합 브랜치 내용 요약** (읽기만 · 자동 병합 안 함): codex/terminal-core 20커밋 53파일
  (+GPT-5.6 루프·구조화 출력) · terminal-rebuild 7커밋 26파일(권한-실효 결속) · terminal-stage1
  8커밋 43파일(rebuild 포함+완성선 동결 문서) · lane/ux 12커밋 28파일(UX 전수 걷기·연속성).
  셋은 충돌 있음 / ux 계열은 충돌 0.

**오늘 수리의 나비효과 후보 — grep 전수 결과 (어긋난 소비자 없음)**
- `granted` 깃발: 생산 `tool-boundary.js:86` 한 곳 · 소비 `local-terminal.js:293`(lifecycle 위험
  게이트)·`:307`(모드) 두 곳 — 전수 grep 에서 다른 소비자 없음. 뜻 주석(:73)이 두 소비를 묶는다.
- `창조만한다/창조만인가`: 생산 `local-terminal.js:77·244` · 소비 `tool-boundary.js:91·128` ·
  `action-plan.js:264-267` — 두 판정 경로 동일 답 검사(13/13) 존재. 그 밖 소비자 없음.
- `막음자국있나`: 정의 `terminal-run.js:236` · 소비 `local-terminal.js:238` 한 곳뿐.
- 어법 동결: `얼린갈래수 = 11`(상한 · 실측) · 양성 대조 검사 존재 · §7-at 이후 갈래는 10.
- ⚠️ 위 배선들과 **codex/terminal-* 3 브랜치의 같은 파일 변경**이 유일한 충돌 예정지다(위 첫 항).

---

## 3. [해소됨] — 오늘까지의 수리로 닫힘 (커밋 해시 · 이 감사가 재확인한 것)

| 무엇 | 해시 | 재확인 |
|---|---|---|
| 승인이 실행에 안 닿던 죽은 버튼(granted) | `ccf33e2f` | 깃발·소비자 전수 grep 정합 · 회귀 4,285 재실행 초록 |
| (나) 기존 것 안 건드리는 생성 자동 | `7d80d6de` (+울타리 `262512bd`·`8a825d82`) | 소비자 전수 grep · 검사 스위트 초록 |
| `find -delete` 거짓 보고(exit 0 도 주장이 아니다) | `709aa339` | `막음자국있나` 배선 실측 |
| 말투 B 대조 불능 → blocked_unproven | `fb2faf83` | 겨눈 서명 0/4 는 문서값 · 코드 갈래 실측 |
| 어법 목록 동결(11 상한) | `938e89ab` | 검사 실물·상한 로직 실측 |
| J12 물음표 오판(파악 물음은 빈손 아님) | `55c284c3` | 게이트 실물 `exit-verification.js:82` |
| J12 자 — 낱말 세기→기제별 정의역 6 | `31170dc9` | A 2(696·3597)+B 3(708·848·852)+C 1 실측 정합 |
| 채점기 판별력(L1·L2 누수) | `0983344e` | 자를 무는 검사 존재 확인 |
| 계측기 로케일(재료실측.mjs 통일) | `6f449b57` | 수집 한 함수 · 하네스 둘 다 사용 확인 |
| 승인 하네스 첫 판 훑기(두 자리→방 전체 재귀) | §7-an-1 (`fb2faf83` 구간) | `승인까지써본다.mjs:59,107` schemaVersion 2 실측 |
| F-119 한글 로케일 · F-120 copy 동사 · F-121 childCount(코드) | 문서 기재 (`c9e8477d` 등) | 검사 파일 5종 존재 + 스위트 초록. **F-121 슬라이스 자체는 ② 로 흡수 — 닫힘 아님** |
| 「병합하면 도구·증거 삭제」 거짓 | §7-q-7 정정 | merge-tree 재실행 — 같은 결론(충돌 유무만 남음) |
| desktop.act 거절 문구 낡음(J5) | `bce1a42f` | 동적 나열·17동사 실측. **지도 §12 는 미갱신** |
| 검문 계약서 4장 추적 | `83d9e147` | — |
| 카드 why 줄 거짓(읽기를 쓰기라 말함) | `d9703e87` 구간(§7-z 3/3) | 「거짓말 사라짐 · 마찰 남음」 구분 유지 — 합쳐 읽지 말 것 |

---

## 4. 오너 결정 필요

1. **게이트 5원인 정리** — /tmp worktree 2(주 저장소 gpao-t5 소속 · 소유 jyp · 2일+ 유휴) 제거
   허가 + 방치 방 3 판정. 죽은 잔재 2(`t5-f114-git-audit`·`t5-terminal-write-effect`)도 함께.
2. **codex/terminal-* 3 브랜치 판정** — HEAD밖 20·7·8커밋 · 충돌 있음 · **오늘 수리한 네 파일을
   전부 고침**. 살릴 내용이 있으면 충돌 해결+내용 판정이 먼저다. lane/ux(충돌 0)·ux-integration(이미 포함) 별도.
3. **남은 미병합 30 커밋**(§7-w~HEAD — 승인카드·창조만·granted·② 선등록)의 본선 병합 시점.
4. **어법 절반 이행 갈라짐**(§7-aq-3) — 사본 재실행 재설계 진행 여부(급소: 홈 cwd 상한 → 거의 항상 「모른다」).
5. **F-120 등급 갈라짐**(copy write 차선 ↔ organize 처방 · 커밋 검문 신고분).
6. **장부 입양**(F-107~F-121 · J12) 및 **지도 §12 갱신**(J12 줄 · S6 자리 · J5 닫힘 — t5-map-keeper 소관).
7. **H04 0/5** — 정본 §5 순서 7이 이 뒤에 서 있다(터미널 3걸음과는 독립).

## 5. 다음 세션이 먼저 읽을 것 (5줄)

1. **기준선과-남은자리.md §7-ay(+ay-3)** — 확정된 다음 착수는 ② 집계 범위다. **NEXT-SESSION §0-②·§7-4(a)는 낡았다** — 따라가면 금지된 재판을 열거나 반박된 진단(§7-ah)을 다시 판다.
2. **§7-ak-2** — 영수증을 재려면 `tools.run` 을 지나라. handler 직접 호출은 영수증이 아니다(§7-ah 를 뒤집은 정정).
3. **§7-au** — 완성의 정의·10칸 성적표·3걸음. 걸음 3 전에 조건 대칭 하네스(멀티 발화·한 세션·승인)와 ⑤⑥⑧ 발화가 있어야 한다.
4. **§7-aq·§7-ao~ap** — 어법 밑 사본 축은 절반 이행 갈라짐으로 오너 결정 대기. 지배 숫자는 결정적 코퍼스로(라이브 n 으로는 영원히 말 못 함 — 윌슨 구간).
5. **이 문서 §1 표** — 걸음 2 착수 전에 §7-ad-3 자 결정 둘을 선등록에 올려라(반 걸음 먼저).
