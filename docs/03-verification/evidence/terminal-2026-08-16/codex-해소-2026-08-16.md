# codex·lane 브랜치 해소 — 2026-08-16

유산 감사에서 지목된 4 브랜치(codex/terminal-core · codex/terminal-rebuild ·
codex/terminal-stage1 · lane/ux)의 판정·보존·처분 기록. 앞 세션이 태그 9개 생성과
codex/terminal-* 3종 삭제까지 마쳤고, 이 세션이 lane/ux 판정과 나머지 정리를 닫는다.

## 1. 브랜치별 판정

### codex/terminal-core · terminal-rebuild · terminal-stage1 (앞 세션 판정 승계)

- 처분: 낡음 — 터미널 축은 본선이 다른 경로(F-116~F-121, 완성재판)로 이미 지나갔다.
- 보존: `archive/codex/terminal-{core,rebuild,stage1}-2026-08-16` + 각 `-wip` 태그 6개.
- 실행: 로컬·원격 삭제는 앞 세션이 이미 완료(이번 세션 재확인 — local:no · remote:no).

### lane/ux (12커밋, 갈래점 d205ffb3) — **유효. 낡지 않았다.**

판정 근거 (기계 사실):

1. **본선 무접촉**: 갈래점 d205ffb3 이후 본선(claude/p-op-1-a-system-view, 00d6b3cd)은
   `src/surface/web/index.html` 을 **0회** 건드렸다(`git log d205ffb3..본선 -- src/surface/web/index.html` 공집합).
   `src/kernel/l5-growth/session-search.js` 도 동일. lane/ux 표면 작업과 오늘 대개편은
   파일 수준에서 겹치지 않는다.
2. **유일 접점은 preflight.mjs**: 통합 기준(491368fe)~본선 110커밋 중 lane/ux 접촉 파일과
   겹친 것은 `scripts/s1/preflight.mjs`(+`test/s1-preflight.test.js`)뿐. 그마저 서로 다른
   hunk — lane/ux 는 허용파일 목록에 session-search.js 한 줄 추가, 본선은 기준지문(스키마
   bdeb6ebc)·허용파일 다른 항목 갱신. 형식적 병합만 필요하고 의미 충돌 없음.
3. **관련 검사 전 초록**: 통합 worktree(본선 조상 491368fe + lane/ux 전량 staged)에서
   그 브랜치가 만진 검사 파일 16개를 `node --test` 로 3조각 실행 —
   48 + 7 + 48 = **103 pass · 0 fail**. (전체 npm test 는 앞 세션 600s 무진행 함정이라
   관련 파일만 실행 — 위임 지시.)

### 살릴 조각 목록 (적용 금지 — 목록만)

보존 위치: 태그 `archive/lane/ux-2026-08-16`(팁 c694d44a) · `archive/lane/ux-wip-2026-08-16`
(lane/ux 미커밋 스냅샷 2bf69090) · **본 폴더 `lane-ux-integration-staged-2026-08-16.patch`**
(통합 worktree 의 staged 셋 4,038줄 — 본선 조상 491368fe 기준이라 현 본선에 그대로 적용 가능).

| 조각 | 내용 | 커밋 |
|---|---|---|
| B | 답 복사·내 말 고쳐 재전송·같은 질문 재전송, 호버 대신 말풍선 아래 상시 줄 | 8481395e · 4aa5a4b7 |
| D | 손 기록이 답 뒤에도 남는다(지우지 않고 접기) | fce99db3 |
| F | 새 화면 추가가 두 곳 수정으로 끝나는 단일 통로 | af929cf6 |
| G | 대화·화면 주소(링크·뒤로가기·새로고침 동일 자리) | ee01d364 |
| ↑ 회수 | `↑` 로 방금 한 말 복원 + 「링크 공유」 거짓 주장 제거 | a487278c |
| 처방① | 답 흐르는 중 보낸 말이 줄 서 있다고 말한다 | 55be5d4a |
| 연속성·검색 | 검색 결과 일치 대목 표시·원 대화 첫 일치로 이동, 새 대화 발화수 비상속, 대형 대화 창 | c694d44a |
| 문서 | UX 비교연구·전수걷기(화면 11·버튼 285)·인간 패턴·원장 갱신 | 0a987e96 외 |

## 2. 처분 실행 내역 (이번 세션)

- `lane/ux` 로컬 삭제(was c694d44a) + 원격 삭제(`push origin :lane/ux` → deleted).
- `lane/ux-integration` worktree 제거(`git worktree remove --force`) + prune + 브랜치 삭제(was 491368fe).
  제거 전 staged 셋을 patch 로 회수(위 보존 위치).
- `/tmp/t5-f114-git-audit` 미커밋 회수 → 본 폴더
  `f114-git-audit-uncommitted-exit-verification.patch`(171줄) ·
  `f114-git-audit-untracked-fresh-observation-retry.test.js.txt`(173줄). 디렉토리 자체는
  위임 범위 밖이라 존치.
- `/Users/jyp/Developer/t5-lanes/{ux,terminal-rebuild,terminal-stage1}` ·
  `/private/tmp/t5-check-271` · `/private/tmp/t5-terminal-core` — 앞 세션이 이미 제거,
  이번 세션 부재 재확인. worktree 목록은 본선 둘만 남음.

## 3. 범위 밖 관찰 (건드리지 않음)

- `codex/*` 로컬 브랜치 **31개** 잔존(automation-* · f64-* · f65-* 등) — 유산 감사 밖, 개수만 보고.
- `/Users/jyp/Developer/t5-lanes/terminal-parser-final-validation` — 이 저장소 worktree 목록에
  없는 별도 체크아웃(팁 6529787), 미커밋 2건(`src/runtime/terminal-run.js` ·
  `test/terminal-outcome-contract.test.js`) 있음. 위임 범위 밖.

## 4. 게이트 전후

- 전: 「프로젝트 진입 감사」 FAIL (앞 세션 관측 — 잊힌 작업이 있는 worktree·브랜치).
- 후(정리 직후 단독 실행): `PROJECT ENTRY AUDIT: PASS (105 active docs, 2 worktrees)`.
- 전체 `npm run gate`(정리 후 1회차): `✓ 프로젝트 정본·퇴역 문서·worktree 경계` — **목표 FAIL 해소 확인**.
  테스트 4297건 통과. 다만 §17 성능 2건 BLOCKED — CPU 108.1s > 기준 107.425s(0.6% 초과) ·
  유휴 26.0s > 20s. 이 해소 작업과 무관한 검사 성능 축(이번 세션은 제품·검사 코드를 한 줄도
  안 바꿈). 재실행(2회차)도 같은 2건 BLOCKED(CPU 112.3s · 유휴 27.3s) — 1회 노이즈가 아니라
  이 기계 현재 상태에서 재현되는 관측. 원인 규명(기준선 드리프트 vs 기계 부하)은 이 해소
  작업 범위 밖, 관측으로만 남긴다.
