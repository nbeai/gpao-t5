# 외과 수술 — 2026-08-16 (유산감사 처방 이행 · 제품 코드 0줄)

**근거**: `유산감사-2026-08-16.md`(커밋 `40170cd4`). **범위**: 감사가 잡은 인용 함정·지도 낡음·
장부 공백·검사끼리의 충돌·폐기물 표식. **제품 코드 동작 변경 0** · 브랜치 병합·삭제 0 ·
증거 원본 삭제 0 · 인계서(`design/NEXT-SESSION.md`) 불가침 유지.

## 0. 판정 기준선이 수술 중에 움직였다 — 먼저 읽어라

수술 1 커밋 직후 병행 세션(② 집계 범위 슬라이스)이 **선빨강 커밋 `7c19d859`** 를 올렸다
(`test/rank-claim-knows-unmeasured.test.js` — 4검사 · **의도된 빨강 2**). 그래서 이후 회귀는
**4,289건 중 4,287 통과 · 실패 2** 가 초록 기준선이다. 실패 2가 정확히 그 선빨강 두 검사임을
매 커밋마다 실행값으로 확인했다 — **이 수술이 만든 빨강은 0** 이다. 지시서의 「4,285 초록」은
착수 시점 값이고, 기존 4,285 는 전 구간 초록으로 유지됐다.

---

## 1. 수술 다섯 — 전/후 · 커밋

### ① 귀속표.mjs 무효 경고 — `cc10f7e5`

- **전**: 파일 안 grep 「무효」 0건(감사 실측). 무효 판정이 기준선 §7-n 과 인계서에만 있어,
  파일만 연 세션이 「16회 전부 :2822」류 표를 다시 만들 수 있었다.
- **후**: 파일 머리에 무효 경고 블록 — 무효 사유(판별 문자열이 요청 60건 전부에 있는 상수 ·
  정합 0건)와 정본 위치 셋(기준선 §7-n · 인계 §3-10 · 유산감사)을 명기. **코드 로직 불변.**

### ② 지도 §12 낡음 3건 — `cef64a70` (감사 확정분만 · 각 줄 근거 해시 명기)

- **J12 줄**: 전 `turn.js:687·3503·838·840` → 후 `696·3597(정직한답)·708·848·852(fallbackReplyFrom)`.
  이 세션이 grep 으로 재실측해 감사값과 일치 확인. 「자는 안 고쳤다」 서술에 자 재건
  사실(`31170dc9` · 기제별 정의역 6)을 이어 붙였다. J12 자체는 남은 5문항으로 **열림 유지**.
- **J5 줄**: 열림 → **닫힘(`bce1a42f`)** 표기. 거절 문구가 `DESKTOP_ACT_ACTIONS.join` 으로
  동적 나열(17동사 · `desktop-act-tool.js:527` 재실측). 지도 규칙대로 행은 지우지 않았다.
- **S6 줄**: 자리 `server.js:1041` → `automation-contracts.js:985·1094·1180 계열`(재실측).
  「유일 활성이면 무조건 선택」 **행동의 존속 여부는 미확인**이므로 위험도는 열림 그대로.

### ③ 장부 공백 이음 — `452c61a2`

- **전**: `design/T5-FOLLOWUP-LEDGER-ko.md` 가 F-106 에서 정지 — F-107~F-121 · J12 한 줄도 없음.
- **후**: 꼬리에 **포인터 블록 하나만** 추가 — 정본은 `기준선과-남은자리.md` §7 계열 + 커밋
  메시지 + 인계 §3-7(J12 자 현재값). **새로 서술하지 않았다**(두 진실 금지). 기존 항목 불변.

### ④ f101 병렬 거짓 빨강 — `6f7607d3` (검사 파일 안 수리로 닫음)

- **원인(기계 재현)**: 방 경로는 mkdtemp 로 고유한데 **가지 이름이 상수**(`lane-test/일하는방` 등).
  가지는 공유 저장소 한 곳에 서므로 gate·test 동시 실행 시 늦은 쪽이
  `fatal: a branch named 'lane-test/일하는방' already exists` — 감사가 본 그 문장 그대로,
  같은 이름 가지를 선점해 두는 것만으로 재현됐다(⑤ 검사 빨강).
- **수리**: `고유가지()` — 가지 이름에 pid+난수를 붙인다. 자(`audit-project-entry.mjs`)는 가지
  이름을 안 보므로 그물 불변. **제품 코드 불변.**
- **수리 후 실측**: 선점 시나리오 5/5 초록 · **동시 2벌 실행 둘 다 5/5 초록** · 전체 회귀 기준선 유지.
- 잔여: `lane-test/*` 떠돌이 가지 0개 확인(수술 후 `git branch --list` 실측).

### ⑤ tasks.mjs 폐기 경고 — `e6822258`

- **전**: 폐기 선언(기준선 §1 「개발자 시험이었다」 · 인계 §3-10)이 파일 밖에만 있었다.
- **후**: 파일 머리에 폐기 경고 블록 — 폐기 사유·정본 위치 셋 명기. 소비자
  (`measure.mjs`·`self-check.mjs`·`run-live.mjs`) 동작 불변(주석만 추가).

---

## 2. 안 한 것과 이유

| 안 한 것 | 이유 |
|---|---|
| /tmp 잔재 2 · 방치 방 3 삭제/정리 | 지시서 명시 금지 — 오너 결재 사항. 아래 §3 처분 권고만 적었다 |
| codex/*·lane/* 브랜치 병합·삭제 | 지시서 절대 금지 — 오너 결재 |
| `design/NEXT-SESSION.md` 수정 | 다른 에이전트가 재작성 중 — 불가침. 읽기만 했다 |
| 지도 §12 의 다른 열림 항목(F3·J7·J8·S6 행동 존속 등) 갱신 | 감사 확정분이 아니다 — 확정 3건만. 확인 안 한 것을 닫으면 그게 인용 함정이 된다 |
| J12 지도 행 자체의 서술 전면 재작성 | 감사 확정은 「줄 번호 낡음」뿐. 남은 5문항의 정본은 인계 §3-7 — 지도에 사본을 늘리지 않았다 |
| f101 외 검사의 병렬 안전성 전수 점검 | 감사가 재현한 결함은 f101 하나. 증상 없는 자리를 미리 고치지 않는다(관측≠결함) |
| 귀속표.mjs·tasks.mjs 파일 삭제 | 자기 정정의 기록이자 기준선 문서가 경로로 인용한다 — 표식이 처방이고 삭제는 아니다 |
| 선빨강 2(rank-claim) 손대기 | 병행 ② 슬라이스의 선등록 실물 — 남의 수술대다 |

## 3. 처분 권고 (건드리지 않았다 — 소유·나이 2026-08-16 재확인 실측 포함)

**/tmp worktree 2 (게이트 BLOCKED 원인 ①②)** — 둘 다 주 저장소 `~/Developer/gpao-t5` 소속 ·
소유 `jyp` · 8-14 이후 유휴(mtime 실측):

- `/private/tmp/t5-check-271` — detached HEAD(`271561c6` · 2일 전) · **작업 트리 깨끗**.
  권고: 주 저장소에서 `git worktree remove` — 잃는 것 없음(커밋은 저장소에 있다).
- `/private/tmp/t5-terminal-core` — `codex/terminal-core` 체크아웃 · ⚠️ **미추적 파일 1 존재**
  (`scripts/terminal-qualification/s2-live.mjs` — 어디에도 커밋 안 됨). 권고: 그 파일을 먼저
  회수(커밋 또는 사본)한 뒤 remove. 브랜치 자체는 worktree 제거로 안 지워진다.

**/tmp 잔재 2 (게이트 밖 · 소유 jyp)**:

- `/private/tmp/t5-f114-git-audit` — 독립 저장소 · origin=`~/Developer/t5-lanes/f114-exit-unsatisfied` ·
  마지막 커밋 3일 전 · ⚠️ **미커밋 수정 1**(`exit-verification.js`) + **미추적 검사 1**
  (`test/f114-fresh-observation-retry.test.js`). 커밋된 것은 origin 과 동기(ahead 0).
  권고: 두 실물의 필요 여부를 판정해 회수 후 삭제.
- `/private/tmp/t5-terminal-write-effect` — **죽은 worktree**(gitdir 이 소멸한
  `/private/tmp/t5-home-terminal-integration/…` 를 가리킴 · `git status` fatal 재확인).
  git 으로는 아무것도 복구 못 하고 파일만 남아 있다. 권고: 파일 내용이 다른 곳에 있는지
  훑은 뒤 삭제(worktree remove 는 불가 — 통째 rm 대상).

**방치 방 3 (게이트 BLOCKED 원인 ③④⑤ · 전부 소유 jyp · 마지막 커밋 나이 재확인)**:

- `../t5-lanes/terminal-rebuild` — 마지막 커밋 `99c3d32b` **2일 전** · 본선 밖 7커밋
- `../t5-lanes/terminal-stage1` — 마지막 커밋 `2bacd15c` **2일 전** · 본선 밖 8커밋(rebuild 포함)
- `../t5-lanes/ux` — 마지막 커밋 `c694d44a` **4일 전** · 본선 밖 12커밋 · 감사 실측 충돌 0

권고: 유산감사 §4-2·3 의 오너 결정과 같은 자리다 — 내용 판정(살릴 것 병합 / 버릴 것 선언) 후
방 제거. ux 계열은 충돌 0 이라 판정 비용이 가장 싸다. **이 보고서는 권고만 하고 아무것도 안 지웠다.**

---

## 숫자 (전부 이 세션 실행값)

- 수술 커밋 5: `cc10f7e5` · `cef64a70` · `452c61a2` · `6f7607d3` · `e6822258` — 전부 푸시됨
- 회귀: 매 커밋 전 실행 — 착수 시 4,285/4,285 → 선빨강 `7c19d859` 유입 후 4,287/4,289
  (실패 2 = 그 선빨강 · 매회 동일 확인)
- f101 재현→수리: 선점 재현 빨강 1 → 수리 후 같은 시나리오 5/5 · 동시 2벌 5/5·5/5
- 제품 코드(src/) 변경: **0줄**
