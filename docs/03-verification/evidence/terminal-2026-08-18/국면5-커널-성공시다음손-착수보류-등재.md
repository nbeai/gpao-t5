# 국면 5 · 커널 「성공했을 때도 다음 걸음 재료를 싣는다」 — **착수 보류 등재**

> 기준 시점: HEAD `c644bcee` · origin/main `11634ed0` · 미푸시 4 · 2026-08-18 01:15 KST
> 재는 법: `git rev-parse --short HEAD` · `git rev-list --count origin/main..HEAD` · `git status --short`
> 이 문서는 **완료 보고가 아니라 멈춤 사유 등재**다. 제품 코드 변경 0 · 라이브 회차 0.

## 1. 멈춘 이유 — **피어 세션이 살아 있고, 지금 이 트리에서 회차를 돌리고 있다**

오너 브리프 순서 0은 「작업트리에 쌓인 CH4 회차 폴더를 커밋·판정·정직 봉인하라」였다.
그 전제가 **틀렸다**. 그 폴더들은 버려진 잔재가 아니라 **진행 중인 배치의 중간 산출물**이다.

```
기계 사실 (2026-08-18 01:11~01:15 · ps · ls 로 직접 밟음)
  01:11  node scripts/live/browser-network-round.mjs …판정-v2-07-n2 --case n2-in-review   (pid 56636)
  01:15  node scripts/live/browser-network-round.mjs …판정-v2-08-n3 --case n3-in-notice   (pid 57004)
  두 프로세스의 PPID = 85096 = /Applications/ChatGPT.app/…/codex app-server (ELAPSED 6일 · 데몬)
  ⇒ 회차마다 새 node 를 띄우는 **살아 있는 Codex 개발 세션**이다. 고아 프로세스가 아니다
  진행도  v2 회차 폴더 8개(01-g1 … 08-n3) · RECOVERY_CASES 는 10개
          (browser-network-round.mjs:28-37 — g1~g5 recovery · n1~n5 control)
  ⇒ 배치는 **8/10 진행 중**. 남은 것은 n4-in-intro · n5-in-detail-method
```

**인계서(design/NEXT-SESSION.md:27)가 이 배치의 주인을 이미 적어 놓았다** —
「★ 다음 개발 세션은 **코덱스**가 받는다(오너 2026-08-17)」.
그 세션이 지금 살아서 자기 슬라이스를 돌고 있다.

## 2. 그래서 순서 0 을 내가 하지 않는다 — 근거 다섯

```
① 배치가 안 끝났다        8/10. 지금 커밋·판정하면 **끝나지 않은 배치를 봉인**하는 것이다
② 주인이 있다            CH4 흐름 ① 슬라이스는 Codex 세션의 것이다(인계서:27 · 오너 지정)
③ 파일이 겹친다          이 슬라이스가 흡수해야 할 `src/runtime/browser-tool.js` 는
                        CH4 선등록 §2 공정감시자 S 의 **소유 파일**로 이미 선언돼 있다
                        (…/국면5-ch4-막힘행선지-선등록.md:33). 지금 고치면
                        공정감시자 §5 즉시 중단 신호 「같은 파일의 동시 수정」이다
④ 전례가 있다            인계서 §② 「한 방에 손이 둘」 — 이 계보에서 **실제로 난 사고**다
                        (전임이 살아 있는데 후임이 개봉해 지난 자리를 「지금」으로 적음)
⑤ 오너 규율             브리프 §6 「다른 손이 살아 있을 수 있다 · 내 것이 아닌 미커밋 변경은
                        지우지 말고 손대지 마라」 · 「두 슬라이스를 겹쳐 열지 마라」
```

⇒ **미커밋 v2 회차 8건에 손대지 않았다.** 스테이지 0 · 삭제 0 · 이동 0.

⚠️ **`npm test` · `npm run gate` 를 돌리지 않았다.** 같은 기계에서 피어의 라이브 회차가
측정 중이다 — 전체 회귀를 얹으면 CPU 를 뺏어 **피어의 회차값을 오염**시킨다.
회귀는 트리가 한 손일 때 단독으로 잰다.

## 3. 읽기 전용으로 세운 것 — 커널 칸의 선빨강 근거 (전부 직접 밟음)

브리프의 실측을 그대로 믿지 않고 다시 쟀다. **셋은 맞았고, 하나는 절반만 맞았다.**

```
✔ 확인   src/kernel/l1-intent/task-context.js:1126
         ...(실패 && r.nextSafeAction ? { nextSafeAction: r.nextSafeAction } : {}),
✔ 확인   src/kernel/l1-intent/task-context.js:1242-1243
         ...((r.failureState ?? 'none') !== 'none' && r.nextSafeAction
           ? { nextSafeAction: r.nextSafeAction } : {}),
✔ 확인   src/kernel/capabilities.js:106   out.push('', '## 막혔을 때', '', …)
✔ 확인   grep -rl "nextSafeAction" src/ | wc -l  →  43 (코드 42 + web/index.html 1)
```

**★ 성공 갈래 손실의 실물 한 건 — 이것이 이 칸의 선빨강이다**

```
src/runtime/browser-tool.js:162-163  (browser.observe 의 **성공** 결과)
  ...(facts.moreBelow
    ? { nextSafeAction: `… 화면 아래쪽이 남아 있어요 — 더 내리면 …` }
    : (stopNote ? { nextSafeAction: stopNote } : {})),
  · 이 반환값에는 blocked 도 failureState 도 없다 — 성공한 관찰이다
  · 손은 다음 걸음 재료(아래가 남았다 = 이어 읽을 자리)를 **이미 쥐고 있다**
  · 영수증에는 실린다(src/kernel/l0-evidence/tool-receipt.js:57 은 무조건 싣는다)
  · 그런데 **모델 입력 패킷**은 1126·1242 두 관문에서 실패가 아니라는 이유로 **버린다**
  ⇒ 손이 쥔 사실이 모델에게 안 간다. 「막히면 어디로」는 가고 「이어서 어디로」는 안 간다
```

**정정 — 브리프 §2 의 local-locate 서술은 절반만 맞다**(§4-c ③ 발견한 세션이 그 자리에서 정정)

```
브리프    "nextSafeAction 은 `고른것.length === 0` 일 때만 붙는다"
실측      src/runtime/local-locate.js:894
          ...(고른것.length === 0 || (물었나 && !짚었나) ? { nextSafeAction: 이름으로골라 } : {}),
          ⇒ 조건은 **둘**이다. 빈손일 때 + 물었는데 못 짚었을 때.
          오너 라이브(정확 일치 3건)는 고른것>0 이고 짚었나 참이라 **두 조건 다 벗어나** 안 붙는다.
          결론(그 발화에서 안 붙는다)은 같지만 **조건 서술이 다르다** — 수리 설계가 이 위에 서면 어긋난다
✔ 확인   src/runtime/local-locate.js:886
          : 정확한것.length > 1 ? `이름이 정확히 맞는 자리가 ${정확한것.length}곳이에요.`
          ⇒ 세기만 하고 경로를 지목하지 않는 갈래는 실재한다(브리프 그대로)
```

**⚠️ 쓰지 않는 숫자** — 정적 스캔으로 「비실패 문맥 32자리」를 얻었으나 **버린다.**
문맥 4줄 정규식이 model-doctor·connector-connect 등 명백한 실패 갈래를 비실패로 오분류했다.
재는 자가 틀리면 재는 것이 전부 거짓이다 — 이 숫자는 등재하지 않는다.
확정 숫자는 위 43 과 **밟은 실물 한 건**(browser-tool.js:162-163)뿐이다.

## 4. 남긴 것 — 다음 손이 이어받을 자리

이 슬라이스는 **선등록도 하지 않았다**(자를 못 박아 두면 겹쳐 여는 것이 된다).
트리가 한 손이 되면 §4-b 1번(선빨강)부터 정상 착수한다. 그때의 출발 재료는 위 §3 이다.

```
착수 조건   ① Codex 배치 10/10 종료 ② CH4 흐름 ① 판정·정직 봉인 커밋 등재
           ③ 트리에 살아 있는 node 회차 프로세스 0 (ps 로 확인)
첫 걸음     browser-tool.js:162-163 의 성공 갈래 nextSafeAction 이 모델 입력 패킷에
           도달하지 않는 것을 라이브/검사로 기계 등재(선빨강)
두 벌 방지   CH4 가 browser-tool.js 안에 지은 「막힘 행선지」는 **아직 한 벌**이다.
           손별 특례로 갈라지기 전에 커널 칸으로 흡수한다 — 이 순서는 유효하다
⛔ 유지     CU(desktop.*) 정의역 밖 — 근거 인용도 금지(§1-0)
```
