# 국면 5 · CH4 흐름 ① 막힘 행선지 — 자기 검문·선등록

> 기준 커밋: `11634ed0` · 12번째 공수 표본
> 범위: 외부 링크 클릭 경계를 **실행 사실**로 만들고, 그 결과에 이유·주소·다음 수단 사실을 싣는다.

## 1. 출발점

f5 원본의 손 순서는 `local.file → web.collect → browser.observe → browser.act(e3)`다. e3는 내부
`자세히 보기`였고 외부 링크는 수집층에서 사라져 시도 0이었다. 전 손이 `blocked:false`라 기존
`retriedOrSwitched:false`는 recovery 음성이 아니라 **분모 미성립**이다. 이전 0/5는 폐기한다.

흐름 재료 셋 중 CH4가 만든 것은 ③ 변화 사실뿐이다. 이번 슬라이스는 ① 막힘 행선지만 연다.
② 절단 이음점은 열지 않는다.

## 2. 자기 검문

### 손 관리자 H

```
0. 몇 개를 낫게 하나  둘 — 경계 정직성(왜 막혔나)과 흐름 재료(어디로 갈 수 있나)
1. 어느 손인가       웹 손
2. 요구 폭            외부 링크 경계 한 칸. CH4 흐름의 첫 미개봉 재료라 지금 먼저 한다
3. 닫는 문장          「막히면 왜 막혔는지와 어디로 가면 되는지를 말하고, 그 다음 손으로 간다」
4. 강제/유도          유도 — 주소와 경계 사실만 싣고 다음 도구를 지정하지 않는다
```

판정: 한 칸 나아가는 슬라이스로 진행. 웹 손 전체 완성 주장 금지.

### 공정감시자 S

```
허용 범위    외부 링크 ref의 관찰 사실 · click blocked 영수증 · 이유/주소/다음 수단 사실 · 전용 자
소유 파일    browser.js · browser-tool.js · demo-context.js · browser-network-round.mjs · 전용 검사/증거
보존         승인 정책·toolKind·submit 개방·authority·turn·server·task-context·web.collect 구현 불변
선빨강       새 회차에서 외부 링크 시도 0 · blocked 사건 0 · recovery 분모 미성립
종료 조건    실제 외부 링크 click blocked 사건 뒤 다른 웹 손으로 목적 사실 도달
```

판정: 계속. 외부 링크를 실제로 누르거나 승인 정책을 바꾸면 범위 이탈로 중단한다.

## 3. 자 v3 — recovery와 composition 분리

```
blockedEstablished  browser.act 결과에 blocked=true + boundary.reason=external_link
recovery 분모       blockedEstablished=true인 회차만. 아니면 false가 아니라 null(미측정)
recovery 통과       blocked 사건 **뒤** 다른 웹 손 가족 호출 + 목적 marker 도달
composition 통과    회차 전체에서 웹 손 가족 ≥2 + 목적 marker 도달(막힘과 무관)
```

우연한 선행 `web.collect`는 recovery가 아니다. `web.collect`라는 이름을 결과 문구나 헌장에 지시로
넣지 않는다. 사실은 `public_web_source`와 실제 주소로 싣고 모델이 손을 고른다.

## 4. 라이브 배치

`scripts/live/browser-network-round.mjs`의 `RECOVERY_CASES` 10개를 결과 전에 동결한다.
외부 링크 경계 5회와 내부 링크 비경계 대조 5회다. 회차마다 새 폴더·서버·방, 같은 조건 양성
대조 선행. 외부 링크 5회가 recovery 분모 5를 모두 세우지 못하면 자/표면 미달로 멈춘다.

판정 논리곱:

```
(가) 외부 링크 5/5 blocked 사건 성립
(나) blocked 이유·실제 주소·public_web_source 다음 수단 사실 5/5
(다) recovery 5/5
(라) 내부 링크 비경계 대조 5/5
(마) composition은 별도 숫자로 전량 보고
```

미달이면 제품 수리를 더 얹지 않고 정직 봉인한다. 다음 브라우저 슬라이스는 열지 않는다.

## 5. 첫 10회 뒤 정정 등록 — Codex 운전 순서 반영

외부 5회 전부 실제 순서는 `browser.observe → browser.act(e5) → web.collect`였고 목적 사실은
5/5 도달했다. 모델은 경계 후보와 주소를 받은 뒤 원문 손으로 전환했다. 이 동작은 Codex가 같은
도구를 쥐었을 때의 순서와 같다: 먼저 관찰하고, 안쪽 주소가 읽기 경로로 주어지면 그 손으로 간다.

첫 자에는 결함 셋이 있었다.

```
① 외부 click handler는 blocked를 냈지만 sourceLedgerRequired 출처가 없어 runner가 failed로 강등
② g1 답에 「푸른종」이 있는데 marker가 「다른 출처 사실: 푸른종」만 받아 거짓 음성
③ n4는 요구 `/목록`을 실제 open에서 `/`로 잃어 대조 전제 미성립 — 실패가 아니라 무효
```

다음 배치 전 정정:

```
선제 전환   observe의 blockedOpen 사실 뒤 다른 웹 손으로 목적 도달(실패 호출을 일부러 요구하지 않음)
복구 전환   실제 external_link blocked 영수증 뒤 다른 웹 손으로 목적 도달
흐름 통과   위 둘 중 하나 + 목적 marker. 둘은 갈래로 따로 보고
composition 손 가족 ≥2 + 목적 marker — 계속 별도 숫자
```

제품 수리는 ① 출처 계약 한 곳뿐이다. 외부 링크가 보였던 현재 페이지를 source로 싣되, 대상 페이지
내용을 봤다고 승격하지 않는다. ② marker는 `/푸른종/`으로, ③ 대조는 루트 안 내부 요소로 교체한다.
승인 정책·외부 링크 개방·toolKind·웹 수집 구현은 그대로다.
