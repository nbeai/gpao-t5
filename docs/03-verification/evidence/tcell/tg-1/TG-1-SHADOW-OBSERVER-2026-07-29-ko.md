# TG-1 · 관찰층 shadow mode (2026-07-29 · Claude 단일 구현선)

- 신규: `src/surface/tcell-store.js`(TCellObserver — record/observeTurn/load).
  공유 파일 수정: `server.js` 3블록(관찰자 조립 · runTurn 뒤 훅 · import) — 추가만, 기존 행 변경 0.
- 저장: `<데이터 자리>/growth/observations.jsonl` append-only(명세 §6). schemaVersion 필수,
  손상 줄은 격리하고 나머지를 살린다. **어떤 코드도 이 파일을 읽어 TaskContext 에 넣지 않는다.**
- 투영: 이번 턴의 새 영수증(ledgerStart 이후)만 → tool_result 관찰(+실패 영수증은 recovery 관찰),
  승인/거절 → approval/rejection 관찰. 원문·비밀 없이 userSafeSummary 와 참조만.
- hot path 격리: 훅은 await 하지 않으며 try + Promise.catch 로 **동기·비동기 실패 모두** 차단.
  실측: 반대시험이 동기 throw 관찰자가 턴을 죽이는 결함을 잡아 이 이중 방어가 들어갔다.

## 완료 증거 (명세 TG-1 검사 4건 전부)
1. 관찰 생성·기록 실패가 답변을 실패시키지 않음 — 디스크 불능(record) + 죽은 관찰자(서버 관통) ✓
2. secret 원문 0 — 요약/참조만 기록, 관찰 파일에 비밀 패턴 부재 검사 ✓
3. 같은 receipt 중복 이벤트 방지(1회만 기록) ✓
4. 영향 0 — 커널 파일들이 관찰 저장소를 참조하지 않음(참조 검사) + 서버 관통에서 답변 불변 ✓
- 검사 4건 신규 · 전체 회귀 **1231건 통과** · 게이트 **PASS**(CPU 21.4s) — 자체 검증.

---

# 감사 보강 반영 (2026-07-29 · 2차) — 고정 종료 조건 9개 전부

**① 사실의 신분·생산 경로**
- receipt 참조 = **원장 위치**(`ledger:세션:번호`) — 가짜 id fixture 제거, 실제 ToolReceipt 그대로 작동.
- 승인/거절 = 서버가 pendingApprovals 로 **유효성 판정한 결정만** 관찰(`유효한결정`). 옛/가짜 ID 관찰 0.
- 정정 생산자 = 구조화 신호(기억 되돌리기 `/memory/rollback` · 학습 기본 대상 되돌리기 `/patterns/rollback`)
  에서 행동 사실+참조만. 발화 기반 정정 추출은 TG-3(모델 추출)의 몫으로 명시.
- 자동화 = 엔진 결과 경계(tick 후 `ran`)에서 `automation:job:exec:번호` 신분으로 투영.

**② 지속 중복 방지**
- 완료 키는 **append 성공 뒤에만** 등록(실패 후 재시도 영구 소실 결함 폐쇄) · 진행 중/완료 키 분리 ·
  쓰기 직렬화(promise 큐) · 재시작 시 기존 로그에서 완료 키 복원(로그 = 지속 인덱스, 키 퇴출 없음).

**③ privacy·읽기 경계**
- 비밀 표식 → 저장 전 일반화 문장으로 교체, 파생(recovery) 관찰까지 containsSecret+비가독 상속.
- 파일 0600(생성 mode + 기존 파일 chmod). schema 불량 줄은 JSON 이 맞아도 격리(corrupted).
- 기본 조회 scope(sessionId) 필수 — 범위 횡단 기본 차단. 전체 읽기는 `loadAllForAudit` 분리.
- 읽기 오류는 빈 저장소로 위장하지 않고 error 로 정직 보고(ENOENT 만 빈 저장소).

종료 조건 9개 반대시험 매핑: 조건1(재시도·재시작·신분) · 조건2(가짜 승인 0/실승인 1, 서버 관통) ·
조건3(정정·자동화 생산 경로) · 조건4(비밀 원문 0·파생 비가독) · 조건5(0600) · 조건6(schema 격리) ·
조건7(scope 차단) · 조건8(답변 비차단·영향 0 유지) · 조건9(전체 회귀 **1233건** · 게이트 **PASS** 23.4s).

---

# 재감사 보류 1건 반영 (2026-07-29 · 3차) — 만료 승인 관찰 0

- 원인 그대로 수정: 서버의 "대기 목록 존재" 판정(유효한결정)을 **폐기**하고, 커널이 실제로
  소비한 결정만 결과 사실(`result.approvalConsumed`)로 싣는다 — 승인 재개는 만료·미발견 검사를
  **통과해 executePlan 에 실제 도달한 경우에만**, 거절은 실제 대기 항목을 지운 경우에만
  (유령 ID 거절 소비 표식 구멍도 함께 폐쇄). 관찰은 그 소비 사실만 본다.
- 반대시험: 만료 승인 클릭 → "만료됐어요" + **관찰 0** · 실거절 소비 → rejection 정확 1 ·
  유령 거절 → 0 · 실승인 → approval 정확 1(기존 조건2 검사 유지).
- 검증: 관찰 검사 8건, 전체 회귀 1235건 — 첫 실행에서 1건 실패가 났으나 대상 미특정 상태로
  재현 불가(이후 전체 4회 + 관찰 파일 5회 연속 0 실패, 타이밍 요동으로 판단·정직 기록).
  게이트 PASS(CPU 22.4s) — 자체 검증.
