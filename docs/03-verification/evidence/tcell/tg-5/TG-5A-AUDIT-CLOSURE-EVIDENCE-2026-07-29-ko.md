# TG-5A §0-C 감사 종료 4건 — 보강 증거

- 작성: 2026-07-29 · Claude 구현선 · **자체 검증**(독립 감사 대기)
- 대상: 인수인계 §0-C(Codex BLOCKED 판정)의 종료 조건 4건 — **한 단위로 구현·제출**(오너 지시)
- 착수 전 선언: `docs/03-verification/T5-TG-5A-AUDIT-CLOSURE-WIRING-2026-07-29-ko.md`
- 오너 고정 조건 준수: 정규식·한국어 목록 확대 0건 / 모르는 project 는 null / grant 실버튼·철회 관통 /
  의미 결합 단일 계약 / 4건 일괄 제출 / 게이트 PASS 전 TG-5B 미진입

---

## 1. 4건의 닫힘 상태

| # | §0-C 종료 조건 | 상태 | 구현 |
|---|---|---|---|
| 1 | 실제 project 신분 | **닫힘**(자체 검증) | `currentPlaceOf(workingState)` — G 행렬이 확정한 「지금 자리」 사실의 **단일 계산 자리**(프롬프트 `지금 자리:` 줄과 같은 원천). `anchorFor`·`supplyAdmissionSources`·`buildTurnFacts.project`·grant scope 전수 교체. **자리를 모르면 null — 추측하지 않는다** |
| 2 | 자유문 경계·현재 지시의 의미 결합 | **닫힘** | 단일 계약 = `FACT_ATOMS`(turn-facts 가 사실을 **생산할 때 쓰는** 원자 어휘) + 추출 **모델**이 자유문을 원자에 결합(`cell.binding`) + 반대 지시는 추출기 관계 판정을 **지시 문장을 열쇠로** 지속(`recordDirectiveRelation`) → `judgeDirective` 가 **이번 턴 지시에만** 적용(부록 A P1). admission 은 순수·동기 유지(핫패스 모델·정규식 0) |
| 3 | bounded grant 생산·철회·공통 대상 신분 | **닫힘** | 승인 카드 [이번만 승인]·[승인하고 24시간 계속 허용] 실버튼(`grantKind`) → 소비 시 원장 기록 → 재사용은 **(손·행동) 단위 면제**(`ctx.grantExempt`) → `/grants` 목록·[철회] 버튼. 신분은 (손·**실제 행동 종류**·대상·범위) 네 요소(부록 A P0) |
| 4 | 확인 원장 오류의 정직한 승계 | **닫힘** | `ConfirmationStore`: ENOENT 만 정상 부재, 그 외 읽기 오류는 **던짐**, 손상 줄은 세어 `degraded:true`(정상 줄은 사용, 바이트 보존). 스냅샷 경계가 degraded 를 `trace.status` 로 승계 |

## 2. 오너 지정 검증 6종

| 검증 | 결과 |
|---|---|
| **실제 OpenAI·Anthropic 어댑터** | **와이어 계층만 검증됨.** `makeProviderModelClient` 실코드가 실제 형식(chat/completions·`/v1/messages`)으로 요청을 보내고 응답을 해석하는 것까지가 이 시험의 범위다. **모델의 답은 시험이 미리 만든 JSON 이므로 의미를 모델이 판단한 증거가 아니다**(감사 P2 지적 — 이전 판의 "실제 모델 검증" 표현은 과장이었고 여기서 정정한다). 실제 판단 검증은 `test/tcell-live-model-semantics.test.js` 가 담당하며, 이 기계에는 두 계열 자격이 모두 없어 **4건 전부 미실행(skipped)** 이다 |
| **서로 다른 프로젝트 2개** | 생산 관통: 터미널 영수증(subjectOf)→workingState 자리→관찰 anchor→세포→admission. 자리A 세션은 원리A 만 읽고 원리B 는 **조회 자체를 안 한다**(scopeFiltered=1). 자리 미확정 첫 턴은 project=null → `scope_unknown`(추측 0) |
| **승인·재사용·철회 버튼** | HTTP E2E: [계속 허용]→발송 1→원장 bounded(범위=실제 자리)→같은 요청 **재확인 0·발송 2·`grantsReused` 표면 고지**→[철회]→재확인 부활·무단 발송 0. [이번만]은 원장 0건·재확인 유지. 자리 미확정이면 [계속 허용]을 눌러도 권한 미생성 |
| **저장 장애 주입** | 원장 자리에 디렉터리(EISDIR)→`snapshot()` 던짐→admission trace `degraded`. 손상 줄+정상 줄→정상분 사용+`degraded:true`+바이트 보존. 복구 후 즉시 깨끗(손상 비캐시) |
| **전체 회귀** | 부록 A 기준 **1,333건 통과 · 실패 0** |
| **고아 계약** | 통과 — 새 export(`FACT_ATOMS`·`grantTargetOf`·`recordCorrection` 등) 전부 소비, 유예 추가 0건 |

## 3. 보강 전 실패 실측 (v3.1 §19.1 L2-①)

기준선 `b7ffa6a` worktree 에 기준선 API 만 쓰는 축소판을 넣어 실행했다
(재현 파일: `BEFORE-0C-REPRO.test.js.txt` — 이 폴더에 보존).

| 검사 | 보강 전(`b7ffa6a`) | 보강 후(현재) |
|---|---|---|
| §0-C-1 관찰 project=실제 자리 | **실패** (store.dir 였다) | 통과 |
| §0-C-2 같은 뜻 경계 매칭 | **실패** (`boundary_not_satisfied`) | 통과 |
| §0-C-3 `/grants` 존재 | **실패** (라우트 없음) | 통과 |
| §0-C-4 읽기 불능이 던짐 | **실패** (빈 원장 위장) | 통과 |

**4/4 실측.** stash 방식은 신규 파일이 남아 import 붕괴만 보여서 쓰지 않았다(전 세션 정정 준수 — 측정은 worktree 로만).

## 4. 게이트·성능

- 게이트: **BLOCKED 1건** — CPU 53.8s/40s. 그 외 전 항목 통과(후속 표현 15 유지 · 의존성 0).
- 이번 변경 A/B(같은 머신 연속): `b7ffa6a` **52.21s**(1,318) ↔ 현재 **53.42s**(1,329).
  +1.21s 는 새 검사 11건(서버 6회 기동 + wire 서버 2계열)의 시험 자체 비용이다 — 핫패스 회귀 아님.
  admission 핫패스에 추가된 것은 원자 문자열 비교(동기)뿐이다. 조용한 환경 재측정은 여전히 종료 조건.
- 지시문 예산: 사실 어휘(원자 8개·원자당 60자 상한)와 고정 지시문(1000자 상한)을 **갈라서** 검사
  (`tcell-extraction-wire.test.js` — 어휘가 대본 비대를 가리지 않게).

## 5. fixture 폴백 전수 (§19.1 L2-③)

이번 변경 주변: `supplyAdmissionSources`·`anchorFor`·grant 경로는 `deps` 폴백을 **새로 만들지 않았다.**
서버의 기존 `deps.tools ?? demoTools()` 등은 시험 주입용이며, 이번 시험은 전부 실제 저장소
(`TCellRegistry`·`TCellObserver`·`ConfirmationStore`·세션 파일)를 쓴다. 라이브가 데모로 새는 신규 지점 0.

## 6. 정직한 한계와 설계 결정 (감사가 먼저 볼 것)

1. **반대 지시의 의미 판정은 다음 턴부터, 그리고 같은 지시가 다시 올 때만 유효하다.**
   판정은 모델이 있는 자리(턴 후 추출)에서 내려져 **지시 문장을 열쇠로** 지속된다.
   열쇠는 정규화 후 같은 문장일 때 맞는다 — 표현이 크게 달라지면 다른 열쇠이고, 그때는
   추출기가 그 표현으로 다시 판정한다(어미 목록을 늘리지 않는다). 이 한계는
   `tcell-audit-closure.test.js` 가 사실로 못 박고 있다.
2. **원자 어휘는 8개다** — turn-facts 가 실제로 생산하는 사실만. 생산되지 않는 원자는 죽은
   어휘이므로 넣지 않았다. 어휘 확장은 turn-facts 의 생산 확장과 함께만 유효하다.
3. **[계속 허용]은 24시간 session 하나다.** persist(영구)는 TG-5C 표면 몫으로 남겼다 —
   원장·조회는 이미 지원한다(`GRANT_REUSABLE_KINDS`).
4. **grant 재사용은 실제 승인 흐름을 바꾼다**(shadow 밖 P-OP 영역). 이는 T-cell 영향이 아니라
   절대 원칙 0-A-1 「bounded grant 안의 반복 실행은 다시 묻지 않는다」의 최초 배선이며,
   오너가 이번 지시로 명시 승인했다.
5. **일회성·범위 한정·장기 철회의 수명 차이**(감사 P1 종료 조건): 일회성은 그 지시가 온 턴에만
   적용되고, 범위 한정은 세포 anchor(project)가 이미 가르며(다른 자리 세포는 조회조차 안 된다),
   장기는 사용자가 그 지시를 확정해 관련 턴마다 다시 공급될 때 성립한다. 셋 다 **지시의 등장**이
   수명을 정하므로 별도 만료 장치를 만들지 않았다.

## 7. 판정

- §0-C 4건: **자체 검증 완료 · 독립 감사 대기.**
- 남은 차단: 조용한 환경 공식 gate 재측정(행렬 10) 하나.
- TG-5B: 진입 금지 유지.
- 운영 순환 칸: ②(실제 자리 사실) · ⑤(bounded grant 실경계) · ⑥(grant 원장·확인 원장 정직성).
  ⑨ 도달: **승인 카드 버튼·허용 범위 목록·철회 버튼·재사용 고지가 웹 표면에 실제로 추가됐다**
  (`index.html` — 이 부분은 shadow 가 아니라 P-OP 표면이다).
- 모델 판단 침범: 없음 — 의미 판정을 정규식·목록으로 대체하지 않고 모델(추출기)에 두었다.

---

# 부록 A · 감사 P0~P2 7건 보강 (2026-07-29 후속)

위 §0-C 보강이 **권한 경계에 더 큰 결함을 만들었다**. 그 판정(BLOCKED)에 대한 보강 기록이다.
7건은 서로 다른 증상이지만 **뿌리가 하나**다: **사실이 있는데 대용물을 신분으로 썼다.**

| 건 | 사실이 사는 곳 | 내가 쓴 대용물 | 닫은 방식 |
|---|---|---|---|
| **P0** grant 가 다른 행동을 같은 권한으로 봄 | `grant.kind`(authority 가 `fileKind` 로 판정) | 도구 ID | 권한 신분 = (손 · **실제 행동 종류** · 대상 · 범위) 네 요소. `kind` 없으면 키도 없다 |
| **P1** 자리가 옛 프로젝트를 집음 | `workingStateFacts` 의 현재 대상 집합 | subjects 첫 경로(옛것 포함) | 현재 대상이 자리를 말하거나, 현재 대상이 옛 자리 **안**일 때만 이어간다. 무관하면 null |
| **P1** 계속 허용 버튼의 빈 약속 | grant 생성 가능 여부 | 항상 표시 | `grantOffer` 사실로 버튼 노출 결정 + 눌렀는데 못 만들면 `grantOutcome` 으로 이유 고지 |
| **P1** 반대 지시가 영구 차단 | 이번 턴의 지시 | 저장된 플래그 하나 | 관계를 **지시 문장을 열쇠로** 저장(경계 `binding` 과 같은 모양). 그 지시가 다시 올 때만 적용 |
| **P1** 화면의 원시 ID | `toolLabel`·채널 라벨·`HUMAN_TARGET` | `g.action`/`g.target` 원문 | 서버가 사람말로 투영. 조회 키도 **불투명 손잡이**로 바꿔 도구 id 자체를 화면에서 제거 |
| **P2** 원장 구조 손상 | 확인 기록 계약 | JSON 파싱 성공 | 계약(kind·tcellId·at·sourceRefs·confirmed) 검증 실패도 손상으로 계수 |
| **P2** 모델 검증 과장 | 실제 모델 판단 | 시험이 쓴 대본 | 문서 정정 + `tcell-live-model-semantics.test.js` 신설(자격 없으면 **미실행로 기록**) |

**이 코드베이스는 P0 를 이미 한 번 치렀다.** `action-plan.js` `fileKind` 주석:
「도구가 아니라 작업으로 판정한다 — 같은 도구가 읽기도 삭제도 하므로 도구 단위로 kind 를
고정하면 삭제가 organize 로 새어 안전 바닥을 건너뛴다(실사용에서 확인)」.
나는 grant 신분을 도구 ID 로 만들며 그 매듭을 되돌렸다.

## 보강 중 시험이 잡은 내 실수 둘 (지적받지 않은 것)

1. **폴더 유도 폴백이 추측이었다.** 자리를 못 찾을 때 파일 경로에서 상위 폴더를 만들었더니,
   후보가 여러 곳이라 **아직 고르지도 않은 턴**(`place_candidates`)에서 "지금 자리"를 지어냈다.
   기존 회귀 검사가 즉시 잡았고 제거했다 — 선언된 자리가 없으면 모르는 것이다.
2. **조회 키가 화면으로 새고 있었다.** 라벨을 아무리 잘 만들어도 `grant:local.file:write:…`
   키가 응답에 실려 도구 id 가 그대로 나갔다. 내가 쓴 검사가 그것을 잡아 불투명 손잡이로 바꿨다.

## 보강 전 재현 실측

기준선 `25242ef` worktree 에서 기준선 API 만 쓰는 축소판 실행
(재현 파일: `BEFORE-P0-REPRO.test.js.txt`).

| 검사 | 보강 전(`25242ef`) | 현재 |
|---|---|---|
| P0 write ≠ delete 권한 신분 | **실패**(같은 키) | 통과 |
| P1 무관한 새 대상에 옛 경로 비사용 | **실패**(옛 경로 반환) | 통과 |
| P1 과거 반대 지시가 무관한 턴 비차단 | **실패**(영구 차단) | 통과 |
| P2 계약 틀린 확인 기록 비조회·degraded | **실패**(정상 조회·ok) | 통과 |

**4/4 실측.** 나머지 3건(버튼 약속·라벨·모델 과장)은 새 계약·표면이라 "부재"이지 "실패"가 아니다.

## 검증 수치

- 전체 회귀 **1,332건 통과 · 실패 0**
- 감사 재현 시험 신설: `tcell-audit-closure.test.js` 14건(P0 파일 쓰기↔삭제 관통 포함)
- 실모델 의미 검증: **4건 미실행**(OpenAI·Anthropic 자격 없음) — 조용히 통과시키지 않았다

## 남은 미충족 (정직하게)

- **실제 두 모델의 독립 의미 판정**: 미실행. 두 계열 자격이 이 기계에 없다. 하네스는 배선했고
  자격이 주어지면 그대로 돈다. 이 종료 조건은 **미충족**이며, 충족 전에는 §0-C-2 를
  "실제 모델로 검증됨"이라고 말하지 않는다.
- **공식 게이트 CPU**: 여전히 기준선 초과(기계 부하 지배). 조용한 환경 재측정이 종료 조건.
- 따라서 **TG-5A 봉인·TG-5B 진입 모두 불가**를 유지한다.

---

# 부록 B · 감사 3회차 7건 (2026-07-29)

부록 A 보강이 남긴 것과 새로 드러난 것. **뿌리는 부록 A와 같다** — 신분을 대용물로 썼다.
다만 이번에는 **내 P0 수정이 닿지 않은 세 자리**가 드러났다: 걸음 경로의 `허락한손`,
되풀이 판정의 `지문`, 그리고 표면으로 나가는 값들.

| # | 문제 | 원인(내가 남긴 대용물) | 닫은 방식 |
|---|---|---|---|
| 1 | write 승인 뒤 같은 손 delete 가 실행 0·승인 0으로 조용히 종료 | ① 되풀이 `지문`이 `path`만 보고 `action`을 버림 ② 걸음 경로 `허락한손`이 **손 단위** ③ 멈춘 이유가 모델·사용자 어디에도 안 감 | 지문 = 대상+**작업** · 허락 신분 = (손·**행동**) · `stoppedBecause`를 모델 사실로 + 폴백 뒤 런타임 고지 |
| 2 | 실모델 검사가 결합·관계 없이도 통과 | 시험이 "던지지 않으면 통과"였다 | 결합 0건·관계 null 이면 **실패**. `insufficient_evidence`를 통과로 세지 않음 |
| 3 | 실모델 검사가 추출만 지나고 생산 경로 미검증 | `extractCandidate` 직접 호출 | **실서버 턴**으로 관찰 생산 → 실모델 추출 → 저장 → **다음 턴 admission**까지 관통 |
| 4 | 표현 변형에서 원리가 입장하는 동작을 정답으로 고정 | 알려진 공백을 단언으로 굳힘 | 그 단언 **삭제**. 대신 "관계가 지속되면 그 표현으로도 막힌다"는 메커니즘만 검증 |
| 5 | `grantsReused`에 도구 ID·원시 대상·내부 키 | 커널 실행 신분을 그대로 응답에 실음 | 서버가 `/grants`와 **같은 계약**으로 사람말 투영 |
| 6 | 서로 다른 파일이 모두 `확인된 대상` | `HUMAN_TARGET` 실패 시 단일 문구 | 파일은 `상위/이름`, 원시 id는 `번호 …끝4자` — **구별 가능**하되 원문 비노출 |
| 7 | 계획 승인과 걸음 승인의 범위 허용 표면이 다름 | 판정이 두 곳에 있었음 | `범위허용가능()` 한 자리에서 두 경로가 같은 계약을 낸다 |

## 보강 중 회귀가 잡은 내 실수 둘

1. **지문을 반대로 과교정했다.** 인자 전체를 신분에 넣었더니 probe 가 덧붙인 부수 칸 때문에
   **같은 명령이 서로 다른 일**이 되어 되풀이 방지가 뚫렸다(`recovery-failure-injection`·
   `tool-steps` 가 즉시 잡음). 대상 한 칸 + 작업 한 칸으로 좁혔다.
2. **멈춤 문구를 폴백 앞에 붙였다.** 답이 비지 않게 되어 `fallbackReplyFrom` 이 안 돌았고,
   **실제로 막힌 사실**("수집을 허용하지 않아요")이 통째로 사라졌다(`boundary-invariants` 가 잡음).
   폴백 뒤로 옮겼다.

## 보강 전 재현 실측

기준선 `fd765d4` worktree 에 이번 시험 파일을 넣어 실행
(재현 파일: `BEFORE-ROUND3-REPRO.test.js.txt`).

| 검사 | 보강 전(`fd765d4`) | 현재 |
|---|---|---|
| #1·#7 write→delete 새 승인 + 걸음 grantOffer | **실패**(조용한 종료) | 통과 |
| #5·#6 원시 ID 비노출 + 대상 구별 | **실패** | 통과 |
| #1 반대 방향(같은 행동 반복은 한 번만) | 통과 | 통과(유지) |

#2·#3 은 실모델 자격이 있어야 도는 시험이라 이 표에 세지 않는다. #4 는 잘못된 단언의 **삭제**다.

## 검증 수치

- 전체 회귀 **1,336건 통과 · 실패 0 · 미실행 4**
- 공식 게이트 **PASS** — CPU **23.1s / 40s** · 벽시계 11.3s (Codex 재측정 23.4s 와 일치)
- 감사 시험 파일 `tcell-audit-closure.test.js` 18건

## 남은 미충족

- **#2·#3 의 종료 조건(실제 OpenAI·Anthropic 연결로 의미 판정 성공 관찰) — 미충족.**
  하네스는 생산 경로 끝까지 배선했고 자격만 있으면 그대로 돈다. 자격이 없어 **4건 미실행**이며,
  이 항목이 닫히기 전에는 §0-C-2 를 "실제 모델로 검증됨"이라고 쓰지 않는다.
