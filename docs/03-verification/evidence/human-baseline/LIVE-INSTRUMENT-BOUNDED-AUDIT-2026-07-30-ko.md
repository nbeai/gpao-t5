# 비교 계측기 고정 범위 감사 (Claude · 1회)

- 대상: `3413c6d`(구현) · `5493ade`(인수인계)
- 범위: 종료 보고 §1의 네 항목 + 기존 성립 회귀. 범위 확장·해법 제안 없음.
- 재개봉 기준: 통과 근거 허위 / 안전 경계 붕괴 / 핵심 성공 조건 무효화 / 후속 판정 왜곡의
  실제 재현만. 그 외는 후속 관찰.
- 판정: **PASS**

## 네 항목 반증 결과

| 항목 | 반증 입력 | 결과 |
|---|---|---|
| 1. HOME 격리 + 실제 사용자 시야 | 격리 홈에서 `ls ~/Downloads`·`ls ~/Developer`·`ls ~/.hermes` | 실제 Downloads·Developer 내용 확인, `.hermes` 부재. 위조 심링크(다른 대상) 사전 배치 → `prepare_user_view` 예외로 차단. OpenClaw 프로브도 임시 홈 격리로 이동 확인 |
| 2. 정본 기계 대조 | `H06.time`에 1자 추가 | 전 원문 SHA-256 불일치로 계약 FAIL. provenance 16/16 필수(`sealed` 13·`extension` 1·나머지 sealed)이며 H04.undo(`방금 기억한 보고서 형식 선호는 취소해줘`)·H05.restart(10월 이어서, B8 숫자 대화 재개)가 봉인 실측과 일치함을 감사자가 원문 대조로 재확인. verify_run이 21행 전부의 원문·출처·분기·세션을 정본과 재대조 |
| 3. 기존 회차 폴더 불변 | 봉인 파일 2개를 둔 `hm-run-1` + 가짜 secret로 같은 번호 실행 | exit 거부, 갱신 파일 0, 경로·메타 해시 동일 (직전 감사 P1-1 재현 소멸) |
| 4. 제품 fixture 접촉 = 제품 결과 | 생성 fixture에 같은 inode로 내용 덧붙임 → 정리 | `content_modified_by_product / snapshotted_and_removed`, 스냅샷에 변경 내용 보존, preserved 0 — 회차 유효 유지. `unsafe_cleanup_failure`만 무효 조건임을 코드·시험으로 확인 |

## 기존 성립 회귀

- `audit:compare-live` PASS(9분기·21턴) · 전체 회귀 **1,227/1,227** · 두 dry-run exit 0에
  Downloads·작업 디렉터리·프로세스 변화 0 · 무과금 preflight **34/34 VALID** 독립 재실행
  (음성 대조·유령/가짜 재개 차단·실제 재개 성공 포함) · 잔여 프로세스 0.
- 제품 `src/`·T-cell 코드 변경 0. 직전 감사에서 성립한 수명주기·소유권·실패 위장 차단은
  변경부에서 유지 확인.
- 공식 gate 성능 항목의 환경 차단(CPU 53.5s, 기준 커밋도 51.97s)은 종료 보고의 분리 판단을
  검토했고 재개봉 조건에 해당하지 않는다(코드 순증 ~1.5s는 비교 반대시험 8건 추가분, 기준선
  변경 없음).

## 후속 관찰 (차단 아님)

1. 분기 **순서 의미**(H04 앞 H01 등)는 실행 전 계약이 아니라 사후 `verify_run`에서만 탐지된다
   (재현: B3의 H01/H04 순서 교환 → 실행 전 계약 PASS). 잘못된 순서는 유료 회차를 소모한 뒤
   무효 판정된다 — 판정 왜곡은 없고 비용 위험만 있다.
2. 제품이 실제 `~/Developer`에 쓰기 가능한 시야를 갖는다. 이는 봉인 T5 기준선과 같은 조건이며
   시나리오 정의에 내재한다. H08의 원본 보존 측정은 fixture 소유권이 담당한다.
3. 실제 도구 다단 턴의 완료 신호 행동은 여전히 회차 1에서만 실측된다(기존 관찰 유지).

## 판정과 다음 단계

**PASS.** 재개봉 4조건에 해당하는 재현이 없다. 합의된 종료 조건에 따라 다음 단계는 준비 보강이
아니라 **오너 자격 입력 한 번 → 유료 회차 1 → 구조 유효성·실제 완료 신호·호출/토큰/비용 실측**이다.
