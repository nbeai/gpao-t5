# F-53 재조사 — 발신 성공 사례는 실재한다 · 문제를 재정의한다 (2026-08-09 · PM 긴급)

PM 전제 폐기 지시에 따라 git log 전량·evidence·원장을 다시 훑었다. **오너 스크린샷이 맞다.**

## ① 성공 회차 — 찾았다 (원문 그대로)

```
commit c614d89  2026-08-06 20:10:01 +0900
  feat(cu): 노드 ②·⑥ 닫힘 — 지시한 메시지가 실제로 카톡에 갔다

  "오너가 준 문장 그대로 n.BEAI 사일런트서비스 방에 전송됐다(화면 확인, 오후 8:03).
   카톡은 AX 요소가 0인 창이다 — 눈으로 보고 자리를 짚어 끝냈다."
```
오너 스크린샷의 **오후 8:03 과 커밋 시각(20:10)·방 이름이 일치**한다. 별도 시험 경로(판 문항
아님)이고, 문구가 판 시험 문구와 다른 이유도 그것이다.

**경로**: 제품 턴(모델이 사다리를 올라 좌표로 짚음). 그 커밋이 넣은 것이 바로 그 사다리다 —
`screen-guidance.js`(노드 ②: 보고 → 짚어서 → 하고 → 확인 · "이 앱은 못 다룬다로 끝내지
않는다") + `image-size.js`(노드 ⑥: **모델이 짚을 자 = 그림 크기**).

## ② 차이를 가른다 — 그때 vs 오늘

| 축 | 2026-08-06 (성공) | 2026-08-09 (실패) |
|---|---|---|
| 대상 지정 | **좌표(x·y)** — 눈으로 보고 짚음 | **label** 만 · 거절 뒤에도 x·y 재시도 0 |
| 카톡 창의 AX | **요소 0개** — 이름으로 짚을 길이 아예 없었다 | **요소 18개** — 이름으로 짚을 길이 "있어 보였다" |
| 자(그림 크기) | 있음 | **있음**(원장: *"네가 본 화면 860×1328 … 짚을 자리는 이 안에서"*) |
| 사다리 가르침 | 있음(그 커밋이 도입) | **있음**(코드 살아 있음 · 회귀 아님) |
| 창 상태 | 방을 여는 것까지 포함해 끝냄 | 목록 화면에서 방 열기부터 막힘 |

**회귀가 아니다.** 자도 사다리도 그대로 있다.

## ③ 오늘 실패의 직접 원인 — 원장이 말한다

오늘 카톡 목록 창의 요소 18개를 모델이 **받았다**. 그런데 그 안에 **방 이름이 없다**:
```
- TextArea: 3개의 채팅방 · TextArea: 넵 명심하겠습니다 · TextArea: 바이오클리어 최고!!!… ·
  Cell(label:"") · Button(label:"버튼") …
```
**나오는 것은 각 방의 마지막 메시지뿐이고, 방 이름("n.BEAI 사일런트서비스")은 요소로 안 나온다.**
그래서 모델이 고른 `대상.label = "n.BEAI 사일런트서비스"` 는 **존재하지 않는 이름**이었고,
드라이버가 옳게 거절했다("Either element_index + window_id or x + y must be provided").

8/6 은 요소가 **0개**라 모델이 처음부터 좌표로 갔다. 오늘은 요소가 18개 **있어서** 이름 길이
있어 보였고 — **그 목록에 방 이름만 없다는 사실**을 모델이 알 수 없었다.

## ④ F-53 재정의 (PM 3번 지시)

- **"발신 불가"가 아니다.** 발신은 2026-08-06 에 실제로 됐다(제품 턴 · 좌표 경로).
- 남은 병은 **방 선택 하나**: *목록 화면에서 원하는 방을 여는 것*. 그리고 그 병의 정체는
  **"요소 목록에 방 이름이 없다"** 는 화면 사실이다 — 이름으로는 영영 못 짚는다.
- 자연스러운 다음 수(설계는 판정 뒤): 이름으로 못 짚는 것이 확정된 화면에서 모델이 **좌표로
  가는 길을 고르게** 하는 것. 재료는 이미 다 있다(그림·자·사다리) — 없는 것은 "이 목록에
  네가 찾는 이름이 없다"는 **사실**이다(관측 결과에 그것을 적을 수 있는지가 설계 자리).

## ⑤ 내 오류도 함께 적는다
나는 M2·F-53 원장만 보고 "발신 미성립"이라고 보고했다. 8/6 성공은 **다른 폴더의 커밋**에
있었고 나는 그 시각을 안 훑었다 — F-56(관측≠부재)을 나도 어겼다. 부재를 주장하려면 전수
확인을 대야 한다는 규칙이 바로 이 자리에 있다.
