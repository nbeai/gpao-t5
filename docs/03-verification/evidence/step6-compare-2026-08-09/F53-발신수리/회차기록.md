# F-53 발신 수리 — 원인 2층 확정 · 발신 미성립 · **내가 남긴 창 하나** (2026-08-09 오너 창)

설치본 0.1.0 · sha 76779e03 → 7bca67af(2층 수리 후) · 상태만 격리 · gpt-5.1 ·
허가 방/문구 외 발신 0(불변 지킴).

## ① 원인 2층 — **확정됐다**
1층 수리 전(회차1): *"그 동작을 실행하지 못했어요."* — 사유 0(예전 그대로).
1층 수리 후 진단면에서 실사유가 처음 나왔다:
```
"Either element_index + window_id or x + y must be provided."
```
→ **드라이버는 label 만으로는 못 누른다.** 카톡 목록은 글자로 못 읽는 창이라 요소 번호·토큰이
   안 나오고, 모델은 label 로만 짚었다 — 두 달간 "방을 못 연다"였던 것의 정체다.

2층 수리(같은 창 안에서): 드라이버가 거절을 **throw** 해 위층 거절 갈래가 영영 못 봤다 →
네 갈래 모양(`effect:'refused'`)으로 돌려주게 고쳤다. 회차2 원장에서 확인:
```
act 대상 {"label":"n.BEAI 사일런트서비스"} · fail: failed
summary: "그렇게 하지 못했어요 — Either element_index + window_id or x + y must be provided."
```
**사유가 사용자면·모델면까지 닿는다**(1·2층 모두 실증). 옛 봉인 5개는 새 계약으로 이행(사유 병기).

## ② 발신 — **미성립**(원장·실물 일치)
회차2·3 모두 모델이 x·y 재시도를 안 골랐다(사유와 남은 길을 받고도). 발신 0.
카톡 실물 전후 동일 — 허가 방 마지막 메시지 "넵 명심하겠습니다"(8월 7일) 그대로.

## ③ ⚠ 내가 남긴 것 — 창 하나 (F-56 절차대로 기록)
```
16:25:41  개발 라인이 **직접** 드라이버 호출(제품 원장 아님) — 목록의 방 행 좌표 더블클릭
          첫 시도: 앞 창이 아니어서 unverifiable · 화면 변화 0
          앞세운 뒤 재시도 → **"조용한 채팅방" 창(4733)이 열렸다 — 의도한 방이 아니다**
이후      닫기 시도 4회 전부 실패(press_key cmd+w = Unknown key name · hotkey/click 은
          effect unverifiable · 창 그대로)
```
- **발신 0 확정**: 그 창은 폴더 **목록 뷰**다(캡처 `오대상-조용한채팅방.jpg` — 입력창·전송 없음).
  허가 문구는 어디에도 입력되지 않았고, 어떤 방에도 메시지가 안 갔다.
- **오너 실홈 변경 0** · 파일 변경 0.
- **남은 것**: 카톡에 "조용한 채팅방" 창이 열려 있다. 제 손으로 못 닫았다 —
  **오너가 그 창의 닫기 버튼을 눌러 주시면 원상복구된다**(다른 조치 불필요).
- 성격: 제품(T5)이 아니라 **내가 직접** 만든 상태다. 제품 원장에는 이 행위가 없다.
  좌표 짚기가 사람 손보다 거친 자리라는 사실도 함께 남는다(오대상 위험 실증 — 승인 실질
  원칙이 "상대를 카드에 보여야 한다"고 한 이유가 여기서 실물로 나왔다).

## 남은 것 (판정 요청)
- 원인 2층은 닫혔다. **발신 성립은 아직** — 모델이 사유를 받고도 x·y 길을 안 고른다
  (F-54 와 같은 계열: 재료는 닿았고 행동이 안 따라온다 · 1표본 2회).
- 승인 실질 설계는 **첫 발신 카드가 실물로 뜨는 순간**이 측정 조건인데 그 순간에 못 닿았다.

---

# 방 열기 뚫림 · 입력 성립 · 전송은 미확인 (2026-08-09 오너 창 · "니가 눌러" 지시)

## 뚫린 것 — **방 선택**(두 달 막혔던 자리)
```
get_window_state{max_elements:60, max_depth:12}   ← 깊이 제한이 핵심
  → 요소 11개 · 그중 ★ {"label":"넵 명심하겠습니다","token":"s0000001c:5"}
double_click{element_token:"s0000001c:5"} → 창 4806 "n.BEAI 사일런트서비스" 열림 · 오대상 0
```
**정체 확정**: 그동안 label 로 못 짚은 이유는 이름이 없어서가 아니라 **AX 트리 전체 걷기가
20초 타임아웃**(드라이버 원문: *"AX tree walk for pid=1048 timed out after 20 s … re-call
with a depth-limited scan"*)이라 요소가 0~18개로만 보였기 때문이다. 깊이 제한을 주면 산다.
방 이름 자체는 목록에 없고 **그 방의 마지막 메시지**가 그 행의 신분이다.

## 성립한 것 — 입력(제품 턴)
방이 열린 상태에서 제품 턴 실행: 승인 카드 뜸(승인 전 실행 0) → 승인 뒤 실행 →
**입력창에 허가 문구가 그대로 들어감**(캡처 `후-실물-방.jpg` — 전송 버튼 활성).
모델의 답도 정직했다: *"입력만 시도한 상태라 실제 전송 여부는 확인해 주세요."*

## 전송 — 눌렀으나 **미확인**
방 창은 AX 요소 0(8/6 기록과 같다: *"카톡은 AX 요소가 0인 창"*), zoom 도 실패해 좌표를 못
세웠다. 그래서 **Enter**(foreground)로 보냈다:
```
press_key{key:"Return", window_id:4806, delivery_mode:"foreground"}
  → { delivery: foreground, effect: "unverifiable", route: "global_input" }
```
그 뒤 확인이 **전부 실패**: zoom(4806·52) 둘 다 ScreenCaptureKit 캡처 오류 ·
get_window_state 둘 다 요소 0. 창 목록만 정상 응답.
→ **전송됐는지 기계로 확정하지 못했다. "보냈다"고 적지 않는다**(절대 게이트: 거짓 성공 금지).
오너 화면에서 그 방의 마지막 메시지를 보시면 즉시 갈린다.

## 오늘 F-53 지도
| 단계 | 상태 |
|---|---|
| 방 선택 | **뚫림**(토큰 · 깊이 제한 스캔) — 수리 설계 근거 확보 |
| 입력 | **성립**(제품 턴 · 승인 카드 정상) |
| 전송 | 눌렀음 · **확인 불가**(드라이버 캡처 계통 응답 불가) |

---

# ★ 발신 완주 — 제품 턴이 방 선택 → 입력 → 전송을 끝냈다 (2026-08-09 20:51)

설치본 0.1.0 · **sha d6f208ab**(깊이 제한 폴백 포함) · 상태만 격리 · gpt-5.1 ·
허가 방/문구만 · 오너가 입력창을 비운 뒤 시작(사람 개입 기록 · F-56).

## 실물 — 갔다
오너 스크린샷(오후 8:52 화면) + 내 캡처(`후-전송확인.jpg`) 일치:
```
n.BEAI 사일런트서비스 · 오후 8:51
  "지파오가 테스트 중입니다. 응답 안해주셔도 됩니다.지파오가 테스트 중입니다. 응답 안해주셔도 됩니다."
목록 화면의 그 방 미리보기도 같은 문구로 갱신됨
```

## 원장 — 제품 턴이 했다
```
턴1 approval  카드 impact: "카카오톡 · n.BEAI 사일런트서비스 · 그 칸 에 글자 넣기"
턴2 approval  카드 impact: "카카오톡 · n.BEAI 사일런트서비스 ·
                            \"지파오가 테스트 중입니다. 응답 안해주셔도 됩니다.\" 보내기"
턴3 reply     desktop.screen:observe → desktop.act:type(값=허가문구) → desktop.act:press_key(return)
```
**승인 전 실행 0**(카드 뜬 턴들의 손 호출 0) · 오대상 0(카드·원장·실물 모두 그 방) ·
오너 실홈 변경 0.

## ★ 승인 실질 원칙 — **이미 서 있다**(실물로 확인)
카드가 **손 이름이 아니라 상대·내용**을 보여 준다: *"카카오톡 · n.BEAI 사일런트서비스 ·
「…」 보내기"*. F-53 이 함께 받기로 한 원칙(카드는 액션이 아니라 실질로)의 **절반이 이미
구현돼 있었다** — 남은 것은 "같은 상대 두 번째는 다시 안 묻는다"(기억의 열쇠가 상대)뿐이다.
그 자리는 이번 측정 조건(첫 발신)에서 잴 수 없다 — 별도 회차가 필요하다.

## 미흡 둘 (정직 부기)
1. **문구가 두 번 붙었다.** 앞 회차(회차6)에서 카드가 두 번 뜨는데 내 대본이 승인을 한 번만
   보내, **첫 승인(글자 넣기)만 실행돼 입력창에 문구가 남았다.** 오너가 비운 뒤 회차7이
   다시 넣어 전송하는 사이 잔여분과 합쳐졌다 — **내 대본의 승인 처리 미흡**이지 제품 결함이
   아니다(회차7 원장의 type 값은 문구 한 번뿐). 다음 회차는 승인 루프를 갖춘 대본으로 잰다.
2. **모델은 "확인 못 했다"고 말했다** — 그 순간 zoom 이 실패해 사실이었다(거짓 성공 아님 ·
   오히려 정직). 확인 계통이 불안정한 것은 별개 관측이다.

## F-53 지도 — 닫힘 후보
| 단계 | 상태 |
|---|---|
| 방 선택 | **닫힘** — 깊이 제한 폴백 + 토큰(수리·봉인 3 · 게이트 초록) |
| 입력 | **닫힘** — 제품 턴 · 승인 카드 정상 |
| 전송 | **닫힘** — 제품 턴 · 실물 실재(오너 스크린샷 + 캡처) |
| 승인 실질 | 절반 확인(카드가 상대·내용) · 나머지(같은 상대 재확인 금지)는 별도 회차 |
