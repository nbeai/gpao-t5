# 6단계 비교 R2 — ⑫ 정산 비교 요약 (2026-08-09 · 2회차)

규약 §10 · 같은 고정물(지문 R1 과 동일 — 러너가 이중 대조) · 같은 발화 · 각자 격리 방.
러너: `scripts/compare-live/step6-round.mjs` (이번 회차부터 저장소의 자로 쟀다).
**채점하지 않는다. 라벨 초안만 단다**(확정은 PM · §10-5 조항 2).

## 기계 사실

| 제품 | 모델 | 걸린 | 실물 | 실물의 숫자 | 오너 바탕화면 이탈 | 고정물 사후 |
|---|---|---|---|---|---|---|
| T5 | gpt-5.1 | 28.7s | 1개(방 Desktop) | 4파일 값 전부 참(2,110,000·520,000·2,120,000·310,000) · **월 합산 없음** | 없음 | 손상 0 · 사라짐 0 |
| Hermes | gpt-5.1 | 162.0s | 1개(방 Desktop) | 합계 참(7월 2,630,000 · 8월 2,430,000) + 거래처별 | 없음 | 손상 0 · 사라짐 0 |
| OpenClaw | — | — | **미측정** | — | — | — |

- T5 손 호출 9회: locate · list×2 · read×4 · capsule · write. 부기: `GPAO-T5/기억.md` 도 새로 생김(시키지 않은 파일 — 내용은 회차.json).
- OpenClaw 미측정 사유: `openclaw-config/구성확정.md` — 기본(최선) 구성이 방에 안 가둬져
  실물이 오너 실제 바탕화면에 생긴다(프로브 + R1 재현). **not_comparable(격리 불성립).**

## 정직 부기 — 무효 1회(같은 파일 `무효이력` 칸에 원본 보존)

Hermes 첫 실행이 932ms 에 끝났다. 기계 사실:

> `No usable credentials found for provider 'openai-api'. Set OPENAI_API_KEY.`

R1 회차기록이 적어 둔 그 자리(죽은 구성으로 세우기)다. auth.json 사본만으로는 부족했고,
제품이 요구한 환경변수(OPENAI_API_KEY — T5 와 같은 저장된 자격)를 준 뒤 완주했다.
하네스 결함이지 제품 결함이 아니므로 Hermes 만 다시 세웠다(유효한 T5 기록은 그대로).

## 라벨 초안 (PM 확정 대상)

- **T5 ↔ Hermes(같은 모델 · 주축): `equal` 초안** — 둘 다 실물을 방 바탕화면에 냈고
  둘 다 숫자가 참이다. 차이: Hermes 는 월 합산+거래처별 비교까지, T5 는 파일별 값·차이까지
  (합산 없음). R1 의 패인(정산내역 누락)은 이번엔 없었다. 합산 유무를 우열로 볼지는 PM 몫.
- T5 ↔ OpenClaw: **not_comparable** (격리 불성립 — 구성확정.md).
