# ⑫ 재측정 R2 — T5 수리 sha (2026-08-09) · Hermes 짝은 기존 R2

규약 §10 · 같은 고정물(지문 이중 대조 통과) · 같은 발화 · 새 방·새 세션. T5 sha `ef48484`.
짝은 기존 `../../R2-item12/회차.json` 의 Hermes(gpt-5.1 · 162.0s · 실물 방 Desktop ·
합계 2,630,000/2,430,000 + 거래처별). **라벨 초안만 단다**(확정은 PM).

## 기계 사실 (T5 · 이번 실행)

| 걸린 | 실물 | 실물의 숫자 | 고정물 사후 | 오너 바탕화면 |
|---|---|---|---|---|
| 22.5s | 1개 · **방 Desktop**(`Desktop/지난달_이번달_정산비교.txt`) | 4파일 값 전부 참 · 각 항목에 파일 범위 명시 · 월 합산 없음 | 손상 0 · 사라짐 0 | 이탈 0 |

- 손 9회: locate · list×2 · (캡슐 안 read×4 + write). 부기: `GPAO-T5/기억.md` 생성.
- 정직 부기 ①: 실물에 시키지 않은 "거래처 수" 열이 있고 값이 전부 `NaN` 이다 — 다만 답이
  그 사실을 스스로 공개했다(*"그 부분은 숫자로 제대로 못 뽑아서 NaN으로 표시된 상태야"*).
  금액 값은 전부 참이다.
- 정직 부기 ②: 서버 기동 시 모델 프리플라이트가 한 번 `unreachable` 을 찍었으나(기계 사실 —
  러너 로그) 본 턴은 정상 완주했다(22.5s · reply). 일시 연결 흔들림이고 회차 무효 사유
  (기계 사실 기준)에 해당하지 않는다.
- 기존 두 패인(자리 이탈·한정 없는 총) 재현 없음.

## 라벨 초안 (PM 확정 대상)

- **T5 ↔ Hermes(주축): `equal` 초안** — 기존 R2(PM `equal` 확정)와 같은 모양: 둘 다 방
  바탕화면 실물 + 참 숫자, Hermes 는 합산까지·T5 는 범위 명시 부분형(139초 빠름).
  NaN 열 부기를 감점으로 볼지는 PM 몫.
