# ⑫ 재측정 회계 — T5만 수리 sha ×3 · Hermes 기존 3회 유지 (2026-08-09)

PM 라벨확정(`../PM-라벨확정-2026-08-09.md`)의 수리·재측정 회계 그대로:

- **T5**: 수리 sha 위에서 3회 새로 쟀다(새 방·새 세션·재실행 0 · 같은 고정물 —
  러너가 R1 지문과 이중 대조). 러너: `scripts/compare-live/step6-round.mjs
  --item 12 --round N --products t5 --out …/재측정-item12` — 기존 회차 원본
  (`../R1-item12` 등)을 덮지 않으려고 out 을 분리했다(불삭제).
- **Hermes**: 기존 3회 유지(고정물 결정론·해시 동일 — 재호출은 낭비이자 재굴림 위험).
  짝: 재측정 R1↔`../R1-item12` Hermes · R2↔`../R2-item12` Hermes · R3↔`../R3-item12` Hermes.

## sha 계보 (짝 회계 병기)

| 쪽 | sha | 내용 |
|---|---|---|
| T5 기존 3회 | `c22485f` 이전 본선 (미달 판정 원본) | ../R1·R2·R3-item12 |
| **수리** | `84f1538` 얼굴 2(다른 집 표준 폴더 절대 경로 → 홈 앵커) · `640693c` 얼굴 1(호명→숫자 대조) | src + 봉인 6건 |
| **T5 재측정 3회** | `ef48484` (수리 둘 + F-50 장부 원인 확정 포함 · 측정 시점 HEAD) | 이 폴더 R1·R2·R3-item12 |
| Hermes | 오너 설치본 그대로(두 sha 무관 · gpt-5.1) | 기존 회차 원본 |

## 세 회차 요약 (기계 사실 — 원본은 각 회차.json)

| 회차 | 실물 자리 | 실물의 숫자 | 걸린 | 고정물 사후 | 오너 바탕화면 |
|---|---|---|---|---|---|
| R1재 | **방 Desktop** | 4파일 값 전부 참 · 파일 범위 명시 부분형(월 합산 없음) | 24.5s | 손상 0 | 이탈 0 |
| R2재 | **방 Desktop** | 4파일 값 전부 참 · 파일 범위 명시 부분형 · 거래처 수 NaN 부기(답에서 스스로 공개) | 22.5s | 손상 0 | 이탈 0 |
| R3재 | **방 Desktop** | **월 합계 2,630,000 / 2,430,000 정확**(구성값 병기) | 16.6s | 손상 0 | 이탈 0 |

미달 사유 둘의 재현: **자리 이탈 0/3** (기존 R3 얼굴) · **숫자 거짓 0/3** (기존 R1 얼굴 —
한정 없는 "총"에 부분합을 붙인 회차 없음). 라벨 초안은 각 회차기록에 — 확정은 PM.
