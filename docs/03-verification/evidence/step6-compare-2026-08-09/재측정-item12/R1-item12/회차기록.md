# ⑫ 재측정 R1 — T5 수리 sha (2026-08-09) · Hermes 짝은 기존 R1

규약 §10 · 같은 고정물(지문 R1 원본과 동일 — 러너 이중 대조 통과) · 같은 발화 · 새 방·새 세션.
T5 sha `ef48484`(수리 `84f1538`+`640693c` 포함) · **Hermes 는 재호출하지 않았다** —
짝은 기존 `../../R1-item12/회차.json` 의 Hermes(gpt-5.1 · 38.8s · 실물 방 Desktop ·
합계 2,630,000/2,430,000). **라벨 초안만 단다**(확정은 PM).

## 기계 사실 (T5 · 이번 실행)

| 걸린 | 실물 | 실물의 숫자 | 고정물 사후 | 오너 바탕화면 |
|---|---|---|---|---|
| 24.5s | 1개 · **방 Desktop**(`Desktop/2026-07_08_정산_비교요약.txt`) | 4파일 값 전부 참(2,110,000·520,000·2,120,000·310,000) · 각 항목에 파일 범위 명시 · 월 합산 없음 | 손상 0 · 사라짐 0 | 이탈 0 |

- 손 9회: locate · list×2 · (캡슐 안 read×4 + write). 부기: `GPAO-T5/기억.md` 생성(기존 회차들과 동일).
- **기존 R1 패인(범위 선택) 재현 없음**: 한정 없는 "총"에 부분합을 붙이지 않았다 — 모든
  합계가 파일 이름으로 범위를 밝힌다(동결 채점 기준의 통과형: *"범위를 명시한 부분 숫자는 통과"*).
- **기존 R3 패인(자리 이탈) 재현 없음**: 실물이 방 바탕화면(Desktop)에 생겼다.
- 정직 부기: 월 기계 합(2,630,000/2,430,000)은 실물에 안 실렸다. 수리 그물이 이 모양을
  무는 것은 오프라인 재현으로 확인했다(그물이 두 달 전체 합을 사실로 되돌려 준다) —
  모델이 받고도 범위 명시 부분형을 유지했다. 되부름이지 차단이 아니므로 계약대로다.

## 라벨 초안 (PM 확정 대상)

- **T5 ↔ Hermes(주축): `equal` 초안** — 둘 다 실물을 방 바탕화면에 냈고 둘 다 숫자가
  참이다(Hermes 는 월 합산까지, T5 는 범위 명시 부분형 · T5 가 14초 빠름). 기존 R2 를
  `equal` 로 확정한 그 자와 같은 모양이다. 합산 유무의 우열은 PM 몫.
