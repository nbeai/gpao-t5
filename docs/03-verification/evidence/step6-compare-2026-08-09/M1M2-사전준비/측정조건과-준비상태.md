# M1·M2 실물 회차 — 측정 조건과 준비 상태 (2026-08-09 02:58~ · 6단계 과업 2)

## 안 된 것 먼저 — 화면 잠금으로 **미측정** (기계 사실)

M1·M2 는 실물 화면 회차다(판 §2-b 소관표: 카드사 화면·카톡 읽기 = 2번 손).
02:58 착수 시점의 기계 사실:

```
CGSessionCopyCurrentDictionary: CGSSessionScreenIsLocked = 1 · onConsole = 1
CGWindowList(onScreenOnly): Chrome·KakaoTalk 창 0개 (잠금 화면이 전부 가림)
전체 화면 캡처: 잠금 화면(암호 입력 필드) — 화면잠금-0258.jpg
```

잠금 해제는 오너의 암호다 — 측정 라인이 할 수 없고 해서도 안 된다(오너 실사용 불가침).
판 §2-b 의 규칙 그대로: **선행 조건이 서기 전에는 미측정 — 통과도 실패도 아니다.**
화면이 열리면 아래 준비물로 회차당 명령 하나로 잰다.

## 준비 완료 (전부 기계로 밟음)

| 준비물 | 상태 | 증거 |
|---|---|---|
| PM 판정 셋 기록 | 완료(측정 전) | `../PM-판정-2026-08-09.md` |
| 러너 실물 모드(m1·m2) | 완료 · 문법검사 통과 | `scripts/compare-live/step6-round.mjs` |
| Hermes 관찰 전용 울타리 | 완료 · **관통 실측** | `scripts/compare-live/cua-observe-guard.mjs` |
| M1 카드 고정 페이지 | 완료 · 서버 127.0.0.1:4777 위 | `M1-카드페이지-고정물.html` (sha1 b284d7a6…) |
| M1 크롬 실탭 | 열림(창 id 282043879) | 잠금 해제 후 화면 확정 스크린샷 필요 |
| T5 설치본 | /tmp/t5-pkg3-Gofr/package · src 대조 diff exit 0 | 5단계 real-rounds 와 같은 산출물 |

### M1 고정 숫자 (측정 전 고정)
- 정산 파일(1번 손 소관): 8월 매출정산 2,120,000 + 8월 정산내역 310,000 = **2,430,000원**
  (러너 동결 고정물 — 6단계 ⑫와 같은 지문)
- 카드사 화면(2번 손 소관): 고정 페이지 "2026년 8월 카드 승인액 합계: **570,000원**"
  (가상의 카드사 '성심카드' · 127.0.0.1 로컬 서버 — 실금융 화면 금지 준수)
- 두 출처 합: **3,000,000원**

### Hermes 화면 손 울타리 — 프로브 실측 (03:01 · gpt-5.1 호출 1회)
격리 방(HOME=방·.hermes 사본) + `HERMES_CUA_DRIVER_CMD=울타리` 로 관찰 발화를 넣었다:
- 모든 `tools/call` 이 울타리 stdio 를 지나갔다 — Hermes 가 드라이버 데몬을
  `--no-permissions-gate --dangerously-bypass-approvals` 로 띄워도 길목은 울타리다.
- `list_windows` 관찰이 잠금 상태에서도 동작(창 목록 답변) — **배관은 선다.**
- 오분류 1건 발견·수정: Hermes 는 개시 때 `set_config{max_image_dimension}` ·
  `set_agent_cursor_enabled{false}` 를 부른다(세션 부기 · 화면 입력 아님). 수정 전
  울타리가 set_config 를 중단급으로 세워 파수를 찍었다 — 실회차였으면 상대의 정상
  개시를 죽였다. 허용으로 정정(원본 파수 파일 보존: `hermes-울타리-프로브-파수(수정전-set_config오분류).txt`).
- 울타리 원본 로그: `hermes-울타리-프로브-울타리로그.txt`

## 화면이 열린 뒤 — 회차 명령 (재현 절차)

```
# 준비 확인: 크롬 카드 페이지 탭 실물 + 카톡 목록 창 → 각각 스크린샷으로 화면 확정
node scripts/compare-live/step6-round.mjs --item m1 --round N --t5-pkg /tmp/t5-pkg3-Gofr/package
node scripts/compare-live/step6-round.mjs --item m2 --round N --t5-pkg /tmp/t5-pkg3-Gofr/package
```

- 2자(T5↔Hermes · 둘 다 gpt-5.1)다. OpenClaw 는 실행하지 않고 비교불가로 적힌다(§10-3 B + PM R1 정정).
- T5: 상태만 격리(HOME 불변 · GPAO_T5_HOME=방 · FILE_ROOTS=방 · 새 stateDir) · m1 은
  GPAO_T5_BROWSER_PROFILE=1 을 측정이 켠다(기본 켬은 아직 아님 — 판 M1 선행 조건 칸의
  판정 요청과 같은 자리. 회차기록에 병기할 것).
- Hermes: 호출 회차당 1회 · 화면 손은 울타리 경유(관찰만 통과 · 입력·클릭류는 차단+즉시 중단).
- 오너 실홈(~ · Desktop · Documents · Downloads) 전후 대조 + 전면 앱 전후(화면 뺏기)가
  회차 기록에 자동으로 남는다.
