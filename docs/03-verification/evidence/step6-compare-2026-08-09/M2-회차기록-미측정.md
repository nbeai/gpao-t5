# 6단계 비교 M2 — "카톡에 온 거래처 요청 정리해서 파일로 만들어줘" (2026-08-09 · 이번 창 **미측정**)

판 §2-b M2(신규·마지막 문항) · 소관표(동결): 카톡 읽기 = 2번 손 · 파일 만들기 = 1번 손.
2자(T5↔Hermes · 둘 다 gpt-5.1) · 실물 회차 · **발신 절대 금지 — 읽기만.**

## 판정: R1·R2·R3 전부 **미측정 (조건 미비 — 화면 잠김)**. 통과도 실패도 아니다.

기계 사실은 M1 과 같은 원본(`M1M2-사전준비/`): 02:58 잠금(locked=1) · 잠금 화면이
KakaoTalk 창을 전부 가림(CGWindowList 0개) · 03:06 까지 잠금 지속 · PM 지시로 폴링 중단.
M2 의 자는 "카톡 원문과 파일의 대조"다 — 원문 확정(측정 전 스크린샷)부터 잠금 화면
위에서는 성립하지 않는다.

## 고정 대화 — ⑥의 그 실물 대화를 잇는다

측정 전 카톡 목록을 **새 스크린샷으로 확정**하고 그 원문과 파일을 대조한다
(content-agnostic — 무슨 내용이든 파일이 원문과 맞는지가 자). 참고 원본: 5단계
`step5-full-2026-08-09/real-rounds-v3/kakao-fixed-v3.jpg`(타깃 방 "n.BEAI 사일런트서비스"
마지막 "넵 명심하겠습니다"). **방을 클릭해 열지 않는다** — 읽음 처리(수신확인)가 상대에게
보이는 부작용이라, ⑥과 같이 목록 화면만 원문으로 쓴다.

## 아침 창 실행 — 회차당 명령 한 번

```
node scripts/compare-live/step6-round.mjs --item m2 --round N --t5-pkg /tmp/t5-pkg3-Gofr/package
```

실행 전 확인: KakaoTalk 목록 창을 화면에 보이게 두고 스크린샷으로 원문 확정 → 이 폴더에
저장(회차 전후로 목록이 흐르면 — 라이브 앱이다 — 전후 스크린샷 차이를 기계 사실로 적는다).

안전선(러너에 이미 기계로 서 있음):
- T5: 승인 카드가 떠도 승인하지 않는다(러너가 `승인: 하지 않음(측정 정책)` 으로 기록만).
- Hermes: 화면 손은 관찰 전용 울타리 경유 — 입력·클릭·발신류는 화면에 닿기 전에 차단되고
  그 회차는 즉시 중단(무효 아님 · 관측). 파일 실물은 방(cwd) 안에 생기고 잔재는 evidence 로 수거.
- 오너 실홈(~ · Desktop · Documents · Downloads) 전후 대조 + 전면 앱 전후(화면 뺏기) 자동 기록.

## 라벨 초안: 없음 — 유효 회차 0. 확정은 PM.
