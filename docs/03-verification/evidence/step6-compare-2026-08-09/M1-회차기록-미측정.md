# 6단계 비교 M1 — "이번 달 얼마 벌었지?" (2026-08-09 · 이번 창 **미측정**)

> **[추기 09:45]** 이 문서는 새벽 창(02:58~03:06)의 기록이다. 오너가 09:37 화면을 열어
> **측정이 실제로 돌았다** — 확정 기록은 `M1-회차기록.md`. 이 파일은 역사로 남긴다(불삭제).

판 §2-b M1(⑤ 승격) · 소관표(동결): 정산 파일 = 1번 손 · 카드사 화면 = 2번 손.
2자(T5↔Hermes · 둘 다 gpt-5.1 · §10-3 B — OpenClaw 비교불가) · 실물 회차(상태만 격리·실화면 공유).

## 판정: R1·R2·R3 전부 **미측정 (조건 미비 — 화면 잠김)**. 통과도 실패도 아니다.

기계 사실(원본: `M1M2-사전준비/`):
```
02:58 CGSSessionScreenIsLocked = 1 · onConsole = 1 (잠금-기계사실-0258.txt)
02:58 전체 화면 캡처 = 잠금 화면(암호 입력 필드) — 화면잠금-0258.jpg
02:58 CGWindowList(onScreenOnly): Chrome·KakaoTalk 창 0개 — 잠금이 전부 가림
03:04~03:06 폴링 locked=1 지속(잠금-폴링로그.txt) · PM 지시로 폴링 중단(오너 취침)
```
M1 의 자(닫는 문장)는 "카드사 **화면**"을 요구한다 — 잠금 화면 위에서는 그 조건이
서지 않고, 잠금 해제는 오너의 암호라 측정 라인이 할 수 없다(오너 실사용 불가침).
판 §2-b 선행 조건 칸 그대로: **서기 전에는 미측정으로 적는다.**

## 고정(측정 전 동결 — 아침 창이 이 숫자로 잰다)

- 정산 파일(1번 손): 러너 동결 고정물 — 8월 매출정산 2,120,000 + 8월 정산내역 310,000 = **2,430,000원**
- 카드사 화면(2번 손): 고정 페이지 `M1M2-사전준비/M1-카드페이지-고정물.html`
  (sha1 `b284d7a6c38e9a2af82a0101c2a3321b6e98d77a`) — "2026년 8월 카드 승인액 합계: **570,000원**"
  가상의 카드사(성심카드) · 127.0.0.1 로컬 서버 — **실금융 화면 금지 준수**
- 두 출처 합: **3,000,000원**
- 크롬 실탭: 창 id 282043879 에 `http://127.0.0.1:4777/card.html` 열려 있음(잠금 뒤라 화면 확정
  스크린샷은 아침 창 몫)

## 아침 창 실행 — 회차당 명령 한 번

```
node scripts/compare-live/step6-round.mjs --item m1 --round N --t5-pkg /tmp/t5-pkg3-Gofr/package
```

실행 전 확인 두 가지(준비 절차 · `M1M2-사전준비/측정조건과-준비상태.md` 상세):
1. `curl -s http://127.0.0.1:4777/card.html` 이 200 인가 — 죽었으면
   `python3 -m http.server 4777 --bind 127.0.0.1 -d docs/03-verification/evidence/step6-compare-2026-08-09/M1M2-사전준비 &`
   로 다시 세우고 크롬 탭을 `/M1-카드페이지-고정물.html` 로 옮긴다(내용·지문 동일).
2. 카드 페이지 창을 화면에 보이게 두고 스크린샷으로 화면 확정 → 이 폴더에 저장.

T5 는 GPAO_T5_BROWSER_PROFILE=1 을 측정이 켠다(기본 켬 아직 아님 — 판 M1 선행 조건
칸의 판정 요청 자리. 회차기록에 병기). Hermes 호출은 회차당 1회, 화면 손은 관찰 전용
울타리 경유(입력·클릭류 즉시 중단 — 무효 아님·관측).

## 라벨 초안: 없음 — 유효 회차 0. 확정은 PM.
