# ② 말귀 축 재측정 — T5 (2026-08-04)

규약: `T5-MALGUI-COMPARISON-PROTOCOL-2026-08-03-ko.md` (채점 전 동결, 커밋 `be8dd05`).
원문: `raw-answers-t5.json` — 손대지 않은 응답 전문. 같은 기준으로 다시 채점할 수 있다.
러너: `scripts/human-use/malgui-run.mjs` (이번에 상설 자산으로 만들었다. 8-03 은 임시 실행이었다).

- 모델 고정 `gpt-5.5` · 첫 턴만 · 문항 8개 · 1회씩 · 문항마다 새 대화 · 격리 상태
- **문항 8개가 8-03 동결값과 글자 그대로 일치**하는 것을 대조로 확인했다.
- 비교군은 다시 돌리지 않았다. 규약과 모델이 그대로이므로 **OpenClaw 8-03 값을 그대로 쓴다.**

## 1. 채점 (동결 기준표 · 문항 8점 · 총 64점)

| # | 문항 | T5 8-03 | **T5 8-04** | OpenClaw 8-03 |
|---|---|---|---|---|
| 1 | 통장이 자꾸 비어 | 7 | 7 | 8 |
| 2 | 부모님 오심 | 8 | 8 | 8 |
| 3 | 보고서 감이 안 와 | 8 | 8 | 8 |
| 4 | 컴퓨터가 느려짐 | 7 | 7 | 8 |
| 5 | 이번 달에 뭐 했는지 | 7 | **5** | 0 |
| 6 | 아이 학원 | 8 | 8 | 8 |
| 7 | 메일이 쌓임 | 7 | **0** | 8 |
| 8 | 매주 자동화 | 8 | 8 | 7 |
| | **합계** | **60** | **51** | **55** |

**떨어졌다. 숨기지 않는다.** 다만 두 하락의 성질이 다르다.

### 7번 0점 — 제품이 아니라 공급자 오류

`ModelProviderError: empty or unreadable response`. T5 는 **정직하게** 답했다:
*"지금은 답을 만드는 연결이 잠시 불안정해요. 방금 요청은 아직 실행하지 않았어요."*
거짓 성공이 아니다. 그러나 **전달된 답이 없으므로 0** 이다 — 8-03 에서 OpenClaw 5번을
같은 기준으로 0 점 준 것과 일관되게 매긴다.

### 5번 7 → 5 — **이건 진짜 제품 결함이다**

답 본문에 내부 채널 이름이 그대로 나갔다:

```
참고한 지난 대화 문장:
`memory.cite`: "부모님이 집에 오셔서 1~3일 머무는 기준으로 잡고 정리할게."
```

**사람 말 항목 0점.** 그리고 다음 걸음이 없어 1점(정리해 주고 끝).

## 2. 완주와 시간

| | 완주 | 중앙값 | 평균 | 최대 |
|---|---|---|---|---|
| T5 8-03 | 8/8 | 15.8s | 18.6s | 44.1s |
| **T5 8-04** | **8/8** | **16.5s** | **17.0s** | **37.4s** |
| OpenClaw 8-03 | 7/8 | 22.4s | 72.7s | 302.1s |

시간은 사실상 그대로다(최대는 44.1 → 37.4 로 줄었다).

## 3. 오늘 고친 것이 실제로 나타난 자리

**8번(자동화)** — *"돼요. 매주 정해진 시각에 **T5가 스스로** 자료를 읽고 정리하게 만들 수 있어요."*
오늘 통제 채널 선언에 **되는 것**을 더한 그 문장이 그대로 답에 나타났다. 같은 날 사람
사용시험에서는 정반대로 *"예약 기능이 없다"* 고 답했다(F-11). **선언 수정은 효과가 있었다.**
다만 F-11 자체는 안 닫혔다 — 그 계열은 도구를 부르는 문제이고 여기는 말로 답하는 문제다.

## 4. 이 측정이 말하지 않는 것

- 도구 사용·완주·자동화 실행 능력을 재지 않는다(첫 턴만 재므로).
- Hermes·Codex 에 대해 아무 것도 말하지 않는다(둘 다 8-03 에서 제외됐고 조건은 그대로다).
- 채점자는 나(Claude)이고 **T5 를 만든 쪽이다.** 원문을 남기는 이유가 그것이다.
  **인간 맹검을 대체하지 않는다** — 여전히 열린 항목이다.
