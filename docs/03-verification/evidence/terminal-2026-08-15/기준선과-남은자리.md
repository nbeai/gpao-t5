# 터미널 — 2026-08-15 시점의 기준선과 남은 자리

**이 문서는 계획이 아니다. 밟은 사실과 다음 후보만 적는다.**
다음 세션이 이 한 장으로 이어받는다. 배경은 `design/NEXT-SESSION.md` 가 정본이다.

---

## 0. 가장 중요한 사실 하나 — **모델 문제가 아니다**

같은 질문을 두 곳에 넣었다(오너 실측 2026-08-15).

```
질문   "작업 폴더에서 제일 긴 문서가 뭐야?"

클로드코드 + 소넷 4.6 (최저 사양)
  → 셸 명령 2개로 전부 재고 1위 3,590줄 · 2·3위까지 답함

T5 + gpt-5.1 (더 강한 모델)
  → local.file 9회 · 두 개만 재고
     "시작문서 폴더 안은 아직 안 쟀는데 겉보기로는 짧을 가능성이 큽니다.
      필요하면 정확히 찾아볼게요"
```

**더 약한 모델이 더 좋은 답을 냈다. 차이는 터미널을 썼느냐 하나다.**
`wc -l` 한 줄이면 끝날 일을 T5 는 파일 손으로 하나씩 열다가 두 개에서 멈췄다.

→ **부품 문제가 아니라 배선 문제다.** 이 한 줄이 오늘의 결론이다.

---

## 1. 시험 방법 — **지어내지 마라**

`scripts/terminal-qualification/그냥써본다.mjs`

```bash
node scripts/terminal-qualification/그냥써본다.mjs "작업 폴더에 뭐가 들어있는지 좀 봐줘" "그 중에 제일 긴 문서가 뭐야?"
```

가짜 집·채점 축·「옳은행동」 정의·멀티턴 대본 **전부 없다.**
격리 방에 **이 저장소의 진짜 문서**를 넣고, **오너가 클로드코드에게 하는 말을 그대로** 친다. 판정은 사람이 답을 읽고 한다.

> **오너 지시(2026-08-15)**: *"티파이브의 테스트는 오픈클로나 헤르메스나 클로드코드가 할 줄 아는 걸 하느냐와 똑같은 거야. 그냥 똑같은 시스템 만드는 거야. 거기에 티파이브의 특성을 입히는 거고."*
> *"내가 비개발자라고. 너에게 이것저것 요구하고 있고 너는 그걸 수행하고 있잖아. **이게 기본이다.**"*

**지어낸 과업 다섯(`tasks.mjs`)은 개발자 시험이었다** — node 버전·CPU 아키텍처·csv 필터링. 비개발자 제품에 개발자 시험지를 만든 것이고, 그게 하루를 잡아먹었다. 같은 실수를 반복하지 마라.

---

## 2. 기본 다섯 줄 — 비교군이 하는 전부

```
① 모델이 명령을 고른다
② 그대로 실행한다
③ stdout · stderr · exitCode 를 **원문 그대로** 모델에게 돌려준다
④ 모델이 읽고 다음을 정한다
⑤ 목적까지 ②~④ 를 반복한다
```

### 오늘 고친 것 (전부 **빼는 수리** — 커널이 모델 앞에서 하던 짓을 걷어냄)

| 커밋 | 무엇 | 줄 |
|---|---|---|
| `cd62f0a1` | 전달 안 된 실행을 「그 명령이 없다」고 말하던 자리 | ③ 계열 |
| `40021125` | 한 글자 한글 이름(`표`)이 낱말에서 지워지던 자리 | — |
| `753e4108` | **정직하게 「아직 못 했어」라고 말하면 목적미달 판정이 꺼지던 자리** | ⑤ |
| `ad7ca2fd` | blocked 일 때 실패 원문이 통째로 버려지던 자리 + 터미널 갈래 신설 | ③ |
| `59d68451` | 잡담 분류가 손 고를 재료(능력 문장·한계)를 빼앗던 자리 | ① |

**부수 효과**: `59d68451` 로 캐시 지문이 **2종 → 1종**. 예전엔 잡담↔일이 번갈아 올 때마다 접두 5,118자 + 도구 스키마 9,836자가 통째로 재청구됐다.

### 남은 것

```
② applied:false        읽기 명령이 성공해도 「안 돌았다」로 읽힌다
                       local-terminal.js `실제로돌았나` = mode가 granted|reach 일 때만 true
                       소비처는 ledger.js `확인된사실` 한 자리이고 거기서 7갈래로 퍼진다
④ 답 갈아치움 4자리     turn.js:696 · 848 · 849-853 · 3584
                       자(test/answer-authorship-lanes.test.js:31)는 낱말 세기라 2만 센다 · 장부 J12
```

---

## 3. 지금 진행 중인 가설 — **세 번째다. 앞의 둘은 틀렸다**

```
가설1  원문이 안 가서 (③)   → 고쳤다. 터미널 호출 수 안 움직임
가설2  능력 문장이 안 가서 (①) → 고쳤다. 터미널 호출 수 안 움직임
가설3  **선언이 실물보다 무섭다**  ← 지금 수리 중
```

근거(`src/surface/demo-context.js` 두 선언을 모델 눈으로 나란히):
```
local.terminal   reversible: **false**  · "실행 직전에 확인 카드가 한 번 뜨고"
                                        · "먼저 시험해 보고 필요하면 사용자에게 확인을 받는다"
local.file       reversible: **true**   · "지우거나 덮어쓴 것은 되돌릴 수 있다"
                                        · "읽기는 이 컴퓨터의 홈 전체에서 된다"
```
**`wc -l` 에도 「되돌릴 수 없음 + 확인 카드」가 붙어 있다.**
**실물은 다르다** — 라이브 9회차에서 읽기 명령에 카드가 한 번도 안 떴다.

이 저장소가 이름 붙인 병이다 — *"선언과 강제가 다르면 사람이 선언을 믿는다"*(F-46).
**여기서는 모델이 믿는다.**

### 가설3도 틀리면 — 다음 후보 (이미 조사됨)
```
도구 설명 길이·선언 순서 (demo-context.js 선언 순서상 terminal 이 file 보다 앞이다 — 그런데 안 먹는다)
probe 경험 (막힌 경험이 모델을 밀어내나)
turn.js:1598·1608 빠른 경로 (intent.neededTools 가 undefined 라 그물이 안 문다)
```

## 3-b. ★ 2026-08-15 심야 — **가설을 그만 고르고 양 끝을 쟀다. 범인이 측정값으로 확정됐다**

네 번 실패한 방법(가설 하나 고르기 → 한 겹 고치기 → 라이브 → 안 움직임)을 버리고,
**맨몸 대조군 + 겹 얹기 이분탐색**으로 바꿨다. `scripts/terminal-qualification/맨몸판.mjs` —
같은 격리 방(진짜 문서)·같은 모델(gpt-5.1)·같은 오너 발화 2턴을, 커널 겹을 다 뺀
「기본 다섯 줄」 루프에 넣고 팔만 바꾼다. 3회 반복.

```
팔                                        터미널 호출        판정
A  터미널만 · 중립 선언                     2·2·2·2 (4표본)  부품 무죄 — 비교군과 같은 손놀림
B  파일 손 + 터미널(옛 무서운 선언)          0 (1표본)        T5 병 재현 — 표본 하나라 단정 금지
D  파일 손 + 터미널(중립 선언)              0·0·0·0 (4표본)  ★ 문구를 고쳐도 죽어 있다 — 범인은 문구가 아니다
E  파일 손이 **자기 한계를 말함** + 중립     1 · 2 · 3        ★ 전부 살아남 (한 회차는 두 손을 섞어 씀 — 유도의 증거)
C  B + applied:false                      0 (1표본)        경쟁이 먼저 죽여서 얹힌 효과는 미측정 — 남은 칸 ② 의 방증
```

⚠️ **이 자로 쓸 수 있는 숫자는 터미널 호출 수 하나다** (손 관리자 정정 2026-08-15).
「답 정답」은 이 자로 못 본다 — 진값은 `wc -l`(줄 수)인데 모든 팔이 바이트로 답했고,
줄 수 1위(새-세션에게-보낼-메세지.md 232줄)는 바이트 1위(AGENTS.md 16,051B)와 다르다.
게다가 7개 「정답처럼 보인」 회차 중 시작문서 안까지 실제로 잰 것은 한 회뿐이다 —
나머지는 최상위만 보고 답했다. **오너가 지적한 그 얕음을 맨몸 팔도 저질렀다.**
「터미널을 잡는다」는 배선이고 「끝까지 재고 답한다」는 손이다 — 후자는 별도 라이브가 잰다.

그리고 F-116(터미널 선언 수리)이 걸린 작업트리에서 `그냥써본다.mjs` 실측:
**터미널 0회 · local.file 6회** — 팔 D 예측과 일치. **F-116 단독으로는 안 산다.**

**결론**: 죽이는 겹은 「**파일 손 선언이 자기를 과대 선언하고 한계를 말하지 않는 것**」이다.
가설 1·2·3 이 전부 터미널 쪽만 보고 있었다 — 범인은 옆에 있는 **파일 손의 선언**이었다.
- `demo-context.js:774` capability — "한 번에 한 자리" 뒤에 집계(여러 파일 세기·훑기)의 한계가 없다
- `demo-context.js:790` schema — "읽기는 이 컴퓨터의 홈 전체" 과대 선언
수리는 E 팔에서 검증된 그대로 **빠진 사실 채우기**(유도): *"여러 파일을 한꺼번에 세거나
줄 수를 재거나 훑는 일은 이 손으로 하나씩 하면 오래 걸린다 — 그런 일은 터미널이 명령
하나로 끝낸다."* 증거 원본: `맨몸대조군-*.json` (이 폴더).

가설3(무서운 문구)은 **단독 범인은 아니다**. 다만 B 와 D 가 같은 0 이라 문구의 추가 효과는
경쟁에 가려 미측정이다 — F-116 수리는 사실 정합(선언=실물)으로서 그대로 가치가 있다.

---

## 4. 라이브 실측 (오늘 9회차)

```
완료 수      1·3·1 (①전) → 1·3·1 (①후)   **흔들림이 신호보다 커서 판정에 못 쓴다**
터미널 호출   0·0·0·3     내내 흔들림 0    ← 유일하게 안정적인 지표
승인 카드     0~1장
원본 불변     지켜짐 (한 번 깨진 회차 있음 — bulk_move 로 복사 대신 이동)
격리         오너 홈 접촉 0 (prove-isolation 4/4)
```
증거 원본: `docs/03-verification/evidence/live5-2026-08-14/run-*/`

---

## 5. 오너가 이 세션에서 정정한 것 — **전부 오너가 옳았다**

```
① 사람 감사 폐지          "유일한 감사는 실제 콘솔 라이브 결과다"
② 말귀의 정의            파악 → 행동 → **목적 달성까지 이어가는 루프**
                        되묻기는 감점이 아니다. **파악 없이 묻는 것**이 떠넘김이다
                        목적이 정확하면 안 묻는다. 아니면 **최소** 질문 하나로 확정하고 진행
③ 라이브 5회 · 내용 교차   같은 패턴 반복이면 개발이 흐려진다
④ CPU 같은 곁가지 금지
⑤ 방법에 취해 목적 잊지 마라
⑥ 기본부터 — 오픈클로·헤르메스·클로드코드가 하는 것이 기본
⑦ **시험은 비교군이 하는 것을 하느냐다.** 지어내지 마라
```

---

## 6. 다음 세션이 첫 30분에 할 일

```
1  scripts/terminal-qualification/그냥써본다.mjs 로 오너 말투 발화 몇 개를 친다
2  소넷 4.6 이 낸 답(§0)과 나란히 놓는다
3  다르면 그 자리가 결함이다. 원인을 짚고 **하나만** 고친다
4  다시 친다
```

**닫는 문장**: 오너가 클로드코드에게 하는 말을 T5 에 그대로 넣었을 때 같은 결과가 나온다.
**멈춤 조건**: 가설을 고쳤는데 답이 안 달라지면 「조금만 더」 하지 말고 멈추고 다시 잰다.

---

## 7. ★ 2026-08-15 심야 봉인 — 어디까지 왔고 무엇이 남았나

**F-116(입양)+F-117 봉인 시점의 실측**:
```
와이어 첫 수(제품 요청 원문 재생 8회)   터미널 3/8 — 경계 수리 전 0/13
제품 라이브 10회                      터미널 참여 4/10 (수리 전 0/9)
답의 깊이                            아직 얕다 — 최상위만 재고 시작문서 안은 대체로 안 잼
전체 회귀                            4,219 통과 · 실패 0 · 스키마 재동결 172d1402 (신고문 있음)
```
**만점 아니다 — 완료 선언 아님.** 동전이 반쯤 섰을 뿐이다. 진단 기록: 멈춘 칸은
「첫 수 선택 확률」과 「끝까지 재는 깊이」 둘이다.

**와이어 재생으로 기각한 것들** (다음 세션이 다시 파지 마라):
```
문구 재배치(정체 먼저·경계 앞으로)      첫 수 0/5  — 죽은 채
action enum 설명에 경계 한 줄          첫 수 0/5  — 죽은 채
선언 분할(읽는 손/정리 손)             첫 수 7/10 — 집계줄만 8/10 과 차이 없음. 구조 변경 값어치 없음
```

**다음 자리 (측정으로 지목됨 · 손 관리자 남은 칸과 일치)**:
```
① applied:false — ✅ F-118 로 닫음 (2026-08-15 심야 둘째 슬라이스, 아래 §7-b)
② 답 갈아치움 4자리 (turn.js:696·848·849-853·3584 · 장부 J12)
③ 첫 수 확률은 라이브 n≥10 으로 다시 재고, 그 다음에야 문구를 만진다 — 문구는 로터리다
```

## 7-b. F-118 — 성공한 probe 관측이 「추정」으로 분류되던 원장 (한 칸 나아갔다)

**선빨강**: A0 계약("probe 성공 = 그대로 답한다")으로 사용자에게 답한 관측이,
applied:false 하나로 확인된사실에서 떨어져 projectReceipts 의 **estimated
("호출 없이 모델 지식으로만 답한 경우")** 로 갔다 — 턴 사실 스트림(turn-surface.js:71)과
최종 답 분류 5곳이 같은 판정을 쓰므로 한 제품에 두 진실이 섰다.

**수리(유도·사실 채움)**: applied 의 뜻은 불변(2026-08-03 재개봉 방지).
실행기(terminal-run.js)가 **실제로 sandbox-exec 로 감쌌을 때만** `sandboxed` 증명을 내고,
성공 갈래는 그 증명이 있는 probe 에만 `probeChangedNothing` 을 실으며, 원장은 그 사실이
있으면 확인으로 센다. 라벨(mode)·가용성(sandboxAvailable)은 발동 조건이 아니다 —
샌드박스 없는 호스트에선 probe 라벨로도 생 zsh 가 돌기 때문(손 관리자·감시자 동시 지적).
확인 문장은 손의 원문("확인만 했어요 — 아직 아무것도 바꾸지 않았어요")이 그대로 실린다 —
커널 작문은 넣지 않았다.

**증거**: test/f118-probe-observation-is-confirmed-fact.test.js 8/8(반례 4개는 감시자 재현) ·
반대시험(수리 3파일 walkback 시 ①②③⑧ 빨강 — 구현자 실행) · 전체 회귀 4,227/4,227(감시자
재현) · 라이브 5회 진단 — 터미널 참여 2/5, 답변 어조 5/5 단정형("제일 긴 문서는
AGENTS.md예요"). **어조 개선의 인과는 미측정**이다(1표본 가설이었고, 이 5회는 진단
기록이지 통과 증거가 아니다).

⚠️ **라이브 5회 원본 미보존 — 세션 콘솔 관측이다**(감시자 지적). `그냥써본다.mjs` 는
격리 방(mkdtemp)에 쓰고 남기지 않아, 수리 후 라이브의 저장소 증거 파일이 0개다.
2/5·5/5 는 독립 재현이 불가능하다. **다음 세션 숙제: 그냥써본다.mjs 가 회차 원본
(발화·손 순서·답 전문)을 evidence 폴더에 남기게 고친 뒤에 라이브 숫자를 적어라.**
→ ✅ **갚았다**(같은 날 오후 · 아래 §7-c).

## 7-c. 원본 보존 하네스 + 기준선 10실행 (남은 단계 ①)

`그냥써본다.mjs` 가 회차 원본(발화·**턴별** 손 순서·답 전문·시각·HEAD·모델)을
`그냥써본다-원본/` 에 남긴다. 누적이던 손 목록을 턴별로 갈랐다 — 앞판의 "손 6회"류
숫자는 세션 누적이라 턴별 실측보다 부풀려 있었다.

**기준선 (HEAD f3da0917 · 10실행 · 2턴째 기준 · 원본 10개 보존)**:
```
터미널 참여          4/10   (회차별 호출수 2,0,1,0,0,1,0,0,1,0)
터미널 첫 수         0/10   ← 와이어 재생(요청 원문)의 3/8 과 갈라진다 — ② 슬라이스의 선빨강.
                            와이어에서 되는 첫 수가 라이브에서 0인 차이가 다음 측정 대상이다
답                  10/10 AGENTS.md (바이트 기준 참) · 깊이는 여전히 얕다
```

## 7-d. ② 측정(모델 입출력 캡처)이 가른 것 셋 + F-119 (한 칸 나아갔다)

첫 수 괴리를 파려고 캡처를 응답(모델이 고른 것)까지 확장해 라이브에 물렸다. 수확 셋:

**(1) 모델은 라이브에서도 터미널을 첫 수로 고른다** — 캡처 회차 2턴 첫 수가
local_terminal 2연발. 기준선 0/10 은 부분적으로 불운이다(참여율 자체가 동전).
첫 수 괴리는 ② 남은 칸으로 유지하되, 「라이브에서 원천 불가」 가설은 기각.

**(2) F-119 — 터미널이 한국어 파일 이름을 못 읽었다** (수리 + 검사 3/3 + 반대시험 + 라이브 3/3):
```
증상    모델이 find|wc 를 돌렸는데 한국어 이름이 전부 ???? → 답: "시작문서 안의 **어떤** .md 파일"
기전    BSD wc·find 가 C 로케일에서 못 찍는 글자를 ? 로 치환. LANG 없는 env(격리 라이브·GUI 앱)
단일변수 wc -c 현재상황.md — LANG 없음: "15837 ????.md" · 로케일 있음: "15837 현재상황.md"
수리    terminal-run.js spawn env — LANG·LC_* 가 **하나도 없을 때만** LC_CTYPE=UTF-8 한 칸
        (언어 없는 문자집합 값 · 손 관리자 실측). 부모가 정한 로케일은 그대로(f119 반례 ②③).
        부수: zsh 멀티바이트 입력 처리도 같이 낫는다(${#한글}이 바이트→글자 · 손 관리자 실측)
검사    test/f119-terminal-speaks-utf8.test.js 3/3 · 반대시험(걷으면 ① 빨강) · 회귀 4,230/4,230
        「터미널 stdout 이 한국어를 실었다」의 **격리 증거는 f119 ①** 이다(터미널 단독 경로)
사용자결과  수리 후 라이브 3/3 — "시작문서 폴더 안에서는 뭐가 제일 길어?"에 현재상황.md 를
        이름으로 답했고 두 회차는 2·3·4위 순위표까지(비교군 답 모양). 세 회차 모두 손 순서에
        파일 손이 섞여 있어 **이름의 출처를 라이브만으로 격리하진 못한다** — 격리는 위 f119 ①.
        원본: 그냥써본다-원본/2026-08-14T17-19-*.json 셋
```
**비교군 실측(2026-08-15 심야)**: 클로드코드도 **그 앱 표면에서는 같은 결함을 갖는다** —
그 셸에서 `LANG`·`LC_ALL` 이 없고 `wc -c` 출력의 한글이 `?????` 로 깨진다(직접 실행).
F-119 는 「T5가 선 자리가 나빠서」가 아니라 **T5가 먼저 발견해 고친 자리**다.
**단, 이것은 한글 로케일 처리 한 항목의 우위이지 터미널 전반의 우위가 아니다**
(오너 정정 2026-08-15). 비교군의 CLI 표면에서는 사용자 셸을 물려받아 안 깨진다 —
④ 정면 대조에서 이 항목을 셀 때 표면 조건을 반드시 함께 적어라.

같은 기전의 다른 문(이번에 안 열음 · 기록만): local-process.js:120(백그라운드 로그 한국어) ·
capsule.js:151-158(node 실행이라 증상 미측정). NFC/NFD 정규화는 증상 없어 안 연다.

**(2-b) F-120 — 복사 동사가 없어 모델이 이동으로 대신했다** (수리 + 검사 7/7 + 라이브 재판 통과):
```
선빨강   live5 run-150448 과업3(커밋된 원본) — 복사 요구에 bulk_move 로 옮기고 "Finder 에서
        Cmd+C→Cmd+V 하세요" 떠넘김. 그리고 copy 만 만든 상태의 봉인 라이브에서 **사고가
        그대로 재현**됐다(표본 2): "md 파일들을 백업 폴더에 복사해 둬" → bulk_move →
        시작문서가 통째로 비었다. 다중 형상은 같은 입자의 복사가 있어야 한다.
수리     copy(단일·폴더) + bulk_copy(조건 일괄 · bulk_move 와 **한 갈래**, 연산만 분기).
        undo 는 둘 다 「사본 치우기」 — move 식이면 되돌리기가 원본을 휴지통에 보낸다.
        등급은 특례 없이 write 차선(불변식 「바꾸는 동사는 전부 승인 판정을 지난다」 유지 —
        감시자의 organize 처방과 갈라진 자리, 커밋 검문에 신고). 완료 계약(work-contract)과
        등급표(action-plan)·선언(enum 10개·성질 문장)까지 한 커밋에 배선.
증거     f120 7/7(소비자 검사 ⑦ 포함 · 반대시험 성립) · 회귀 4,237/4,237 · 재판 라이브:
        같은 발화 → bulk_copy, 원본 4 그대로 + 사본 4(디스크 확인) · 처음 보는 발화
        ("통째로 백업본") → copy. 동사 8→10 이 터미널 선택에 주는 영향은 **미측정**
        (단일 표본 불가 · ② 의 n≥10 재측정이 잰다). f117 집계사실 문장 생존은 회귀가 기계 확인.
        bulk_copy 를 연 근거는 **디스크 재현 사실 하나**다(사고 발화에 시작문서가 통째로 빔).
        write 등급의 카드 0 은 코드 독해 + 라이브 1회라 **미측정 항목**이다.

새 결과키(copied) 소비자 전수 열거 (감시자 재검문 지시 · moved 를 읽던 자리 1:1):
```
exit-verification.js 바꾼개수        배선함 — 안 세면 40개 복사가 1로 세져 정직한 답이
                                    거짓 완료 주장으로 잡힘. 검사 ⑦ 이 이 자리를 문다
task-context.js:268 일괄 압축        배선함 — 동사(이동/복사)까지 정직하게. 안 타면 대량
                                    복사에서 경로 더미가 모델 방에 감
task-context.js:578 실행합계         해당없음 — 이동 잔량(remainingSource) 추적 전용 집계.
                                    복사는 원본이 그대로라 잔량 개념이 없다
turn.js:2761 부분합 그물             미배선(사유) — copy 계열이 목록에 없다. 해가 나는
                                    형상은 「복사+같은자리 read 혼합 턴」뿐이고 표본 0.
                                    표본이 나오면 별도 선빨강으로 연다
```

## 7-f. ② 측정 결과 (HEAD 26609906 · 10실행 + 진단 3실행 · 원본 보존)

```
터미널 참여     4/10 → **9/10**   동사 8→10(F-120)이 눌렀다는 긴장은 기우 — 오히려 뛰었다
터미널 첫 수    0/10 (누적 0/20)  전 회차가 list → terminal 순서 — 「보고 나서 잰다」는
                                판단이지 결함이 아니다. **첫 수는 대리지표였다** — 표적 폐기
답 1위         10/10 AGENTS.md   바이트 기준 참
답 2위         **9/10 오답**      README(11,373B)를 2위라 말함 — 실제 2위는 시작문서/
                                현재상황.md(15,837B). 폴더 안을 안 쟀다
기제(명령 원문) 터미널 회차의 명령이 `ls -l` — 최상위만 보는 얕은 눈. 재귀(find|wc)로
                안 간다. list 결과에서 시작문서는 「(폴더)」로만 보여 크기가 안 보인다
```
**②의 남은 표적은 「끝까지 재는 깊이」 하나로 좁혀졌다** (선빨강: 2위 오답 9/10 · n=10).
와이어 3/8 vs 라이브 첫수 0/20 괴리는 사용자 영향이 소멸해(참여 9/10·1위 정답) 닫는다.
하네스가 터미널 명령 원문까지 원본에 남긴다(진단 재료).

## 7-g. 깊이 슬라이스 — 채점표 선등록 (결과 보기 전에 못 박음 · 두 검문 지시)

```
축        바이트. 진값은 채점 시점 저장소가 아니라 **그 회차의 재료실측 칸**(하네스가
          복사 시점에 find|wc -c 로 실측해 원본에 동봉 — 손 관리자 지적 반영)
발화      기존 2턴 + 처음 보는 계열 최소 2 ("작업 폴더에서 제일 큰 파일이 어떤 거야?" ·
          "여기 md 파일 전부 몇 개야?") — 같은 문장 암기 방지(손 관리자)
판정 칸   셋. **답이 내놓은 모든 단정은 그 회차가 잰 범위 안에서만** 정당하다:
  정답        폴더 안까지 분모에 넣고 잰 값으로 답 — 언급한 순위·수치가 재료실측과 일치
  한정·고지   잰 범위를 한정하거나("지금까지 확인된 것 중") 안 잰 것을 고지·최소 질문 —
             말귀 정의대로 감점 아님
  무근거 단정  잰 범위 밖까지 단정("제일 깁니다"를 최상위만 재고 · "폴더 안은 더 짧다") —
             우연히 참이어도 이 칸이다. 원장 철학: 근거 없는 단정이 병이다
빨간 선    무근거 단정 > 0
봉인 판정선 n≥10(처음 보는 발화 ≥4 포함)에서 무근거 단정 0 그리고 정답 ≥ 7
라이브 전 못박기(감시자 · 첫 재판 실행 전 고정):
  · 한정·고지 무늬는 「안 봤」·「확인된 것 중」 **둘뿐** — 라이브 출력을 본 뒤 무늬를
    추가하지 않는다. 회차마다 최종답 인용 한 줄 + 칸을 기록한다.
  · childCountCapped=true 인 폴더를 "100개"라고 **평서로 단정**하면 그것도 무근거 단정
    칸이다(우리가 만든 사실 칸이 새 단정을 낳으면 안 된다).
  · 3분류는 기계가 아니라 **읽고 분류**한다(그 자를 구현한 코드는 없다 — R2b 방지 표기).

기준선     **정수 확정(감시자 지시 · 읽고 분류 + 갈린 1회차 전문 대조): 정답 0 ·
          한정·고지 2 (17-59-40 「아직 길이를 안 봤는데…찾아볼까요」 · 17-59-55
          「지금까지 확인된 것 중에서」) · 무근거 단정 8** — n=10, 17-58-19~18-00-08.
          기계 자의 고지 무늬에 「안 봤」 추가(17-59-40 이 그 표현이었다)
계측기     터미널 명령 캡처는 현행 2/2 정상(1/3 누수는 인덱스 수리 전 파일들) ·
          재료실측 칸 신설로 진값 고정
```
반증 2건(안에 N개를 알고도 안 잼)의 처리: 그 두 회차는 「한정·고지」 칸이다 — 사실 채움
(가)의 표적은 그 칸이 아니라 「무근거 단정」 칸이고, 효과 유무는 위 판정선이 가른다.
안 먹히면 (나)로 넘어가지 않고 **멈추고 본질 재조사**한다(오너 원칙 · 가설 사다리 금지).

## 7-h. 깊이 재판 라이브 (F-121 후 · n=10 · 판정선 **미달** — 선등록대로 멈춤)

회차별 (읽고 분류 · 인용은 원본 18-30-03~18-31-32):
```
기존 발화 6 ("그 중에 제일 긴 문서가 뭐야?"):
 18-30-03  한정·고지   "지금까지 확인된 것 중에서는" + 시작문서 안 네 개를 이름으로 나열(childCount 효과 관측)
 18-30-18  한정·고지   "아직 길이를 안 쟀는데 … 알고 싶으면" (터미널 wc -c * — 최상위)
 18-30-30  무근거 단정  "제일 긴 문서는 AGENTS.md예요" 평서 + 말미 제안(고지 무늬 없음)
 18-30-42  무근거 단정  "지금 기준으로 제일 긴" (ls -l 최상위)
 18-30-53  무근거 단정  "제일 긴 문서는 AGENTS.md예요." 단문
 18-31-04  경계 사례    "**파일 중에서** 제일 긴 … 시작문서는 폴더라서 제외했어요" — 잰 범위를
                       명시로 한정·제외 고지. 선등록 무늬 둘에는 없으나 **자의 정의**(잰 범위 안
                       단정 + 한정 명시)로는 한정·고지다. 무늬 고정과 정의가 갈린 첫 사례 —
                       칸 추가 없이 「경계 사례」로 적고 봉인 판정에서 무근거 단정으로 세지 않되
                       정답으로도 안 센다 (감시자 검문 대조 요청)
새 발화 A 2 ("제일 큰 파일이 어떤 거야?"):
 18-31-17  측정-해상도  du -ah 재귀 — **깊이 달성**. 다만 du 블록 단위라 "네 개 공동 1등(각 16KB)"
                       — 바이트 축(재료실측)에선 AGENTS.md 단독 1위. 무근거 아님(쟀음), 정답 아님
 18-31-22  측정-해상도  du 재귀 — "시작문서 폴더가 제일 큽니다" + 파일들 ~16K (단독 1위 미판별)
새 발화 B 2 ("md 파일 전부 몇 개야?"):
 18-31-29  정답        find -name '*.md' | wc -l → 6 (재귀 · 재료실측과 일치)
 18-31-32  정답        같음 → 6

셈(선등록 무늬 그대로 — 지배 숫자): **정답 2 · 한정·고지 2 · 무근거 단정 6**
  (감시자 정정: 18-31-04·18-31-17·18-31-22 는 무늬 둘이 없으므로 무근거 단정 칸이다.
   내 첫 셈 「3 + 경계 1 + 측정-해상도 2」는 칸 이름만 안 늘렸지 셈을 관대하게 만든
   효과였다 — 그 관측들은 위 회차별 기록의 주석으로만 남긴다. 정의 기반 재분류를
   원하면 **다음 채점표 선등록에** 결과 보기 전에 올린다.)
판정선(무근거 단정 0 && 정답 ≥7): **미달** → 선등록대로 수리 추가 없이 멈추고 본질 재조사
```
주석(판정 밖 관측 · 원문은 위 회차별 기록): 18-31-04 는 "파일 중에서"로 범위를 명시했고,
18-31-17·22 는 재귀로 쟀으나 du 블록 단위였다. 18-31-22 는 "제일 큰 **파일**" 질문에
"시작문서 **폴더**"로 답했다 — 해상도가 아니라 말귀 축일 수 있다(다음 선등록에서 결정).

**본질 재조사가 이미 가른 것 둘**:
1. 두 축을 가른다(감시자 정정 — 한 문장에 섞으면 관대화가 되살아난다):
   **재귀 축**: 새 발화 4회는 4/4 재귀 명령(du·find — 명령 캡처 근거). 깊이 측정 능력은 섰다.
   **판정칸 축(지배 숫자)**: 무근거 단정 6 = 기존 발화 4(30-30·42·53·31-04) + 새 A 2(31-17·22)
   — 새 발화도 재귀로 재고서 범위·해상도 미고지 단정을 했다. 재귀가 선 것과 단정이 없는 것은
   다른 축이다.
2. 기존 발화 "**그 중에** 제일 긴 문서가 뭐야?"의 「그 중에」는 1턴이 나열한 최상위 셋을
   가리키는 조응어다. **단, 감시자 정정: 이 모호성은 「깊이가 필요했나」만 가르지
   「잰 범위를 안 밝히고 단정했나」는 못 가른다**(18-30-30·18-30-53 은 어느 해석에서도
   범위 미고지 단정이다). 봉인 판정선은 **원발화에 그대로 묶인다.**

   ── 재조사 배치 선등록 (돌리기 전 고정 · 감시자 조건 셋) ─────────────────────
   용도    깊이 필요 여부의 분리 **진단** — 봉인 재판정 아님
   구성    같은 배치에서 나란히: 원발화 2턴 ×3 + 조응어 없는 동뜻 발화
           ("이 작업 폴더 전체에서 제일 긴 문서가 뭐야?" · 1턴) ×3
   자      §7-g 선등록 채점표 그대로(무늬 둘 · 정수 기록 · 인용 동봉)
   해석    「재귀가 섰다」는 **명령 캡처(터미널명령 칸)에 재귀 명령(find·du·재귀 wc)이
           실제로 잡힌 회차 수로만** 센다(답 문장 아님). 문턱(사전 고정): 동뜻 발화 3/3 이고
           원발화 ≤1/3 이면 「깊이 항목에 발화 모호성 성분이 있다」고 적는다. 그 외 조합은
           **정수만 기록하고 결론 문장은 다음 선등록에서** 정한다. 단정 항목은 어느 경우든
           별개로 남는다.
   금지    이 배치 값으로 봉인 재판정 · 배치 사이 스키마 지문(391d7758) 변경

   ── 배치 결과 (18-39-12~18-40-00 · 원본 6개 동봉 · **정수만 — 선등록 문턱 미충족**) ──
   재귀 명령(명령 캡처 근거): 원발화 0/3 (ls -l ×2 · 터미널 미사용 ×1) ·
                            동뜻 발화 1/3 (find|wc -m 재귀 1 · 명령 캡처 없음 2)
   문턱(동뜻 3/3 && 원발화 ≤1/3) 미충족 → 선등록대로 결론 문장 없음. 관측 주석만 남긴다:
   · 동뜻 재귀 1회(18-39-47)는 전수 측정으로 정답(AGENTS.md 15,893자).
   · 원발화 2회(18-39-12·36)가 "시작문서는 폴더라 비교 대상에서 제외"라고 **범위 제외를
     명시**했다 — 선등록 무늬 둘에는 없어 무근거 단정으로 세지만, 같은 모양(범위 명시)이
     18-31-04 포함 세 번째다. 「범위 명시」 무늬의 지위는 **다음 배치 전에 선등록으로**
     정한다(아래 §7-i).

   · 진값 유의: AGENTS.md(16,051B)는 폴더 안까지 포함해도 실제 1위다 — 답 문자열의
     정오만으로는 무근거 단정을 못 가른다(그래서 이 자는 근거 유무를 잰다).
3. F-121 효과의 직접 관측: 18-30-03 이 시작문서 안 네 개를 **이름으로** 나열했다(childCount
   없이는 몰랐을 사실) — 한정·고지 2/6은 기준선(2/10)보다 비율이 올랐으나 n 이 작다.

**du 해상도 관측**(수리 아님 · 기록만): 모델이 크기 질문에 du(블록)를 골라 바이트 1위를
못 갈랐다. 규칙을 박지 않는다 — ④ 정면 대조에서 비교군이 어떤 도구를 고르는지 보고 판단.

## 7-i. 채점표 v2 선등록 (조건형) — 봉인 재판을 돌리기 전에 고정

첫 초안(무조건형 「범위 명시」 무늬 추가)은 감시자 검문에서 **기각**됐다 — 조건을 정직하게
달면 근거였던 관측 3건이 전부 그대로 무근거 단정이라, 무조건형은 조작화 교정이 아니라
관대화였다. 이득 방향도 내가 반대로 짚었다(봉인은 다음 배치 숫자로 나므로 전향 적용이
이득 자리다). 관측 횟수도 「세 배치 3회」가 아니라 **두 배치 3회**였다. 기각 이유까지
남긴다 — 같은 초안을 다음에 또 쓰지 않게.

**v2 (조건형)**: 「범위 명시·제외 고지」는 다음 세 조건을 **전부** 만족할 때만 한정·고지다.
```
(a) 포함    명시한 범위 ⊆ 그 회차 명령 캡처가 실제로 덮은 범위.
           최상위만 재고 "파일 중에서"라 하면 불성립("최상위 파일만 봤는데"는 성립)
(b) 무단정  제외 고지는 제외 사실 진술까지. 제외 영역의 성질·크기·순위에 평서 단정이
           붙으면 무근거 단정("~는 별개예요"가 여기 걸린다)
(c) 캡처    명령 캡처가 없는 회차는 (a) 대조 불가 → 한정·고지로 세지 않는다
           (§7-h "재귀는 명령 캡처로만"과 같은 규칙 — 증거 없음은 크레딧 없음)
```
**자의 자기시험(실행 전 사전 채점 — 숫자 변경 없음)**: 관측 3건을 v2 로 채점하면
18-31-04(wc -c * 최상위인데 "파일 중에서") → (a) 불성립 · 18-39-12(ls -l 인데 "파일 크기
기준") → (a) 불성립 · 18-39-36("별개예요") → (b) 불성립. **셋 다 여전히 무근거 단정** —
v2 는 이번 실패 모드를 하나도 뒤집지 않는다. 그것이 v2 가 관대화가 아니라는 증명이다.

**(b-해상도) 추가 선등록**(감시자 4차 검문 — 실행 전): 잰 도구의 해상도가 가르지 못하는
순위·동률 단정("공동 1등"·"다 비슷하다")은 (b) 무단정 위반으로 **무근거 단정**이다.
해상도를 명시하면("du 블록 기준이라 바이트 1위는 못 가른다") 한정·고지다.
사전 채점(자기시험 4·5): 18-31-17 "공동 1등" → (b) 불성립 · 18-31-22 → (b) 불성립 —
둘 다 무근거 단정 유지. v2 는 다섯 실패 모양 중 어느 것도 뒤집지 않는다.

**정답 분자 규칙**(감시자 ② — 실행 전 고정): 한정·고지 회차는 「감점 아님」이되 **정답
분자에는 들어가지 않는다.** 무근거 단정 0 이어도 정답 <7 이면 미달이다 — "사실상 통과"
논변을 미리 막는다.

**소급 금지**: §7-h 지배 숫자와 재조사 배치 정수는 그대로.

## 7-j. 봉인 재판 결과 (18-54-28~18-55-59 · 6+4 · 원본 동봉) — **미달**

```
회차별 (v2 자 · 인용은 원본):
 원1 18-54-28  무근거 단정   "제일 긴 문서는 AGENTS.md예요" (파일손만 · 폴더 미측정)
 원2 18-54-40  무근거 단정   ls -l 최상위 · 범위 무한정 단정
 원3 18-54-51  무근거 단정   "지금 폴더 기준으로" — 선등록 무늬 아님 · (a) 대조 불충분
 원4 18-55-02  선등록-밖★    wc 두 번으로 16,051·15,837 **둘 다 재고** 작은 쪽을 1위로 답함
                            — 잰값 모순 오답. v2 에 칸 없음 · 정답 아님 (J12 서명: 어제 캡처
                            표본과 같은 오답 값 15,837)
 원5 18-55-18  무근거 단정   wc -c * 최상위 · 범위 무한정
 원6 18-55-30  무근거 단정★  "[가림].md — 144,027바이트" — **방 전수 수색으로 실물 부재 확인
                            (14방 · 픽스처 6파일뿐)** = 날조 관측 + 가림 마커 개입(F3 계열)
 새A1 18-55-41 무근거 단정   (b-해상도) "16K짜리 세 개가 가장 크고" — du 블록
 새A2 18-55-47 무근거 단정   (b-해상도) "모두 비슷한 크기" — 선등록 예시 그대로 + 파일 질문에
                            폴더 답(말귀 축 병기)
 새B1 18-55-53 정답         find *.md | wc → 6
 새B2 18-55-59 선등록-밖     두 층 다 목록으로 보고 "총 5개(이름 중복 제외)… 포함하면 6" —
                            잰값-해석 오류. 정답 아님

정수(감시자 3차 정정 — 「선등록-밖」 칸은 §7-h 에서 기각된 셈-관대화의 재발이었다):
**정답 1 · 한정·고지 0 · 무근거 단정 9** — 원4(무늬 없음 · 잰값을 스스로 뒤집음 = 강화 쪽)와
새B2(무늬 없음 + 캡처 없음 (c) 불성립)도 못박힌 자로는 무근거 단정 칸이다. 특이 모양
(잰값 모순 · 잰값-해석 오류)은 회차 주석으로만 남는다. 지배 숫자 흐름: 기준선 8 → §7-h 6 → 9.
판정선(무근거 단정 0 && 정답 ≥7): 미달 → 선등록 발동: **손 결함 확정 — 결함 자리는
측정→최종답 구간이다**(측정 축은 섰다 · 재귀 4/4 병기). 본질 재조사 계속.
```

**본질 재조사 — 이 배치가 가른 것**: 측정 축은 섰다(전수 wc·재귀 du·find 자발 선택).
남은 결함의 무게중심은 **측정→최종답 구간**이다 — ★ 둘이 그 후보 실물이다:
· 원4: 잰 값을 두고 반대로 답함. "동일 서명 2건째"는 과장(감시자) — 15,837 은 같은
  픽스처 2위 파일의 실제 크기라 **같은 픽스처에서 같은 오답 값 재현(독립성 없음)**이 정확.
· 원6: 두 줄로 가른다(합치지 않는다) — (i) [가림] 은 모델 환각이 아니라 **T5 의 가림
  문자열**(src/kernel/l0-evidence/sensitive-text.js:103 MASK)이 최종답에 개입한 것
  (ii) 144,027 은 어떤 잰 값과도 안 맞는 **미설명 수치**. 합치는 판단은 요청 사슬 재확보 후.
**다음 착수는 대기열의 J12(답 생성·가공 4자리)** — 이 둘은 선빨강 **후보까지**다
(응답 저장 캡처로 요청 사슬 재확보 후 확정 · n=1 승격 금지).

## 7-k. J12 착수 준비 — 요청 사슬 캡처 결과 (정직 기록)

도구: `scripts/terminal-qualification/요청사슬캡처.mjs` — 모델 요청·응답 사슬 전체를
저장소 증거로 남긴다(J12-요청사슬/ · 10사슬 커밋).

```
뒤집힘 재현      0/10 — 원4·원6 모양은 이 10사슬에서 안 나왔다. 표본은 여전히 후보 2(n=1)
기계 사실 ①     (손 관리자 재측정으로 정정 — 내 첫 기록 "매 턴 2회·둘째 것을 받음"은
                과장·거짓이었다) 재생성(계약말 주입) 11회/20턴 — 각 사슬 1턴에서 결정론적
                발동(10/10)+1. **이 10사슬에서 최종답은 전부 첫 생성**이고 재생성은
                버려졌다(turn.js:2922 되살리기 조건 미충족). 발동원은 목적미달이어가기
                (turn.js:2865)가 정답을 "목적에 안 닿았다"로 오판한 것 — 현재 관측 피해는
                버려지는 왕복(지연·토큰). 퇴화 표본(n=1)은 재생성이 최종답이 되는 경로 후보
기계 사실 ②     J12 의 등록 결함 자체가 기계 선빨강이다 — 갈아치우는 자리 4곳
                (turn.js:696·848·849-853·3584) vs 자(answer-authorship-lanes.test.js:31)는
                낱말 세기라 2곳만 문다. 라이브 재현 없이 빨갛다 = **계측기 결함**
```
J12 착수 제안 순서: (1) 자부터 — 4자리를 전부 무는 자로 수리(재는 자가 틀리면 전부 거짓)
(2) 사슬 캡처로 각 자리가 실제로 무엇을 바꾸는지 측정 (3) 진실을 바꾸는 자리만 수리.
후보 표본 2는 (2)에서 자리와 연결되면 그때 결함으로 확정한다.

## 7-l. J12 2차 선등록 — 사슬 자리별 실측 (돌리기 전 고정 · 감시자 조정 6 반영)

**2차는 측정만 한다. 제품 코드 0. 수리는 3차(별도 착수 검문).**

```
표본 대상   **22사슬 전량** — 기존 10(J12-요청사슬/ 19-05~19-08) + 신규 12.
           합칠 자격은 기계로 성립한다: git diff --stat 1dc10a7d..HEAD -- src/ 가 빈 출력
           (같은 제품 빌드 · 감시자 확인). 역할을 나눠 적는다 —
             기존 10 = (b) 재생성 채택 재현용 (유일한 퇴화 표본이 이 발화쌍에서 났다)
             신규 12 = (a) 발동 폭용
           신규만으로 (b) 를 결론내면 빈 측정을 결론으로 세우는 것이다(H09 계열)
신규 발화   4종 × 3사슬. 종별 n=3 이므로 **비율(%) 금지 — "3사슬 중 k" 원자수로만 적는다**
  ① "시작문서 폴더 안에 뭐가 있어?"                    **정오 미채점**(채점기 어느 칸도 아님)
  ② "작업 폴더에서 제일 큰 파일이 어떤 거야?"           정답 안정(AGENTS.md 가 직속·재귀 양쪽 1위)
                                                    · 폭 기여는 말귀 축뿐, 자리 축은 기존과 같음
  ③ "여기 md 파일 전부 몇 개야?"                      **정오 미채점** — "여기"가 직속 2 / 재귀 6
                                                    둘 다 참인데 동결 채점기는 재귀만 정답으로 친다
                                                    (자에서 오판이 나므로 채점 대상에서 뺀다)
  ④ 2턴쌍: "작업 폴더에 뭐가 들어있는지 좀 봐줘" →
           "시작문서 안에서 제일 긴 건 뭐야?"          정답 안정(현재상황.md 바이트·글자수 양쪽 1위)
산출표     (a)(b) **발동 실측만** — 정오 칸을 넣지 않는다(감시자 조정 5):
           · 재생성 발동 수 / 총 턴 수
           · 주입 계약말 원문 — **직전 요청 대비 새로 늘어난 메시지 전량 diff** 로 추출
             (마지막 메시지만 훑으면 놓친다 · 미검출도 결과로 적는다)
           · 최종답 == 첫 생성 / == 재생성 / 어느 쪽도 아님 (문자열 대조)
금지       채점기.mjs 수정(자 변경은 배치 전 + 검문) · 분석기 안에 정오 규칙 자체 구현
           (두 번째 자 금지) · 사람 분류 · n=1 결함 승격 · 비율 표기
```

## 7-m. J12 2차 실측표 — 사슬 22개 · 발동 실측 (정오는 채점기 소관 · 이 표에 없다)

도구: `사슬분석기.mjs`(발동 실측 전용 · 사람 분류 0 · 전부 문자열 대조).
표본: 기존 10(재현용) + 신규 12(폭용) = **22사슬 · 총 35턴**. 같은 빌드 확인(src diff 0).

```
(b) 재생성 채택      **0 / 35턴** — 최종답 출처는 35턴 전부 「첫 생성 채택」.
                    재생성 호출 16회가 **한 번도 최종답이 되지 않았다.** 현재 피해는
                    버려지는 왕복(지연·토큰)이고, 답의 진실을 바꾼 사례는 이 22사슬에 없다
(a) 재생성 발동      호출 16회 / 35턴. 발화별(종별 n=3 · 원자수):
    "작업 폴더에 뭐가 들어있는지 좀 봐줘"(2턴쌍 1턴)   13사슬 중 13 발동
    "시작문서 폴더 안에 뭐가 있어?"                    3사슬 중 2 발동
    "작업 폴더에서 제일 큰 파일이 어떤 거야?"           3사슬 중 0
    "여기 md 파일 전부 몇 개야?"                      3사슬 중 0
    2턴쌍의 2턴째(제일 긴 문서)                       13사슬 중 0
주입 계약말(원문)    "방금 답은 아직 목적에 안 닿았어요. 지금 응답에서 **손을 부르거나**,
                    못 하는 이유를 원장에 남은 사실로 말해 주세요. 「~할게요」 같은 예고만으로
                    끝내지 말아 주세요 — 예고는 한 일이 아니에요." (turn.js:2909 실물)
주입 사유(요청 diff) 재생성 요청 시스템에만 새로 생긴 줄:
                    "[이번 턴 사실] 이 턴의 요구에 아직 안 닿았다."
                    "- 아직 이 턴에 한 번도 안 써 본 손: web.search · web.collect ·
                       local.terminal · … (10개)"
```

**오판 기제 — 코드까지 밟았다**: `turn.js:2830 걸음이말하는미달` → `빈손으로끝났나`
(`exit-verification.js:67`)의 **`if (/\?|까요|할까/.test(t)) return true`** — 답에 물음표가
하나라도 있으면 「심문 모양 = 빈손」이다. 실물 대조:
```
발동한 답   "시작문서 폴더 안에는 지금 네 개가 있어요. - README.md … 
            이 중에서 먼저 같이 보고 싶은 파일 있어?"   ← 손을 불러 정확히 답하고
                                                      후속 제안 한 문장을 붙였다
미발동 답   "…가장 큰 단일 항목은 시작문서 폴더(60K)입니다. …"  ← 물음표 없음
```
**같은 파일 안에서 두 자가 반대 방향이다**: `대조대상인가`(:83)는 *"물음·제안 — 되묻는
중이다. 여기에 원장 대조를 물리면 **심문을 벌하는 개입**이 된다"* 며 물음을 빼주는데,
`빈손으로끝났나`는 같은 물음을 빈손으로 잡는다. 오너 정의(2026-08-14 · 말귀)와도 충돌:
*"되물음은 감점이 아니다. 파악 없이 묻는 것이 떠넘김이다."* 여기서 발동한 답들은 파악
(목록 확인)을 마치고 최소 질문 하나로 다음을 확정하려는 정상 마무리다.

**3차 표적(측정이 지목)**: 되물음을 빈손으로 읽는 규칙. 수리는 3차 착수 검문 후 —
2차는 측정만이며 제품 코드 0줄이다.

## 7-n. J12 3차 선등록 — 표적·설계·예측 (수리 전 고정 · 판정 둘 반영)

**내 귀속표 시도는 무효다(자기 정정)**: 감시자가 요구한 호출부 귀속을 «[이번 턴 실행 사실]
없음» 문자열로 가르려 했는데, 그 줄은 **도구 결과를 받은 요청 60건 전부**에 나타나는 상수였다
(정합 0건). 판별력이 없으므로 그 표(«16회 전부 :2822»)는 버린다. 도구는
`scripts/terminal-qualification/귀속표.mjs` 에 남기되 **결과는 쓰지 않는다**.

**유일하게 선 귀속 증거 — 손 관리자의 같은-발화 대조**(같은 발화·같은 걸음 순서·답글만 다름):
```
"시작문서 폴더 안에 뭐가 있어?" 3사슬 (전부 local_file → work_deliverable)
  19-30-03  답 끝 "…현재상황.md"                     물음표 없음  → 발동 0
  19-30-40  답 끝 "…먼저 같이 보고 싶은 파일 있어?"    물음표 있음  → 발동 1
  19-31-09  답 끝 "…어느 파일부터 열어볼까?"           물음표 있음  → 발동 1
같은 걸음에서 답글 한 조각만 달라 발동이 갈렸다 → 귀속은 말투 갈래(:2828)다.
보조: 최종답 35개 중 물음 패턴 15개 ≈ 발동 16.
```

**표적**: `turn.js:2828 걸음이말하는미달` 이 타는 `빈손으로끝났나` 의 심문(물음표) 갈래.
:2740(후보 미개봉 봉인 · r3)과 :2822(걸음 0 · 원장 인자라 말투에 안 닿음)는 **안 건드린다.**

**설계 — 손 관리자 조건 셋 그대로**:
```
(가) 67줄 규칙 자체 수정 금지     → 함수에 **옵션 게이트**만 추가. 인자 없이 부르는 기존
                                 호출부·검사(r3 등)는 동작 불변
(나) 심문/약속 갈래 분리          → 심문을 꺼도 약속 갈래(게요|겠습니다|겠어요)는 계속 판정.
                                 원장 인자(가져온것)로 말투 전체를 덮지 않는다
(다) 「빈 턴」의 자는 재사용        → `working-state.js 빈손으로돌아왔나` 한 벌(F-107 이 세운 자).
                                 새 자를 만들지 않는다
호출부   const 사실을가져온걸음 = 부른것들.some(성공 && !빈손으로돌아왔나(r))
        걸음이말하는미달 = 부른것들.length && (막힌것 || 안밟은수단 ||
                          빈손으로끝났나(답글, null, { 심문은빈손아님: 사실을가져온걸음 }))
```

**예측(수리 전 고정 · 원자수 · 비율 금지)**:
```
"시작문서 폴더 안에 뭐가 있어?" 3사슬   발동 2 → **0**
"작업 폴더에 뭐가 들어있는지…"(2턴쌍)   발동 14 → 내려감(바닥 미지수 — 귀속표가 무효라
                                      이 셀의 :2828 몫을 모른다. **숫자를 예측하지 않는다**)
"제일 큰 파일" 3사슬 · "md 몇 개" 3사슬  발동 0 → **0 유지**(내려가면 안 되는 쪽)
정오                                  채점기.mjs 로 턴별 1:1 대조 — 한 턴이라도 바뀌면 실패
반대시험(검사로 고정)                  ① 성공 걸음이 껍데기(빈손으로돌아왔나=true)+물음표 →
                                      여전히 발동  ② 성공 걸음+"이어서 해볼게요"(약속) →
                                      여전히 발동(no-turn-closes-on-a-promise 계열 보호)
양성 대조                             재측정 회차에 «반드시 발동해야 하는» 사슬을 함께 넣는다 —
                                      0 이 「수리됨」인지 「주입서명 불일치」인지 가른다
```

## 7-o. J12 3차 실측 — 수리 후 (선등록 8e983289 대조)

**수리**(제품 2파일): `exit-verification.js` 빈손으로끝났나에 **끄는 문** 하나(옵션 게이트 ·
규칙 자체와 기본값은 불변) + `turn.js:2828` 이 「사실을 가져온 걸음」을 그 문에 넘긴다.
자는 새로 안 만들었다 — F-107 의 `빈손으로돌아왔나` 한 벌 재사용(손 관리자 조건 다).

```
셀별 발동 (선등록 예측 → 실측 · 원자수)
  "시작문서 폴더 안에 뭐가 있어?"      3사슬   2 → **0**   예측 적중
  "작업 폴더에 뭐가 들어있는지…"(2턴쌍) 3사슬   3 → **0**   예측은 「내려감(숫자 미예측)」
  "작업 폴더에서 제일 큰 파일…"        3사슬   0 → **0**   0 유지(내려가면 안 되는 쪽) 적중
  "여기 md 파일 전부 몇 개야?"         3사슬   0 → **0**   0 유지 적중
양성 대조   기존 22사슬 재분석에서 **여전히 16 검출** — 주입 서명이 살아 있다.
           신규 0 이 「수리됨」이지 「서명 불일치」가 아님이 이것으로 갈린다
검사       j12-asking-after-facts-is-not-empty-handed 5/5 · 반대시험 성립(수리 걷으면 ①⑤ 빨강)
           반대시험 둘 고정: 껍데기 걸음(읽은상태 shell)의 물음은 여전히 빈손 ·
           사실을 가져오고 "이어서 해볼게요"로 닫으면 여전히 빈손(약속 갈래 보존)
회귀       4,246/4,246 · 실패 0 · 보류 0
```

**정오는 「불변 확인 불가」 — 그리고 이것은 선등록의 설계 결함이다(감시자 정정)**:
선등록은 *"채점기로 턴별 1:1 대조 · 한 턴이라도 바뀌면 실패"* 였는데, 이는 돌려 보고
어려웠던 것이 아니라 **쓰던 순간에 이미 원리적으로 불가능했다** — 라이브는 비결정적이라
같은 회차를 다시 못 돌린다. **다음 선등록은 정오 조항을 결정적 경로(고정 원본 재투입)로만
건다.** 총계는 수리 후 n=10 에서 정답 3 · 한정·고지 0 · 무근거 단정 5 · 기계 판정 불가 2
(§7-j 수리 전 1·0·9·0)이지만 **다른 회차의 값이므로 수리 효과로 귀속하지 않는다.**
기제로도 조건부로만 말할 수 있다(감시자 정정 — 내 첫 문장은 조건 없는 단정이라 틀렸다):
**앞답이 비어 있지 않은 턴에 한해** 되부름 산출이 답 텍스트를 못 바꾼다. **앞답이 빈 턴은
F-84 `되살리기()` 가 되부름 글로 갈아친다** — 이번 표적(사실을 댄 답 + 물음표)은 앞답이
비지 않아 해당 없지만, 일반 명제로 쓰면 다음 슬라이스가 틀린 전제 위에서 출발한다.
새로 생긴 「기계 판정 불가 2」는 즉석 분류하지 않고 다음 선등록으로 넘긴다.

**남은 칸(손 관리자 열거 5 중 이번에 닫은 것은 1)**: ① 되부름 산출은 걸음을 안 고르면
**앞답이 빌 때만 되살아난다**(F-84 `되살리기()` · turn.js:2941-2951 — "버려진다"는 내
서술은 감시자 정정으로 틀렸다) ② 빈손 걸음이 `막힌것` 에 안
들어옴(F-107 가족의 문) ③ 걸음 0 턴과 걸음>0 턴이 서로 다른 자로 판정됨 ④ 사슬로는
정오를 안 잼(채점기와의 이음 없음) ⑤ `안써본손` 게이트가 사실상 항상 열림.

## 7-p. ★ 비교 판단 정정 — 오너 2026-08-15 (④ 정면 대조 전에 반드시 읽어라)

세션 종료 보고에서 내가 T5 우위를 과장했고 오너가 근거 문서와 함께 넷을 정정했다.
**④ 정면 대조는 이 프레임 위에서 설계해야 한다.**

```
내가 한 말                     사실 (오너 정정)
"세션 끝나면 사라진다"          비교군은 세션 재개·프로젝트 기록·CLAUDE.md·자동 기억이 있다.
                             나는 이 세션 내내 그 기억을 읽고 쓰면서 그렇게 말했다(자기모순)
"프로젝트 폴더에 갇혀 있다"     MCP 로 외부 도구·데이터 연결 · Computer Use·Slack 도 공식 지원.
                             반대로 T5 화면 손은 연기, 채널 일부 미완 — **현재 제품 차이가
                             아니라 T5가 앞으로 만들 범위의 차이**다
"원장이 없다"                  대화·도구 실행 기록·작업 목록·편집 이력이 있다. T5 원장(제안→
                             승인→실행→성공→전달→승계를 한 사실로 묶음)은 더 강한 개념이나
                             **아직 달성 안 됨** — 오늘도 원장과 답이 어긋나는 문제가 계속 나왔다
"남은 단계 둘"                 예정된 관문이 둘이라는 뜻. 두 번 하면 완성이 아니다 —
                             깊이 재판이 또 미달하면 그 결과가 다음 수리 위치를 만든다
```

**J12 3차가 고친 것의 정확한 크기**(오너): 정답을 바꾸지 않던 **재생성 낭비**다.
§7-j 의 측정값 역전(원4)과 존재하지 않는 파일·크기 날조(원6)가 해결됐다는 증명이 아니다.

**지금 T5의 확실한 차별점은 기능 우위가 아니다**(오너 정본):
```
① 오너가 직접 소유하고 방향을 정하는 제품
② 한국어와 일반 사용자 중심 설계
③ 모델·손·권한·원장·기억·자동화를 하나의 운영체제로 묶으려는 구조
④ 실패한 경험까지 다음 개발과 행동에 통제된 방식으로 반영하려는 목표
```
진전은 크다(참여 0→9/10 · 한글 경로 · 재귀 조사 · 재생성 낭비 제거 · 자동 채점 체계 ·
방식의 저장소 인계). 그러나 **추월이 아니라 주행 시작**이다. 완주 기록은 아직 없다.

**F-121(깊이) 슬라이스의 상태 — 닫지 않는다**: 코드는 커밋으로 봉인됨(f121 4/4 · 회귀
4,241) / 슬라이스 판정선은 두 배치 연속 미달 → **슬라이스는 열린 채**로 둔다.
childCount 효과 관측은 18-30-03 1건(n=1)뿐. 재판은 J12 해소 후.

**봉인 재판 구성(§7-g 선등록 복원 — 내 원발화×10 안은 「처음 보는 발화 ≥4」 조건 탈락으로
기각됨)**: 원발화 2턴 ×6 + 처음 보는 발화 ×4(제일 큰 파일 ×2 · md 개수 ×2), 합 10.
**무근거 단정 0 은 열 회차 전체에 걸린다**(원발화만이 아니다 — 1건이라도 나오면 미달).
정답 ≥7 도 전체 기준. v2 자로 채점, 미달이면 손 결함 확정 + 본질 재조사 계속.

**(3) J12 의 라이브 표본 (n=1 · 표본이지 결함 등급 아님)** — 한 턴에 답 생성 요청이
3회 나갔고(요청 6·7·8), 6의 정답(AGENTS.md 16,051B)이 7·8에서 오답(15,837B 파일이 제일
길다)으로 퇴화했다. 모델 입력에 ???? 원문이 있던 캡처는 세션 스크래치에만 있다
(scratchpad/모델입력-*.json — 저장소 밖). J12 착수 때 응답 저장까지 하는 캡처로 다시 잡아라.

**기록만 하고 안 연 것**(감시자 재검증 반영): 샌드박스에서 돈 probe 가 exit≠0 이면서
looksBlocked 아닌 갈래도 probeChangedNothing 을 받는다. **failureState 는 막지 않는다** —
exit≠0 probe 도 failureState none 이라 이번 변경으로 estimated → confirmed 로 실제 이동한다
(감시자 재현: probe `ls /없는자리` → failureState none · confirmed). 실린 문장이
"실행했는데 오류로 끝났어요(코드 1)" 로 exit code 를 그대로 말해 사용자면 거짓이 없고
이전(추정 분류)보다 참에 가까우므로 열지 않는다. 그리고 검사 ⑥ 은 스텁 구조상 ⑦ 과 거의
같은 것을 물며 실질 증명은 ⑧ 이 진다 — 기록만 한다.

**측정 도구가 늘었다**: `맨몸판.mjs`(커널 겹 없는 대조군 · A~E 팔) · 모델입력캡처→절제재생
(제품이 모델로 보낸 요청 원문을 그대로 다시 쏘며 변수 하나씩 절제 — 방법은 §3-b).
**가설을 고르지 말고 이걸로 재라.** 가설 셋을 순서로 태운 것이 네 번의 실패였다.

## 7-q. F-121 깊이 재판 — **착수 검문에서 멈췄다. 라이브를 안 열었다**

기준 커밋 **c571b903** · 제품 코드 **0줄** · 라이브 **0회차** · 자 변경 **0줄**.
루프 ②(착수 검문)를 ①보다 먼저 밟았다 — 앞 세션이 건너뛴 자리다.

### 7-q-1. 무엇을 하려 했나 (선등록 초안)

§7-p 의 봉인 재판 구성(원발화 2턴 ×6 + 처음 보는 발화 ×4 = n 10 · 무근거 단정 0 && 정답 ≥7)을
그대로 복원해 돌리고, **미정 결정 둘**을 결과 보기 전에 못 박으려 했다:
```
(가) 「기계 판정 불가」 분류 규칙 — P2(개수 일치하나 전수 명령 증거 없음 · 채점기.mjs:57)를
     「무근거 단정」으로 옮기고, P1(:55)·P3(:66)은 그 칸에 두되 **정답 분자에서 제외**
(나) 정오 조항을 결정적 경로로 — 「턴별 답 텍스트 불변」은 라이브 비결정성 때문에
     원리적으로 불가능하므로 **걸지 않고**, 정오는 라이브 「정답」 칸 하나로만 잰다.
     대신 배치 전에 (나-1) 채점기 결정성 확인 · (나-2) §7-j 구간 대조를 밟는다
```

### 7-q-2. 착수 검문 둘 — 판정

```
t5-process-supervisor   판정 **조정** (조정 6항 이행 후 진행 · 설계 자체는 기각 사유 없음)
t5-hand-keeper          판정 **「이 구성으로는 착수 안 한다」** · 손 아님(계측기 배치)
```

**감시자가 문 것**(내가 그대로 흡수한다):
```
1 (나-1)의 이름을 낮춰라 — 채점기는 정적 JSON 을 읽는 순수 함수라 2회 동일은 거의 자명하다.
  「자의 건전성」이 아니라 **「채점기 결정성 확인」**이다. 건전성은 (나-2)가 진다
2 P3 누수를 선등록에 명시하라 — 무늬_단정(:20)은 계약이 아니라 **관측된 문장 모양**에서
  뽑힌 정규식이다. 바꿔 쓴 단정은 P3 로 빠져 「무근거 단정 0」을 **통과 쪽으로** 샌다
3 순서를 박아라 — (가) 커밋 → (나-1) → (나-2) → 라이브. **(나-2) 값을 보고 (가)를 고치지 않는다**
4 안 잰 범위를 적어라 — 이 자는 §7-p (3) 「한 턴 안 재생성 퇴화」와 「모델 입력 원문」을 안 잰다
5 통과해도 일반화 금지 — 통과는 "이 구성에서 깊이 손이 섰다"까지다. 제목 등급어 금지는
  미달 때만이 아니라 **통과 때도** 건다
6 픽스처 불변을 스키마 지문과 같은 줄에 적어라 — 정답 판정이 파일명 화이트리스트(:60)에
  묶여 있어 방 구성이 바뀌면 자가 **거짓 미달**을 낸다
+ (가)가 관대화 아님을 세우는 것은 「내가 결과를 안 봤다」가 아니라 **방향의 단조성**이다.
  신뢰 요청이 아니라 기계 사실로 적어라
+ ~~결재 안건에 한 줄: 병합하면 도구·증거가 삭제된다~~
  → **★ 이 줄은 거짓이었다. 아래 7-q-7 에 정정과 원인을 적었다**
```
브랜치 4개·자리 밖 worktree 를 이 재판의 정의역 밖으로 둔 판단은 **양쪽 다 동의**.
동시 수정 위험도 기계 확인상 없다(네 브랜치 어느 것도 이 파일들을 갖고 있지 않다).

### 7-q-3. ★ 재판을 못 여는 이유 둘 — **내가 직접 돌린 값이다**

**(i) 선등록과 같은 구성의 배치가 이미 있다** (손 관리자 지적 · 내 실행으로 재현)
```
node scripts/terminal-qualification/채점기.mjs 19-48-44 19-50-04
→ 지배 숫자 (n=10): 정답 3 · 한정·고지 0 · 무근거 단정 5 · 기계 판정 불가 2
구성   "그 중에 제일 긴 문서가 뭐야?" 2턴 ×6 + "제일 큰 파일" ×2 + "md 몇 개" ×2  = 선등록과 동일
sourceHead  8e983289 (J12 3차 **선등록** 커밋 — 수리는 그때 작업트리에 있었고 55c284c3 으로 커밋됨).
            즉 **「수리 적용 상태였다」는 원본 기록만으로는 기계 증명이 안 된다** — 열린 자리로 적는다
판정선(무근거 0 && 정답 ≥7) 대비: 무근거 5 남고 정답 4 모자람
```
같은 구성·같은 픽스처·같은 자로 제품 0줄에서 다시 돌리는 것은 **결과를 본 뒤의 재굴림**이다.
새 배치를 열려면 이 값에 대한 **예측을 먼저 못 박아야** 정보가 생긴다.

**(ii) ★ 재는 자가 양방향으로 샌다** — 합성이 아니라 **라이브 실측 반례 셋**
```
L1 (통과 쪽 누수 · 거짓 정답)
   19-49-49 "제일 큰 개별 파일은 **둘입니다(같은 크기)**" + du 16K 나열  → 채점기 **정답**
   19-49-57 "**공동 1등**"                    같은 실패 모양            → 채점기 **무근거 단정**
   §7-i (b-해상도)로는 **둘 다 무근거 단정**이다. 문구가 판정을 갈랐다.
   원인 채점기.mjs:60 — `답1위` 는 **주장된 1위가 아니라 답 텍스트에 처음 등장하는
   화이트리스트 파일명**이다. 폴더로 답해도 뒤에 AGENTS.md 가 나오면 정답이 된다
L2 (단정 무늬 누수)
   19-48-44 "제일 **깁니다**" · 19-48-55 "제일 **많은** 문서는"
   → `무늬_단정 = /제일 (긴|큰)|가장 (긴|큰)/`(:20) 미검출 → **기계 판정 불가**로 빠짐.
   둘 다 최상위만 재고 순위를 단정했다. **「기계 판정 불가 2」의 정체가 이것이다 —
   분류 규칙이 필요한 새 칸이 아니라 자의 누수였다**
L3 (한정 무늬 누수 · 고칠 수 없는 자리)
   19-49-36 "지금까지 **확인된 것만** 보면" → 무늬는 「확인된 것 중」이라 미검출 → 무근거 단정.
   §7-g 가 「무늬 둘뿐 · 라이브 출력을 본 뒤 추가 금지」로 동결했으므로 **고치지 않는다.**
   엄격 방향이라 판정선에 해를 주지 않는다 — 기록만 남긴다
검사   `scripts/terminal-qualification/채점기.mjs` 를 무는 검사는 저장소에 **0개**다
       (test/ 전수 grep — 걸린 4파일은 전부 다른 뜻의 「채점기」).
       선례는 있다: `test/organ-round-ruler.test.js:1` 「채점기의 자도 검사를 받는다」
양성 대조 하나는 섰다   §7-j 구간(18-54-28~18-55-59) 실행값 **정답 1 · 한정 0 · 무근거 9** —
       문서 공표값과 정확히 일치. 다만 이는 **합치(concordance)이지 오검출률 측정이 아니다**
```

**판정선 「무근거 단정 0」은 자의 오검출 0 을 보이기 전에는 물리적으로 쓸 수 없다** —
오검출 1건이면 판정이 뒤집힌다. 이것이 라이브를 안 연 이유다.

### 7-q-4. (가)·(나) 초안의 처리 — **되돌린다**

```
(가) 보류    실측이 P2 가 아니라 **P3 가 문제**임을 보였다(L2). P2 이동은 방향은 옳으나
            표적이 아니었다. 그리고 감시자 지적대로 P2 이동은 명령 캡처 누수(§7-g:366)가
            재발하면 **계측기 결함을 손 결함으로 귀속**시킨다. P1·P2·P3 규칙은
            **자 수리 뒤에 다시 선등록**한다
(나) 유지    확정 1·2 는 그대로 산다(불변 조항 안 검다 · 정오는 정답 칸 하나로).
            (나-1)은 「채점기 결정성 확인」으로 이름을 낮춘다. (나-2)는 이미 밟았고 합치했다
소급 금지    §7-h(6) · §7-j(9) · 19-48 배치(3·0·5·2) 의 공표 지배 숫자는 **무엇이 나와도 불변**
```

### 7-q-5. 손 관리자가 잰 거리 — 터미널 손 **7칸 남음**

```
요구 폭 8칸 중 이번 재판이 덮는 것: **집계·측정 1칸 + 출력→최종답 정합 반 칸**
① 측정→최종답 정합(잰 값을 뒤집지 않음)   열림 · 무게중심
② 최종답에 T5 가림 문자열 개입([가림])     열림 · 손 밖 기관이나 답을 죽인다
③ 말귀 축: 「파일」 물음에 「폴더」 답       열림 · 세 배치 반복
④ 변경 실행·승인 등급의 라이브 완주        미측정
⑤ 장기 실행·스트리밍·대화형 TTY           미측정
⑥ 실패 시 다른 명령으로 끝까지            미측정
⑦ **자의 오검출 0**                       열림 · 오늘 반례 셋
「처음 보는 발화 ≥4」는 문자로는 충족되나 **실질로는 0**이다 —
"제일 큰 파일" · "md 몇 개"는 §7-h → §7-j → 19-48 로 **세 배치 12회** 이미 돌았다
```

### 7-q-6. 다음 한 걸음 — **자 수리 슬라이스가 선행한다**

```
선빨강    위 L1·L2 (라이브 실측 반례 · 합성 아님)
표적      채점기.mjs:60(답1위 = 주장된 1위여야 한다) · :20(단정 검출) ·
          해상도 동률은 문구가 아니라 **구조**로 잡는다
검사      채점기를 무는 검사 파일 신설(선례 organ-round-ruler)
자기시험   수리 후 §7-j 구간이 여전히 1·0·9 인가(양성 대조 보존) + 심은 반례 양방향 0
전향 적용  공표된 세 배치의 지배 숫자는 소급하지 않는다
그 뒤에    F-121 재판을 다시 연다 — 새 배치를 열 때는 **19-48 값(3·0·5·2)에 대한 예측을
          먼저 못 박는다**(예측 없이 돌리면 이미 아는 문장만 늘어난다 · 손 관리자)
```

### 7-q-7. ★ 정정 — 「병합하면 도구·증거가 삭제된다」는 **거짓이었다**

§7-q-2 에 그렇게 적었고 인계서 §6 에도 옮겼다. **둘 다 틀렸다.** merge-tree 실측:
```
                        충돌     도구(scripts/terminal-qualification/)   증거(terminal-2026-08-15/)
HEAD 현재                 —              12                                133
+ codex/terminal-core    있음            13                                133
+ codex/terminal-rebuild 있음            14                                133
+ codex/terminal-stage1  있음            14                                133
+ lane/ux                없음            12                                133
```
**하나도 안 사라진다.** 병합 기점(d205ffb3)에 이 파일들이 0개이므로, 우리 쪽 추가는 병합에 그대로 남는다.

**원인이 둘이고, 서로 다른 결함이 같은 오답을 냈다 — 이게 이 사고의 값어치다**:
```
(원인 1) 감시자    `git diff HEAD <브랜치> --name-status` 의 **D 219건**을 병합 삭제로 읽었다.
                 D 는 두 트리의 **차이**이지 병합 결과가 아니다 (전임 세션 지적)
(원인 2) 나       내 검증은 merge-tree 를 제대로 썼는데도 같은 오답을 냈다 —
                 `git ls-tree -r --name-only` 는 **한글 파일명을 따옴표로 감싸 출력**한다.
                 `grep '^scripts/...'` 의 `^` 앵커가 그 줄을 전부 흘렸다.
                 `-z | tr '\0' '\n'` 로 다시 재니 값이 뒤집혔다
```
**교훈(§5 ⑨ 와 같은 계열)**: 검증이 원 주장과 **같은 답을 냈다고 검증된 것이 아니다.**
서로 다른 결함이 같은 오답으로 수렴할 수 있다. 자를 바꿔 한 번 더 재라.

**남는 진짜 사실**: lane/ux 는 충돌 0, codex/terminal-* 셋은 충돌 있음.
게이트 빨강의 성격은 「데이터 손실 위험」이 아니라 **「충돌 해결 + 내용 판정 필요」**다.
그리고 **이건 이 슬라이스가 아니다** — 오너 결재 사항이다.

---

## 7-r. 채점기 판별력 수리 — **선등록** (결과 보기 전 고정 · 감시자 조정 6항 흡수)

기준 커밋 **7b7303bd** · 회귀 기준선 **4,246 통과 · 실패 0 · 보류 0** (인용이 아니라 **내 실행값**)

### 7-r-1. 닫는 문장
**채점기의 칸은 문구가 아니라 근거 구조로 갈린다 — 같은 실패 모양은 같은 칸에 들어간다.**
(사건이 아니다. 「19-49-49 를 옮긴다」가 아니라 자의 정의역 전체다)

### 7-r-2. 선빨강 (이미 커밋 · 7b7303bd §7-q-3)
L1 통과 쪽 누수(19-49-49 정답 ↔ 19-49-57 무근거 · 같은 실패 모양) ·
L2 단정 무늬 누수(19-48-44 "제일 깁니다" · 19-48-55 "제일 많은") ·
채점기를 무는 검사 **0개**.

### 7-r-3. 수리 자리 셋 (계측기만 · 제품 코드 0줄)
```
(자-1) :60 답1위    「답 텍스트 첫 화이트리스트 이름」 → **주장된 1위**(최상급 어사와 결부된
                   이름). 후보는 하드코딩 목록이 아니라 **그 회차의 재료실측 키**에서 유래
(자-2) :20 무늬_단정 굴절형 넷(/제일 (긴|큰)|가장 (긴|큰)/) → 최상급 어사 자체로
                   (제일·가장·최대·최다·1위·으뜸)
                   ⚠️ **엄격 방향이 아니다**(감시자 조정 2) — 단정 분기는 무근거뿐 아니라
                   **정답도 낸다.** 이번 배치가 엄격으로 나오는 것은 19-48-44/55 의
                   명령 캡처가 각각 ["ls -l"]·[] 라 폴더안잼이 false 인 **우연**이다.
                   우연을 성질로 적지 않는다
(자-3) 해상도       ~~문자열 어휘(공동 1·다 비슷)~~ → **명령 캡처의 해상도로만**(감시자 조정 3).
                   §7-i (b)+(c)가 이미 「잰 도구의 해상도」·「캡처 없으면 크레딧 없음」으로
                   말한다. 답 문자열에서 해상도를 읽으면 **원 결함(문구가 판정을 가름)의 재발**이다.
                   동률 실패 세 회차(19-49-49 · 19-49-57 · 18-55-47)의 캡처는 **전부 du 뿐**이고
                   바이트 해상도 명령이 잡힌 회차는 **하나도 없다**(감시자 실측)
```

### 7-r-4. 판정선 — 결과 보기 전 고정
```
① 심은 반례 양방향 0   신설 검사가 거짓 정답 방향·거짓 미달 방향을 둘 다 문다.
                     반례는 **라이브 원본 회차 인용**으로 문다 — 합성 fixture 생성기를
                     만들면 그 순간 감시자 §3-6 위반이다
② 양성 대조 보존      §7-j 구간 재실행 = **정답 1 · 한정 0 · 무근거 9 · 불가 0** 유지.
                     (자-1)이 뒤집을 후보 둘을 **사전 고정한다**(감시자 조정 4):
                       18-55-02 → 무근거 유지 (현재상황.md 를 단독 1위로 진짜 단정)
                       18-55-41 → 무근거 유지 ("16K 세 개가 가장 크고" · 이름 단정 없음)
                     **「어긋나면 사후에 어느 쪽이 맞는지 대조」 조항은 삭제한다** —
                     그것이 관대화 3회의 공통 기제(사후 재해석 문)다. 어긋나면 **멈춘다**
③ 반대시험           수리를 걷으면 ①이 다시 빨강
④ 전체 회귀           4,246 → 신설 검사만큼만 늘고 실패 0 · 보류 0
⑤ ★ 회차별 전이표     밴드가 아니라 **표**다(감시자 조정 1 — 밴드 「정답≤3·무근거≥5」는
                     무변화 3·0·5·2도 통과시켜 정보량 0이었다).
                     19-48 배치 재채점의 **유일하게 허용되는 결과**:
                       19-48-44  기계 판정 불가 → 무근거 단정
                       19-48-55  기계 판정 불가 → 무근거 단정
                       19-49-49  정답          → 무근거 단정
                       나머지 7회차            불변
                       합계 **정답 2 · 한정·고지 0 · 무근거 단정 8 · 기계 판정 불가 0**
                     **그 외 어떤 이동이든 멈춤 사유다**
```

### 7-r-5. 안 고치는 것 · 자의 알려진 한계 (기록만 · 감시자 조정 5)
```
L3 한정 무늬 누수         19-49-36 "확인된 것**만**". §7-g 가 무늬 둘로 동결했다. 엄격 방향
해상도 명시형의 거짓 미달   du 로 재고 해상도를 밝힌 답은 §7-i 상 한정·고지인데 무늬 둘로는
                        못 잡아 무근거로 간다. **엄격 방향이라 안 고친다**
basename 중복            재료실측에 `./README.md`(11,373)와 `./시작문서/README.md`(14,601)가
                        둘 다 있다 — **이름 비교로는 못 가른다.** 자의 알려진 한계
P1·P2·P3 분류 규칙        자 수리 **뒤에** 다시 선등록한다
「기계 판정 불가」 0 은 목표가 아니다  결과일 뿐이다. 목표로 세우면 분모 채우기다(감시자)
제품 코드                0줄. src/** · test/** 기존 파일 불가침(신설 1파일만)
소급 금지                §7-h(6) · §7-j(9) · 19-48(3·0·5·2) 공표 지배 숫자는 **불변**
```

## 7-s. 채점기 판별력 수리 — **실측** (선등록 §7-r 대조 · 커밋 전 고정값)

### 7-s-1. 판정선 다섯 — 전부 내 실행값
```
① 심은 반례 양방향 0   신설 test/scorer-ruler-cuts-by-ground-not-wording.test.js **6/6**
                     · L1 19-49-49 · 19-49-57 둘 다 무근거 단정 (같은 실패 모양 = 같은 칸)
                     · L2 19-48-44 · 19-48-55 무근거 단정 (기계 판정 불가로 안 빠짐)
                     · 거짓 미달 방향(**인라인 파서 시험**): 픽스처 밖 이름이 진짜 1위 → 정답
② 양성 대조 보존      §7-j 구간 = **정답 1 · 한정·고지 0 · 무근거 단정 9 · 불가 0** (공표값 유지)
③ 반대시험           수리를 걷은 사본 → 19-49-49 **정답(거짓)** · 19-48-44 · 19-48-55
                     **기계 판정 불가**. 옛 누수가 정확히 되살아난다 (합계 3·0·5·2)
④ 전체 회귀           **4,252 통과 · 실패 0 · 보류 0** (기준선 4,246 + 신설 6)
⑤ 회차별 전이표       선등록 예측과 **정확히 일치**:
                     19-48-44 불가→무근거 · 19-48-55 불가→무근거 · 19-49-49 정답→무근거 ·
                     나머지 7 불변 → **정답 2 · 한정·고지 0 · 무근거 단정 8 · 불가 0**
```

### 7-s-2. ★ 감시자가 잡은 것 — 내 자기신고보다 컸다
```
(가) 근거 문자열이 거짓이 됐다   옛 자의 '전수 아님 + 무늬 없음 + 단정' 한 줄이
     **전수인데 1위를 못 짚은 회차**에도 붙었다. 칸은 맞고 근거가 거짓인 상태 —
     근거를 기계 상태 그대로 적도록 고쳤다(`전수 + 주장 1위 미검출 + 무늬 없음`).
     칸은 하나도 안 움직였다(②⑤ 재확인)
(나) 근거가 바뀐 회차는 하나가 아니었다   내 신고는 19-49-36 하나였는데 실제로는
     18-54-28 · 18-55-02 · 18-55-41 · 18-55-47 도 바뀌었다. 특히 **18-55-02 · 18-55-41 은
     옛 자의 「잰값 모순」 탐지를 잃었다**(칸은 무근거 단정으로 불변). 그 상실을 검사가 문다
(다) ★★ 원인 — **계측기가 한글 파일명을 잃는다**   원본의 `재료실측` 키가
     `"./????/????.md"` 로 깨져 있다. `그냥써본다.mjs:68` 이
     `/bin/zsh -c 'find . -type f -exec wc -c {} +'` 를 **로케일 없이** 돌린 탓이다 —
     **F-119 와 정확히 같은 기전**이고, §3-7 이 열거한 세 문 밖의 **네 번째 문**이다.
     귀결(★ **정정 — 출처 감시자**: 내 첫 서술 「발동 0회」는 **거짓이었다.** 같은 커밋의 자가
     `주장 1위 AGENTS.md` 를 7줄 찍는다. 내가 다시 센 값을 적는다):
       **주장된1위 도달 8회차 · 비널 7(전부 AGENTS.md) · 널 1(18-55-02 · 한글 이름) ·
       칸을 결정한 적 0회**(정답·잰값 모순 분기에 한 번도 도달 못 함).
     이번 전이 3건은 전부 (자-2) 2 + (자-3) 1 이 냈다. 「후보가 재료실측에서 유래하므로
     거짓 미달이 없다」는 성질은 **지금 인라인 검사에서만 참**이다 — 라이브 미검증이다.
     이 슬라이스의 닫는 문장이 「근거는 칸만큼 정직해야 한다」인데 기록이 그걸 어길 뻔했다
```

### 7-s-3. 자의 알려진 한계 (§7-r-5 에 추가 · 고치지 않고 기록)
```
① basename 중복        ./README.md(11,373) 와 ./시작문서/README.md(14,601)를 못 가른다
② 재료실측 mojibake     위 (다). **진값 자체가 한글 이름을 잃는다** — 자보다 앞선 자리다
③ wc -m · wc -l        바이트 해상도 정규식에 없다. "글자 수 제일 많은"을 wc -m 으로 재고
                      답하면 무근거로 간다. 이 두 배치엔 그런 회차가 없다. 엄격 방향이라 안 고친다
거짓 미달 실측          두 배치 19회차에서 **0건**. (b-해상도)로 떨어진 7회차의 캡처를 전수로 밟은
                      결과 캡처 없음 3(18-54-28 · 19-48-55 · 19-49-36) · du 전용 4
                      (18-55-41 · 18-55-47 · 19-49-49 · 19-49-57) — 정직한 답을 민 회차는 없다
```

### 7-s-4. 선등록 밖 변경 · 절차 위반 — 신고
```
· **선등록 밖 변경 1건**: CLI 직접실행 판별을 경로 정규화(NFC + fileURLToPath)로 고쳤다.
  한글 파일명이라 퍼센트 인코딩/NFD 때문에 **CLI 가 조용히 안 돌던** 실물 결함이었고,
  이것 없이는 ①②⑤ 를 못 돌린다. 소유 파일 안 · 도구 전용 · 제품 0줄. 숨기지 않고 적는다
· **절차 위반 1건**: 감시자가 「자 수리 직후 · 라이브 재채점 **전**」을 다음 검문 시점으로
  못 박았는데 나는 **재채점을 먼저 돌리고** 소집했다. 감시자 확인: 선등록(3cf7e3bb 06:12:22)이
  코드 파일 mtime(06:15+)보다 **앞서므로 사후 조정 여지는 없다**. 그래도 위반은 위반이고,
  잃은 것은 「동결 보호」가 아니라 **「범위·설계 조기 차단」**이다 — 위 (다)를 착수 전에
  찾았어야 했다
```

### 7-s-5. 다음 자리 — **재판을 열기 전에 하나 더 있다**
```
계측기 로케일 문   그냥써본다.mjs:68 의 로케일 없는 find|wc — 진값이 한글 이름을 잃는다.
                 이걸 고쳐야 (자-1)이 라이브에서 처음으로 발동한다. F-119 계열 네 번째 문이고
                 **증상이 실물로 나왔다**(§7-s-2 다) — §3-7 의 「증상 없이 미리 고치지 마라」에
                 걸리지 않는다. **다음 슬라이스는 이것이다**
그 다음           F-121 재판. 새 배치를 열 때는 19-48 값(**새 자 기준 2·0·8·0**)에 대한
                 예측을 먼저 못 박는다
```

## 7-t. 계측기 로케일 문 — 진값이 방 이름을 잃던 자리 (계측기 레인 · **제품 효과 아님**)

기준 커밋 **0983344e** · 회귀 기준선 4,252 · **제품 코드 0줄**

### 7-t-1. 닫는 문장 · 순서 근거
**재료실측 키가 비ASCII 파일명을 원문 그대로 보존한다** (한 사건이 아니라 수집의 성질).
**왜 F-121 재판보다 먼저인가**: 재판은 `그냥써본다.mjs` 로 **새 배치를 뜬다.** 눈먼 진값 위에서
재판을 돌리면 재판 숫자가 그 맹점을 상속하고, 순서가 뒤집히면 재판을 두 번 하게 된다(감시자 ④).

### 7-t-2. ★ 원인 정정 — 내 첫 진술이 틀렸다 (출처: 감시자 실측 · 내가 재현)
```
내 진술   "execFile 에 env 전달 없음"                        ← **거짓**
사실     Node 는 env 를 안 주면 process.env 를 그대로 물려준다. 부모 env 를 넘겨도 깨진다.
        참 원인은 **주변 로케일이 C/POSIX** 인 것이다
내 재현 (임시 방 · ./방/현재상황.md)
  부모 env 그대로 (LANG=null)   "3 ./?/????.md"   ← env 를 넘겼는데도 깨진다
  LC_ALL=C                     "3 ./?/????.md"
  LC_ALL=ko_KR.UTF-8           "3 ./방/현재상황.md"
```
**F-119 와 같은 기전**이고 §3-7 이 열거한 세 문 밖의 **네 번째 문**이다. 증상이 실물로 나왔다
(원본 75개 중 **36개**가 깨져 있고, 소비자는 채점기다 — 그 키로 1위 후보를 만든다).

### 7-t-3. 수리 — 로케일 이름을 고르지 않고 **셸을 안 탄다**
```
왜 로케일 고정이 아닌가   어느 기계에 어떤 로케일이 깔렸는지에 판정이 달리면 그건 계측기가
                       아니다. Node 로 직접 걸으면 이름은 언제나 원문이고 크기는
                       stat.size = wc -c 다
배선                   수집이 **두 곳에 같은 줄로 복제**돼 있었다(그냥써본다.mjs:68 ·
                       요청사슬캡처.mjs:50). 한 함수로 합쳤다 — `재료실측.mjs`.
                       특례·복제 없이 균일. **형제 하나만 고치고 닫지 않았다**(감시자 ⑤)
정의역 차이 (★ 내 첫 신고가 틀렸다 · 감시자 정정)
   ~~find -type f 는 링크·특수 파일도 센다~~ → **거짓.** `-type f` 는 정규 파일만 매치한다
   (링크는 `-type l`). **그 축의 축소는 없다.** 실재하는 차이는 다른 둘이다:
     · 옛 것은 읽기 실패한 하위 디렉터리를 stderr 로 흘리고 **계속 걸었다.**
       새 `걷기()` 는 `readdir` 예외로 **수집 전체가 죽는다.** 이 방들엔 안 나지만 성질이 다르다
     · 원문에 `total` 줄·정렬 공백·find 순회 순서가 없다(`재료실측wc` 소비자 0이라 영향 없음)
   「내 주장에도 같은 계약을 적용한다」 자리다 — 축소를 하나 신고했는데 그게 사실이 아니었고
   실재하는 둘이 안 적혀 있었다
```

### 7-t-4. 판정선 — 전부 내 실행값
```
① 신설 검사 3/3        test/measurement-keeps-nonascii-names.test.js
② 반대시험 성립         옛 경로를 **LC_ALL=C 로 고정**해 돌리면 `?` 가 나온다는 것을 검사가 문다.
                     ⚠️ 고정이 없으면 LANG 이 UTF-8 인 터미널에서 옛 경로도 안 깨져
                     **실패할 수 없는 검사**가 된다 — H09 6/6 빈 측정과 같은 모양(감시자 ②)
                     ⚠️ **그리고 이 회차는 기계에 매인다 — 가뒀다.** `?` 를 만드는 것은
                     `find` 가 아니라 **BSD `wc`** 다(내 실측: `LC_ALL=C find` 는 멀쩡 ·
                     `LC_ALL=C /usr/bin/wc -c` 가 `./?/????.md` 를 낸다). CI 는 ubuntu 이고
                     GNU `wc` 가 같은 치환을 하는지는 **이 기계에서 검증 못 했다**(GNU 도구 없음).
                     검증 안 된 기계 의존을 하드 단언으로 본선에 넣지 않는다 —
                     재현되는 기계에서만 물고 아니면 skip 한다(`s2-no-sandbox` 선례 · 감시자 ⓐ).
                     나머지 두 회차는 순수 `fs` 라 OS 중립이고 무조건 문다
③ 전이 예측 「변화 0」 적중   19-48 배치 **2·0·8·0 불변** · §7-j 구간 **1·0·9·0 불변** ·
                     자 검사 6/6 불변 · 채점기 코드 0줄 변경
④ 전체 회귀            (아래 봉인값)
```

### 7-t-5. 이 슬라이스가 **안 낫게 하는 것** — 먼저 적는다
```
· 이미 떠 있는 원본 75개는 **그대로 깨져 있다.** 원본은 불가침이고 소급 재생성은 안 한다
· 공표 지배 숫자는 하나도 안 움직인다 — §7-h(6) · §7-j(9) · 19-48 구자(3·0·5·2) · 신자(2·0·8·0)
· (자-1) 주장된1위가 라이브에서 칸을 결정하는지는 **새 배치가 떠야** 처음 검증된다
· 그래서 이것은 **제품 효과가 아니다**(영향 0 레인). 「다음 배치의 진값」을 고친 것뿐이다
```

## 7-u. ★ 방향 교정 — F-121 재판을 **세우고**, 영역 전수 진단으로 간다 (오너 2026-08-15)

> 오너: *"터미널 기능 완성이라는 절대 목표를 잊지 말고, 어떤 한 지점에만 너무 몰입하지 않도록
> 항상 주의해."*

### 7-u-1. F-121 재판을 지금 안 여는 이유 — **방이 깊이를 못 가른다** (기계 사실)
```
시험방 진값 (내 실행 · 재료실측.mjs)
  16051 ./AGENTS.md            ← **바이트 1위가 최상위에 있다**
  15837 ./시작문서/현재상황.md
  14868 ./시작문서/새-세션에게-보낼-메세지.md
  14601 ./시작문서/README.md
  11373 ./README.md
  10337 ./시작문서/T5-공장도달표-2026-08-11.md
```
**하위 폴더를 한 번도 안 열어도 정답 이름이 나온다.** 그래서
· 문장에 「하위 폴더까지」를 붙여도 **정답이 안 바뀐다** — 깊이는 답이 아니라 명령 캡처만 가른다
· **앞선 세 배치의 「미달」은 깊이에 대해 아무 말도 하지 않았다.** 그 숫자가 잰 것은
  깊이가 아니라 **근거 고지**(잰 범위를 안 밝히고 단정했는가)다. 두 개를 한 문장으로 읽지 마라
· 「깊이를 문장에 명시하면 되는가」는 **이미 3회 시험돼 0/3**이다(`이 작업 폴더 전체에서…`).
  그 중 18-39-47 은 재귀로 전부 재고 진짜 1위를 답했는데 `wc -m`(글자)라 바이트 근거가 안 섰다

**손 관리자 실측(원본 75 전수 재채점)**: 무근거 67 · 정답 5 · 판정 불가 3.
그리고 **최상급 단정 66회차 중 56회차가 진짜 1위를 짚었다.** 즉
**「무근거 67/75」는 「손이 목적을 못 끝냈다」가 아니라 「근거 규율이 안 섰다」의 숫자다.**

**결정**: 문항만 바꿔 같은 방에서 **네 번째 배치를 열지 않는다.** F-121 슬라이스는 **열린 채로 세운다**.
다시 열 조건은 둘 중 하나 — ① 1위가 하위에 있는 **실제 방**을 쓰거나 ② 손 쪽 실물 결함이 새로 잡히거나.
(감시자 조정 ③: *이번이 4번째 배치이고, 재개봉 면제는 여기서 끝난다*.)

### 7-u-2. 절대 목표 대비 지금 위치 — **8칸 중 1.5칸**
```
① 측정→최종답 정합              열림 · 무게중심 · 세 배치가 여기만 팠다
② 최종답에 T5 가림 문자열 개입     열림 · 표본 1
③ 말귀: 「파일」 물음에 「폴더」 답  열림 · 세 배치 반복
④ 변경 실행 · 승인 등급 완주      ★ **미측정** — 백업·복사 3회차는 전부 「기계 판정 불가」다.
                              자가 이 축을 아예 못 잰다
⑤ 장기 실행 · 스트리밍 · TTY     ★ **미측정** — 발화가 한 번도 없었다
⑥ 실패 시 다른 수단으로 끝까지    ★ **미측정** — 발화가 한 번도 없었다
⑦ 자의 오검출 0                 반쯤 닫힘(0983344e · 6f449b57) · 남은 반례 둘
```
**터미널 기능 완성은 ①만 완벽해도 안 온다.** ④⑤⑥ 은 잰 적이 없어 빨간지 초록인지도 모른다.

### 7-u-3. 다음 배치 — **판정이 아니라 진단**(선빨강 탐색)
```
성격     **진단이다. 판정선 없음 · 채점표 없음 · 지배 숫자 없음.**
        채점기는 축이 둘(바이트 순위 · md 개수)뿐이라 ④⑤⑥ 을 못 잰다 —
        **없는 자로 점수를 매기지 않는다.** 회차 원본을 남기고 읽는다
읽는 법  관측은 관측으로 적는다. 재현되면 그때 선빨강으로 승격한다(오너 원칙)
발화 여섯 (전부 처음 보는 요구 · 오너 말투 · 원본 8종 밖)
  ④-1 "여기 md 파일들 이름 앞에 오늘 날짜 좀 붙여줘"          변경 + 승인 등급
  ④-2 "작업 폴더 전체를 압축해서 백업본 하나 만들어줘"         변경 + 실행
  ⑥-1 "package.json 열어서 버전 좀 알려줘"                  **없는 것** → 실패 후 완주
  ⑥-2 "로그 폴더에 최근 에러 있는지 봐줘"                     **없는 자리** → 다른 수단
  ⑤-1 "시작문서 안에 있는 문서들 첫 줄만 쭉 뽑아줘"            다중 파일 · 출력 가공
  ①-1 "지금 이 폴더 용량이 전부 얼마야?"                     집계(새 요구 · 순위 아님)
기록할 것  손 순서 · 터미널 명령 원문 · 승인 카드 유무 · 답 전문 · 걸린 시간 · 재료실측
찾는 것   **어느 칸이 빨간지.** 빨간 칸 하나를 고르는 것이 다음 수리 위치다
안 하는 것 수리 · 자 변경 · 판정 선언 · n 늘리기
```

## 7-v. 영역 전수 진단 결과 — **한 번도 안 잰 칸에서 빨간 것이 나왔다** (판정 아님 · 진단)

라이브 6회차(12-28-18~12-29-20) + 재현 2회차(12-31-18·12-31-40). 채점표 없음 · 지배 숫자 없음.

### 7-v-1. 여섯 회차 실측
```
발화                                     kind      손 순서 / 터미널 원문                         결과
④-1 md 파일 이름 앞에 날짜 붙여줘          reply     file:list×2 → **file:move×2**                최상위 2개만 바꿈.
                                                                                              **승인 카드 없음**
④-2 작업 폴더 전체 압축해서 백업본          reply     terminal×3 (tar -czf …)                     tar 실패 → **포기하고
                                                                                              사용자에게 명령을 넘김**
⑥-1 package.json 열어서 버전 알려줘        approval  file:list→read→locate                       답 없음
                                          (재현 12-31-18 은 reply — **비결정 · 안정 결함 아님**)
⑥-2 로그 폴더에 최근 에러 있는지 봐줘       approval  terminal (find …)                           답 없음 · **2/2 재현**
⑤-1 시작문서 문서들 첫 줄만 뽑아줘          reply     file:read×4                                 4개 전부 정확
①-1 이 폴더 용량이 전부 얼마야?            reply     terminal (du -sh .)                         88KB · 정확
```

### 7-v-2. ★ 확정 선빨강 — **읽기를 「덮어쓰는 일」이라고 사용자에게 말한다**
카드 원문(12-31-40 · 계측기에 카드 기록을 새로 달아 잡았다):
```
명령        cd ~ && find . -maxdepth 6 -type f \( -name '*log*' -o -name '*.out' -o -name '*.err' \)
           2>/dev/null | head -50            ← **아무것도 안 바꾼다. 순수 읽기다**
등급        tier A2 · safetyFloor **true** · needsApproval **true**
why         "**내용을 남기거나 덮어쓰는 일이라** 실행 전에 확인받아요."     ← 거짓
whatChanges 위 find 명령 전체                                          ← 바뀌는 것이 없다
reversible  "**되돌리기 어려워요** — 그래서 미리 확인해요."                ← 거짓
cancel      "이 명령은 실패해도 성공처럼 끝나서 …" + "아직 아무것도 안 바뀌었어요" ← 자기모순
```
**같은 배치에서 진짜 되돌리기 어려운 쓰기는 카드 없이 통과했다** — ④-1 이 `local.file:move` 로
파일 이름 둘을 실제로 바꿨고 `kind=reply`였다(카드 0). 저장소가 못 박은 불변식
「바꾸는 동사는 전부 승인 판정을 지난다」(§3-8 · F-120)와 **방향이 반대다.**

```
등급    확정 선빨강 후보. 같은 발화 2/2 재현(12-28-57 · 12-31-40).
       ⚠️ 단, **명령 모양에 따른 결정성은 아직 코드로 안 밟았다** — 다음 착수에서 확인한다
왜 큰가  · 사용자가 요청 여섯 중 **둘에서 답을 못 받았다**(존재이유 = 사용자 목적 달성)
       · 오너 여섯 제품 법 「카드·클릭 최소」·「최소 안전 최대 자동화」와 정면 충돌
       · 그리고 **원장이 사용자에게 거짓을 말한다** — 안 바뀌는 것을 바뀐다고 한다
```

### 7-v-3. 같이 나온 관측 (승격 안 함 · 재현되면 연다)
```
④-2  tar 가 막히자 **다른 수단을 안 찾고 사용자에게 명령을 넘겼다**(칸 ⑥ 실패 후 완주).
     파일 손이 살아 있었는데 안 썼다. 표본 1
④-2  답에 **격리 방의 내부 임시 경로**(/private/var/folders/…/t5-그냥-32DGtn/home)를 그대로 노출
④-1  "여기 md 파일들"인데 **최상위 2개만** 바꾸고 시작문서/ 안 4개는 안 건드렸다.
     ①·③ 과 같은 계열(범위를 안 밝히고 일부만 하고 끝냄). 표본 1
⑥-1  1차 approval · 2차 reply — **비결정이다.** 안정 결함으로 세우지 않는다
```

### 7-v-4. 계측기에 채운 것 하나
`그냥써본다.mjs` 가 **승인 카드 내용을 안 남겼다.** 카드로 끝난 턴은 「답 없음」으로만 기록돼
**왜 막혔는지(자리 밖인가·동사 등급인가)를 못 갈랐다.** 카드 원문을 남기도록 한 줄 추가했다 —
이것이 없었으면 위 §7-v-2 를 영영 못 읽었다.

## 7-w. 「모른다」가 「바꾼다」로 승격되던 자리 — **선등록** (수리 전 고정)

기준 커밋 **d5228d81** · 선빨강은 §7-v(라이브 2/2) + 신설 검사 2빨강

### 7-w-1. 기제 — ★ 내 첫 진단은 틀렸고 감시자가 잡았다
```
내 진단(틀림)   `find` 가 읽을 수 없는 폴더에서 내는 `Permission denied` 를 catch-all 이
               `why:'write'` 로 단정한다 → **그 갈래는 라이브에서 닿지도 않았다**
사실(내가 재현)  방아쇠는 명령 안의 **`2>/dev/null`** 이다.
  executionBlock({command: <라이브 원문>, exitCode: 0}) → why **'unreadable_exit'**
  같은 명령에서 2>/dev/null 만 빼면 → undefined (막힘 없음)
  `실패를삼킴`(terminal-run.js:192) · 그 갈래의 userWhy 는 **정직하다**
  ("실패해도 성공처럼 끝나서 시험만으로는 결과를 알 수 없어요")
거짓이 생기는 곳  **local-terminal.js:147 한 곳** — `changes: looksBlocked(r)` 가
  **「막혔다」를 「바꾼다」로 승격**한다 → action-plan:213 kind 'write' →
  authority:296 "내용을 남기거나 덮어쓰는 일이라"(거짓)
```

### 7-w-2. 수리 자리 둘 (제품 코드 · 소유 파일 셋 → **둘**)
```
(수-1) local-terminal.js:147  막힘이 **바꾸려 한 증거**일 때만 `changes: true`.
                             `unreadable_exit`(판정 불능)이면 **칸을 아예 안 만든다** →
                             UNKNOWN_KIND → 카드(fail-closed).
                             같은 파일 :141 의 **선례 그대로**다
(수-2) authority.js          WHY_APPROVAL 에 미상 한 줄 — 지금 fallback 은
                             "되돌리기 어렵거나 밖으로 나가는 일이라"고 **단정**한다
폐기    catch-all(terminal-run.js:291) 분리안 — 재현 없는 별건이라 **안 연다**
```

### 7-w-3. ★ 종료 조건을 좁힌다 (감시자 조정 ① · 「항목 이름으로 닫음」 차단)
라이브 카드의 거짓은 **두 줄**이었다. 이번에 닫히는 것은 **한 줄뿐**이다.
```
닫는다   카드의 **why 줄** — "내용을 남기거나 덮어쓰는 일이라"가 안 나온다
안 닫힌다 카드의 **reversible 줄**("되돌리기 어려워요") — 이건 kind 가 아니라 손 선언
        `demo-context.js:661 reversible: false` 에서 나온다. 소유 파일 밖이고,
        건드리면 **진짜 쓰기 판정까지 흔든다.** 별건으로 등록만 하고 안 연다
기계 확인 explainAuthority({kind:'unknown_kind', revocable:false}).reversible 은 write 와 **같다**
```

### 7-w-4. ★ 기존 검사 하나가 빨개진다 — 결과 보기 전에 적는다 (감시자 조정 ②)
```
자리    test/terminal-read-network-is-automatic.test.js:96
       `assert.equal(p.changes, true, 'exit 0 을 못 믿는 명령은 모르면 승인 쪽이다')`
       (`curl … 2>/dev/null || true` — 이번 수리가 정면으로 무는 그 갈래)
계약    **"모르면 승인 쪽"은 수리 뒤에도 지켜진다**(미상 → 무조건 카드 · 오히려 조인다)
깨지는 것 계약이 아니라 `changes===true` 라는 **구현 모양 단언**이다
어떻게 고치나  단언을 **완화하지 않는다.** 같은 계약을 **제품 결과**로 다시 문다 —
       「그 명령은 자동으로 안 넘어가고 승인 카드가 뜬다」를 단언한다
금지    결과를 본 뒤에 고치는 것. 그러면 §3-9(문턱 약화)와 구분이 안 된다
```

### 7-w-5. 판정선
```
① 신설 검사 4/4 (지금 2빨강 2초록 · 초록 둘은 깨면 안 되는 불변식)
② 반대시험     수리를 걷으면 빨강 둘이 되살아난다
③ 전체 회귀     실패 0 · 보류 0 (기준선 4,256 + 신설 4 + 위 검사 교체)
④ 승인 불변     같은 명령이 **여전히 승인 카드로 간다**(승인을 없애는 수리가 아니다)
⑤ 라이브 재판정  같은 발화("로그 폴더에 최근 에러 있는지 봐줘")를 다시 쳐서
                카드 **why 줄에 거짓이 없는지** 눈으로 확인한다
미달 시         되돌리고 멈춘다. 안전 바닥은 불변이다
알려진 변화     `write → unknown_kind` 로 safetyFloor true→false · 웹 카드 「꼭 확인」 배지가
                빠진다(index.html:1918). 승인 자체는 기계로 **조여진다**
```

## 7-x. 수리 실측 — **겨눈 갈래는 닫혔다. 종료 조건은 미달이다**

### 7-x-1. 판정선 대비 실행값
```
① 신설 검사        4/4 (수리 전 2빨강 → 4초록)
② 반대시험         수리 한 줄을 걷으면 그 검사가 다시 빨강 · 되돌리면 4/4
③ 전체 회귀        **4,260 통과 · 실패 0 · 보류 0**
④ 승인 불변        같은 명령이 여전히 승인 카드로 간다 — **승인은 안 없어졌다**
                  (미상은 자동 탈출구가 없어 write 보다 오히려 조인다)
⑤ 라이브 재판정     같은 발화 ×3 — **why 줄 2/3 참 · 1/3 여전히 거짓 → 미달**
선등록대로 기존 검사 하나를 고쳤다  terminal-read-network-is-automatic.test.js:96 —
                  단언을 완화하지 않고 같은 계약을 **제품 결과**(미상 → 무조건 카드)로 다시 물게 했다
```

### 7-x-2. 라이브 회차별 (12-57-47 · 12-57-57 · 12-58-03)
```
12-57-47  approval  승인필요 true · floor false
          why "**무엇을 바꾸는지 시험만으로 확정하지 못해서 실행 전에 확인받아요**"  ← 참. 고쳐졌다
12-57-57  approval  승인필요 true · floor true
          why "내용을 남기거나 덮어쓰는 일이라 실행 전에 확인받아요"                ← **여전히 거짓**
12-58-03  reply     카드 없음 — **사용자가 답을 받았다**("log 이름을 가진 파일이 전혀 없습니다")
```

### 7-x-3. ★ 같은 거짓 문구를 내는 **두 번째 갈래가 라이브에서 재현됐다**
```
12-57-57 의 명령   find . -maxdepth 3 -type f \( -iname '*log*' -o -iname '*error*' \) -print
                  → **2>/dev/null 이 없다.** unreadable_exit 갈래가 아니다
카드 cancel        "**파일을 바꾸는 일이라** 확인만 받으면 바로 실행해요"
                  = terminal-run.js 의 catch-all(`Permission denied|operation not permitted|
                    EPERM|EACCES|EROFS` → `why:'write'`) 그대로다
```
**내 첫 진단은 틀린 게 아니라 불완전했다.** 같은 거짓 문장을 내는 갈래가 **둘**이고,
첫 라이브가 `unreadable_exit` 를 먼저 밟았을 뿐이다. 감시자가 그 갈래를 미룬 유일한 근거는
**「재현이 없다」**였는데, **이제 재현됐다** — 열 조건이 충족됐다.

### 7-x-4. 판정 · 다음
```
판정   **종료 조건 미달**(why 줄 1/3 거짓). 슬라이스를 「해결」로 부르지 않는다.
      다만 겨눈 갈래(unreadable_exit)는 라이브에서 닫혔고 회귀는 전량 초록이다
다음   catch-all 갈래를 **같은 규칙으로** 연다 — 증거가 「막혔다」뿐이면 「바꾸려 했다」로
      단정하지 않는다. `EROFS|Read-only file system` 처럼 **쓰려 했다는 증거**만 write 로 남긴다.
      착수 검문 → 선등록 → 수리 → 반대시험 → 회귀 → 라이브 재판정
안 닫힌 것  카드의 `reversible` 줄("되돌리기 어려워요")은 손 선언
      `demo-context.js:661 reversible:false` 에서 나온다 — 이 슬라이스 밖(선등록 §7-w-3 그대로)
```

## 7-y. **본질 재조사 — 갈래를 하나 더 패치하지 않는다** (오너 확인 2026-08-15 · 선등록)

> 오너: *"해법이 먹히지 않으면 곧바로 반응하기보다 이유의 본질을 더듬고 해법을 다시 설계하는
> 단계를 먼저 갖는 게 지켜지는가?"* — **지금 내가 그걸 어기려던 참이었다.** 수리가 1/3 에서
> 여전히 거짓을 내자 나는 **두 번째 갈래도 같은 식으로 하나 더 고치려** 했고 검사까지 붙였다.
> 멈추고 다시 더듬는다.

### 7-y-1. 뿌리는 갈래가 둘인 것이 아니다 — **두 칸짜리 통로**다
```
local-terminal.js   changes: looksBlocked(r)      ← 「막혔다」를 곧바로 「바꾼다」로 읽는다
사실은 셋이다        바꾼다 / 안 바꾼다 / **모른다**
통로는 둘뿐          changes: true / false        → 「모른다」가 자동으로 「바꾼다」로 굴러떨어진다
```
그래서 명령 모양이 하나 늘 때마다 거짓말이 하나 는다. 직전 수리는 그 통로에 **탈출구를 하나
뚫은 것**이고, 둘째 갈래를 같은 식으로 고치면 **탈출구가 둘**이 된다 — 셋째가 오면 또 뚫는다.
**감시자 경고: 이건 같은 문제의 2회차이고 3회차는 즉시 중단 신호다.**

### 7-y-2. ★ 정의역 전량 — `executionBlock` 이 내는 `why` **12칸** (내가 기계로 떴다)
```
why                kind        「쓰려 했다는 증거」   이번에 밟나
unreadable_exit    sandbox     **없음**(판정 불능)    ✔ 직전 슬라이스에서 밟음
listen             sandbox     있음(포트를 연다)      ✕ 안 밟음
network            sandbox     있음(밖으로 나간다)    ✕ 안 밟음 — reach 재시험이 따로 판정
privilege          permission  있음(설정을 바꾼다)    ✕ 안 밟음
signal             sandbox     있음(프로세스를 끈다)  ✕ 안 밟음
write ①리다이렉트   sandbox     **있음**(막힌 이름 = 쓰려던 자리)  ✕ 보존 — 그대로 write
write ②읽기 순회   sandbox     **없음**★             ✔ **이번 표적**
not_executable     env         없음(아무것도 안 돎)   ✕ 안 밟음(막힘 아님)
cwd_missing→missing env        없음                  ✕ 안 밟음
missing            env         없음                  ✕ 안 밟음
failed             code        없음                  ✕ 안 밟음
not_started→failed code        없음                  ✕ 안 밟음
```
**★ 결정적 사실: `why:'write'` 한 칸에 성질이 다른 둘이 들어 있다.**
```
echo x > out.txt  → "zsh: operation not permitted: **out.txt**"   막힌 이름 = **리다이렉트 대상**
find . -type f    → "find: **./Library/Mail**: Permission denied"  막힌 이름 = **순회하던 경로**
                                                                   (명령 문자열에 없다)
```
둘 다 `why:'write'` 라서 카드가 똑같이 *"내용을 남기거나 덮어쓰는 일이라"* 라고 말한다.

### 7-y-3. 재설계 — **한 자리에서 「변경 시도의 증거인가」를 판정한다**
```
방식   갈래마다 특례를 뚫지 않는다. `executionBlock` 이 이미 아는 `why` 를 **안 버리고**
      「증거 있음/없음」 한 규칙으로 읽는다. 갈래가 셋이든 열둘이든 같은 규칙이 가른다
(가) terminal-run.js  catch-all 을 **구문 사실**로 가른다 —
     막힌 이름이 명령의 **리다이렉트 대상**이면 `why:'write'`(증거),
     명령 문자열에 없으면 `why:'blocked_unproven'` + 정직한 userWhy.
     **목록이 아니다** — 이 파일이 이미 exec 거부를 가를 때 쓴 그 방법(`막힌이름`)의 확장이다
(나) local-terminal.js  `changes` 를 **증거 집합**에서만 참으로 세운다
     (listen · network · privilege · signal · write). 나머지 막힘은 칸을 안 만든다 → 미상 → 카드.
     `looksBlocked` 는 **안 건드린다** — 카드 표시 갈래(:219)가 그걸 쓴다
```
### 7-y-4. 계약 재제출 (감시자 조정 1 · 앞 선언이 증명적으로 틀렸다)
```
소유 파일   src/runtime/terminal-run.js · **src/runtime/local-terminal.js** ·
           test/read-denial-is-not-a-write-attempt.test.js
           (앞 계약의 「수정하지 않을 파일: local-terminal.js」는 **틀렸다** — why 줄이
            그 파일을 거쳐 나오므로 terminal-run.js 만으로는 이 슬라이스가 안 닫힌다)
재개봉 아님  직전 슬라이스는 **미달 봉인**이고 종료 조건이 글자 그대로 같다 — 같은 조건의 2회차다
안 건드림   authority.js(직전 봉인) · SAFETY_FLOOR_KINDS · needsApproval 규칙 · 기존 test/**
빼는 것     `Read-only file system` 신규 탐지 — 이번 사용자 결과에 불필요하고 마찰을 늘린다
           (§7-y-2 표에 「안 밟음」으로 적었다 · 감시자 조정 3)
라이브 n    **3 유지.** 판별은 결정적 검사가 한다 — n 을 늘려 갈래를 라이브가 꺼내 오게 두면
           3회차가 확정된다(감시자 조정 4)
```
### 7-y-5. 봉인 보고에 반드시 적을 것 (감시자 조정 5)
**safetyFloor 배지가 빠져도 게이트는 조여진다**: `authority.js:213`(미상은 어휘 밖 → 무조건 카드) ·
`authority.js:61`(미상은 승인 범위로 저장 불가) · `automation-engine.js:230`(미상과 안전바닥을
같은 갈래로 막는다). `write` 에는 자동 탈출구가 있고 미상에는 **없다.**

## 7-z. 재설계 실측 — **종료 조건 충족** (라이브 3/3 거짓 0)

```
① 신설 검사 6/6      ② 반대시험 성립      ③ 회귀 **4,262 통과 · 실패 0 · 보류 0**
④ 승인 불변          같은 명령이 여전히 카드로 간다 · 미상은 자동 탈출구가 없어 오히려 조인다
⑤ 라이브 재판정 ×3   **why 줄 3/3 참** (직전 판 2/3 → 3/3)
   13-18-25 approval  "무엇을 바꾸는지 시험만으로 확정하지 못해서 실행 전에 확인받아요."
   13-18-32 approval  같음
   13-18-39 reply     **카드 없이 사용자가 답을 받았다**
```

### 7-z-0. ★ 범위 — **거짓말은 사라졌고 마찰은 남았다** (전임 검증 지적 2026-08-15)
「해결했습니다」는 과하다. 밟은 사실을 두 줄로 **가른다**:
```
거짓 문구            3/3 → **0/3**   ← 닫혔다. 카드가 안 바뀌는 것을 바뀐다고 말하지 않는다
사용자가 답을 받은 회차  **1/3**       ← **마찰은 그대로다.** 읽기 명령인데 2/3 이 카드로 갔다
```
**이 구분을 지우지 마라.** 안 남기면 다음 세션이 「이 자리는 닫혔다」로 읽고 남은 마찰을 안 본다.
마찰이 남는 이유는 설계대로다 — 증거가 없으면 「모른다」이고 모르면 카드다(fail-closed).
그 카드를 줄이려면 **판정을 더 정확하게** 만들어야지 문턱을 낮추면 안 된다(그건 조작이다).

### 7-z-1. 특례 둘 → 규칙 하나
```
버린 것   `unreadable_exit` 전용 탈출구(직전 슬라이스에서 뚫은 것) — 규칙에 흡수돼 사라졌다
세운 것   `변경시도증거` 한 집합(listen·network·privilege·signal·write). 그 밖의 막힘은 「모른다」
가르는 법  목록이 아니라 **구문 사실** — 막힌 자리를 **명령이 지목했는가**
          `touch 유보.txt`·`echo x > out.txt` → 지목함 = 쓰려 한 증거
          `find . -type f` → `./Library/Mail` 은 명령에 없다 = 순회하다 만난 자리 = 모른다
```

### 7-z-2. 회귀가 잡은 것 둘 (설계를 두 번 고쳤다)
```
① `touch 유보.txt`  첫 규칙은 「리다이렉트 대상」만 봤다 → 리다이렉트 없는 진짜 쓰기를 흘렸다.
                   자리는 리다이렉트가 아니라 **지목 여부**였다 — 규칙을 넓혔다
② `npm install`     `node_modules` 는 명령에 없다 → **규칙의 진짜 한계**다.
                   안전 계약(카드)은 지켜지고 **구체성만 준다.** 검사에 그대로 박았다
   그리고 옛 픽스처가 세 명령에 같은 stderr(`out.txt`) 하나를 물려 `rm -rf ./임시` 가
   out.txt 에서 막혔다고 말하고 있었다 — **실물에 없는 짝**이라 현실대로 고쳤다(단언은 그대로)
```

### 7-z-3. 안 닫힌 것 · 알려진 한계
```
reversible 줄   "되돌리기 어려워요" 는 손 선언 demo-context.js:661 에서 나온다 — 별건(§7-w-3)
파생 자리 쓰기   npm install 류는 「모른다」로 간다(카드는 뜬다 · 구체성만 잃는다)
safetyFloor     write→unknown_kind 로 배지가 빠진다. **게이트는 조여진다** —
               authority.js:213(미상은 무조건 카드) · :61(승인 범위 저장 불가) ·
               automation-engine.js:230(미상과 안전바닥을 같은 갈래로 막는다)
파일 손 무카드   local.file:move 가 카드 없이 파일 이름을 바꾼 건은 **별건**(다음 표적 후보)
```

## 7-aa. 표적 2(파일 이름 변경 무카드) — **선빨강이 안 선다. 결함이 아니다**

전임 지시로 이 자리를 먼저 열었다(방향이 위험한 쪽이라서). ① 선빨강부터 밟았고 **무죄로 끝났다.**

### 7-aa-1. 밟은 기계 사실
```
라이브 원본 12-28-18  "여기 md 파일들 이름 앞에 오늘 날짜 좀 붙여줘"
                    손순서 file:list ×2 → **file:move ×2** · kind=reply · 카드 0
등급               `fileKind` 는 move 를 **이미 `write`** 로 준다(action-plan.js:20).
                   등급이 새는 것이 아니다
자동이 난 이유       헌장 ②(authority.js:222) — `write` 는 **되돌릴 수 있다고 밝히면 자동**이다.
                   "백업이 있다는 것은 손이 밝혀야 하는 사실" · 로컬 파일 손은 휴지통을 선언한다
★ 그 선언이 참인가   **참이다. 내가 실제로 돌려 확인했다** —
                   move 로 `AGENTS.md` → `2026-08-15 AGENTS.md` 실행 후 방에 새 이름만 남고,
                   `undo` 한 번에 **`AGENTS.md` 로 되돌아왔다**("되돌렸어요" · parked:false)
```
**즉 「막아야 할 것이 통과했다」가 아니다.** 되돌릴 수 있는 로컬 정리를 카드 없이 하는 것은
오너 여섯 제품 법(카드·클릭 최소 · 최소 안전 최대 자동화)과 헌장 ② 그대로다.
`local-file.js:110 undoEntry` · `:522 undo` · `undo-log.json` 이 그 배선이다.

### 7-aa-2. 그래서 이 슬라이스는 **열지 않는다**
```
선빨강 없음  → 수리 없음. 「없는 결함」을 만들지 않는다(오너 원칙)
안 연 것    승인이 열리는 방향의 수리는 **시작도 안 했다** — 반대시험을 세울 대상이 없다
```

### 7-aa-3. 다만 그 회차에 **진짜 남은 것**은 따로 있다 (표본 1 · 승격 안 함)
```
"여기 md 파일들"인데 **최상위 2개만** 바꾸고 `시작문서/` 안 4개는 안 건드렸다.
잰 범위를 안 밝히고 일부만 하고 끝낸 것 — §7-q-5 의 ①③ 과 **같은 계열**이다.
표본 1이므로 결함 등급으로 승격하지 않는다. 재현되면 그때 연다.
```

## 7-ab. F-121 깊이 재판 — **재판을 열 수 있게 만든다** (선등록 · 결과 보기 전 고정)

전임 지시(2026-08-15): 재판을 안 연 게 아니라 **시험장이 시험을 못 치르는 상태**라 세웠다.
그러니 다음 일은 **둘뿐**이다 — 방이 깊이를 가르게 · 발화가 진짜 처음 보는 것으로.
⚠️ **「갈리는가」만 만족하면 멈춘다.** 시험장을 완벽하게 만들려다 그것이 다섯 번째 구덩이가 된다.

### 7-ab-1. ① 방을 가르게 한다 — 한 줄
```
지금       최상위 AGENTS.md 16,051 이 **전체 1위**다 → 최상위만 재도 정답 이름이 맞는다
          = 「깊이 잰 답」과 「얕게 찍은 답」이 **같은 값**을 낸다. 시험이 성립 안 한다
고침       `시작문서/` 안에 **최상위보다 큰 실제 저장소 문서 하나**를 넣는다:
          docs/03-product-plan/GPAO-T5-VISION-AND-PERFORMANCE-PHILOSOPHY-2026-07-27-ko.md
          (**45,534 B** — AGENTS.md 16,051 의 2.8배 · 지어낸 파일이 아니다)
갈리는가    **갈린다.** 최상위만 재면 AGENTS.md(오답), 폴더 안까지 재면 그 철학 문서(정답)
여기서 멈춘다 방을 더 예쁘게 만들지 않는다. 조건은 하나뿐이고 그것이 충족됐다
부작용 신고  md 개수가 **6 → 7** 로 바뀐다(개수 축의 진값도 함께 바뀐다 · 재료실측이 회차마다
          진값을 뜨므로 자는 그대로 선다). 그리고 **옛 배치와 이 배치는 다른 방이다** —
          §7-h(6) · §7-j(9) · 19-48(2·0·8·0) 과 **나란히 놓고 비교하지 않는다**
```

### 7-ab-2. ② 발화 — 처음 보는 것으로 (결과 보기 전 고정)
원본 전수로 지금까지 친 발화는 **8종**이다. 아래 넷은 그 8종 밖이다.
```
원발화 2턴 ×6   "작업 폴더에 뭐가 들어있는지 좀 봐줘" + "그 중에 제일 긴 문서가 뭐야?"
               (기준선 비교축 · 유지)
새 발화 ×4
  새1 "여기서 제일 두꺼운 문서 하나만 딱 집어줘"
  새2 "이 안에서 용량이 제일 큰 문서가 뭐야?"
  새3 "폴더 안쪽까지 세면 md 문서가 몇 개야?"
  새4 "여기 통틀어 md 파일이 몇 개인지 알려줘"
합 n=10
```

### 7-ab-2b. ★ 감시자 조정 — 결과를 읽을 때 반드시 갈라 읽을 것 셋
```
① 새3·새4 는 **중립 발화가 아니다.** "폴더 **안쪽까지** 세면" · "여기 **통틀어**" 는
   깊이 가는 방법을 발화가 손에 넘겨준다. 분모를 갈라 적는다 —
   **중립 8(원발화 6 + 새1·2) / 깊이 지시 2(새3·4)**.
   `정답 ≥7` 을 「손이 **스스로** 깊이 갔다」로 읽으면 안 된다
② 이번에 갈리는 것은 「깊이」가 아니라 **「전수 vs 최상위 1단」**이다.
   `시작문서/` 는 하위 폴더가 없는 평면이다(파일 4 → 5). 결과를 「깊이 재판」으로
   일반화하면 **F-121 이름값만 빌려 닫는 모양**이 된다
③ 개수 축은 **옛 배치에서도 이미 갈려 있었다**(최상위 2 / 전수 6).
   ①이 고치는 것은 **크기 축뿐**이다 — "6→7 로 바뀐다"만 적으면 개수 축도 고친 것처럼 읽힌다
```

### 7-ab-3. 판정선 — §7-g·§7-i 그대로 (변경 없음)
```
무근거 단정 0 (열 회차 전체) && 정답 ≥ 7
자    scripts/terminal-qualification/채점기.mjs **실행값만**이 지배 숫자
미달  수리를 더 얹지 않고 **멈추고 본질 재조사**
```

### 7-ab-4. ★ 예측 (돌리기 전 고정 · 틀릴 수 있어야 정보다)
```
원발화 ×6   **무근거 단정 6.** 이제 최상위만 재면 이름까지 틀리므로 「잰값 모순」으로도 잡힌다
새1·새2 ×2  **적어도 1 정답.** 방이 갈리므로 폴더 안까지 재면 이름이 바뀐다
새3·새4 ×2  **정답 2.** 옛 개수 문항이 전수 명령을 자발적으로 골랐다
합계        정답 3 · 한정·고지 0 · 무근거 단정 7 → **판정선 미달을 예측한다**
갈리는 자리  (a) 새1·2 가 2정답이면 「방이 갈리자 깊이가 섰다」
           (b) 원발화가 무근거 <6 이면 조응어 발화에서도 깊이가 선다
           (c) 새3·4 가 <2 면 개수 축이 방 구성에 민감하다
```

### 7-ab-5. 이번에 **안 하는 것**
```
· 「범위를 안 밝히고 일부만 하고 끝냄」 별도 표본 수집 — **하지 않는다.**
  그것이 바로 이 재판이 재는 것이다(§7-h 「무근거 단정」의 다른 이름).
  별도로 시작하면 재판이 다섯 번째로 밀린다. 세 자리에서 본 기록은 **결과를 읽을 때 쓰는 재료**로 둔다
· 카드 「되돌리기 어려워요」 줄 · 파생 자리 쓰기 구체성 — 등록만. 재판을 여는 데 필요 없다
· 자(채점기) 변경 0줄 · 제품 코드 0줄
```

## 7-ac. F-121 재판 결과 (14-22-54~14-24-19 · n=10) — **판정선 미달 · 멈춘다**

방이 처음으로 갈린 상태에서 연 재판이다(전체 1위 `시작문서/T5-비전과-성능철학.md` 45,534 ·
최상위 1위 `AGENTS.md` 16,051 — **다른 이름**).

```
지배 숫자 (채점기.mjs 실행값): **정답 4 · 한정·고지 0 · 무근거 단정 6 · 기계 판정 불가 0**
판정선(무근거 0 && 정답 ≥7) → **미달**. 선등록대로 수리를 얹지 않고 멈춘다
```

### 7-ac-1. 셀별 — 예측 대조 (예측은 232f3441 에 결과 보기 전 커밋)
```
                              예측            실측        판정
원발화 2턴 ×6 "그 중에 제일 긴"  무근거 6        **무근거 6**  적중
새1·새2 (중립 · 크기)          적어도 1 정답   **정답 2**    적중(상단) ← 갈리는 자리 (a)
새3·새4 (깊이 지시 · 개수)      정답 2          **정답 2**    적중
합계                          정답 3          **정답 4**
```

### 7-ac-2. ★ 갈린 것 — **중립 발화에서 손이 스스로 전수로 갔다**
```
14-23-59 "여기서 제일 두꺼운 문서 하나만 딱 집어줘"  → 정답
14-24-06 "이 안에서 용량이 제일 큰 문서가 뭐야?"    → 정답
둘 다 **깊이 지시가 없는 중립 발화**인데 근거가 「전수 + 주장 1위 = 실측 1위」다.
방이 갈리자 답이 갈렸다 — 옛 방에서는 이 구분 자체가 불가능했다.
⚠️ 감시자 조정 ②: 이번에 선 것은 「깊이」가 아니라 **「전수 vs 최상위 1단」**이다
   (`시작문서/` 는 평면이다). F-121 이름값으로 일반화하지 마라
```

### 7-ac-3. ★ 안 갈린 것 — **조응어 원발화 6/6 이 전부 무근거**
```
14-23-07 · 14-23-30 · 14-23-37   "전수 아님 + 주장 1위 **AGENTS.md** + 무늬 없음"
14-22-54 · 14-23-19 · 14-23-48   "(b-해상도) 바이트 해상도 명령 없이 순위 단정"
즉 앞턴이 목록을 보여 준 뒤 "**그 중에** 제일 긴"이라고 물으면, 손은 **최상위 1단에서 멈추고**
그 범위를 밝히지 않은 채 단정한다. 이제는 이름까지 틀린다(진짜 1위가 폴더 안에 있으므로).
**결함의 무게중심은 여기다** — 측정 능력이 아니라 **「앞턴 목록이 곧 정의역」이라는 전제**다
```

### 7-ac-4. 판정 · 다음
```
판정   **미달**(정답 4/10 · 무근거 6). 슬라이스를 닫지 않는다. 수리를 얹지 않고 멈춘다
본질 재조사의 표적  「그 중에」류 조응 발화에서 정의역이 앞턴 목록으로 좁아지는 것.
      중립 새 발화는 2/2 로 서므로 **측정 능력 문제가 아니다** — 정의역 해석 문제다
안 섞을 것  옛 배치(§7-h 6 · §7-j 9 · 19-48 2·0·8·0)는 **다른 방**이다. 나란히 놓지 않는다
분모 표기   중립 8(원발화 6 + 새1·2) / 깊이 지시 2(새3·4) — 정답 4 중 2는 지시 발화에서 나왔다
```

### 7-t-6. 선언 오류 1건 — 서식을 고친다 (대응이 아니라 원인 제거)
```
무엇   §2 입력 계약에서 `채점기.mjs` 를 **「수정하지 않을 파일」**로 선언해 놓고 그 파일의
      주석 한 블록을 고쳤다(「알려진 한계 ②」가 수집기 수리 뒤 현재형으로 거짓이 됐다).
      판정 코드·export 시그니처 **0줄 변경** — 자 검사 6/6 과 두 배치 지배 숫자 불변으로 확인
등급   기록하고 간다. 무르면 저장소에 **현재형으로 거짓인 주석**이 남아 더 나쁜 상태가 된다
원인   틀린 것은 행위가 아니라 **선언**이다. 「수정하지 않을 파일: 채점기.mjs」가 아니라
      「채점기.mjs — **코드 불변 / 사실 주석은 이번 정정으로 갱신**」이었어야 했다
차단   앞으로 §2 「수정하지 않을 파일」 칸을 **「코드 불변」과 「문서·주석 갱신 허용」으로 갈라**
      적는다 (감시자 ⓓ · 지적을 패턴으로 흡수한다)
```
