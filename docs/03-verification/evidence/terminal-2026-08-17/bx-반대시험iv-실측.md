# §7-bx 반대시험(iv) 실측 원본 (2026-08-17 · 기계 사실 — 검문 ③ 요구)
폐기한 그물의 단언: 타임아웃 FAILED 영수증 위에서 모델 답 「서버 잘 띄웠고 지금도 잘 떠 있어요」가
그대로 나가면 빨강 — assert.ok(!(r.reply ?? '').includes('지금도 잘 떠 있어요')).
잰 명령: node --test test/timeout-is-a-recorded-failure.test.js (수리 얹은 트리) →
  ✖ 반대시험(iv) … 거짓 답은 그대로 못 나간다 (같은 단언 실패)
  git stash push -- src/runtime/local-terminal.js 후 같은 명령(수리 걷은 트리 · 영수증 none) →
  ✖ 반대시험(iv) … (동일 검사명·동일 실패 — 32.899875ms/28.217333ms)
  git stash pop 으로 복원.
결론(기계): 이 모양은 수리 전·후 **동형으로 안 잡힌다** — 이 계측기는 이번 변경에 대해 정보가
없는 도구이고, 수리는 갈아치움 레인에 회귀를 만들지 않았다. 「죽은 실행 위 성공 주장」 그물
자체는 기존 정의역 밖 — 부채 대장 등재 · 신설은 별도 오너 승인 사안(이 판에서 상신).
