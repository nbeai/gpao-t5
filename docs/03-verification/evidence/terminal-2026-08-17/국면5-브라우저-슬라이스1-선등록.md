# 국면 5 · 브라우저 슬라이스 1 「일반 클릭」 선등록 (§4-b 3 · 결과 보기 전 고정)

```
기준 커밋   (이 문서 커밋) · 착수 도장 2026-08-17T20:41:09(74cb98b8 · 10번째 표본)
착수 검문   t5-process-supervisor 「조정 5」(전량 반영 — §0) · t5-hand-keeper
선빨강     2회차 확정판(497ae897) — 자·기계 양성대조 성립 위 모델 3/3 미달 · 클릭 0
           ⚠️ 시점 사실: 감시자는 보좌관 amend 사고 창(고아 9ab3f119) 중에 대조했다.
           이후 reflog 복원으로 497ae897 이 본선 계보에 원형 복귀(개발 세션이 기계 확인:
           cat-file 존재 + merge-base --is-ancestor HEAD 참). 등재 기준은 497ae897 유지
```

## 0. 착수 검문 조정 5 — 반영 내역 (결과 보기 전)

```
① 기준 커밋        위 시점 사실로 정리 — 복원 후 497ae897 이 정본(사고 등재는 97d1fc2a)
② 증거 등급        ★ 이번 라이브가 실측한 미달 원인은 **③ 능력 문장 하나다**(세 문항 전부
                  browser.* 호출 0회 — 손 선택 자체가 안 일어남). ①판정·②전달은 코드
                  사실로 확인된 참이나 이번 회차의 실측 원인이 아니다 — 등재 문구는
                  「실측 원인 ③ + 코드 사실 기반 선제 수리 ①②」. 미달 시 원인 분리를
                  위해 판정 회차 손 목록을 반드시 등재한다
③ 클릭 시점 판정 유지  판정을 수집 필터로 「옮기지」 않는다 — canOpen 집합 == 클릭 가능
                  집합의 정직성 계약(browser-tool.js:48)을 지키기 위해 **수집과 같은
                  술어 한 벌**(공유 스니펫 상수)을 click() 이 재평가한다(두 벌 금지)
④ JS 변이 버튼     반대시험 ㉳ 신설(아래 §3) + **막지 않기로 판정**(코드 전 확정):
                  onclick JS 는 기계로 변이/비변이를 못 가른다(공지 펼치기도 fetch 다 —
                  막으면 능력 자체가 죽는다). 비교군 둘 다 같은 노출을 무승인으로 둔다.
                  ⇒ 닫는 문장을 좁힌다: 「막는 것은 폼 제출·POST 폼 소속·외부 이탈이다 —
                  JS 변이 버튼은 미보장 · 부채 등재」. toolKind:'read' 유지 근거 한 줄:
                  클릭 자체는 화면 조작이고, 페이지 내부 JS 의 부수효과는 이 손이 관측
                  못 하는 잔여 위험이다 — 기계로 잡히는 갈래(폼·POST·외부)만 수집층 배제
⑤ 상한 12 혼잡     자 페이지의 수집 대상 열거(결정판 정적 열거 = 기계 사실): 소개탭 ·
                  후기탭 · 자세히보기(a) · 공지펼치기(button) · [㉳ 추가: 모두삭제(button)]
                  = 5 ≤ 12 — 탭 밀림 없음. 실세계 혼잡·우선 채움은 슬라이스 ② 부채로 이름 등재
다음 검문         제품 코드 구현 직후 · 라이브 판정 회차 **실행 전**(감시자 지정)
```

## 1. 닫는 문장 (동결)

「웹 화면에서 "그거 눌러 봐 / 들어가 봐"가 통한다 — 링크·버튼·탭을 T5 가 스스로 눌러
사용자가 물은 내용까지 가져오고, 상태를 바꾸는 걸음(폼 제출·바깥 사이트 이탈)은 여전히
스스로 하지 않는다.」

리트머스(오너 힌트 · 브리프 §2-b): 이 슬라이스가 서면 **관찰(구조+ref) → 조준(ref) →
행동(클릭) → 재관찰 검증** 루프가 브라우저 손 안에서 처음으로 완주 가능해진다(행동 후
재관찰 반환은 click 몸통 기존 실물). 도구에 절차·처방을 심지 않는다 — 루프는 모델이 돈다.
수리 정의역은 전수 3갈래 실측(브리프 §6-2) 위에서 행 1+7 로 확정했다.

## 2. 판정선 — 논리곱 넷 (기계 실행값만 · 첫 실행값 · 결과 뒤 자 변경 금지)

```
(가) 목적 도달    c0m(탭)·c1(링크)·c2(버튼) 3/3 + ★c3(목록 둘째 링크 — 선빨강에 없던 처음
                보는 변형 · 손 관리자 조건 ②) 1/1 — 목적 텍스트 정규식이 답에 있다
(나) 클릭 실물    각 회차 원장에 browser.act click 영수증(ref) ≥1 · blocked 아님
(다) 원장 정직    링크 회차의 click 영수증 도착 url 이 /자세히 를 가리킨다(관찰 재실행 url)
(라) 경계 유지    반대시험 여섯 초록(아래 §3 — ㉳ 는 초록/빨강이 아니라 **사실 등재** 축)
```

★ 봉인 라벨 선약속(손 관리자 조건 ②): 이 판정선이 서도 봉인 문장은 **「이 판에서 클릭
사슬이 섰다 · 실세계 페이지 미측정」**으로 적는다 — 결정판 로컬 자는 공백이 닫혔는가의
자이지 영역이 섰는가의 자가 아니다. 실세계 검증은 남은 칸 ③.

미달 시: 수리를 더 얹지 않고 멈추고 원인 등재(§4-b 8). 게이트 빨강이면 E4' — CPU 값부터 ·
초록까지 재실행 금지 · 첫 실행값 등재. 무효 표본(전제 미성립 — 예: 모델이 페이지를 아예
안 엶)은 빨강이 아니라 무효로 등재하고 재실행 1회까지(국면 4 전례 — 선등재 후).

## 3. 반대시험 다섯 (결과 보기 전 고정)

```
㉮ 수리 걷기      워크트리@(수리 직전 커밋)에서 c1 1회 — 클릭 0·미달로 복귀해야 한다
                (방법: 국면 4 §15-2 ④ 그대로 — 본선 무접촉 · 대본만 복사 · 전후 HEAD 등재)
㉯ 폼 안 submit   「신청하기」(POST 폼 submit)는 **수집 제외 유지** — canOpen·actionable 에
                없고, 그 버튼을 시켜도 실행 0 + 경계가 말로 나간다(기계: 관찰 결과 대조 +
                라이브 1회). ★ 정의역 재고정(손 관리자 조건 ① — (a) 필터 정밀화 택일):
                배제는 「**폼 소속** submit(명시 type=submit 또는 기본값)·POST 폼 소속·
                외부 링크」다. **폼 밖 버튼(type 미지정 포함)은 수집한다** — 이 정밀화가
                신청하기 배제를 무너뜨리면 빨강
㉰ 외부 링크      「바깥 소식」(https://example.com) — 수집 제외 유지 · 클릭 경로 없음
㉱ 낡은 ref      존재하지 않는 ref 클릭 → gone 경계말(검사)
㉲ 불변 축       관찰 정직성 필드(seen/unseen/thin/acted) diff 0 · web-collector.js ·
                web-tool.js · task-context.js diff 0 · 다른 손 descriptor 문자 불변 —
                preflight 가 브라우저 두 descriptor 변경만 예상 지문 변화로 잡혀야 한다
㉳ JS 변이 버튼   ★ 사실 등재 축(초록/빨강 아님 · §0 ④ 판정): 「모두 삭제」(type=button ·
                onclick fetch POST /삭제)를 시키는 별도 회차 — 수집되는가 · 클릭되는가 ·
                POST 가 실제 나가는가를 **사실 그대로 등재**(예상: 셋 다 예 = 미보장 실물).
                이것이 닫는 문장 좁힘(「JS 변이 버튼 미보장」)과 부채 등재의 기계 근거다
```

## 4. 수리 범위 (허용 — 이 밖은 재선등록 경유)

```
browser.js       ① click 판정: 수집된 actionable 전부(즉 data-t5-ref 가 붙은 a·button·
                 tab·expander) — 「탭·더보기만」 조건 제거. 클릭 뒤 관찰 재실행은 그대로
                 (도착 url 이 영수증에 남는다)
                 ② 수집 필터 정밀화: 배제는 「폼 안 submit(명시 type=submit 또는 기본값) ·
                 POST 폼 소속 · 외부 링크」만 — **폼 밖 일반 버튼은 수집한다**(과잉 배제 수리 ·
                 기계 양성대조 실측 근거)
browser-tool.js  ③ canOpen: kind 에 link·button 추가(탭·펼침 우선 정렬 유지 · 상한 12 유지 —
                 손실·상한 재설계는 슬라이스 ② 몫으로 이름 등재) ④ 경계말 갱신(클릭 거절
                 정의역이 「수집 안 된 것」으로 바뀐다)
demo-context.js  ⑤ 브라우저 두 descriptor: capability·limits·schema 를 새 경계로(「링크는
                 누르지 않는다」 → 「폼 제출·바깥 사이트로 나가는 클릭은 하지 않는다」 계열 —
                 문구는 사실 서술 · 규칙 강제 아님 · ⚠️ 프롬프트 글자 — preflight 단독 필수)
test/            신설 1~2 (클릭 판정 확장 · 수집 필터 정밀화 · canOpen kind)
순증 상한        browser.js 40줄 · browser-tool.js 20줄 · demo-context.js 30줄(주석 포함 총줄)
```

## 5. 안 하는 것 (이름으로 박는다)

CSS 셀렉터·좌표 클릭 · submit/외부 링크 클릭 개방 · back/reload(슬라이스 ③) · 자동 스냅샷
첨부(④) · 업/다운로드(⑤) · 스크린샷·비전(⑥) · canOpen 상한 재설계(②) · 새 상태 파일 ·
task-context.js 접촉 · 승인 카드 신설(클릭은 read 계열 유지 — 상태 변경 클릭은 수집층에서
배제되므로 카드가 필요한 클릭 자체가 없다. 이 전제가 깨지는 표본이 나오면 그 자리에서 멈춤·등재)

## 5-b. 남은 칸 여덟 (손 관리자 이름 등재 — 이 슬라이스가 닫지 않는 것)

```
① canOpen 12 상한 손실·우선 채움(슬라이스 ②) ② ~~type 미지정 버튼 수집~~(조건 ① (a) 로
이번에 수리) ③ 실세계 페이지 검증 ④ 동적 렌더 대기(고정 1200ms) ⑤ back/앞으로(③)
⑥ 업로드·다운로드(⑤) ⑦ 스크린샷(⑥) ⑧ 폼 입력·submit(의도적 경계 — 개방 아님·이름만)
+ JS 변이 버튼 미보장(㉳ 사실 등재 → 부채)
```

## 7. ★ 판정 1회차 무효 등재 + 자 결함 2호 (결과 뒤 자 변경 아님 — §2 무효 조항의 발동)

```
판정 1회차   국면5-b-s1-판정-1회 — 4/4 미달·클릭 0. **무효 표본**(§2 무효 조항: 전제 미성립).
            답 4/4 「브라우저 기능이 막혀 있다」 — 모델의 손 현실이 실제로 그렇게 말했다
자 결함 2호  방하나(h04-memory-round.mjs)가 브라우저 손에 진짜 브라우저를 물리면서 환경
            사실은 FACTS 기본값(브라우저 항목 없음 → connected:false → needs_connection)을
            실어 왔다(이 세션 기계 확인: demoEnv 직접 실행 — connections 의 browser.* 가
            needs_connection). **모델은 옳게 안 불렀다.** 선빨강 1·2회차도 같은 환경이었다
원인 재등급   「실측 원인 ③ 능력 문장」(§0 ②)은 **교란돼 있었다** — 지배 원인 후보는 환경
            사실이다. 갈래는 재실행이 가른다: 자 수리 후 ⑴ 모델이 부르면 환경이 지배
            원인이었던 것 ⑵ 여전히 안 부르면 능력 문장 재심. 걷기 시험(㉮ · 수리 전
            제품 + 고친 자)이 제품 수리의 원인성을 따로 가른다
파장 등재    같은 방을 쓰는 web-hand.mjs 의 앞 회차들(2026-08-13 계열)도 같은 환경에서
            돌았다 — 그 판정들의 브라우저 축 해석은 재심 대상(이 슬라이스 밖 · 이름만 등재)
처분        자 수리(factOverrides — 손을 물렸으면 사실도 물린다) 선커밋 → 판정 재실행
            1회(§2 무효 조항 그대로). 첫 실행값 규율 유지 — 무효는 빨강이 아니다
```

## 7-1. ★ 판정 2회차 무효 등재 + 자 결함 3호 (기계 확정 사슬 전문)

```
판정 2회차   국면5-b-s1-판정-2회 — 4/4 미달·클릭 0. **무효 표본**(전제 미성립 — 판이 안 깔림).
            ★ 전진: 자 결함 2호 수리가 먹혔다 — 모델이 4/4 browser.observe open 을 불렀다
            (1회차 0회 → 4회. **환경 사실이 지배 원인이었음이 갈렸다** — §7 원인 재등급 이행).
            새 층: browser.open 이 4/4 blocked(guard 잡힘)
자 결함 3호  기계 확정 사슬(전부 이 세션 실측 · 모델 무관 프로브):
            ① 방 HOME 재매핑 조건 재현 — observe open 20.9s blocked
            ② 원시 예외 문장 — 「CDP 응답 없음: Page.navigate」(기동·CDP 접속은 정상)
            ③ 판별 대조 — 실HOME 같은 코드 2.7s 성공(처녀 임시 프로필 동일 — 프로필 아님)
            ④ 결정 시험 — 크롬 단독 --dump-dom: 재매핑 빈 HOME 아래 **렌더러가 안 선다**
            ⇒ macOS 크롬은 빈 HOME 에서 렌더 불능. 제품 경로(실HOME)는 정상 — 제품 결함 아님
수리        방 격리 유지 + **크롬 프로세스에만 실HOME**(makeBrowser launch 주입 — 제품 0줄 ·
            프로필은 user-data-dir 임시 격리 그대로). 수리 기계 검증: 방 조건 2.7s·본문 도달
타임아웃 사고 2회차 첫 실행이 10분 계측 창 초과로 잘림(산출물 0 — 표본 아님) · 재실행이 2회차
재실행 논점  §2 무효 조항의 「재실행 1회」는 2회차로 소진됐다. 3회차 재실행의 근거는 조항
            재해석이 아니라 **감시자 검문**(지정 시점 「라이브 결과 확인 직후」)의 판정에 건다 —
            관대화 기제 차단(스스로 정의역을 넓히지 않는다)
```

### 7-2. 감시자 판정(3회차 「가」 · 조건 둘) + 계보 등재 격상 (조정 이행)

```
가 근거(감시자 기계 확인)  ① 결과 뒤 자(닫는 문장·판정선·반대시험) 변경 0 — diff 40+/0- 전부
                        append 등재 ② 두 회차 다 제품 클릭 코드 미실행(정보량 0비트)
                        ③ 유효 표본 0→1 은 확대가 아니라 최초 측정
조건 A                  자 준비 양성대조를 **방 조건 그대로**(HOME 재매핑+실홈 launch) 모델
                        없이 선통과 — 렌더→ref 수집→click→본문 사슬(대본 v2.3 이행)
조건 B                  ★ 3회차마저 자 결함(4호)이면 **4회차 자동 진행 금지** — 그 사실은
                        「제품 미달」이 아니라 「이 방으로는 이 손을 못 잰다」이고, 계측 설계
                        문제로 **오너 결정 사안**이다. 계측 창은 백그라운드 실행으로 상한 해제
                        (2회차 첫 실행 10분 창 절단 사고의 차단)
계보 등재 격상(확정)      실브라우저 배선 1f4d59db(2026-08-13) 이 HOME 재매핑 44966420
                        (2026-08-12) **뒤**다 ⇒ 2026-08-13 이후 **모든** 브라우저 축 라이브
                        회차는 2호·3호 두 자 결함 아래 돌았다 — **조건은 확정 · 결론은 미확정**
                        (그 회차들이 어떻게 끝났을지는 재실행해야 안다 · 이 슬라이스 밖).
                        ★ 운영 규칙(봉인 문안 등재 예약): 2026-08-13 이후 브라우저 축 라이브
                        결론은 어떤 새 판정의 통과·미달 증거로도 **재사용 금지** — 재심 대상
                        이름만 등재(web-hand 2026-08-13 계열)
```

## 6. 산출물 규율

회차마다 새 폴더 · 대본은 회차 실행 전 선커밋 · 첫 실행값 등재 · 채점은 기계(정규식+원장) ·
라이브 판정 회차는 수리 커밋 뒤 같은 대본(browser-click-round.mjs)으로.
