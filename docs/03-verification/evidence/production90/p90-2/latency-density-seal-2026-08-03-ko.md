# P90-2 지연·응답 밀도 봉인 판정

- 판정: `P90_2_DEVELOPMENT_SEAL_PASS_WITH_RECORDED_LIMITS`
- 기준선: `609d71d` (직전 기준선 `90e3e38`)
- 범위: 도구 턴 지연의 병목 귀속과 그 원인 제거, 사용자면 오염 차단
- 역할: 본선 구현·자체검증. 독립 감사 전
- 이 판정은 **공식 성능 점수가 아니다.** 공식 채점은 설치 산출물에서 한다(아래 §5)

## 1. 사용자에게 달라지는 것

파일을 다루는 요청에서 답이 나오기까지 기다리는 시간이 **중앙 27.4초 → 19.8초**로 줄었다.
첫 내용이 보이기까지도 **24.9초 → 17.5초**다. 그리고 답이 `memory.cite:` 같은 내부 이름으로
시작하던 일이 없어졌다.

## 2. 병목 귀속 (계획서 §4-2 "실제 병목을 먼저 확정한다")

로컬 파일 경로 6회, 제품 HTTP/SSE, 실제 모델(gpt-5.5), 격리 홈. 제품 코드 수정 0인 상태에서 측정.

| 귀속 후보 | 실측 | 판정 |
|---|---:|---|
| T5 자체 처리 | **0.12%** · 중앙 28.6ms | 배제 — 목표 1,500ms의 1/50 |
| 도구 실행 | 0.01% · 2~9ms | 배제 |
| 프롬프트 성장 | 6.5K → 8.4~9.8K자 | 왕복당 지연에 흡수 |
| **모델 왕복** | **99.9%** · 왕복 4~6회 | **확정** |

왕복 안에서 하나가 튀었다.

| 왕복 종류 | 횟수 | 합 | 평균 |
|---|---:|---:|---:|
| 본선(도구 선택·답) | 22 | 101.6초 | 4.62초 |
| **완료 형태 판정** | **8** | **58.6초** | **7.33초** |

6회 실행에 판정 호출이 8번 — 재시도가 돌았다. 도구도 없고 답이 4글자인데 본선보다 1.6배 느렸다.
전체 모델 시간의 **36.6%**가 여기였다.

## 3. 고친 것

### 3.1 완료 형태 판정을 산문 파싱에서 구조 채널로 (`d27ea27`)

`directWrite` 가 아닐 때 **도구 없이 산문을 받아 정규식으로 읽고** 있었다. 6회 중 2회는 파싱
안 되는 산문(47·49·55자)이 와서 재시도가 돌았고, 한 회는 두 번 다 실패해 17초를 태우고 보수
폴백으로 떨어졌다.

분기를 없애고 항상 구조 채널을 준다. 판정 스키마는 `{output:'chat'|'file'}` 하나뿐이고
`requiredTool` 로 강제하므로 모델이 고를 수 있는 실제 걸음이 목록에 없다 — 예전 주석의 걱정
("실행 도구를 하나 더 제시하면 다음 걸음을 판정 응답으로 소비")은 실행 도구를 함께 줄 때의
것이다. 산문 파싱은 구조 채널을 모르는 provider 를 위한 폴백으로 그대로 남는다.

### 3.2 통제 접두어가 스트리밍 조각으로 새던 것 (`609d71d`)

라이브 24개 답 중 3개(12.5%)가 `memory.cite: [현재 합의] …` 로 시작했다. 차단 장치
(`userFacingModelText`)는 **최종 답에만** 붙어 있었고, 스트리밍 조각은 그 경계를 안 지났다.
정제된 답이 오염된 누적으로 시작하지 않으니 정렬이 둘을 이어 붙이거나 조각 쪽을 돌려줬다.

조각이 쌓이는 자리에서 **같은 경계 하나를 공유**하게 했다. 아직 아무것도 안 나간 동안에만
적용한다(접두어는 맨 앞에서만 뜻을 가진다). 새 문구 규칙을 만들지 않았다.

## 4. 실측 (같은 하네스, 수정 전후)

| 지표 | 수정 전 | 수정 후 | 변화 |
|---|---:|---:|---|
| 도구 턴 총 중앙 | 27,366ms | **19,840ms** | **−27.5%** |
| 첫 유용한 내용 중앙 | 24,854ms | **17,509ms** | **−29.6%** |
| 판정 호출 평균 | 7,330ms | **2,750ms** | −62% |
| 판정 재시도 | 2회/6 | **0회** | 소멸 |
| T5 자체 오버헤드 중앙 | 28.0ms | **22.1ms** | −21% |
| 통제 이름 노출(라이브 24 표본) | **3** | **0** | 소멸 |

왕복 **횟수**는 줄지 않았다(4~6 → 4~7). 줄인 것은 **왕복당 지연**이다. 왕복 횟수는 모델 판단
영역이라 이번 범위에서 건드리지 않았다.

게이트: 회귀 **2,026/2,026** · 돌연변이 **270/270**(소스 지문 실행 전후 동일) ·
plan·docs·workspace·gate PASS.

## 5. 비교군 — 채점 전에 확정하고, 없는 축은 뺀다

상대 기준을 쓰려면 비교군 데이터가 T5 결과보다 먼저 존재해야 한다. 확인 결과:

| 축 | 비교군 대응 | 처분 |
|---|---|---|
| 기억으로 인한 전경 지연 | OpenClaw H02-new 52.9초 · H07 41.6초 (`COMPARISON-CONCLUSION-2026-07-30-ko.md`) | **참고만.** T5 는 성장을 배경으로 옮겨 전경 durable I/O 가 없다(이번 실측 T5 자체 28.6ms) |
| 도구 턴 총 지연 | **대응 없음** | **기준에서 뺀다** |
| 첫 유용한 내용 | **대응 없음** | **기준에서 뺀다** |
| 응답 밀도 | **대응 없음** | **기준에서 뺀다** |

비교군 문서가 스스로 *"이 비교에서 어떤 성능 순위도 만들지 않는다(모델 상이·1회 관찰)"* 고
못박았고, 모델도 다르다(T5·Hermes gpt-5.1 / OpenClaw gpt-5.5). 따라서 이 봉인은
**절대 개선폭(자기 대비)** 으로만 판정하며 비교 우위를 주장하지 않는다.

계획서 §4 종료 조건의 *"현재 기준선 대비 도구 턴 중앙값 30% 이상 단축 또는 절대 기준 충족"* 에
대해 **−27.5%** 로 근접했으나 30% 문턱에는 미달이다. 문턱을 낮추지 않는다 — 미달로 적는다.

## 6. 기록된 한계

- 측정은 **로컬 파일 경로 한 종류·6회**다. chat·web·agent 경로는 재지 않았다.
- 공식 `perf.*` 채점은 시나리오 환경이 `artifact: "installed"` 로 동결돼 있어 **설치 산출물에서만**
  성립한다. 이 개발 트리 측정은 계획서 §4-1·§4-2 의 **진단**이지 점수가 아니다.
- 도구 턴 중앙 30% 단축 문턱 **미달**(−27.5%). 남은 여지는 왕복 횟수이며 그것은 모델 판단 영역이다.
- **반복 요약**(같은 숫자를 표 두 개로 다시 말함, 3회 중 2회 관측)은 사람 평가가 기준인 항목이라
  HumanRealTest 다듬기로 이월한다. 계획서 §4-6 이 문구 정규식 고정을 금지하므로 구조 원인이
  확인되기 전에는 손대지 않는다.
- 아침 90항 검증의 "형식 지시 턴에 내용 없이 확인만" 은 **3/3 재현되지 않았다**(다른 작업선의
  human-use 다듬기가 닫은 것으로 보인다). 죽은 결함을 고치지 않았다.

따라서 P90-2 는 `DEVELOPMENT_SEAL_PASS_WITH_RECORDED_LIMITS` 로 종료한다.
이 판정은 T5 전체 제품이나 다른 P90 작업선의 완료 선언이 아니다.
