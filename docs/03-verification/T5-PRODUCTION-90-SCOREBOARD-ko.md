# T5 Production 90 점수판

- 상태: `WAVE0_SCORE_INPUT_HARDENED_PENDING_INDEPENDENT_AUDIT / INSTRUMENTATION_IMPLEMENTED / FORMAL_BASELINE_PENDING`
- 점수 정본: `scripts/production90/metric-registry.json`
- 시나리오 정본: `scripts/production90/scenario-manifest.json`
- 계산기: `scripts/production90/score-production90.mjs`
- 현재 지표: 원자 지표 73개 · 사전 등록 시나리오 29개

이 문서는 수기 점수표가 아니다. 네 작업선, 18개 평가 항목, 일곱 코어의 제목과 현재 측정 상태를
사람이 읽도록 투영한다. 배점·분모·P0·중복 가산 규칙은 JSON 정본만이 소유한다. 실행 증거가 없는
현재 상태는 0점이 아니라 **미측정**이다. 계측 제품 관통 1회는 배선 증거이지 공식 점수 표본이 아니다.
`NOT_RUN`이 실행 결과에 명시된 경우에만 해당 분모에서 0점이다.

Wave 0에서 서버 입력·큐·첫 진행·첫 답·지속 완료·SSE 완료와 브라우저의 실제 표시·유용한 첫 내용·최종
투영 완료를 같은 `measurementId`로 결합하는 계측이 제품 경로에 편입됐다. 모델·도구 외부 대기는 겹친
구간의 합집합으로 계산하고 T5 내부 오버헤드와 분리한다. 원문 발화·답변·경로·URL·도구 인자는 계측
저장소에 들어가지 않는다. 공식 p50/p95는 경로별 cold/warm 30회 수집 뒤에만 이 표에 반영한다.

## 1. 판정 규칙

- 각 원자 사실은 유일한 `factKey`를 가지며 한 투영 안에서 한 번만 점수에 들어간다.
- 같은 실행이 서로 다른 사실을 증명할 수는 있지만 같은 사실을 여러 축에 반복 가산하지 않는다.
- 결정적 계약은 등록된 모든 사례가 통과해야 점수를 얻는다. 부분점수는 없다.
- 모델·사람 변동 계약은 사전 등록 분모의 성공률로 계산한다.
- 모델 실패와 외부 차단은 사용자 end-to-end에서 실패다. 외부 장애를 정직하게 복구하는 것이 목표인
  시나리오는 외부 성공이 아니라 정직성·복구·중복 효과 0으로 판정한다.
- `HARNESS_INVALID`는 점수에서 제외하지만 반드시 재실행하며, 남아 있으면 최종 점수를 낼 수 없다.
- P0 한 건은 총점과 무관하게 후보판을 막는다. 독립 감사 PASS는 추가 점수가 아니다.
- P90 작업선은 총점 90 이상과 각 축 80% 이상을 함께 만족해야 한다.
- 점수 입력은 schema를 통과하고 등록 시나리오·시나리오 표본 번호·지표 표본 번호에 결합돼야 한다.
- manifest와 환경 계약 digest는 정본에서 계산하고, build digest는 점수 파일 밖에서 고정한 신뢰 기준과 대조한다.
- 지표가 요구한 증거 종류와 digest가 모두 있어야 하며, 모델 증거 지표는 구조화된 모델 영수증도 필요하다.
- 같은 `runId`, 시나리오 표본, 지표 표본은 중복 가산하지 않는다. 73개 지표 모두 측정 시나리오가 있다.
- 지표 표본이 차도 사전 등록된 시나리오 회차가 비면 최종 점수는 내지 않는다. 실패·미실행은 성공 증거가
  아니라 `status-receipt`로 남겨 0점과 재실행 필요를 구분한다.

## 2. P90 작업선

| 작업선 | 기능 | 반복 | 실환경 | 인간 체감 | 운영·복구 | 현재 |
|---|---:|---:|---:|---:|---:|---|
| P90-1 장기 작업상태와 말귀 | 30 | 20 | 25 | 15 | 10 | **미측정** |
| P90-2 도구 턴 지연과 응답 밀도 | 30 | 20 | 25 | 15 | 10 | **미측정** |
| P90-3 비개발자 설치 생명주기 | 30 | 20 | 25 | 15 | 10 | **미측정** |
| P90-4 실제 사용 폭과 장기 인간 사용 | 30 | 20 | 25 | 15 | 10 | **미측정** |

## 3. 18개 평가 항목

| # | 항목 | 현재 | # | 항목 | 현재 |
|---:|---|---|---:|---|---|
| 1 | 현재 요청·짧은 말귀 | 미측정 | 10 | 연결·커넥터·채널 | 미측정 |
| 2 | 장기 대화·프로젝트 상태 | 미측정 | 11 | Skill·Trigger·자동화 | 미측정 |
| 3 | 자연스러운 응답·밀도 | 미측정 | 12 | 제한 에이전트·H10 | 미측정 |
| 4 | Selfhood | 미측정 | 13 | Work Surface·UX | 미측정 |
| 5 | BEAI5 모델 운용 | 미측정 | 14 | Truth Ledger·Recovery | 미측정 |
| 6 | T-cell 기억·학습·성장 | 미측정 | 15 | 개인정보·실행 안전 | 미측정 |
| 7 | ActionPlan·Authority | 미측정 | 16 | 응답 속도·체감 성능 | 미측정 |
| 8 | 로컬 파일·문서 손발 | 미측정 | 17 | 설치·업데이트·제거 | 미측정 |
| 9 | 웹 조사·브라우저 | 미측정 | 18 | 구조·유연한 진화성 | 미측정 |

## 4. 일곱 코어

| 코어 | 현재 |
|---|---|
| Selfhood / 자기파악 | **미측정** |
| BEAI5 Model Operation / 모델 운용 | **미측정** |
| Intent / Context / T-cell | **미측정** |
| ActionPlan / Authority | **미측정** |
| Router / Execution | **미측정** |
| Work Surface / UX | **미측정** |
| Truth Ledger / Recovery / Growth | **미측정** |

## 5. 실행 결과 계약

각 실행은 `runId`, `scenarioId`, `manifestDigest`, `buildDigest`, `environmentDigest`, 모델 영수증,
비민감 증거 ref와 아래 상태 중 하나를 남긴다.

`PASS · PRODUCT_FAIL · MODEL_FAIL · EXTERNAL_BLOCKED · HARNESS_INVALID · NOT_RUN`

원시 비밀, 실제 사용자 파일 내용, 외부 수신처 원문은 점수 증거에 저장하지 않는다. 결과를 본 뒤 배점이나
분모를 바꾸면 기존 표본을 새 계약 점수에 재사용하지 않는다.
