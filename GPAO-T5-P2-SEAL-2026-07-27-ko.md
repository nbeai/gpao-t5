# GPAO-T5 Phase 2 봉인 — Conversational Quality Layer

봉인일: 2026-07-27 · 판정 기준: 계획서 v3.0 「Phase 2 완료 기준」 + v3.1 §16 「완료의 정의」

> 봉인은 "다 했다"는 선언이 아니라 **증거로 닫는 것**이다. 아래는 전부 실측이다.
> 검증 못 한 것은 완료로 적지 않았다(§9).

---

## 0. 게이트

```text
cwd            /Users/jyp/Developer/gpao-t5
npm test       pass / 743건
npm run gate   PASS / 743건 / 1.47s   (§17 기준선 5s)
  선언 7 = 손 7 = 화면 7, 스텁 0
  안전 바닥 불변식 통과
  프롬프트 1,944자 = 고정 1,915(헌장 1,485) + 가변 29
  도구 이름·설명이 descriptor 단일 진실 (8개)
  런타임 의존성 0
```

---

## 1. Phase 2 완료 기준 (계획서 정본) — 라이브 실측

### ① 단순 대화는 빠르고 자연스럽다 — **통과**

| 발화 | 시간 | 길이 | 답 |
| --- | --- | --- | --- |
| 안녕 | 1,445ms | 10자 | "안녕. 다시 왔네." |
| 고마워 | 1,269ms | 4자 | "천만에." |
| 지금 몇 시야? | 1,525ms | 42자 | "지금은 한국 시간 기준 2026년 7월 27일 월요일 오전 6시 53분이야." |

대비(같은 계열 발화, 이번 Phase 착수 시점): **"오늘 날씨" → 두 번 되묻고 1분 52초.**
짧은 말에 짧게 답하고, 한 대화 안에서 말투를 유지한다.

### ② 복잡한 작업은 구조가 있지만 딱딱하지 않다 — **통과**

"팔식당 보고 회식 장소로 괜찮을지 판단해줘" → 1,391자 · **제목줄 0개** · 목록 8개 · 출처 링크 포함.
판단 기준을 먼저 밝힌다("일반적인 6~12명 저녁 회식 기준으로 보면").
구조는 있는데 보고서로 꾸미지 않는다.

### ③ 승인/복구 안내가 정책문처럼 보이지 않는다 — **봉인 검사에서 결함 발견 → 고치고 통과**

**봉인 과정이 잡은 것**: 전송 대상이 없을 때 이렇게 물었다 —

```text
어디로 보낼지 알려주세요. (텔레그램 전송의 채널/받는 사람)
```

괄호 안에 **내부 구조가 그대로 노출**됐다. 기준 ③ 정면 위반이다.
그리고 라벨 조사가 틀려 "슬랙 게시**으로**"가 나왔다 — T5 가 한국어를 못 하는 것처럼 보인다.

고친 뒤(라이브):

```text
슬랙 게시가 아직 연결/권한 준비 전이라 제가 바로 올리진 못해요.
대신 복사해서 붙여 넣을 문구는 이렇게 쓰면 됩니다: > 회의 30분 뒤로 미뤄주세요.
슬랙 연결/권한이 준비되면 제가 이어서 게시할 수 있어요.
```

```text
지금은 데스크탑 폴더가 제 작업 범위 밖이라 직접 볼 수 없어요.
제가 볼 수 있는 건 /Users/jyp/GPAO-T5 안쪽뿐이에요.
데스크탑 내용을 보려면 둘 중 하나로 해주세요.
 1. 데스크탑 폴더를 제 작업 범위로 열어주기
 2. 확인할 파일/폴더를 /Users/jyp/GPAO-T5 안으로 옮기기
```

아침에 같은 질문이 **"터미널에서 `ls` 결과를 붙여 주세요"**였다. 떠넘김이 사라졌다.

### ④ 모델 입력은 가볍고 정확하다 — **통과**

4턴 대화 실측: 호출 5회 · **최대 5,962자** · 평균 3,976자 (게이트 상한 12,000자).
고정 접두 1,944자(정체성·헌장 1,485·말투·환경)는 매 턴 같아 프롬프트 캐시에 얹힌다.

---

## 2. §16 완료의 정의 — 다섯 규칙

### A. 부족한 점을 댈 수 없는가

**댈 수 있다. 그래서 아래를 이월로 명시한다**(§16-B 의 "다른 Phase 범위"에 해당):

| 남은 것 | 왜 P2 가 아닌가 |
| --- | --- |
| 상호명만으로 네이버 플레이스 도달 | 검색 색인에 없다(실측). browser 손이 커진 뒤 재확인 |
| 폴더 권한을 사용자가 여는 길 | Local Surface 6.2 — 오너가 Surface 설계 때로 배정 |
| 탭 전환(방문자/블로그 구분) 라이브 확인 | 브라우저 손은 만들었으나 네이버 429 로 그 시나리오만 미확인 |
| #7 외부 도구 연결 · #8 의미 검색 | §15.1 에서 P5-B · P7-1 로 배정 |

**P2 범위 안에서 부딪히는 것은 없다.** 위는 전부 다른 Phase 의 몫이다.

### B. "후속"이 허용 범위인가 — 예

위 넷 모두 (a) 명시적으로 다른 Phase 범위다. 사용자가 P2 기능을 쓰다 부딪히는 것은 없다.

### C. 선언과 구현의 어긋남 — 게이트가 막는다

이번 Phase 에서 **수동 맵 네 개를 없앴다**(1축). 각각이 실제 사고를 냈다:

| 맵 | 실제로 낸 사고 |
| --- | --- |
| `tool-labels.js` LABELS | `buildSelfState` 가 descriptor 의 label 을 흘려서 생겼다 |
| `tool-labels.js` CAPABILITIES | `session.search` 누락 → 자기파악에 이름만 |
| `tool-schema.js` SCHEMAS | **"세션 찾을 수 없어요"**(오너 실사용). `mail.send` 도 누락 |
| `live-context.js` LIVE_TOOL_IDS | 손 없는 `mail.send` 를 "연결됨"으로 말한 이력 |

게이트 불변식(목록 아님): 선언된 도구는 이름·설명·스키마를 갖는다 / 커널이 보는 값이 descriptor 와
같다 / 수동 맵이 다시 생기면 실패 / **양방향** — 손 없는 선언과 선언 없는 손발을 모두 잡는다.

### D. 능력 완결 — 웹은 한 단위로 닫혔다

검색 → 읽기 → **못 읽은 범위 표시** → 실패 처리 → 다음 길 → **화면 관찰(브라우저)** → 예의(캐시·백오프).
쪼개서 반쪽을 내보내지 않았다.

### E. OpenClaw · Hermes 대비 — **무엇이 같고 무엇이 없는가**

**같아진 것**

| 그들이 가진 것 | T5 |
| --- | --- |
| 매 턴 system prompt 조립(OpenClaw) | `buildModelMessages` 단일 계약 + 캐시 경계(고정 접두/가변 접미) |
| SOUL.md 로 voice 분리(둘 다) | 말투를 SOUL 이 갖고 **매 턴** 간다. 취향(SOUL)과 계약(코드)을 위치로 분리 |
| 세션 큐·쓰기 락(OpenClaw) | 턴 전체(로드→실행→저장) 직렬화 + 원자적 저장 |
| 검색과 추출 분리(Hermes) | `web.collect`(읽기) / 모델 내장 검색 / `browser.observe`(화면) |
| 접근성 트리 기반 snapshot(Hermes) | CDP 로 화면 관찰 — **의존성 0**(그들은 Playwright·CDP 라이브러리를 쓴다) |
| browser navigate/click/scroll(둘 다) | `browser.observe` / `browser.act`(스크롤·탭·더보기) |
| 긴 결과 절단·재조회(Hermes) | 종류별 **구조화 요약** + 가운데 접기 + 생략량 표시 |

**T5 가 더 가진 것**

- **출처 원장** — 봤다고 주장하려면 근거가 남는다. 둘 다 없다.
- **안전 바닥 이중화** — 등급 분류가 흔들려도 `SAFETY_FLOOR_KINDS` 가 독립으로 막는다.
- **못 한 것의 정직성** — 안 해 본 일을 "막혔다"고 하지 않는다 / 열린 것과 읽은 것을 구분한다 /
  본 범위·못 본 범위를 사실로 남긴다. 이건 프롬프트 문구가 아니라 **영수증 계약**이다.
- **사이트에 대한 예의** — 캐시·최소 간격·백오프를 **두 손이 공유**한다.
- **descriptor 단일 진실 + 양방향 게이트** — 도구를 더하거나 빼면 다섯 표면이 함께 따라온다.

**아직 없는 것 (정직하게)**

- 브라우저: 탭 label/targetId 관리, 다중 탭, 사용자 프로필(로그인 세션), 스크린샷·비전
- 입력·전송 조작(type/submit) — **일부러 만들지 않았다**. 없으므로 실수로도 못 한다
- computer_use(데스크톱 전체 조작) — 근거가 생기면 연다

---

## 3. 이번 Phase 에서 반복된 패턴 (다음 작업에 그대로 적용한다)

**"모델이 이상하다" → 재보니 런타임이 못 하거나 안 알려줬다.** 다섯 번 연속 그랬다.

1. 모델이 리뷰 주소를 정확히 골랐는데 → 런타임이 **발화 원문으로 실행**했다
2. 모델이 플레이스를 요청했는데 → 런타임이 블로그를 주고 **"성공"으로 기록**했다
3. 모델이 "그거"를 알 수 있었는데 → **정규식이 먼저 답을 닫았다**
4. 모델이 터미널을 제안한 건 → **되는 길이 그것뿐이라서**였다
5. 모델이 "하단부 1812~1902줄을 봤다"고 지어낸 건 → **브라우저가 모델에게 안 보였기** 때문이다

그래서 판정 질문은 하나다: **이 슬라이스는 앞·중간·뒤·다음 턴 중 어디를 채우는가**(§0).
그리고 **지킬 수 없는 규칙은 모델을 무시하게 만든다** — 규칙을 넣기 전에 되는 길이 있는지 먼저 본다.

## 4. 회귀 보호 — 무엇이 무엇을 잠갔는가

| 잠근 것 | 어디서 |
| --- | --- |
| 흐름 이해(멀티턴) | `scenario-replay.test.js` — 대상 잇기·풀기·크기 상한 |
| 세션 유실·원자성 | `session-concurrency.test.js` — 동시 턴·방 분할·쓰기 원자성 |
| 권한 등급 뜻 | `authority-tier-contract.test.js` — 목록이 아니라 불변식 |
| 도구 단일 진실 | `scripts/gate.mjs` — 양방향(손 없는 선언·선언 없는 손발) |
| 요청↔결과 정합 | `external-surface.test.js` — 무엇을 찾고 무엇을 읽었나 |
| 화면 관찰의 정직성 | `browser-surface.test.js` — 본 범위·못 본 범위·얇은 화면 |
| 사이트에 대한 예의 | `host-manners.test.js` — 캐시·백오프·두 손 공유 |
| 말투와 계약의 분리 | `soul-voice.test.js` — 계약은 SOUL 에 두지 않는다 |
| 답변 렌더링 안전 | `markdown.test.js` — 12건 중 6건이 "무엇이 통과하지 않는가" |

---

## 5. 봉인 판정

Phase 2 완료 기준 넷을 **라이브 실측으로** 통과했고, §16 다섯 규칙에 답했다.
봉인 과정에서 기준 ③ 위반 한 건을 찾아 고쳤다 — 그게 봉인을 미리 하지 않은 이유다.

**Phase 2 봉인.** 다음은 계획서 순서대로 Phase 3(First Real Model Path)이며,
이월 넷은 각자 배정된 Phase 에서 근거와 함께 연다.
